# 交接记录（.qoder/handoff/HANDOFF.md）

> **规则**：任何角色（含领队）处理完一个队列项，**必须在此追加一条记录**，否则 `script/gate.py check`
> 判 FAIL——即该轮视为"未发生"。子代理没有主会话的记忆，本文件是它唯一的输入交接面。
> 追加位置：文末。一条记录不得跨轮修改；要更正就加新记录并写"更正 Q-xxx#n"。
>
> 记录格式（五个字段一律必填，缺一个即判不合格交接）：
>
> ```
> - [Q-xxx] 角色=<planner|designer|verifier|decider|auditor|reviewer|investigator|leader> 日期=<YYYY-MM-DD> 轮次=<n>
>   - 产物: <改了哪些文件/新增什么，带路径；无产物写 "无（纯核查）">
>   - 依据: <台账号 / spec 节号 / 知识库出处 / 命令与其输出位置>
>   - 机判: <跑了什么判据、结果数字；未跑写 "未跑（原因）">
>   - 未完成: <剩余分歧、已知缺口；确无则写 "无">
>   - 下一手: <交接给谁、做什么；或 "等待 R0 裁决：…">
> ```

---

- [Q-000] 角色=leader 日期=2026-09-23 轮次=1
  - 产物: `run_cmd/AutoQueue.yaml`（15 项）、`script/gate.py`（14 项机械判据）、`.qoder/agents/` 下 7 份角色定义
  - 依据: `doc/00` ISS-030/033；`doc/项目开发流程.md` §12.1~12.4；《AI进行IC开发验证工作流程》v2.1 §1.1/§1.3/§6.1
  - 机判: `gate.py check` 首跑 7 PASS / 4 FAIL，其中真实缺陷一处（看板台账计数镜像落后 31→32）；修工具与文档后 13 PASS / 0 FAIL
  - 未完成: `vr1-decider` 未进本会话派发列表（新建定义需重开会话，知识库卡 01 实测坑），本轮决策由领队代行
  - 下一手: 领队按 `gate.py loop` 逐轮派发；生产型角色（planner/designer/verifier）每次派发必须以本文件为交接输入

- [Q-005] 角色=auditor 日期=2026-09-23 轮次=1
  - 产物: 码点/结构核查内联进 `script/gate.py`（`codepoints` 硬判据 + `rare-chars`/`width-unverifiable` 两级 WARN + `md-integrity`）
  - 依据: ISS-026（本会话实测 7 处中文静默错字）、ISS-031（看板计数脱钩）；错词表逐条对应真实事故，无出处者已删（"跳页同偏"实为 ISS-020 正文用词，属工具自造假阳）
  - 机判: `check_text`/`check_md` 均 PASS；字频法阈值 `<=1` 作 FAIL 会误报"苗子/够狠/淡化/隶属"四个正词，故降为 WARN
  - 未完成: 本仓尚无 `doc/05`（属 Step 5 回归记录），输出记录暂落本文件与队列 `landable`；单字形近字表已判定不可用作独立判据
  - 下一手: `doc/05` 建立后补档；领队继续 Q-006

- [Q-014] 角色=designer 日期=2026-09-23 轮次=1
  - 产物: `doc/spec/01-流水线与寄存器接口.md` 两处表格结构修复（L167 `ftq_id` 行补齐 4 列；L418 `csr_new` 跨行合并回单行）
  - 依据: `gate.py check` 的 `md-integrity` 实报 2 行；修复不改任何位宽与语义内容，只恢复表格结构
  - 机判: 修复前 `md-integrity` FAIL 2 行 → 修复后 PASS；`gate.py check` 13 PASS / 0 FAIL
  - 未完成: `spec/01` 另有 18/20 个 struct 合计无法机器核对，见 Q-004 下一条记录
  - 下一手: 无（本项已 done）

- [Q-004] 角色=reviewer 日期=2026-09-23 轮次=3（纯复核轮）
  - 产物: 无（纯核查，评审者无写权限，符合 §2 边界）。差异表输出见下条 Q-004b
  - 依据: 被复核产物 `doc/spec/01`（v0.2）+ `doc/spec/01-评审记录.md` 38 条 + `doc/spec/00` §4 参数表
  - 机判: 评审记录 38 条中落实 **1** 条（Q21）、未落实 **36** 条、无法判定 1 条；另报 13 处新算错 + 5 个死锁/假绿场景。领队独立复算：`rob_alloc_t` 140、`rt_t` 531、`uop_t` 238/`wb_t` 80 与评审者逐项一致 ⇒ 采信。已触 §6.1 熔断（3 轮未闭），队列置 fused
  - 未完成: spec/01 v0.2 声称的 6 项核心改动正文无一处落点；**结论：所谓返工未发生，正文仍是 v0.1**
  - 下一手: 等待 R0 裁决返工方式（A/B/C/D 已呈报，人选择 A：先机器算差异表 → 逐条返工 → 纯复核轮）

- [Q-004b] 角色=designer 日期=2026-09-23 轮次=2
  - 产物: `script/width_check.py`（从成员位宽表实算合计、对撞文档声明值；无法单值解析一律记 UNRESOLVED 并拒绝猜测）
  - 依据: ISS-018 根因即"声称相加核对过但从未加过"；可机器判定的量不许靠人算与声明
  - 机判: 20 个 struct 小节 → 一致 2（`ftq_req_t` 141、`fetch_raw_t` 92）；硬不符 3（`rob_alloc_t` 实算140/声明125、`wb_t` 80/79、`rt_t` 531/554）；无成员表 8；成员为复合格式不可解析 7。**即 18/20 当前无人可验证**
  - 未完成: 三个硬不符值的下游引用（`spec/00` §4.8 位量、§3.5 推导链）尚未同步改正；15 个 struct 需补逐成员位宽表
  - 下一手: 按 A 方案推第一批（机械可确定批），每改一处跑 `width_check`；触及 `disp_x_t` 四域与 ISS-020 待定数值时停下出决策包

- [Q-000] 角色=leader 日期=2026-09-23 轮次=2
  - 产物: 子代理编队由 7 份收敛为 5 份——新增 `.qoder/agents/vr1-auditor.md`（合并原 R6 审计与 R7 对抗评审，内分模式 T 技术打靶 / 模式 P 流程审计），删除 `r6-ledger-auditor.md`、`r7-adversarial-reviewer.md`、`r5-failure-investigator.md`；取证纪律并入 `vr1-verifier.md`（四方归因优先序 + 七段骨架 + 自报文件清单）
  - 依据: 人的指令（审核合一、取证归验证）；`script/gate.py` 的 ROLE2AGENT 映射同步更新，`loop` 输出改派 vr1-auditor
  - 机判: 新增 `gate.py check` 两项判据——`handoff-exists`（已开工/已完/已熔断项必须有交接记录且五字段齐）与 `handoff-no-orphan`；本文件即新判据的核查对象
  - 未完成: `doc/AI角色与职责.md` §2 仍按 R5/R6/R7 三个独立职责面描述，与“5 份子代理定义”不一致（一人可持多顶帽子，映射已写进 `AutoQueue.yaml` 头部协议块）；该文件属流程定义，待人批后同步；`vr1-decider` 仍需重开会话才进派发列表
  - 下一手: 领队按 `gate.py loop` 继续推 Q-004 的机械可确定批（`rob_alloc_t`/`wb_t`/`rt_t` 三处声明值改正 + 下游引用同步）

- [Q-004] 角色=leader 日期=2026-09-23 轮次=4
  - 产物: Q-004 由 fused 改回 in_progress（人已选定 A 方案，熔断前提解除）；`gate.py` 删掉自造假阳词“计数脉”（它在 spec/01 里是“计数脉冲”的前三字）
  - 依据: 人的选择（A：先机器算差异表→逐条返工→纯复核轮）；`gate.py check` 实跑输出
  - 机判: check 由 15 PASS / 2 FAIL 收敛到 15 PASS / 1 FAIL（仅剩 width-mismatch，即待返工的实质项）
  - 未完成: **修正一个关键判断——原计划里“先改三处声明值”并不安全**。`rt_t` 实算 531 与声明 554 差 23，而评审 Q4 同时要求新增 `seq[11:0]`（12 bit）——即成员表本身不完整。直接把声明改成实算值，会造出一个“自洽但缺字段”的假绿版本
  - 下一手: 派 vr1-designer 先按评审记录补全字段（Q4 的 seq、Q14 的 `disp_x_t` 四域、Q8 的 `fdirty`），再让 `width_check` 定唯一合计值；需 ISS-020 待定数值的项归集后出决策包

- [Q-004] 角色=leader 日期=2026-09-23 轮次=5
  - 产物: spec/01 v0.2 重建——§2/§3/§4/§5/§6/§7/§8 逐节落实 38 条（27 个 struct 全部表化，行内算式一律废弃）；`spec/00` 同步 5 处（§4.1 `VA_STORE_W`、§4.7 `NMI_EN=0`、§1.2 非目标加 NMI、§4.8 ROB 17,408 / FTQ 2,368 / IQ 16,064）；评审记录 38 条逐条回填“落点”；`width_check.py` 增强（复合位宽求和/乘积/只认合计行/表头识别）
  - 依据: `doc/spec/01-评审记录.md` 38 条处置列；Q-004 队列项；判决“行内算式→表”的结构性要求
  - 机判: `width_check.py` = 27 个 struct **可机器核对 27 / 不符 0 / 未能核实 0**；`gate.py check` = **17 PASS / 1 WARN / 0 FAIL**（width-mismatch 已由 FAIL 转 PASS）
  - 未完成: §6.1 **C2 纯复核轮未跑**；6 项待同步清单（`spec/04`、`spec/06`、`spec/10`、`iss/README.md` §3、`doc/02`、tb 超时）；Q2 处置列写 `ret_pkt` 50 与成员表相加 48 不符——已按成员表取 48 并在正文与评审记录落点里留痕（不追改问题描述列）
  - 下一手: 派 vr1-auditor（模式 T）跑**纯复核轮**：只查旧问题是否真改 + 本轮有无新增；零新增方可关闭 Q-004（RA5）

- [Q-004] 角色=vr1-auditor 日期=2026-09-23 轮次=5（纯复核轮）
  - 产物: 无文件改动（只读）。报告全文已落地到 `doc/spec/01-评审记录.md` 附 B（含 B1~B10 / C1~C10 / S1 与证据行号）
  - 依据: 被审物 `doc/spec/01` + `doc/spec/00` + `iss/README.md`；问题单 38 条；领队提供的两条机判输出（复核者对其数字做了自洽复算：48、27 项合计、64/256 set、16,064、21 拍均复算通过）
  - 机判: 落点抽查 38 条：通过 33 / 部分 3 / **未落实 2（Q3 CSR 链、Q10 A6+needs_ckpt）**；新增候选 6 项（B5~B10）；结论：**未收敛**
  - 未完成: 轮次用尽（5/5）→ 按 RA5 不得默认通过；Q-004 置 fused 并 escalate 待 R0 裁决；新增问题是否计入 C2“新增 0”由 R0/领队裁定，本轮未下结论
  - 下一手: R0 看决策包（A 修完 B/C 类后再批一轮 / B 只修机械项、设计级转 spec/04 设计评审一并定 / C 其他）；一旦获批，领队先修 B1~B10 再跑确认轮

- [Q-004] 角色=leader 日期=2026-09-23 轮次=6（流程）
  - 产物: 队列新增 `attempts` 字段与失败重派规则（派发失败自动重派 ≤2 次，第 3 次置 blocked 转人；每次失败也写交接）；`gate.py` 增 `queue-attempts-cap` 判据
  - 依据: 人问“超时能否自动重启”；已完成两次真实派发失败（一次空返回、一次被取消）
  - 机判: 待 `python script/gate.py check` 验证新增判据
  - 未完成: 仿真 watchdog 超时一律不自动重启（R9/T6 红线），本项不适用于仿真侧
  - 下一手: 等 R0 对 Q-004 的裁决（见上一条）

- [Q-004] 角色=leader 日期=2026-09-23 轮次=6（处置轮，R0 已批“继续直到收敛”）
  - 产物: B1~B10 + C1~C10 逐条处置并落点（评审记录附 C 对账表）：新增 `csrq_entry_t` CSR 写队列 + `type[6]=is_csr` + A12（B1）；`needs_ckpt` 入 ROB 两表 + A6 改持有唯一性（B2）；A2 改安全属性（B3）；BP2 门控含 `R` + 四引理论证（B4）；`icache_rsp` 改 `win_base_pc`+`slot16_vld/start`（B5）；机判输出入 `doc/05-回归记录.md`（B6）；回滚压栈点/`br_mark` 起点/`ROLLBACK_MAX` 参数（B7）；`spec/00` MMIO `len=0` 与 §4.8 ROB 17,664（B8）；门控范围定死（B9）；清零改 lane 粒度（B10）；C1~C10 全落点；S1 未自裁，转 `spec/04`
  - 依据: 评审记录附 B（复核者 B1~B10/C1~C10/S1）；R0 指令“继续直到收敛”
  - 机判: `width_check.py` = 28 个 struct（含新增 `csrq_entry_t` 88 bit）**可机器核对 28 / 不符 0 / 未能核实 0**；`gate.py check` = 17 PASS / 1 WARN / 0 FAIL；新增 `cap_waiver` 机制（无人批准时越限仍 FAIL）与 `attempts`（失败重派 ≤2 次）判据
  - 未完成: 附 C 全部处置均为**作者自证**，必须由第 6 轮纯复核轮独立核验；S1（操作数读点口径）待 `spec/04`
  - 下一手: 派 vr1-auditor 跑第 6 轮纯复核轮（重点：B1~B10 是否真改 + 有无新增；不得再信“落点列”）
