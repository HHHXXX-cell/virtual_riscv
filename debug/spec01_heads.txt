1 # VR1 流水线与级间接口（doc/spec/01）
21 ## 1. 流水级定义
23 ### 1.1 级号与职责
54 ### 1.2 为什么 IBUF 在译码之前（F2 写、D 读）
63 ### 1.3 每拍吞吐
81 ## 2. 类型与位宽约定
108 ## 3. 级间接口（struct 位域）
110 ### 3.1 `ftq_req_t` — `BP3 → FTQ`（写，1/拍）
131 ### 3.2 `ftq_entry_t` — `FTQ → IF`（读，≤1/拍）
143 ### 3.3 `fetch_raw_t` — `IBUF → D`（每拍至多 16 个候选槽，`D` 级消费 ≤4 条）
162 ### 3.4 `uop_t` — `D → R`（4 条/拍）
200 ### 3.5 `rmap_uop_t` — `R → S`（4 条/拍）
221 ### 3.6 `rob_alloc_t` — `S → ROB`（4 条/拍）
249 ### 3.7 `rob_entry_t`（ROB 存储位域，128 项）
282 ### 3.8 `iq_wr_t` — `S → IQ×3`（4 条/拍，带组选）
324 ### 3.9 `iq_entry_t` 就绪位（每条目额外）
344 ### 3.10 `wb_t` — `P15`/`WB` → PRF/ROB/IQ 结果写回与唤醒广播（6 条/拍）
358 ### 3.11 `disp_x_t` — `IQ → 执行单元`（6 条/拍）
384 ### 3.12 `br_resolve_t` — `BRU → 冲刷控制`（1 条/拍）
403 ### 3.13 `mem_req_t` — `LSU/IF → L1D/L1I`（含 `mmu_transl_*` 的下游请求形态）
426 ### 3.14 MMU 翻译接口（req / rsp）
428 #### `mmu_transl_req_t` — `LSU/IF → MMU`（请求）
443 #### `mmu_transl_rsp_t` — `MMU → LSU/IF`（回填）
460 ### 3.15 缺失处理接口（miss / fill / wake）
462 #### `miss_req_t` — L1 → MSHR/L2（缺失请求）
477 #### `fill_t` — L2 → L1（行填充）
493 #### `mshr_wake_t` — `MSHR/L2 → L1D/L1I` 唤醒（9 bit）
506 ### 3.16 cache 响应接口
508 #### `icache_rsp_t` — L1I → IF（取指返回）
526 #### `dcache_rsp_t` — L1D → LSU（数据返回）
539 ### 3.17 `commit_t` — `ROB → CSR / freelist / SQ`（4 条/拍）
564 ### 3.18 异常包与返回控制（`excp_t` 177 bit / `ret_ctrl_t` 49 bit）
566 #### `excp_t` — trap 现场（177 bit）
594 #### `ret_ctrl_t` — MRET/SRET 返回控制（49 bit）
628 ### 3.19 `csr_wr_t` — CSR 写端口（81 bit）
644 #### `csrq_entry_t` — CSR 写队列（`R` 级入队 → 提交拍出队）
670 ### 3.20 执行/访存完成回报
672 #### `x_complete_t` — `EU → WB` 执行单元结果（79 bit）
685 #### `ls_complete_t` — `LSU → LQ/ROB` 访存完成（21 bit）
702 ### 3.21 `rt_t` — 退休 trace（`P17` 输出，验证平台唯一真值接口）
749 ## 4. 反压与停顿
767 ## 5. 冲刷与恢复点
769 ### 5.1 触发源（4 类）
778 ### 5.2 恢复点表（每级各拍必须做的动作，逐字冻结）
799 ### 5.3 冲刷与反压的交互
844 ## 6. 顶层端口表（`vr1_core`）
862 ## 7. 断言（`spec/01` 侧，SVA 写法遵循 `spec/11`）
906 ## 8. 本篇出口自检（v0.2 重建版）
913 ### 8.1 机器可核项（本轮实跑，输出已留档）
924 ### 8.2 人判项（机器看不到，须评审轮核查）
934 ### 新增自检纪律（由 ISS-018 促生，适用于 `doc/spec/*` 全部篇目）