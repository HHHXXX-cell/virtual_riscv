# doc/00 — 问题记录台账（红线 R4）

> 分类口径：**平台** / **代码**（DUT 设计，AI 只记录不改）/ **文档** / **流程** / **验证代码**。
> 发现即登记（状态=待决策/处理中）；解决后在**原条目**回填【已解决】+ 时间 + 方案摘要。
> Sign-off Review 前本表必须无「待决策/处理中」残留。

## 状态总览

| 状态 | 计数 | 条目 |
|---|---|---|
| 待决策 | 13 | ISS-001 / 002 / 007 / 008 / 017 / **023 ~ 030** |
| 处理中 | 12 | ISS-003 / 004 / 005 / 009 / 010 / 012 / 016 / 018 / 019 / 020 / 021 / **022** |
| 已解决 | 6 | ISS-006 / 011 / 013 / 014 / 015 / **031** |
| **合计** | **31** | ISS-023 ~ 030 为 2026-09-22 学习《IC验证知识库》`virtual_test经验` 13 卡后登记的**外部经验缺口**；其中 **ISS-026 已在本次登记过程中实测复现**（见该行证据列），余 7 条为纯外部经验，处置动作＝是否吸收进流程文件，全部待决策。ISS-031 为同期三同步核查发现的**本项目自有缺陷**（已闭环） |

---

## 台账

| ID | 分类 | 发现时间 | 发现步骤/用例 | 问题描述 | 证据 | 状态 | 决策人/决策 | 解决方案 | 解决时间 |
|---|---|---|---|---|---|---|---|---|---|
| **ISS-001** | 平台 | 2026-09-22 | 开工环境探测 | 本机**未安装 git**（`Get-Command git` 空），方案第 11 节第 1 步的 `git init` 无法执行。当前仓库无版本管理，RTL/UVM 代码将无法 diff 追溯，违背 R3「可回溯」的落地便利性 | 探测输出：`git --`（未找到）；无 `scoop`/`choco` | 待决策 | 待定：①随 WSL 安装一并 `apt install git`（推荐，仓库主体在 Windows 侧由 WSL git 管）；②Windows 侧另装 Git for Windows；③暂以目录快照 + `doc/10` 版本表手工管理 | 选 ① 或 ② 后，补 `.gitattributes`（tcl/do/bat 统一 LF）并首提交 | — |
| **ISS-002** | 平台 | 2026-09-22 | 开工环境探测 | **WSL 无发行版**（`wsl -l -v` 返回"未安装适用于 Linux 的 Windows 子系统"），因此无 riscv64 GCC、无 Spike、无 Verilator；本机亦无 gcc/g++/make/cmake/JDK。测试程序的编译环节与 ISS-vs-ISS 互检当前全部阻塞 | `wsl -l -v` 报错并提示 `wsl --install`；`riscv64-unknown-elf-gcc --` / `spike --` / `verilator --` | 待决策 | **需人执行**：安装动作需管理员权限与一次重启，AI 不能代做。清单见 `doc/环境搭建.md` | 按 `doc/环境搭建.md` §1~§5 执行后，回填 M0 各命令版本号 | — |
| **ISS-003** | 文档 | 2026-09-22 | Step 1 规格（`doc/spec/00` 参数表自检） | **PRF 容量与书式最坏界冲突**：【超标量】p.187-188 给 `inflight_max = wdw + nEU + nLQ + nSQ`。按 VR1 基线代入 `4 + 9 + 48 + 32 = 93`，而参数表定的整数 PRF 只有 **64 项**，小于该界。经验关系 `wdw ≤ nr ≤ nROB`（`4 ≤ 64 ≤ 128`）是满足的，但**必须给出"PRF 耗尽只反压、不死锁"的论证**，否则是个真 bug 苗子 | `doc/spec/00-总体规格.md` §4 参数表；出处 `Verilog与CPU设计知识库/CPU卷/04` 第 4 章「数量」维度 | 处理中 | 待定：PRF 扩到 96 / 保持 64 并补回收时机论证 / 缩小 LQ-SQ | 在 `doc/spec/04-重命名与PRF.md` 写清：①条目回收时机=提交（融合 PRF 只改状态不搬数据）；②PRF 空 → 反压分发而非停顿提交通路；③形式化不变式 `Σ未提交目的寄存器数 ≤ nPRF`，并列为 SVA 与功能覆盖项；④给出端口数推导（读 ≥ wdw×3=12、写 ≥ 发射宽度 6，面积按端口平方律） | — |
| **ISS-004** | 平台 | 2026-09-22 | Step 3 计划（激励选型） | **riscv-dv 的 `yaml/simulator.yaml` 无 ModelSim 条目**，官方只列 vcs / ius / questa / dsim / qrun；本机唯一 HDL 仿真器是 ModelSim SE-64 2020.4，直接 `--simulator questa` 会因 `vmap mtiUvm $QUESTA_HOME/uvm-1.2` 与 `qrun`/`-outdir` 语法差异失败 | `D:\IC验证知识库\_tools\riscv-dv-master\yaml\simulator.yaml`（grep 确认无 msim/modelsim 条） | 处理中 | 两条腿走路（已写进 AGENTS.md §3.3）：主用 **`pygen` 纯 Python 生成路线**规避"生成器本身要仿真器"；同时自加 `modelsim` 条目（参照 `questa` 条改写为 `vlog -64`/`vopt`/`vsim -64 -c`） | 自加条目后在 `run_cmd/riscv_dv_target/` 留下可用记录，并以 `+ntb_random_seed` 打通种子复现 | — |
| **ISS-005** | 文档 | 2026-09-22 | Step 1 规格（Sv39 章节可用性核查） | 知识库《RISC-V专项/03-特权架构与机器模式速览》明示"**§4.4+ Sv39/48/57 仅综述**"，即本地笔记**没有** Sv39 细节；VR1 要做 Sv39，`doc/spec/08` 不能只依赖该笔记 | `RISC-V专项/03` 第 24 行「本速览覆盖声明」 | 处理中 | 直接以规范全文为准：`_tools\extracted\riscv_spec\riscv_spec_full.txt` 定位 §4.4 Sv39 原文 + `_tools\riscv-spec.pdf` 卷 II 复核 | 写 `doc/spec/08` 时逐条带 §号引用，出处注明"规范原文（非笔记）"；同步在知识库《RISC-V专项》补一篇 Sv39 精读（由人决定是否入库） | — |
| **ISS-006** | 平台 | 2026-09-22 | Step 3 计划（ISS 耦合方式） | ModelSim SE 在 Windows 侧做 **DPI-C 需宿主 C++ 编译器**，本机无 gcc/MSVC/msys2 → golden ISS 不能以 DPI 方式挂进仿真器 | `gcc/g++ --`、无 `C:\msys64`、无 VS 目录（探测记录） | 已解决 | 设计决策：改为**文件级解耦** | RTL 提交级 trace 由 tb 内 monitor 落文件 → `iss/tools/` 归一化为标准 CSV → 独立 Python 比对脚本；避免 DPI 依赖，且比对逻辑可复用于 Verilator 侧。已写入 `AGENTS.md` §3.4 坑位 1 与方案第 1 节 | 2026-09-22 |
| **ISS-007** | 文档 | 2026-09-22 | Step 1 规格（规范版本口径） | 本地规范为**特权 1.13**（含 `menvcfg`/`mseccfg`/双 trap 16 号等新增项），而参考实现口径不一：ibex 按 1.11、笔记提到 picorv32 中断**完全不对应特权规范任何一节**。照抄参考实现会引入版本混杂 | `RISC-V专项/03` 第 5 行（1.13）与第 124 行（picorv32/ibex 两套词汇表） | 待决策 | 待定：VR1 锁 1.13 全量 / 锁 1.11 子集（便于对照 ibex） | 在 `doc/spec/10-特权与中断异常.md` 开头立"规范版本声明"表：逐 CSR 标注引入版本与是否实现；`doc/01` 功能点全部回指该表 | — |
| **ISS-008** | 流程 | 2026-09-22 | Step 1 规格（评审门语义） | 九步流程假设"规格是外部输入"，本项目 spec 由 AI 自产 → **Requirement Review 存在作者=被评审人**的自证风险，红线 R6/R8 的诚实性前提被削弱 | 方案第 7 节阶段图；《AI验证工作流》1.1 节角色边界 | 待决策 | **需人明确**：Review G1 必须由人逐篇评审签署（尤其 `spec/10` CSR 表、`spec/04` 端口数、`spec/07` forwarding 规则三处），不接受 AI 自评通过 | 评审结论按"通过/有条件通过/不通过/暂停"四级落盘到各 spec 篇首 + `doc/00`；冻结签署写 `doc/spec/14`。**2026-09-22 缓解方案升级**：除人逐篇签署外，新增（a）R0~R7 角色读写边界与 RACI（b）信息隔离的对抗评审子代理 R7（c）关键结论必绑外部锚点——详见 `doc/AI角色与职责.md`；已写入流程文档 §9.2 第 5 条与 `AGENTS.md` 规则 19~21 | — |
| **ISS-009** | 平台 | 2026-09-22 | Step 4 平台（构建入口） | 本机**无 make/cmake**（Windows 侧），而知识库模板与方案均以 `make regress` 为回归入口；直接写 Makefile 在 PowerShell 下不可用 | `make --`、`cmake --` | 处理中 | 入口改为 **Python driver**（`script/regress.py`，Python 3.14 已在）+ `run_cmd/*.ps1` 薄封装；Makefile 仅在 WSL 侧用于 Spike/Verilator 构建 | 平台搭建时交付 `script/regress.py`（含 R9 watchdog、R10 并发与独占沙箱、T1/T5/T6 终止判据），并在 `AGENTS.md` §3.1 命令区同步 | — |
| **ISS-010** | 文档 | 2026-09-22 | Step 1 规格（`doc/spec/00` §2/§8） | **非对齐访问两侧口径与方案示例命令不一致**：方案第 1 节给出的 Spike 命令带 `--misaligned`，而 `spec/00` 定下的口径是一期**不支持非对齐**（报 address-misaligned）。若 ISS 开了 `--misaligned` 而 RTL 不开，非对齐激励会产生“ISS 能跑 / DUT 报 fault”的**假 DUT bug**。现改口径：一期 `MISALIGNED_EN=0`，两侧**均不开** `--misaligned` | `spec/00` §2 口径统一段、§4.7 `MISALIGNED_EN=0`；与 `AGENTS.md` §3.2 原示例命令对照 | 处理中 | 技术决策已定（以 spec 为权威源，同步修订 AGENTS.md 示例）；若三期要支持硬件非对齐拆包，需重过 Requirement Review | 已同步修订 `AGENTS.md` §3.2 的 Spike 行（删除 `--misaligned`）；`iss/` 与回归脚本的 Spike 模板一律不带该选项 | — |
| **ISS-011** | 验证代码 | 2026-09-22 | Step 1 规格（`spec/01` §3.20 草稿自查） | `spec/01` 初稿把 `ls_complete_t.lqid` 写成 5 bit，但 `spec/00` 定 `LQ_ENTRIES=48`（非 2 的幂）→ 索引需 **6 bit**（环形指针 7 bit，见 `spec/01` §2 `lqptr_t`）。位宽不足会造成 LQ 回抄错项一类的隐蔽 bug | `spec/01` §2 类型表 vs §3.20 字段表 | 已解决 | 自查发现，属规格文档自身位宽错误，直接修正 | 已改 `lqid[5:0]` 并在 `spec/01` §3.20 写明依据；`spec/01` §8 自检表同步 | 2026-09-22 |
| **ISS-012** | 文档 | 2026-09-22 | Step 1 规格（`spec/01` §1.1 时序推论） | **ALU→ALU 最短依赖间距 3 拍与 IPC ≥ 1.8 目标存在张力**：`spec/01` 采用“后读 + 唤醒/选择分离”（WB 拍广播唤醒、次拍选择发射，依据【超】p.221-223），代价是串行依赖链每拍只能推进一步；若乱序窗口填不满空洞，IPC 会系统性低于目标 | `spec/01` §1.1“关键时序结论”段；`spec/00` §9 IPC 目标 | 处理中 | 待 `spec/13` 周期模型给出定量 IPC 再定；候选回炉：① 唤醒同拍完成选择（2 拍，时序风险）② 改前读 + 加宽 PRF 读口（面积代价，与 ISS-003 相互牵连）| 已写入 `spec/00` §10 风险表与 `spec/01` §1.1；`spec/13` 交付时必须给出两方案 IPC 对比数据供人裁决 | — |
| **ISS-013** | 文档 | 2026-09-22 | Step 1 规格（ISS 实现时发现） | `doc/spec/01` §3.21 的 `rt_t` 只有 `csr_old`，而 Spike commit log 与 riscv-dv trace CSV 的 `csr` 字段记的是**写后新值**。只给旧值时 CSR 行为无法直接对查，只能靠后续读回间接推，会漏掉 csrrw 写错值这类 bug | `iss/vriss/trace.py` 与 spike_log_to_trace_csv.py 对照 | 已解决 | 以规格为修正对象（不是改检查器迁就） | `spec/01` §3.21 新增 `csr_new`（总宽 490→554 bit）；`trace.py` 的 `csr_field()` 改报新值；`trace_compare.py` 默认比对列含 `csr` | 2026-09-22 |
| **ISS-014** | 验证代码 | 2026-09-22 | ISS 冒烟首跑 | `machine.py` 把 I/S/B/J 四类立即数统一用“按 bit63 判符号”的 `_s()` 扩展，**没有按各自位宽（12/12/13/21）扩展** → 所有负立即数（`ADDI rd,rs1,-1`、负偏移访存/分支）全错；实测 `x9=0xfff` 而非 `0xffff_ffff_ffff_ffff` | 冒烟输出 `x9 = 0xfff，期望 0xffffffffffffffff` | 已解决 | 自查发现，属 ISS 自身错误，直接修正 | 改用 `consts.sext(v,bits)` 逐类扩展；`ADDI(9,0,-1)`、`BEQ`、`JAL` 均转为正确 | 2026-09-22 |
| **ISS-015** | 验证代码 | 2026-09-22 | ISS 冒烟二跑 | `encode.py` 对超范围立即数做 `& mask` **静默截断**：`ADDI(rd,rs1,0x1000)` 编成 `+0`（十进制 12 位字段装不下）。表现为“程序不停机”这种极难归因的假故障；后续手写定向用例时必再踩 | 冒烟：改 TOHOST=0x4000_1000 后仍不停机；另 `0xDEAD` 也被静默截断 | 已解决 | 技术决策：宁可报错不可静默 | 加 `_chk(val,bits,name)` 范围检查（I/S 12、B 13、J 21）+ B/J 偶数偏移检查；`LUI/AUIPC` 统一改为接 20 位高位立即数（消除量纲歧义）；检查当场拦下两处自身用例错误 | 2026-09-22 |
| **ISS-016** | 文档 | 2026-09-22 | ISS 冒烟三跑 | **tohost/signature 窗无后级支撑**：`consts.PMA_TABLE` 把 `0x2000_0000` 窗标为 non-cacheable 且未挂设备，对其做普通 store 会正确报 store-access-fault（模型行为合规，但会让所有用 `tohost` 收班的 bare-metal 用例全部进 trap）。需在 `doc/spec/09` 明确：该窗必须挂 signature sink 设备，且 `sw/env` 的 tohost 段必须落在有支撑区 | 冒烟首轮 `mcause=1, mtval=0x100` 死循环 trap 的排查记录 | 处理中 | 暂时把 tohost 改到 DRAM 区（`0x4000_0010`）跑通；正式方案待写进规格 | `doc/spec/09` 补“signature/tohost 窗设备挂载 + PMA 属性”一节，`sw/env/link.ld` 的 tohost 段位置与之绑定 | — |
| **ISS-017** | 文档 | 2026-09-22 | 学习知识库（《开源IP导读/03-ibex架构导读》+ `_tools\ibex-master\rtl\ibex_top_tracing.sv` 实测） | **退休接口选型：自定 `rt_t` 还是 RVFI 兼容接口**。本地 ibex 源码已确认采 RVFI 标准集（`rvfi_valid/order/insn/trap/halt/intr/mode/ixl/rs1-3_addr+rdata/rd_addr/rd_wdata/pc_rdata+wdata/mem_addr/rmask/wmask/rdata` + `csr_*`）。`doc/spec/01` §3.21 目前的 `rt_t` 是自定名，字段语义与 RVFI 高度重叠但不完全对齐 | `ibex_top_tracing.sv` L143-L169 信号声明；`doc/spec/01` §3.21 | 待决策 | **建议采纳 RVFI 兼容超集**（保留 `rt_t` 名字与现有字段，另按 RVFI 命名对外映射一层），理由：① 可直接喂 riscv-formal（《AI验证工作流》不适用清单里的“形式验证”短板得以补齐）；② 复用现成 RVFI→CSV/比对工具；③ ibex 已验证该接口在 RTL 侧可落地。代价：`spec/01` §3.21 需升版并重过 Requirement Review | 已写入 `doc/项目开发流程.md` §2.4、§10 第 1 项；等人拍板后改 `spec/01` | — |
| **ISS-018** | 文档 | 2026-09-22 | 流程试点：R7 信息隔离对抗评审首轮（针对 `doc/spec/01` v0.1） | **作者在 `spec/01` §8 出口自检表写下「合计值已逐项相加核对 ✅」等四项 ✅，但当时从未执行过相加**。独立评审实测：13 个 struct 里 12 个合计与成员位宽之和不符（差 1~34 bit 不等，作者已独立复算确认评审者全对：`uop_t` 实 238/写 251、`rob_alloc_t` 实 140/写 125、`rob_entry_t` 实 135/写 122、`rt_t` 实 531/写 554 而 §6 又写 490）。同时共报 **21 项阻塞 / 15 项需澄清 / 3 项建议**，含三类真漏洞：回滚与 ROB 释放互相拆台（Q17）、CSR 写数据通路在冻结接口上断开（Q3）、trace 只有 VA 导致翻译/PA 错误两侧都看不见（Q19） | `doc/spec/01-评审记录.md` 全文；作者复算记录于本条 | 处理中 | 技术决定：① `spec/01` 降为 v0.2 返工中，**不再是 G1 冻结候选**；② §8 自检表整体作废并保留更正痕迹；③ 新增自检纪律——**无复核手段不得打 ✅**，可机器判定的事（位宽相加/参数名一致/值域引用）一律写脚本；④ 下一轮对抗评审**先查 C 类（死锁/假绿/漏测）**，算术错反而不是最危险的 | 返工完成 + 位宽自检脚本输出留档后转【已解决】（登记于 `doc/spec/01` §8） | — |
| **ISS-019** | 验证代码 | 2026-09-22 | 第二轮 R7 对抗评审（目标：golden ISS 语义），评审者实跑探针脚本取证 | **golden ISS 共 31 项问题（会掩盖 bug 15 / 会导致错判 16）**。最致命三项：① `riscv-tests` 的 PASS/FAIL **判定方向相反**（`halt_code=data>>1` 与 `==1` 判通过 ⇒ 写 1 报失败、写 3（=test 1 失败）报成功）；② `MRET/SRET/WFI` 三条编码与官方 `encoding.h` 不一致 ⇒ 工具链编的 `mret` 被当成 SRET 抛异常，而自造编码能跑（所冒烟“假绿”的真成因）；③ trace CSV **完全没有 `exc_v/exc_cause/mem_*` 列** ⇒ 异常类别在整个比对链上不可见。另：SRA 算成 SRL、SRAI/SRAIW 判非法、LBU/LHU 读 16/32 字节、MULW 族全判非法、`jal x0,0` 自旋被误推一条 | 评审探针输出（逐条带实跑值）；锚点 `_tools\ibex-master\vendor\riscv-test-env\encoding.h` 与规范提取件 | 处理中 | 技术决定：本轮只修“会反转红绿灯”的 8 类，其余按整批返工，不做零敲碎修 | **已修并回归**：tohost 判据方向、MRET/SRET/WFI 编码、SRA/SRAI/SRAIW、LBU/LHU/LWU 宽度与对齐、MULW 族、BRANCH 保留编码 KeyError、`jal x0,0` 自旋、MPP 保留值崩溃、`link()` 空洞改填非法字。**新建 `iss/tests/test_isa_semantics.py`：23 项断言全过（官方指令字写死为常量，避开“自编自解”自洽）**；余 13 类列为 `KNOWN_OPEN` 并随每次运行打印，**关闭前 ISS 不得作为 golden model 参与签核** | — |
| **ISS-020** | 文档 | 2026-09-22 | 第二轮 R7 对抗评审（目标：`doc/spec/00` 参数表与资源约束），共 35 项 | **参数不自洽 / 不可达类 16 项阻塞**，关键四条：① 融合式 PRF 64 项中 **31 项被架构常驻值钉住** ⇒ 可投机写回只剩 **33** 项 ⇒ `LQ=48`、`ROB=128` **不可达**，且 IPC≥1.8 与参数量级矛盾；② `FWD_MAX_SOURCES=2` 在“store 只在提交后写 cache”下是**真死锁**，而 §9 把它当成“截断次数”统计量（评审原话：把死锁登记成指标）；③ `FWD_CMP_BITS=12` 不比页号/PPN ⇒跳页同偏移误转发，LR/SC 同病；④ §4.8 SRAM 表 9 行中 6 行不符或不可复算，§6 冻结清单里 **14 个类型在 spec/01 无任何定义** | 评审逐项重算；本地规范提取件实测版本为 **20260922 Intermediate**，非“卷 II 1.13” ⇒ 全库【权】§号锚点版本口径不实 | 处理中 | 需人定两个数：PRF 扩到多少（建议 ≥ `ROB − 架构常驻 31 + 裕量` ≈ 100）与 forwarding 源数（建议按最大重叠 8 或改为字节拆分归约）；同时定【权】锚点版本基线 | 待写入 `spec/00` v0.2 升版：PRF/ROB/LQ/SQ/FWD 四组数值重定 + 死锁排除规则上正文；`spec/00` §4.8 全部改成可复算行（禁 `≈`/`…`）；ISS-003 原“保持 64 十补论证”结论**推翻** | — |
| **ISS-021** | 流程 | 2026-09-22 | 同上（对第一轮处置的回头核查） | **第一轮评审记录的“处置”列本身不可信**：已批的三个修正（`VA_STORE_W`、`rob_entry_t.fdirty`、ROB 位量 15,616→17,408）**一个都没落到正文**；且两条处置自身是错的：写“`exc_v=1` 条目仍逐条比”但 CSV 压根没有异常列（不可执行）；写“IALIGN=32 下 `mepc[1]` 同理”但一期使能 C 使 IALIGN=16，该前提不成立 | `spec/00` §4.8 与 `spec/01` §3.7 正文 vs `spec/01-评审记录.md` Q8/Q22 处置行 | 处理中 | 技术决定：处置列也要有“落实证据”才能算闭环 | 新增硬规则：**处置分为“已接受”与“已落实”，只有附落点（文件+节号/脚本输出）的才算闭环**；`doc/09` 一致性清单增加“评审处置落实率”一行；本条与 ISS-018 同源（无手段的自证） | — |
| **ISS-022** | 流程 | 2026-09-22 | 多 AI 角色可执行性验证轮（R7 两轮评审后的边界核查） | **子代理的“只读”约束仅靠提示词承诺，无机械保障**：两名 R7 评审者在仓库 `script/tmp/` 新建了 **34 个探针与输出文件**（提示词明写“不得修改仓库文件”）。更危险的是 `script/tmp/` 被 `.gitignore` 排除 ⇒ **评审证据（ISS-019/020 的全部实跑凭据）会随清理丢失**，直接违背红线 R3「结论可回溯到具体 log/代码行」 | 目录清单比对（登记 23 个文件 vs 实际 57 个，多出 34 个全在 `script/tmp/`）；mtime 窗口比对证明**无任何既有文件被改** | 处理中 | 技术决定：写禁令必须配机械验收，不能只写在提示词里 | ① 证据升格：29 个迁 `iss/tests/probes/`、5 个迁 `doc/spec/01-评审探针/`（移出忽略区）；② `AGENTS.md` 新增规则 22：**每轮评审后跑“仓库清单比对 + mtime 窗口”核查并记 `doc/05`**；③ `doc/AI角色与职责.md` §8.3 已将该障碍写为可执行性结论的一部分 | — |
| **ISS-023** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/02`） | **R1 缺「受控放宽」第三条路**：`AGENTS.md` §5.1 现口径为「G1 冻结后 `rtl/` 绝对只读、修 RTL 永远是人在人的明确指令下的单独动作」，无任何受控放宽模式——一旦接口冻结后遇到「缺陷必须改 RTL 才能继续验证」，流水线只能停等。知识库给出可复制的**五约束协议**：① 任务限定给单一角色 ② 依据前置（无规格/台账依据不改）③ 两道客观门＝编译 0 错 + `not bound` 计数 0，且须与基线对照（实测形态「mismatch 4→0」），**没过门不得宣布完成** ④ diff + 时间留痕 ⑤ 只落本地副本、不回写不自动提交。配套工程点：`rtl/` 必须是**独立实体拷贝**（若为 junction 会污染设计方仓库），解联要守卫式（先验 REPARSE 位 + 目标路径、非递归删除、复验文件数不变），拷贝后逐文件 md5 留证 | 出处 `D:\IC验证知识库\virtual_test经验\02-R1受控放宽-RTL修改协议.md`；对照 `AGENTS.md` §5.1（全文无该模式） | 待决策 | 待定：VR1 是否采纳。一期 G1 未达，暂不紧迫；但基线机制一旦触发就需先定好 | 若采纳：写入 `doc/项目开发流程.md` 门禁节 + `AGENTS.md` §5.1 细化；**必须与 R5（基线后先批后改）合并口径**，避免两条规则互相打架 | — |
| **ISS-024** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/05`） | **基线指纹判据过粗，会被纯 mtime 漂移刷红**：规则 22 只规定「仓库清单比对 + mtime 窗口」核查。知识库实测：拷贝/同步/junction 改实体只动 mtime，整体比较三元组会触发假「修改 448 项」（另 same 49，**内容实际零变更**）。给出**三方对比定性法**：now-vs-base 全为 mtime-only 且 src-vs-base 全为 same ⇒ 假告警；一旦出现 content-diff ⇒ 真变更，停报。**禁止报红就刷基线**；刷基线正当程序＝定性→备份→init-baseline→check 复验（目标告警消失且其余计数不变）→登记。VR1 当前「未基线」，正是定义基线指纹判据的窗口期 | 出处 `virtual_test经验\05-门禁完整性基线实务.md`；对照 `AGENTS.md` 规则 22 与本表 ISS-022 | 待决策 | 待定：基线指纹是否改为「`md5:size` 才计修改、纯 mtime 单列 INFO」（知识库自身亦标注该改动属「监督通道自身改动，须人批」） | 若采纳：写入 `doc/10-基线记录.md` 模板与规则 22 核查条款；换机后（`doc/交接说明.md` 校验命令）首跑必做一次三方对比定性 | — |
| **ISS-025** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/13`） | **`vsim -do` 三种静默假成功，EXIT 码不可作判据**：① `-do "..\x.do"`（双引号+反斜杠）→ 反斜杠被 Tcl 吞 → `invalid command name`，0 条命令执行；② `-do "../x.do; quit -f"`（裸路径）→ 被当外部程序 → `couldn't execute...no such file`。两种情况 **EXIT 均为 0**，日志仅 137/163/247 字节、无 `-- Compiling` 行。若以退出码判「编译成功」，会把环境故障送进归因链伪装成 DUT bug（正是 R8 要防的「跑完没 error = 通过」）。可靠写法：`-do "source ../script/build/x.do; quit -f"`（**显式 source + Tcl 系内一律正斜杠**）；判据改用**产物证据行**（`-- Compiling` 行数 + 段末 `Errors: N`）；`-quiet` 与取证互斥（吞掉逐单元回显）。VR1 §3.1 命令模板与 `script/regress.py`（ISS-009）尚未建立，属**必须先吸收再写脚本**的前置项 | 出处 `virtual_test经验\13-vsim批处理do加载姿势与假成功.md`（该库最新一张）；`AGENTS.md` §3.1 现模板未涉及 | 待决策 | **建议采纳**（纯技术判据，不放宽任何检查口径，不触 R6/R7） | 建 runner 时写入 `AGENTS.md` §3.1 与 `doc/项目开发流程.md` 平台交付判据；`regress.py` 的 PASS 判定必须含「产物证据行」正则，且与 ISS-009 交付同批 | — |
| **ISS-026** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/08`） | **编辑工具会静默替换中文形近字并注入零宽字符，且 diff 视图不可信**：实测形近字替换（U+9AA8「骨」→ U+9AB8/9AB7）与伪空格注入（U+200B/200C/200D/FEFF/00A0/3000），且 **diff 里「新旧都错」**——靠看 diff 自检发现不了。VR1 主体产出是 14 篇中文规格 + `doc/01~10`，而 ISS-018（作者自检打 ✅ 但从未相加）与 ISS-021（处置列自身不可信）已两度发生「无手段的自证」危机，同属一类。防线：批量写中文后**扫磁盘字节**（形近字表 + 零宽 + CJK 扩展区），归零才算完成；关键标识用 `\uXXXX` 码点直写；**独立回读不信 diff 视图**；跨库同步前后各扫一遍 | 出处 `virtual_test经验\08-AI中文文档码点防线.md`；本项目同源风险见 ISS-018 / ISS-021。**2026-09-22 登记本条时实测复现**：写完 ISS-023~030 后扫磁盘字节，发现 **4 处静默错字**（3 处「扫」U+626B →「扇」U+6247，1 处「环境」→「环环境」多出字符），而 **diff 视图未暴露**；同时 `✅` 在 PowerShell 控制台显示为 `?` 但文件内 5 处 U+2705 完好——**反向坐实“必须扫字节、不信视图”**；零宽字符扫描为 0。后续追加 ISS-031 时**再犯 2 处**（「脱钩」误作「脉钩」、「缺陷」误作「脉陷」），经码点复核 **至少这 2 处系产出方生成时选错码点，不能归因于工具静默替换**——因此本条结论修正为：**无论成因是工具替换还是作者选错码点，人眼与 diff 视图都拦不住，只有落盘后的字节扫描能兜**（“建议采纳”的理由由「工具会替换」改为「产出方无法自证无误」，论证更强而非更弱） | 待决策 | **建议采纳**（成本≈一个自检脚本，收益覆盖全部中文产出） | 若采纳：脚本进 `script/`，与 ISS-018 处置项③的位宽自检脚本**同批交付**；规则并入「打 ✅ 前必跑脚本」硬纪律（ISS-018 ④） | — |
| **ISS-027** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/07`） | **ModelSim 泛参覆盖存在两种完全无感的失效形态**：`-g<name>`（裸名）只覆盖「未显式 map 传值」的实例；`-G<路径>/<name>` 才连显式 map 一并覆盖、可穿透深链。失效侧：路径写错仅一条警告 `vopt-3040` 后继续；**类型不匹配完全静默，0 警告 0 报错**。生效验证法＝正向微探针三态 + **反向注入错误值 → 预期成批 `not bound`**（实测 `-G/.../TECH=0` → 1753 条 `vsim-3473`）。VR1 将以 `doc/spec/00` §4 参数表做配置扫描（`MISALIGNED_EN`、`LQ_ENTRIES` 等），不配这套验证 = 扫的是「以为的配置」而非实际配置 | 出处 `virtual_test经验\07-ModelSim泛参命令行覆盖.md` | 待决策 | **建议采纳为平台纪律**：每加一个可覆盖参数配一次反向注入；覆盖项固化进 `.do` 作单一真源 | 随 ISS-025 一并写入 §3.1；参数扫描矩阵在 `doc/03-验证计划.md` 中列出 | — |
| **ISS-028** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/11`） | **「行为一致但超时」须先查日志量，勿先怀疑 DUT**：根因为 `numeric_std` metavalue 警告洪流——每轮 113,200 行 / 19.3 MB；在 `vsim -do` 序列**最前**加 `set NumericStdNoWarnings 1;` 后实测单模式 74.2→30.6 s、日志 19.3→0.65 MB、批 208.9→97.8 s、6/6 PASS。定性：这是**日志开关而非检查器放宽**（不触 R6/R7），但抑制后必须复核证据行 / bins / 结论不变。关联风险：R9 规定 ModelSim 挂钟上限 20 分钟/用例，警告洪流会把它伪装成 watchdog 超时并按 R2 停报，属可避免的空转 | 出处 `virtual_test经验\11-仿真日志噪声治理.md`；对照 `AGENTS.md` 规则 9 | 待决策 | **建议采纳**，附「抑制后复核证据不变」的自检要求，防止退化为变相降噪过关 | 建平台时写入 `AGENTS.md` §3.1 仿真命令行；`doc/05` 记抑制前后各一例的日志体积与结论对照 | — |
| **ISS-029** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/06`） | **R10 终止判据缺「环境故障」档，许可/加载失败会被算成用例 FAIL**：VR1 现有 T1/T5/T6 覆盖编译加载失败计数、FATAL 占比、watchdog 超时，**全部计入用例结果**。知识库做法：**两张词表故意不交叉**——许可专串表（`License Issue` / `Failure to obtain...simulation license` / `Cannot find license file` / `Invalid host.`）vs 通用表（含 `Error loading design`）；`Error loading design` **故意只入通用表**（许可失效与编译错误都打这条，入许可表必误判），判定顺序先许可后通用；批级 `ENV_BLOCKED` 优先级高于 `ABORTED`，退出码 5。另：开跑前做隔离库许可预检（`sim/_licprobe/`），**预检自身不可执行时打印 UNVERIFIED 放行而不阻塞全批**；唯一可靠判据是「实跑能否正常 `$finish`」，禁用 hostid 静态判据；重试 1 小时 1 次限量、运行器不 sleep（交一次性任务承载） | 出处 `virtual_test经验\06-仿真许可与环境级阻塞判据.md`；对照 `AGENTS.md` 规则 10 判据表 | 待决策 | **建议采纳**（ISS-001/002 未决期环境本就不稳，误判成本高） | 新增 `ENV_BLOCKED` 档与退出码写入规则 10 判据表；两词表与预检逻辑由 `script/regress.py` 实现 | — |
| **ISS-030** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/03` + `/12`） | **规则 19~22 只有角色声明，无机器门禁通道**：知识库把「AI 说做完了不可采信」做成工具——`agent_gate.py` 四子命令 `init-baseline / check / dispatch / report`；缺口须过四道校验才转指令，未过的进「需人裁决队列」（只报不做）；**熔断＝同一指令累计 >3 轮未闭则置 FUSED 挂人**；核销只认机器判据。防假阳性细节：标注即豁免（词形清单）、`check` 与复核器**共用同一判据函数**（防两套逻辑漂移）、裸数字加边界 `(?<!\d)1200(?!\d)`、台账状态列 ≤60 字符。审计方硬约束：只采信文件原文、每条结论带 `文件:行号` 逐字命中、**出现模糊词即判审计无效**、与被审方无共同上下文。另有 Auto Queue（每项＝ID / 工作项 / 归属角色 / 依据(文件:行号) / 可机判完成判据 / 状态）与修复 V 型环（发现→判定+最小修复→换角色独立复验→断言转正→基线刷新→台账闭环）；涉评审门 / waiver / 基线签核的结构改动**不入队列**，转「批量批准包」 | 出处 `virtual_test经验\03-多agent监督与门禁通道.md`、`\12-多agent自动流水线.md`；对照 `doc/AI角色与职责.md` §8.3 与 ISS-022（已实测发生过一次只读违反） | 待决策 | 待定：VR1 是否建 gate 工具。当前靠规则 22 手工核查，且已被证伪一次 | 若采纳：工具进 `script/`，核查记录格式写入 `doc/05`；与 ISS-026 码点扫描、ISS-018 位宽自检合为**「机械验收三件套」** | — |
| **ISS-031** | 文档 | 2026-09-22 | 登记 ISS-023~030 后的三同步核查 | **看板与台账计数脱钩**：`doc/04` §②「近期进度」行写「问题台账 已记 **12** 条（4 待决策 / 6 处理中 / 2 已解决）」，而 `doc/00` 实际已有 **22** 条（5 / 12 / 5）——差额 10 条正是上一会话 ISS-018~022 评审轮期间新增的条目，当时未回写看板。违背规则 11「状态变更即记」与三同步纪律；危害是换机接手人（走 `doc/交接说明.md`）会按看板**低估开放问题量**，尤其低估 13 条待决策 | `doc/04` L55 vs `doc/00` 状态总览（本次登记前 22 / 后 31） | 已解决 | 自查发现，属文档同步缺陷，直接修正 | ① 本条登记（分类=文档，当场闭环）；② `doc/04` L55 计数回填至与台账一致；③ **本条是 ISS-030「机械验收三件套」的第 3 个实证**（前有 ISS-021 处置未落实、ISS-022 只读未核查）——建议将「台账计数 / ID 连续性 / 看板一致性」纳入机器判据脚本 | 2026-09-22 |

---

## 待决策清单（需人拍板，AI 不自行推进）

| ID | 决策点 | 影响面 | 建议 |
|---|---|---|---|
| ISS-001 | 是否装 git（及装在哪一侧） | 全仓库版本管理与 diff 可追溯 | 随 WSL 一并 `apt install git` |
| ISS-002 | **WSL2 + 工具链安装（需管理员 + 重启）** | M0/编译/ISS 互检/性能测试全阻塞 | 按 `doc/环境搭建.md` 一次做完 |
| ISS-003 | PRF 64 项 vs 书式最坏界 93 | 乱序核心容量与死锁论证 | 保持 64，由 `spec/04` 补反压不变式与 SVA 论证 |
| ISS-017 | 退休接口是否兼容 RVFI | 形式验证（riscv-formal）与现成工具链能否白捡 | **建议采纳**：`rt_t` 保留，另加 RVFI 命名映射层 |
| ISS-012 | ALU→ALU 3 拍依赖间距 vs IPC ≥ 1.8 | 整个后端微架构选型，也决定本项目能否达到宣传的性能 | 先写 `spec/13` 周期模型定量，再定是否回炉 2 拍方案 |
| ISS-007 | 特权规范锁 1.13 还是 1.11 | CSR 表规模、Svade/PMP 细节 | 锁 1.13（与本地规范文本一致），实现分期 |
| ISS-008 | Review G1 由人逐篇签署的安排 | 规格质量门禁的有效性 | 三处高风险篇目优先人工精读 |
| ISS-025 / 027 / 028 | 三项 ModelSim 平台判据是否采纳（`-do` 假成功与产物证据行判定、泛参覆盖生效验证、`numeric_std` 日志噪声治理） | 回归 runner 的正确性；不采纳则环境故障会伪装成 DUT bug、参数扫描会扫错配置、警告洪流会伪装成 watchdog 超时 | **建议全部采纳**，随 Step 4 平台交付一并写入 §3.1 |
| ISS-026 | 中文文档码点扫描防线是否成为交付前置 | 全部中文 spec 与 `doc/` 产出的完整性防线（已两度发生「无手段自证」：ISS-018/021） | 建议采纳，与 ISS-018 位宽自检脚本同批交付 |
| ISS-029 | 是否新增 `ENV_BLOCKED`环境级阻塞档与许可词表 | 回归判定口径（R10）；环境故障不再计入用例 FAIL | 建议采纳：批级优先级高于 ABORTED、退出码 5 |
| ISS-023 | G1 冻结后是否开「R1 受控放宽」通道 | RTL 修改权限边界（需与 R5 口径合并） | 暂不设；但应在宣布基线前先定好协议 |
| ISS-024 | 基线指纹判据是否改为「`md5:size` 才计修改、纯 mtime 单列 INFO」 | 基线机制与换机校验（规则 22）；VR1 当前未基线，正是定口径的窗口期 | 建议于基线触发前定稿 |
| ISS-030 | 是否建 `agent_gate` 机器门禁 + Auto Queue | 规则 19~22 的可执行性（已实测发生过一次只读违反，ISS-022） | 待定；建议至少先把「清单比对 + mtime 窗口」自动化，扫描工具可后补 |
