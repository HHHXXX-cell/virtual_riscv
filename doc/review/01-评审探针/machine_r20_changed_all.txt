⚠ 自检：本轮仅提取到 3 个标识符，低于可信阈值 8——扫描可能不完整（diff 为空/编码/路径前缀漏配），输出按**不可信**处理。
== 改动集（增量复核轮的复核面，doc/项目开发流程.md §9.4）：基线 = 53dac0d ==
改动文件：
  .qoder/handoff/HANDOFF.md                      +28/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/process/00-问题记录.md                         +2/-2，2 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：台账
      节：状态总览
  doc/review/01-评审探针/machine_r19_changed_all.txt +61/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/machine_r19_check.txt       +26/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/machine_r19_width.txt       +33/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/machine_r20_check.txt       +26/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/machine_r20_width.txt       +33/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/snapshot_r19_spec01.md      +927/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审探针/snapshot_r20_spec01.md      +927/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  doc/review/01-评审记录.md                          +233/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：统计
  doc/spec/01-流水线与寄存器接口.md                       +2/-2，2 处 hunk
      节：3.4 `uop_t` — `D → R`（4 条/拍）
      节：3.7 `rob_entry_t`（ROB 存储位域，128 项）
  doc/verify/04-验证进度.md                          +2/-2，2 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：下一批（按依赖顺序，无需用户输入即可推进）
      节：近期进度（工作项 + 完成情况 + 产出物路径）
  doc/verify/05-回归记录.md                          +4/-0，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  run_cmd/AutoQueue.yaml                         +1/-1，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  script/gate/window_state.json                  +1/-1，1 处 hunk（不参与标识符闭包：记录/台账/探针/脚本只列不计）
      节：(篇首)
  未入版本控制的新增文件 1 个（diff 看不到其内容，须并入复核面）：
      .qoder/settings.json
被改标识符 → 引用闭包（命中处数；**＝1 即"只在被改处出现、别处不引用"**，逐条问生产者/消费者是否断链；全表）：
  csrq                        55  00-总体×4 01-流水×37 02-指令×1 04-重命×13
  fdirty                       3  00-总体×1 01-流水×2             
  fp_dirty                     2  00-总体×1 01-流水×1             
提示：本清单只界定**复核面下界**（§9.4 来源①），不替代语义推演。①"未展开"项必须逐条分流再定级——可能是真断链（改了 A 而别处不引用），也可能是模块名/未写篇目持有的端口（属 §9.4 来源③ 的复述），还可能是改名留痕；②缺失型缺陷（"该写的动作没写"，如 N1）不在本集内，须由终局全量轮覆盖（上限 5 次）。
