# doc/verify/01 — 理解清单（Step 1 产物）

> 输入＝`doc/spec/` 自产规格（`spec/00` 参数总表为数值唯一权威源；`spec/01`~`spec/03` 已收口，
> `spec/04` 已有起稿，`spec/05`~`spec/14` 未成稿——引用未建篇目处一律标「待建」）。
> **引用纪律**：本清单**只引不赋值**——数值与语义一律指回 spec 篇号＋节号，不在此另定义；
> 与 spec 冲突时以 spec 为准并登记差异。
> 状态：**首建（S-10，2026-09-24，`doc/verify/05` R-141）**；评审＝Requirement Review（Review G1 材料之一）。
> 节次：§1 接口表 · §2 寄存器表 · §3 时钟复位假设 · §4 功能点清单 · §5 异常边界＋存疑清单 · §6 出口自检。

---

## 1. 接口表（模块边界索引；权威定义在 `spec/01`）

冻结对象＝**接口名、方向、位宽与语义**（`spec/00` §6 冻结判据：每个 `*_t` 有成员级位域与合法值域）。
本表为索引，**不复制位域**——位域细节见 `spec/01` §3.x（已逐 struct 定义，28 个 struct 位宽机判在册）。

| # | 模块 | 目录 | 上游接口（消费） | 下游接口（提供） | 出处 |
|---|---|---|---|---|---|
| M01 | `vr1_bp` 预测器子系统 | `frontend/` | `bp_pc_i`、`bp_upd_t`（提交反馈） | `ftq_req_t` | `spec/00` §6；`spec/03` §2 |
| M02 | `vr1_ftq` | `frontend/` | `ftq_req_t` | `ftq_entry_t`、`ftq_pop_valid` | `spec/00` §6；`spec/01` §3.2 |
| M03 | `vr1_ifetch` | `frontend/` | `ftq_entry_t`、`icache_rsp_t` | `fetch_raw_t`（32B＋边界） | `spec/00` §6；`spec/01` §3.3 |
| M04 | `vr1_cexpand` | `frontend/` | `fetch_raw_t` | `ibuf_wr_t`（展开后 32bit 流） | `spec/00` §6；`spec/02` §2.2 |
| M05 | `vr1_decode` | `ctrl/` | `ibuf_wr_t` | `uop_t[4]` | `spec/00` §6；`spec/01` §3.4 |
| M06 | `vr1_rename`（RAT＋PRF＋free-list） | `rename/` | `uop_t[4]`、`ckpt_id` | `rmap_uop_t[4]` | `spec/00` §6；`spec/01` §3.5 |
| M07 | `vr1_dispatch` | `ctrl/` | `rmap_uop_t[4]` | `iq_wr_t`、`rob_alloc_t`、`lq_alloc_t`/`sq_alloc_t` | `spec/00` §6；`spec/01` §3.6/§3.8 |
| M08 | `vr1_iq_int/mem/br` | `issue/` | `iq_wr_t`、`wb_t`（唤醒广播） | `disp_x_t[6]` | `spec/00` §6；`spec/01` §3.9/§3.11 |
| M09 | `vr1_exu_int`（ALU/SHIFT/MUL/DIV） | `exu/` | `disp_x_t`、`rs1/rs2` | `wb_t`、`excp_t` | `spec/00` §6；`spec/01` §3.10/§3.20 |
| M10 | `vr1_bru` | `exu/` | `disp_x_t` | `wb_t`、`br_resolve_t`（冲刷请求） | `spec/00` §6；`spec/01` §3.12 |
| M11 | `vr1_lsu` | `lsu/` | `disp_x_t`、`rob_head_t` | `mem_req_t`、LQ/SQ 状态、`wb_t` | `spec/00` §6；`spec/01` §3.13 |
| M12 | `vr1_mmu`（TLB/PTW/PMP/PMA） | `mmu/` | `mmu_transl_req_t` | `mmu_transl_rsp_t`、`ptw_mem_req_t` | `spec/00` §6；`spec/01` §3.14 |
| M13 | `vr1_l1i` / `vr1_l1d` | `cache/` | `mem_req_t` | `icache_rsp_t`/`dcache_rsp_t`、`mshr_wake_t` | `spec/00` §6；`spec/01` §3.16 |
| M14 | `vr1_l2` ＋ `vr1_axi_br` | `cache/` | `miss_req_t` | `fill_t`、AXI4 主端口 | `spec/00` §6；`spec/09`（待建） |
| M15 | `vr1_rob` | `ctrl/` | `rob_alloc_t`、`x_complete_t[6]` | `rob_head_t`、`commit_t[4]`、`rt_t` | `spec/00` §6；`spec/01` §3.7/§3.17/§3.21 |
| M16 | `vr1_commit` | `ctrl/` | `commit_t` | `csr_wr_t`、`trap_req_t`、`freelist_ret_t` | `spec/00` §6；`spec/01` §3.18/§3.19 |
| M17 | `vr1_csr` | `ctrl/` | `csr_wr_t`、`x_csr_rd` | `mstatus_t`、`satp_t`、`ipend_t` | `spec/00` §6；`spec/10`（待建） |
| M18 | `vr1_trap` | `ctrl/` | `excp_t`、`ipend_t` | `flush_all_t`、`redirect_t` | `spec/00` §6；`spec/10`（待建） |
| M19 | `vr1_clint`/`vr1_plic` | `ctrl/` | MMIO 从端口 | `irq_t`（MTIP/MEIP/…） | `spec/00` §6；`spec/10`（待建） |
| M20 | `vr1_core` | `core/` | 时钟复位、`irq_t`、AXI 主端口、trace 端口 | — | `spec/00` §6；`spec/01` §6 |

**顶层（`vr1_core`，`spec/01` §6）**：时钟/复位；`irq_t` 输入（含 `nmi_i`——`NMI_EN=0`，输入必须 tie 0，
`spec/00` §4.7）；AXI4 主端口（128 bit data／44 bit addr／ID 4 bit，`spec/00` §4.7）；trace 端口（`rt_t` 类）。
**跨篇引用未建篇目**处一律标「待建」（口径见 `spec/01` §8.2 待同步清单）。

---

## 2. 寄存器表（架构可见状态；实现细节归各篇）

| 类 | 项 | 一期口径 | 出处 |
|---|---|---|---|
| GPR | `x0`~`x31`（XLEN=64） | `x0` 硬连零（读恒 0、写丢弃）；架构号 5 bit、物理号 6 bit（融合式 PRF 64 项） | `spec/00` §4.1/§4.3；`spec/02` SI-1；`spec/01` §3.4/§3.5 |
| PC | 取指 PC／`pc` 存储宽度 40 bit（`VA_STORE_W`） | 复位值＝`RESET_PC`＝`44'h0000_1000`（实现定义） | `spec/00` §4.1；`spec/01` §3.2/§3.3 |
| CSR（M 侧） | `mstatus`／`misa`／`medeleg`／`mideleg`／`mie`／`mtvec`／`mscratch`／`mepc`／`mcause`／`mtval`／`mip`／`mhartid`／`mvendorid`／`marchid`／`mimpid`／`mcounteren`／`mcountinhibit` | 全表（地址/复位值/位段/WARL/访问权限）归 `spec/10`（待建）；**一期已定**：`mtvec` 支持 Direct＋Vectored；`mstatus.FS` 读回恒 0（Off）、非零写忽略；`NMI_EN=0` | `spec/00` §4.7（`CSR_TRAP_SET`/`MTVEC_MODES`）＋§7.3 |
| CSR（S 侧） | `sstatus`／`stvec`／`sie`／`sscratch`／`sepc`／`scause`／`stval`／`sip`／`scounteren`／`satp` | 同上（全表归 `spec/10`）；Sv39（`satp`，`VA_BITS=39`） | `spec/00` §4.1/§4.7 |
| 计数器 | `mcycle`／`minstret`／`time`（读外部 `mtime`） | `mhpcounter3..31` 不实现（读回 0）；`mcountinhibit` 只显 2 位；LCOFI 位存在但永不被置 | `spec/00` §4.7（`COUNTERS`） |
| 特权级 | M／S／U（+ Sv39、PMP 8 项） | 一期 ISA 字符串＝`rv64imac_zicsr_zifencei`；**不实现**：F/D（报 illegal）、Debug Module/D-mode、NMI/`Smrnni` | `spec/00` §2／§4.7／§7 |
| 复位后可见态 | 不重命名、无 checkpoint、free-list 全空、`flush` 无效 | 复位↔首条取指的握手归 `spec/01` §5.1（实现侧） | `spec/01` §5.1 |

> 与 `spec/01` §3.4/§3.8 的 `dst_kind` 口径：`11`＝CSR 旧值→GPR；CSR 读的旧值经 `csrq_entry_t` 与 `rt_t.csr_old` 两路上报（`spec/01` §3.19/§3.21）。

---

## 3. 时钟复位假设

| 项 | 一期假设 | 出处 |
|---|---|---|
| 时钟域 | **`CLK_DOMAINS=1`**：单时钟域、无 CDC；日后加总线异步 FIFO 属二期（走《Cummings 卷03》） | `spec/00` §4.7 |
| 复位方案 | **异步复位、同步释放**；全核单复位树；`ASYNC_REG` 类属性留后端 | `spec/00` §4.7（`RESET_SCHEME`） |
| 复位向量 | `RESET_PC`＝`44'h0000_1000`；`sw/env/link.ld` 必须与之一致 | `spec/00` §4.1 |
| 门控时钟 | **一期不开**（`GATE_CLOCK_EN=0`）：避免验证复杂度，二期按军规加 | `spec/00` §4.7 |
| 首次取指 | 复位释放后从 `RESET_PC` 取指；块起始 2 字节对齐（`pc[0]==0`） | `spec/01` §3.2（N-03） |
| 时序前提 | 各接口同一时钟域；`spec/04` §3.2「同时钟域前提」用于 `K` 推导 | `spec/04` §3.2 |
| 周期计数 | 周期计数自 `RESET_PC` 首条退休起（度量口径） | `spec/00` §9 |

---

## 4. 功能点清单（逐条带 spec 出处；**本节即 Review G1 的「理解面」核对对象**）

**4.1 指令集与特权**

| # | 功能点 | 出处 |
|---|---|---|
| F-01 | RV64I 全量 63 条（解码/语义/异常条件/μop 面） | `spec/02` §10（63 条逐条） |
| F-02 | RV64C（Zca 旧称）16→32 位展开（含保留点非法化） | `spec/02` §4.2（C 展开表）；§16.4-(1)（`is_comp` 恒 0） |
| F-03 | RV64M 13 成员（含除零/溢出语义、W 型 sext） | `spec/02` §17（T-15 闭合） |
| F-04 | RV64A 11 成员（LR/SC＋9 AMO；`aq`/`rl`；自然对齐） | `spec/02` §18（T-14 闭合）；`spec/00` §8 |
| F-05 | Zicsr（CSR 读写/置位/清位、旧值语义、队头门控） | `spec/02` §10.9；`spec/01` §3.19/§3.10 |
| F-06 | Zifencei（`FENCE.I`：排空前端＋无效化 L1I 行） | `spec/02` §10.8；`spec/00` §8 |
| F-07 | 特权 M/S/U＋Sv39＋PMP 8 项 | `spec/00` §2／§4.7 |
| F-08 | 异常/中断：12 类同步异常＋7 类中断（优先级定死） | `spec/00` §4.7（`INTR_PRIORITY`）；本清单 §5.1 |
| F-09 | 非对齐访存不支持（报 fault） | `spec/00` §2 口径统一／§4.7 `MISALIGNED_EN=0` |
| F-10 | F/D 一期报 illegal（`mstatus.FS` 恒 0）；二期预留形式 | `spec/00` §7；`spec/02` §1 |

**4.2 前端（预测/取指）**

| # | 功能点 | 出处 |
|---|---|---|
| F-11 | 3 级预测＋3 级取指解耦（FTQ 衔接） | `spec/00` §3／§4.2；`spec/03` §1/§3 |
| F-12 | TAGE-SC-lite 主预测（4 表＋基表）＋ITTAGE-lite＋RAS/SARAS | `spec/00` §4.2；`spec/03` §2.5/§2.7/§2.8 |
| F-13 | L1BTB（直接映射）/L2BTB（4 路）两级 BTB | `spec/00` §4.2；`spec/03` §2.6 |
| F-14 | 预测块（FTQ 项）语义：`nib`/`seq_of_ftq`/块起始 2 字节对齐 | `spec/01` §3.2；`spec/03` §2.4 |
| F-15 | RAS（`ras_op` 4 值）与 SARAS 校正队列（含满停 `P2` 口径） | `spec/03` §2.8（含 I3-9/I3-10、全组合 1,188 格） |
| F-16 | 32B 取指窗口＋L1I 32KB/8w/64B VIPT＋对齐/IBUF | `spec/00` §4.1/§4.2/§4.6 |
| F-17 | 误预测/重定向与重取三路（分支/串行化/trap 类 B） | `spec/01` §5.3（D3-① 三路） |

**4.3 后端（译码→提交）**

| # | 功能点 | 出处 |
|---|---|---|
| F-18 | 4 宽译码/重命名/分发；RAT 32＋PRF 64（融合式）＋free-list | `spec/00` §4.3；`spec/01` §3.4~§3.6 |
| F-19 | 三路发射队列（INT 24/MEM 20/BR 20）＋就绪/年龄位 | `spec/00` §4.3/§4.4；`spec/01` §3.8/§3.9 |
| F-20 | 6 宽发射（INT≤4/MEM≤2/BR≤1，年龄仲裁）＋后读（发射选择拍读 PRF） | `spec/00` §4.4；`spec/01` §3.11；`spec/04` §4.1 |
| F-21 | DIV/AMO μop 拆分（`UOP_MAX_SPLIT=2`）与 rd 分派承接 | `spec/00` §4.3；`spec/02` §17.2/§18.4 |
| F-22 | ROB 128（不存结果）＋4 宽提交＋trace | `spec/00` §4.3/§4.5；`spec/01` §3.7/§3.17/§3.21 |
| F-23 | checkpoint×4＋映射历史栈回滚（`HIST_DEPTH=ROLLBACK_MAX=33`） | `spec/00` §4.3；`spec/01` §5.2/§5.3；`spec/04` §3/§5 |
| F-24 | 反压而非全局停（BP1~BP6）＋无死锁论证 | `spec/01` §4（BP1~BP6）；`spec/04` §2（`INV-1`~`INV-5`） |
| F-25 | `csrq` 8 项＋两命中写路＋`K=2` 迟到写窗口＋丢弃两分类 | `spec/01` §3.19；`spec/04` §3（`K` 定值） |
| F-26 | 同步刷新：`fence`/`fence.i`/CSR 隐式序；异常提交抑制写 | `spec/00` §8；`spec/02` §19（对照表 11 行） |

**4.4 LSU/MMU/Cache/总线**

| # | 功能点 | 出处 |
|---|---|---|
| F-27 | LQ 48/SQ 32＋store-forward（PA 全宽比较＋字节掩码）＋延迟重试 | `spec/00` §4.6；`spec/01` §3.13 |
| F-28 | Sv39 翻译（TLB＋PTW）＋PMP/PMA＋access-fault 路径 | `spec/00` §4.6；`spec/01` §3.14/§3.15 |
| F-29 | L1D 32KB/8w/64B VIPT；**L2 256KB/16w**；AXI4 128 bit 主端口（附 AL N3 更正：原写 512KB，与权威源 `spec/00` §4.6 相抵） | `spec/00` §4.6/§4.7；`spec/01` §3.16 |
| F-30 | 原子性：LR/SC（LQ 内保留、SQ 命中即清）＋AMO（L1D 内 RMW） | `spec/00` §8；`spec/02` §18.2/§18.3 |
| F-31 | CLINT/PLIC-lite（8 source）＋中断优先级与委托 | `spec/00` §4.7 |
| F-32 | 单时钟域、异步复位同步释放、无门控时钟 | `spec/00` §4.7（本清单 §3） |

---

## 5. 异常边界＋存疑清单

### 5.1 异常边界（一期口径；逐条可由验证点清单细化）

| 类 | 边界口径 | 出处 |
|---|---|---|
| 同步异常 12 类 | instruction address misaligned／instruction access fault／illegal instruction／breakpoint／load address misaligned／load access fault／store-AMO address misaligned／store-AMO access fault／ECALL(U/S/M)／instruction page fault／load page fault／store-AMO page fault | `spec/00` §4.7；`spec/02` §10 各行异常条件 |
| 中断 7 类 | MSI／MTI／MEI／SSI／STI／SEI／LCOFI；优先级 `MEI > MSI > MTI > SEI > SSI > STI > LCOFI` | `spec/00` §4.7（`INTR_PRIORITY`） |
| 非对齐 | **一律报 address-misaligned**（LR/SC/AMO 同规则；**不实现 `Zama16b`**——MAG PMA 不配置） | `spec/00` §2／§4.7；`spec/02` §18.2/§18.3/§18.6（ADR-S06-A） |
| NMI | 不实现（`nmi_i` 必须 tie 0）；致命错误上报走「置复位」 | `spec/00` §4.7（`NMI_EN=0`） |
| Debug | 仅 `EBREAK`→breakpoint 异常；Debug Module/D-mode 后置 | `spec/00` §4.7（`DECODE_SUPPORT`） |
| 浮点 | 任何 F/D 指令报 illegal；`mstatus.FS` 读回恒 0、非零写忽略 | `spec/00` §7.3 |
| 异常提交副作用 | 不退休 ⇒ GPR/CSR/内存写全不发生；`csr_wr` 抑制＋队头丢弃＋重新起算；RAT 撤销＋物理号归还 | `spec/02` §19；`spec/01` §3.19 规则 5／§5.2 `CT` 行① |
| 保留编码 | 保留≠非法 的判读口径；非法化路径与异常码归 `spec/10`（待建） | `spec/02` §9.3／§16.3.1（D-1/D-4）；T-9 |
| 原子性 | 失败 SC 在内存保护口径上可按 store 对待；LR/SC 不受 MAG PMA 影响 | `spec/02` §18.2（行 8268–8271／47328） |

### 5.2 存疑清单（单列，不臆测；逐条给归口）

| # | 存疑项 | 现状态／归口 |
|---|---|---|
| S-01 | `PRF_RD_PORTS` 2 个「恢复读」口的承载方 | **已裁（ADR-S08，2026-09-24）：降值 16（＝4 提交读＋12 发射级读）**——逐消费者枚举无第三读者；`spec/00` §4.3 已落、版本批行 `v0.1 续6`；回退点＝`spec/08` 收口若证重试须专用读口则升版恢复；`doc/verify/05` R-142 |
| S-02 | `spec/01` §7「等待集⊆更老」类新断言未落 | 归 **Q-037**（spec/01 后续对账片）；`doc/process/00` ISS-020 残余清点 |
| S-03 | `bp_upd_id` 提交侧载体（预测更新表结构/深度） | 归 **ISS-082**（G1 前闭合；承接 `spec/03` T3-4＋`spec/01` 接口片） |
| S-04 | `SAME_GROUP_BYPASS_DEPTH=4` 同组前递链的拍序作用点 | **已裁（ADR-S12，2026-09-24）**：前递在前——`R`（P7）级完成、严格先于 `W` 读出拍；值侧经写口回灌；未就绪＝留队重判（不入 `fwd_blocked_replay`）；落文 `spec/01` §1.1/§3.5/§4；`doc/verify/05` R-146 |
| S-05 | L1I/L1D/L2 的条目位域表（替换位/字节使能/non-inclusive 目录位）与 TAGE 折入口径 | 归 **`spec/09`（待建）**／`spec/03` 升版；`spec/00` §4.8 注 |
| S-06 | 特权行为面（trap 进场/模式切换/TLB 失效/CSR 全表值） | 归 **`spec/10`（待建）**；`spec/02` §16.5（T-16） |
| S-07 | 无 RISCV 工具链与 WSL2（编译/Spike 互检/长跑回归不可执行） | **ISS-002 待人**（`doc/环境搭建.md` §7 M0 验收表 7/7） |
| S-08 | 一期 IPC≥1.8 的适用条件（平均驻留 ≲25 拍）与参数量级的关系 | ADR-S2-1 收窄宣称；随 `spec/13` 实测登记（`spec/00` §4.3 注） |
| S-09 | `doc/verify/02` 验证点清单未建（12 维场景分解＋34 法） | 归 **Step 2**（本清单为其输入之一；`doc/verify/04` §①） |
| S-10 | `spec/04`~`spec/14` 未成稿篇目对接口冻结的连带影响 | 逐篇随成稿复核；G1 冻结以 `spec/14` 签署为界（`spec/00` §6） |

---

## 6. 出口自检（可复核）

| # | 检查项 | 复核手段 | 结果 |
|---|---|---|---|
| 1 | 五节齐（接口/寄存器/时钟复位/功能点/异常边界＋存疑） | 本文 §1~§5 逐节在位 | 5/5 |
| 2 | 功能点 **100% 带 spec 出处** | §4 表 32 行（F-01~F-32）逐行「出处」列非空且指篇号＋节号 | 32/32（本片实读） |
| 3 | 存疑单列且给归口 | §5.2 表 10 行，每行「现状态／归口」非空 | 10/10 |
| 4 | 只引不赋值（无自造数值） | 本文数值仅 `RESET_PC` 等引用项（出处 `spec/00` §4.1）；`width_check`/`gate` 不受影响 | `gate.py check` 全 PASS；`width_check` 28/28 |
| 5 | 与已收口篇（01/02/03）无引用漂移 | 抽查 §1/§2/§4 引用的节号在对应篇存在 | 抽查 12 处命中 |
