# doc/verify/03 — 验证计划

> 本篇为**计划 AI（vr1-planner）的产物边界**：§0《驱动计划》与 §1 切片计划由计划 AI 维护，R8 只执行。
> 验证策略/环境/覆盖度等正式内容（Step 3 本体）随 spec 篇目推进在此篇内续写；
> 队列与状态看 `run_cmd/AutoQueue.yaml` 与 `doc/verify/04`，**本表的"序"是唯一权威的工作顺序**。

---

## §0 驱动计划（计划 AI 唯一作者；R8 只执行、不得改序或跳项）

**当前相位**：Step 1 理解规格（九步流程；出口判据＝14 篇 spec 成稿＋Review G1 冻结）。

**维护规则**：①每次重排必须留痕（本表"重排记录"行 + `doc/verify/05`）；②项状态只许 `待办/进行中/已完成/【待人】/【阻塞】`；③出口判据必须可核（机判优先；**不写死工具项数，以实跑输出末行为准**）；④派发词要点只写一句「目标与边界」（不展开子步骤；预算入 §1 切片表）；⑤**行位＝执行顺序，D-xx 编号为稳定标识**（前移/后移以行位为准）——已完成行置前作历史、在跑行次之、待办行自上而下即执行序、`【待人】`行置尾只呈报不代做。

**当前相位（L0，随九步流程给定；2026-09-24 记）**：`Step 1 理解规格`——`doc/spec/` 成 2/14 篇（`spec/00` v0.1 待评审、`spec/01` v0.3 经 D-14 收敛、`spec/02` 经 Q-010 收口、`spec/03` 首片＋续写片已成待评审）；相位出口判据＝**14 篇 spec 成稿 ＋ Review G1 接口冻结签署**（触发基线，见 `AGENTS.md` §1）。

**角色合并声明（R0 指令，2026-09-24）**：计划角色并入 **R8 本体**（子代理 `vr1-planner` 保留备用）；即 §0／§1 由 R8 兼写——**独立性已被合并**，R6／auditor 对本表的检查按"R8 兼计划"口径执行；留痕＝`doc/process/00` **ISS-075**、`doc/verify/05` **R-110**（ISS-074 系并行会话登记的格式缺陷项，两者不同物）。

**角色合并声明·扩（R0 指令，2026-09-24 二批）**：**开发（设计面）与验证（验证面）角色亦并入 R8 本体**——即 `vr1-designer`／`vr1-verifier` 不再默认派发，spec/RTL 与 tb/iss/裁判类切片由本体直接执行；**唯审计（`vr1-auditor`，角色R6＋R7）保持独立子代理派发**（独立性四来源之②「信息隔离的独立读者」仅存此一环，不得再合并）。独立性的兜底＝①类外部锚点（规范原文／ISS 实测／探针输出／覆盖率原始报告）＋R0 签署；规则 21 既有的"残余风险声明"不变。留痕＝`doc/process/00` **ISS-079**、`doc/verify/05` **R-114**。

**D-nn 编号域对照与 Q-nn 映射纪律（L5 收口片补，2026-09-23）**：

- **D-nn 两池对照**：**池一·驱动计划项** `D-1`~`D-21`——载体即本 §0 表（计划 AI 唯一作者；行位＝执行顺序、`D-xx` 为稳定标识），引用格式 `D-nn（本表）`；**池二·评审附件条目**——评审记录各附件内的决策审核/复核条目（如 `D-1`~`D-9`，收录见 `doc/verify/05` R-027），仅在附件与记录域内使用、不得与池一互引，引用格式 `附X-Dn`。两池同号相遇时以**载体域名**（本表 / 附X）判别。
- **Q-nn 映射纪律**：一切 Q-nn 与队列项的映射以 `run_cmd/AutoQueue.yaml` 的 `id`/`task` 字段为**唯一权威**（权威在队列）；本表与 `doc/verify/04` 的 Q-nn 引用按此对账；历史轮次队列号曾重排/复用（如 Q-031 曾用作 spec/02 终片轮标识、现行 Q-031 为送审项）——同号两义一律以队列现行 `task` 为准。「队列无对应项」者须标出：L5 实扫结果＝v03/v04 全部 Q 引用 ID 均在队列中存在（无缺项）。

| 序 | 工作项 | 角色 | 产物落点 | 出口判据 | 派发词要点 | 依赖/阻塞 | 状态 |
|---|---|---|---|---|---|---|---|
| D-1 | 第 13″ 轮确认（r13′ 盲区补核＋五项 ADR 送审） | auditor | 评审记录附 L；`doc/verify/05` R-031 | 已满足：本体 10/12 在＋2 轻级矛盾已修；五项 ADR 风险记录 11 条（可接受 5／④段 2／需回应 4） | 只读核盲区与 ADR 风险记录，不改正文 | — | 已完成 |
| D-2 | ISS-026（码点扫描列交付前置） | 决策 AI | `doc/process/00`；`doc/verify/05` R-030 | 已满足：ADR-8 四要素齐（升 G1 冻结材料必附件＋扫描面收口＋坏词表维护责任） | 只出 ADR 与收口口径，不改实现 | — | 已完成 |
| D-3 | spec/02 续片（`isop_t` 成员表＋RV64I 全量） | 设计 AI | `doc/spec/02` v0.2；`doc/verify/05` R-031 | 已满足：87 成员（编码列全"待定"）＋RV64I 63 条＋锚点 5 处；T-17 关闭、T-18/T-19 新增 | 只补成员表与 RV64I 全量，编码面留待定 | 编码面→D-11；余项→D-6 | 已完成 |
| D-4 | ISS-044/045 过闸判定 → 状态字改【已解决】 | R8（领队执行，不派新片） | `doc/process/00` 两行；`doc/verify/05` R-038 | 已满足：条件①~③逐条机上核过；ISS-044/ISS-045 已转【已解决】并回填时间（2026-09-23） | 只做过闸核对与状态字回填，不动正文 | — | 已完成 |
| D-6 | spec/02 终片：余项清账＋编码列落地并入 | 设计 AI | `doc/spec/02`；`doc/verify/05` R-039／R-108 | 主体交付（R-039）＋Q-010 收口裁定（R-108）在案；残余（T-2~T-16 清账／M/A 24／跨篇清单①）整链迁 D-18 承接 | 只做主体收口，残余整链转 D-18 | 残余见 D-18 | 已完成 |
| D-8 | spec/04 起稿（`INV-1~INV-5` 权威对照表、`K` 推导、S1/SUS-01 收口） | 设计 AI | `doc/spec/04`；`doc/verify/05` R-048 | 首片＋归一片已成（`K`＝2 定值链在）；残余（T-04-5b~d 缺项＋跨篇④⑤笔）迁 D-19 承接 | 起稿只给对照表与 `K` 推导，缺项列明不自裁 | 残余见 D-19 | 已完成 |
| D-9 | spec/00 v0.2（§4.8 四行重算：TAGE/L1D/L2/合计，禁 `≈`） | 设计 AI | `doc/spec/00`；`doc/verify/05` R-035 | 四行显式加式且 `width_check` 认；`≈` 归零（R-035）；§4.8 合计末值链至 2,970,816（Q-036 复算在案） | 单片算术：只重算 §4.8 四行，数值引唯一源 | — | 已完成 |
| D-10 | Q-016 第 3 批余项（批 2／批 3 全回收） | 决策 AI | `doc/process/00` 决策列 | 已满足：批 2（R-022）／批 3（R-026／R-029／R-030）／ISS-020＝ADR-S2（R-028）全回填 | 批量自决回填；无锚点项不得写"已定" | 余下义务→D-12／D-13 | 已完成 |
| D-11 | `isop_t` 编码面（T-18/T-19）：87 成员 10 bit 具值＋`cls`/`src_sel`/`dm`/`sext` 值域定位 | 决策 AI（ADR）→设计 AI（落文本） | `doc/spec/02` §9；`doc/verify/05` R-039／R-070 | 已满足：87/87 具值、两两互异、⊆10 bit、`待定（T-1 编码面）`归零；T-19 四域成文；`spec/01` 4 处引用给指针 | ADR 先、落文本后；编码不得自造 | — | 已完成 |
| D-12 | B5：附 L 四条风险项回应（R-4.1／R-4.2／R-6.1／R-7.1） | 决策 AI | 评审记录附 L L§2b | 已满足：四条逐条回应（全"接受"、收紧方向）＋Q-028 `done_when` 扩充（R-025） | 只回应风险项取舍，不改机制 | — | 已完成 |
| D-14 | Q-004 终局全量复核轮（§9.4/§9.4b 全量档，判 §6.1 C2） | auditor（多片） | 评审记录附 M~附 Y；`doc/verify/05` R-062~R-094 | 已满足：A12 新增 0＋旧问题 56/56；R0 裁决 C2 成立（R-093／R-094）；Q-004/Q-034 置 done | 全量档核整篇与缺失型；门禁权留 R0 | — | 已完成 |
| D-15 | spec/03 起稿（篇序续推：首片→收口片→续写片） | 设计 AI | `doc/spec/03`；`doc/verify/05` R-053／R-109 | 成稿至 v0.1 续3（Q-036 改引片）；T3-5 合并定稿（R-109）；残余（Q-006 收口链）迁 D-17 承接 | 数值带【超】页码；收口条款在位 | 残余见 D-17 | 已完成 |
| H-1 | push 认证（原【待人】项） | R0 | `doc/verify/05` R-042 | 已达成：`git push` 实测成功 2 次、本地与远端同步（R-042） | 人做项：认证一次，不改仓 | — | 已完成 |
| D-16 | `spec/01` 接口补片（Q-036：闭 ISS-073 两条缺口） | 设计 AI→auditor→R8 | `doc/spec/01` v0.4；`doc/spec/03` 改引片；`doc/spec/00` §4.8 | 已满足：落文（ROB 三表补 `ftq_id`、`iq_wr_t`/`disp_x_t` 补 `rs1_arch`/`rd_arch`；L13 后合计 2,970,836）→ S-01 复核（R-112）→ L13（R-114）→ S-01b（R-118）→ 核销 S-02（R-120）；ISS-073 已解决 | 接口补片只补载体与改引，走接口升版回写 | 复核机判件先于派发生成 | 已完成 |
| D-17 | Q-006 收口路径（评审→处置→关闭） | auditor→设计 AI→R8 | `doc/spec/03`；评审记录附件；`doc/verify/05` | 续写片已成（R-109：T3-5／T3-2／T3-3／T3-6／T3-7）；余＝收口链三片（§1 S-03~S-05） | 收口链只审改动面与 `done_when` 项 | S-02（Q-036 落定） | 进行中 |
| D-5 | Q-028 实施（`gate.py snapshot/rocheck`＋规则 22） | R8／R4 | `script/gate.py`；`script/gate/integrity_baseline.json`；`AGENTS.md` 规则 22 | ①~④已落（R-034：两子命令可用／基线入版控／规则 22 改毕／首跑留痕）；余＝⑤送审（§1 S-11） | 单片实施加一次送审，不扩写面 | — | 进行中 |
| D-7 | 平台骨架（ModelSim-only：tb 骨架＋filelist＋do/tcl） | R4 | `tb/vr1_uvm/`；`filelist/tb.f`；`run_cmd/` | `vlog` 0 error 已过（R-054，证据行口径＝ADR-5）；余＝`vopt`／`vsim`（待 DUT）＋`vr1_if` 回填（待接口冻结） | ModelSim-only；判据用产物证据行 | DUT／接口冻结 | 进行中 |
| D-13 | §9.5 汇聚清单（④段材料；G1 前由人读） | R8（汇集）＋决策/复核按 §12.5 增补 | `doc/spec/14` §9.5 ④段（随 Q-026）；先行条目随附件 | 条目数与各轮"需进 ④段"标注一致；④段已含 B-1~B-9＋ADR-D3/D4/D5 全链（逐条双指针明细见 R-078／R-081）；出口＝随 `spec/14` 首版并入 G1 材料 | 每轮增补，只汇总不裁决 | — | 进行中 |
| D-18 | `spec/02` 第二批片（承 Q-010 关闭裁定残余） | 设计 AI | `doc/spec/02`；`doc/verify/05` | M/A 24 成员（T-14/T-15）＋跨篇清单①（异常提交抑制写对照）落文可回查；§14／§16.5 余项（T-2~T-16 未闭项）清零或逐项归属；`gate.py check` 全 PASS（末行为准） | 只落 M/A 与清单①及余项清账，逐条带【权】锚点 | — | 待办 |
| D-19 | `spec/04` 续片收口（T-04-5b~d＋跨篇清单④⑤） | 设计 AI | `doc/spec/04`；`doc/verify/05` | T-04-5b~d 清零或列缺带归属；跨篇④（`d_S=1` 对账）与⑤（L50 残引）落文；`gate.py check` 全 PASS（末行为准） | 只清缺项与对账，`K` 链只引用不改值 | Q-002 解阻复核（R8 转录义务④） | 待办 |
| D-20 | `spec/00` 收口＋ISS-020 关闭面 | 设计 AI＋R8 | `doc/spec/00`；`doc/process/00` ISS-020 行 | §4 表内待定项按已定 ADR 回写清零；ISS-020 转【已解决】或明列残余与等谁 | 只回写已定值，不新定参数 | Q-003 解阻复核（R8 转录义务④） | 待办 |
| D-21 | `doc/verify/01` 首建（理解清单五节＋存疑清单） | R1 | `doc/verify/01`（新建） | 五节齐；每条功能点带 spec 出处（篇号＋节号）；存疑单列不臆测 | 只铺五节与出处，不推导验证点 | Q-007 解阻（待 ISS-020 关闭） | 待办 |
| H-2 | 【待人】ISS-002 工具链安装（挡 M0/M1/M6/M8） | R0 | `doc/环境搭建.md` §7 M0 验收表 | 7/7 通过 | 按 `doc/环境搭建.md` 一次做完并回贴 | 人 | 【待人】 |
| H-3 | 【待人】ISS-023／ISS-029 | R0 | `doc/process/00` | 两行状态变更 | 两项口径各给一句取舍 | 人 | 【待人】 |

**重排记录**

| 日期 | 触发 | 变更摘要 | 留痕 |
|---|---|---|---|
| 2026-09-23 | 首版（R8 依计划轮 W-1~W-10 与 §12.2 计划引领制转录） | 建 D-1~D-10 + H-1~H-3 | `doc/verify/05` R-030 |
| 2026-09-23 | **§12.2 计划引领制第④步首次重排**（D-1/D-2/D-3/D-10 回收） | D-1/D-2/D-3/D-10 → 已完成；**D-4 判"可判"升队首**；D-5/D-8/D-9 解阻前移；**新增 D-11（`isop_t` 编码面）/D-12（B5 回应·已在跑）/D-13（§9.5 汇聚）/D-14（终局全量轮）/D-15（spec/03 起稿）** | `doc/verify/05` R-038 |
| 2026-09-24 | **Q-010 关闭＋Q-006 续写片成文（R0 全 A 裁决后首次重排）** | 逐项按实态取证更新：D-4／D-6／D-8／D-9／D-11／D-14／D-15→已完成（各附落点）；D-5→进行中（余 Q-028 送审，可与 Q-031 并批）；D-7／D-13 续跑；H-1 判据按 R-036/R-042 改写；**新增 D-16（`spec/01` 接口补片·Q-036）／D-17（Q-006 收口路径）／D-18（`spec/02` 第二批片）／D-19（`spec/04` 续片收口）／D-20（`spec/00` 收口＋ISS-020）／D-21（`doc/verify/01` 首建）**；R8 转录义务块更新 | `doc/verify/05` R-110 |

> **与既有产物关系**：`doc/verify/04` §② 计划快照＝只读快照；队列＝执行记录；**本表＝唯一权威顺序**。三者冲突时以本表为准并回填另两处。
> **R8 转录义务（本轮）**：队列侧——①按 D-16 核对/派发 Q-036（P1；含 `spec/03` §2.8 改引）②Q-006 在 Q-036 落定后派增量复核（收口链见 D-17；机判件先于派发生成）③为 D-18 建新队列号（role=R1；done_when 取 D-18 出口判据）④Q-002 解阻复核（现 blocked 的阻塞前提已消）并按 D-19 编续片范围；Q-003 随 D-20 复核 ⑤Q-007 待 ISS-020 关闭后解 blocked_by（D-21 派发）⑥landable/状态按实况刷新：Q-016（五项 ADR 均已交付，S1~S4 链）／Q-027（骨架已落 R-054）／Q-028（①~④已落、余送审）；`doc/verify/04`——§②「下一批」与「计划快照」段按本表新序刷新（清除已关闭项的过期行）；`doc/verify/05` 留痕＝R-110。

---

## §1 切片计划（2~4 小时可派发；计划 AI 维护，R8 转录为队列项）

> 用途：§0 行位＝流程级顺序；本表把**当前在跑／待派项**切成 **2~4 小时可派发**的切片——R8 取片派发，核销后回填状态与证据。
> 维护规则：①切片状态只许 `待派／进行中／已核销`；②`已核销` 必须带 R-行号或台账行 token（机判可查）；③本表只写「目标＋判据＋写面」，不含执行步骤；④`挂 L1 项`＝所挂 §0 行位（D-nn）与队列项；⑤切片↔队列号对账由 R8 转录（`done_when` 取本表验收判据；同篇／同面微片可并批，见 `run_cmd/派发词模板.md` §3）。
> 未切片项（不足一个可独立派发的 2~4 h 单元）：D-7 余量（待 DUT／接口冻结）、D-13（每轮增补）。
> 首批覆盖对照：Q-006 收口路径＝S-03~S-05；Q-036＝S-01~S-02；`spec/02` 第二批片＝S-06~S-07。首版留痕＝`doc/verify/05` R-110。

| 切片 | 挂 L1 项 | 目标一句 | 写面 | 验收判据（可核） | 预算（时长/工具调用上限） | 依赖 | 状态（待派/进行中/已核销） | 核销证据（R-行号） |
|---|---|---|---|---|---|---|---|---|
| S-01 | D-16（Q-036） | Q-036 交付面增量复核：`spec/01` v0.4 接口补片＋`spec/00` §4.8 三行＋`spec/03` 改引片 | 无（只读；报告由 R8 代录入 `doc/review/01-评审记录.md`） | 逐项核销表（已改／部分／未改）＋阻塞／澄清／存疑计数＋规则 22 窗口留痕；`gate.py check` 全 PASS（末行为准）且 `width_check` 28/28 | 2 h／≤30 调用 | Q-036 落文已完成（HANDOFF `- [Q-036]`）；机判件先于派发生成 | 已核销 | R-112（附 AE：阻塞 3 → 插 L13 处置片 → 见 S-01b） |
| S-01b | D-16（Q-036） | L13 处置批交付面 δ 复核（B1/B2/B3＋C1~C6 回收） | 无（只读；报告 R8 代录） | 逐项核销表＋阻塞／澄清计数＋规则 22 窗口留痕 | 2 h／≤30 调用 | L13 落文（R-114） | 已核销 | R-118（附 AF：0 阻塞；K-1/K-2/K-4 即修、K-3 转 RD） |
| S-02 | D-16（Q-036） | Q-036 核销＋ISS-073 关闭面：状态字与队列／台账／看板镜像 | `doc/process/00` ISS-073 行；`run_cmd/AutoQueue.yaml` Q-036；`doc/verify/04` 镜像行；`doc/verify/05` | ISS-073 行＝【已解决】＋时间＋可回查落点；队列按实况置态；`gate.py check` 全 PASS（末行为准） | 1 h／≤12 调用（闭合微片，随 S-01 批） | S-01 | 已核销 | R-120（ISS-073 关闭＋ADR-S1 落文；队列回填滞后 → 由 R-124/ISS-086 补救） |
| S-03 | D-17（Q-006） | Q-006 收口·评审片：续写片已成面（R-109）＋Q-036 落定后合并复核（增量档） | 无（只读；报告 R8 代录） | `done_when` 逐项核销表（FTQ 四规则＋T3-5 合并定稿＋数值【超】页码）＋阻塞／澄清计数＋窗口留痕 | 2.5 h／≤35 调用 | S-02 | 已核销 | R-123（附 AG：阻塞 3 → 插处置片 S-04） |
| S-04 | D-17（Q-006） | Q-006 收口·处置片：复核发现逐项落文（跨篇项登记入队列清单） | `doc/spec/03`；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | 复核项全数处置且逐锚点唯一命中断言在；`gate.py check` 全 PASS（末行为准）；`width_check` 28/28 | 3 h／≤40 调用 | S-03 | 待派 | — |
| S-05 | D-17（Q-006） | Q-006 收口·关闭片：核销关闭面（`done_when` 全项＋队列／台账回填） | `run_cmd/AutoQueue.yaml` Q-006；`doc/verify/04` 镜像行；`doc/verify/05` | `done_when` 全项可回查满足；无开放阻塞；`gate.py check` 全 PASS（末行为准） | 1.5 h／≤16 调用（闭合微片，随 S-04 批） | S-04 | 待派 | — |
| S-06 | D-18（spec/02 第二批） | M/A 24 成员语义成文（T-14／T-15，承接 R-108 裁定） | `doc/spec/02`；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | 24 成员条目落文可回查（【权】锚点不得再退化）；§14／§16.5 状态回填；`width_check` 28/28＋`gate.py check` 全 PASS（末行为准） | 3 h／≤45 调用 | Q-010 关闭批（R-108） | 待派 | — |
| S-07 | D-18（spec/02 第二批） | 跨篇清单①（异常提交抑制写对照）＋§14／§16.5 余项清账成文 | `doc/spec/02`；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | 对照表落文并挂 `spec/01` 回引；余项清零或逐项归属；`gate.py check` 全 PASS（末行为准） | 3 h／≤40 调用 | S-06（同篇写者互斥） | 待派 | — |
| S-08 | D-19（spec/04 续片） | `spec/04` 续片收口：T-04-5b~d＋跨篇清单④（`d_S=1` 对账）⑤（L50 残引） | `doc/spec/04`；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | 清单④⑤落文或其归属逐项给定；T-04-5b~d 清零或列缺带归属；`gate.py check` 全 PASS（末行为准） | 3 h／≤40 调用 | `K` 定值链在（R-048）；Q-002 解阻复核 | 待派 | — |
| S-09 | D-20（spec/00 收口） | `spec/00` 收口＋ISS-020 关闭面：已定值回写与残余清点 | `doc/spec/00`；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | §4 表内待定项按已定 ADR 回写清零；ISS-020 残余清点（转已解决或列缺＋等谁）；`gate.py check` 全 PASS（末行为准） | 2.5 h／≤30 调用 | Q-003 解阻复核（R8 转录义务④） | 待派 | — |
| S-10 | D-21（doc/verify/01） | `doc/verify/01` 首建：接口表／寄存器表／时钟复位假设／功能点清单／异常边界＋存疑清单 | `doc/verify/01`（新建）；`doc/verify/05`；`.qoder/handoff/HANDOFF.md` | 五节齐；功能点 100% 带 spec 出处；存疑单列；`gate.py check` 全 PASS（末行为准，含 md-integrity） | 4 h／≤50 调用 | Q-007 解阻（待 ISS-020 关闭） | 待派 | — |
| S-11 | D-5（Q-028） | 送审批：Q-028（ADR-4 落件）＋Q-031（`ra-token-defined` 扩面）一次非决定性审核 | 无（auditor；报告入评审记录／`doc/verify/05`） | 审核记录入档（一次·只记录）＋规则 22 窗口留痕；Q-028 `done_when` ⑤ 口封闭 | 1.5 h／≤20 调用（送审微片） | Q-030 已成（R-033） | 待派 | — |
