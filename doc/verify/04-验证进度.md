# doc/verify/04 — 验证流程执行看板

> 更新规则（事件驱动，不依赖"会话结束"信号）：①状态变更即记（主机制）②用户收尾指令 ③里程碑完成
> ④上下文压缩兜底。本看板与 `AGENTS.md` §1「验证阶段」「基线状态」两处同步；
> 阶段与门禁的上位定义见 `doc/项目开发流程.md` §1。
> 最近更新：**2026-09-23**（纵①-F13 修正：原"2026-09-22 暂停点"与实际内容不符；「暂停点」已于换机后失效）——P0 完成，P1 规格 + P2 ISS 并行已铺开；
> ISS 骨架跑通冒烟。换机继续的交接说明见 `doc/交接说明.md`。
> 本次追加（同日）：精读《IC验证知识库》后登记外部经验缺口 **ISS-023 ~ 030**，并闭环三同步缺陷 **ISS-031**；台账 22 → **31** 条。
> **2026-09-23 停机（R0 指令）**：计划 AI **停止派发**、自动推进循环暂停；在飞的 D-14 章节片⑤被 R0 手动停止（只读审计，未产出回收件）⇒ **D-14 章节覆盖仍缺 `spec/01` §2/§4/§5/§7/§8**，C2 判定未作；队列 **Q-034 置 blocked**（原因见该项 `block_reason`），`spec/02`／`spec/00` 收口片待派。恢复条件＝R0 明示恢复（留痕见 `doc/verify/05` R-061）。
> **2026-09-23 恢复（R0 明示）**：R0 指令「先关联下git cli」「根据自动推进循环进行项目」⇒ 计划 AI 恢复派发、D-14 续跑（章节片⑤重派＋`spec/02` 收口片同轮派出）；**r17 基线已立**（r16 基线后插入了 R0 指令的 doc 分类改造三提交，按 §9.4 重立）；长基线按 ADR-4 例行折入并登记 **ISS-056**。留痕见 `doc/verify/05` R-062。

---

## ① 九步流程全量状态

| Step | 名称 | 产出 | 评审 | 状态 |
|---|---|---|---|---|
| — | 阶段 P0 环境与骨架 | 目录骨架、`AGENTS.md`、`.gitignore`、本看板、`doc/process/00` 台账、`doc/环境搭建.md` | — | ✅ 完成（WSL 部分待用户执行 → ISS-002） |
| 1 | 理解规格 | `doc/verify/01-理解清单.md`（输入 = `doc/spec/*` 自产规格） | Requirement Review = **Review G1** | 🔄 进行（spec 已出 2/14 篇） |
| 2 | 验证点提取 | `doc/verify/02-验证点清单.md`（12 维场景分解 + 34 法推导） | Verification Point Review | ⬜ 未达 |
| 3 | 验证计划 | `doc/verify/03-验证计划.md` + 本看板 | 人确认架构 | ⬜ 未达（激励策略/工具链选型结论已散见 `AGENTS.md` §3，待正式化） |
| 4 | 平台搭建 | `tb/vr1_uvm/` + smoke | 过目 diff（重点审检查器） | ⬜ 未达 |
| 5 | 用例 + 仿真循环 | 用例 + `doc/verify/05-回归记录.md` | 抽查 | ⬜ 未达 |
| 6 | 失败调试 | `doc/verify/06-缺陷归因.md` + `doc/process/00` | 确认根因与修复决策 | ⬜ 未达 |
| 7 | 一致性与收敛检查 | `doc/verify/09-一致性与收敛检查报告.md` | Sign-off 前置 | ⬜ 未达 |
| 8 | 覆盖率收敛 | `doc/verify/07-覆盖率报告.md` | **Review 3 = Sign-off Review** | ⬜ 未达 |
| 9 | 验证报告 | `doc/verify/08-验证报告.md` | 归档 | ⬜ 未达 |

**并行线（不依赖 DUT RTL，与 Step 1 同期推进）**

| 线 | 交付 | 状态 |
|---|---|---|
| ISS（`iss/`） | Python golden ISS 骨架 → RV64IMAC 解释器 → trace 归一化与比对脚本 | ✅ **骨架完成并跑通冒烟**（retired=29，trap/MRET/CSR/CSV 全断言通过；比对器注入单点错误可被抓出）；🟡 指令覆盖与 Spike 互检未完（阻塞 ISS-002） |
| 规格书（`doc/spec/`） | 14 篇，逐篇出口判据见方案第 4 节 | 🔄 2/14（`00`、`01` 成稿待评审） |
| 工具链（`doc/环境搭建.md`） | M0 验收表 7 项 | ⏸ 阻塞在用户执行（ISS-002） |

### 里程碑（M0~M8）权威表（2026-09-23 补，登记 `doc/process/00` ISS-048）

> 此前 M 系列散见于 5 处文档、**无权威表**（ISS-048 的成因）；本表为**唯一权威源**，其余文件引用以本表为准。
> 口径：M 系列是**过程验收点**，与评审门（TR/G1/G3）不同层；**关键节点只有 3 个**——Review G1、M5、G3（`doc/项目开发流程.md` §8），M 系列是它们之间的过程点。

| 里程碑 | 定义 | 验收判据（可核查） | 出处（原散点） | 现状 |
|---|---|---|---|---|
| **M0** 环境 | 工具链与平台就绪 | `doc/环境搭建.md` §7 的 **M0 验收表 7 项**全通过（命令与版本号回贴） | `doc/环境搭建.md` §7；本文件 §② B1 | ⏸ 阻塞（ISS-002 待人执行；`_tools` 已恢复） |
| **M1** ISS-vs-ISS 互检 | 自研 ISS 与 Spike 逐指令对拍 | 定向程序 ISS 自跑出标准 CSV ↔ Spike `--log-commits` CSV，逐指令 **0 mismatch** | `doc/环境搭建.md` §0；本文件 §③ T-A/T-B | 未达（依赖 M0） |
| **M2** | **未使用**——仓内零出现、原开发方案未入仓、无定义 | — | — | 无定义；如有人认领请补定义，否则明确废弃 |
| **M3** 平台 smoke | UVM 平台可编译并跑通 smoke | `vlog` 0 error → smoke 用例 0 UVM_FATAL → **宣布平台基线**（建 `doc/verify/10`，`rtl/` 转只读） | `doc/环境搭建.md` §9 交付表行；本文件 §③ T-C（C-03 更正：原引 §6 系 git 决策段，无表） | 未达（`tb/` 未建） |
| **M4** 单元对拍 | 每模块 unit TB 与 Python 模型对拍 | 该模块 unit 回归全绿 ＋ 与模型对拍 **≥1000 随机事务** | `doc/项目开发流程.md` §5 行（C-03 更正：M4 句在 §5，原引 §4 度量表未载） | 未达（S3） |
| **M5** 核级首条 riscv-test | 首条 riscv-test 通过并取实测 IPC | 首条 riscv-test 通过（0 UVM_ERROR/FATAL）＋ **采到实测 IPC** | `AGENTS.md` §1；`doc/项目开发流程.md` §8；本文件 §③ T-E | 未达（S3 出口；**三个关键节点之一**） |
| **M6** 合规套件 | riscv-tests 与 riscv-arch-test | `AGENTS.md` §4 合规行：riscv-tests rv64ui/um/ua/uc **100%**、arch-test **≥95%** | `doc/环境搭建.md` §0；`AGENTS.md` §4 | 未达 |
| **M7** | **未使用**（同 M2） | — | — | 无定义 |
| **M8** 性能 | Verilator 高速仿真出性能数 | 实测 IPC/频率类指标产出（Verilator 为 M8 唯一合法测法） | `doc/环境搭建.md` §5/§0 | 未达（依赖 M0） |

---

## ② 当前阶段详细工作拆分

### 阻塞 / 挂起项（等什么 + 恢复动作）

| # | 项 | 等什么 | 恢复后的动作 | 台账 |
|---|---|---|---|---|
| B1 | WSL2 + 工具链 + Spike + Verilator | **用户执行 `doc/环境搭建.md` §1~§5 并回贴 M0 验收表** | 关闭 ISS-002 → 编 Spike → 打通 ISS-vs-ISS（M1） | ISS-002 |
| ~~B2~~ | ~~git 纳管方式~~ | **已闭（ISS-001 关闭）**：取方案 ② 本机 Git for Windows＋每轮基线纪律 | 见 `doc/项目开发流程.md` §9.4 | ~~ISS-001~~ |
| B3 | `spec/04` 的 PRF 不变式 | **2026-09-23 已决**（决策 AI ADR：保持 64＋不变式 `INV-1`~`INV-5`＋扩容触发）⇒ 待 `spec/04`（Q-002）落成 | `spec/04` 落 `INV-1`~`INV-5` 后 ISS-003 转已解决 | ISS-003 |
| B4 | 特权规范版本 1.13 vs 1.11 | **2026-09-23 已决**（决策 AI ADR：锁 1.13＋分期实现）⇒ 待 `spec/10`（Q-020）立三段式 | `spec/10` 落表后回填 | ISS-007 |
| B5 | Review G1 评审安排 | **2026-09-23 已定**（ISS-008 关闭）：材料＝**AI 出评审 list，人逐项签署**（§9.5 四段） | 结论落各篇篇首 + `doc/spec/14` 冻结记录 | ~~ISS-008~~ |
| B6 | ALU→ALU 3 拍 vs IPC ≥1.8 | **2026-09-23 已决**（决策 AI ADR：保持 3 拍＋PT1/PT2 触发）⇒ `spec/13`（Q-025）核验 PT1/PT2 | 命中即 G1 前升版翻选 2 拍 | ISS-012 |
| B7 | 外部经验 8 条是否吸收进流程文件 | **2026-09-23 已转**：ISS-024~028 转决策 AI（Q-016 第 3 批，**ISS-024 先呈人**）；ISS-023/029 留人 | 按 ADR 写入 `AGENTS.md` §3.1 等并回填台账 | ISS-023 ~ 030 |
| **B8** | ✅ 已解除（2026-09-23）：知识库 `_tools/` 已由人从原机补拷 | 无需再等（成因＝换机漏拷，见 ISS-032） | 已跑全库路径核查（19 条声明路径 0 缺失，并重建换机丢的 16 个空目录）+ 抽 4 条原文结论复验全部命中 → 解 ISS-032 / ISS-005，Q-009 / Q-010 解除 blocked | **ISS-032（已解决）** |

### 近期进度（工作项 + 完成情况 + 产出物路径）

| 工作项 | 情况 | 产出 |
|---|---|---|
| 仓库骨架 | ✅ | `d:\virtual_riscv\{doc,rtl,tb,iss,sw,filelist,sim,script,run_cmd,debug}` |
| AI 项目上下文 | ✅ 全部占位符已填实（含 R9/R10 数值、终止判据阈值、签核标准） | `AGENTS.md` |
| 问题台账 | ✅ **72** 条（3 待决策 / 24 处理中 / 45 已解决）——计数以 `doc/process/00` 状态总览为唯一权威源，本行为其镜像（由 `script/gate.py check` 的 board-mirrors-ledger 项校核）。**本次：ISS-066 关闭（已解决）＋ISS-067（已闭环）／ISS-068（待决策）**——Q-010 处置批 A13 增量复核（附 AA）：阻塞 0／六项低烈度同批处置／存疑-1 随呈判呈 R0（R-102~R-104）。**本次新增并即闭 ISS-067**、**新增 ISS-068（待决策）**、**新增并即闭 ISS-069（已解决：A14 轮干净窗口达成——MODIFIED 1／ADDED 0／DELETED 0）**、**新增并即闭 ISS-070（A14 终局 δ 复核四项：B-1 r25 窗口段相抵／C-1 r26 基线两说／C-2 Q-010 队列字段滞后／C-3 工具措辞；R-105／附 AB）**、**新增并即闭 ISS-071（A15 δ 复核五项：C-14 landable 过期／C-15 留痕缺口＋时序失真／C-16 单槽窗口语义误述／C-17 跨篇清单①未编入／C-18 长基线未折入；R-106／附 AC）**、**新增并即闭 ISS-072（A16 δ 终局确认四项：D-1 ISS-070 行残留／D-2 HANDOFF 跨轮行内改写／D-3 槽态两分支／D-4 landable 链缺 A14 段；＋存量看板行刷新；R-107／附 AD）**、**关闭 ISS-068（2026-09-24 R0 裁决「全 A」：认可 md5 替代核验；随 Q-010 关闭批，R-108）**。**本次新增并即闭 ISS-063/064/065（已解决）**：Q-010 互校发现 ISS 侧缺陷（SLLI 保留编码漏检／MRET 误清 MPRV）＋修复期同族两项（W 型 imm[5] 静默接受／ADDIW 过拒绝）——verifier 直修＋探针落盘＋定向用例（R-097：17/17 PASS、语义全量 RC=0）。**本次（2026-09-24 R0 裁决 C2 成立）关闭 ISS-057/058/059/061/062（已解决）**：D-14 全链闭环（附 M~附 Y；R-062~R-093），Q-034/Q-004 置 done。**本次新增 ISS-062（处理中）／关闭 ISS-053（已解决）**：A7 终局全量（R0 指定）发现——BLK-1/2 与 CLR-3/4 即处置、BLK-3/4/CLR-1/2 归 L10（详见 `doc/process/00` ISS-062）。**本次新增 ISS-061（处理中）**：L7 增量复核片 A3 发现（1 阻塞＋4 澄清，附 P；D5/L8 续处置）。**本次新增 ISS-060（处理中）**：`spec/13` 性能建模输入登记载体缺失（ADR-D4-④ 组合载体，随 L7 落地）。**本次新增 ISS-058/ISS-059（处理中）**：D-14 增量复核片 A1（4 阻塞/2 澄清/1 存疑，附 N）＋§12.5 决策审核 A2（七 ADR 17 行风险发现，附 O）；两片只读（规则 22 窗口核查 0 改），**C2 仍不成立**、D3/D4 决策片待派（`doc/verify/05` R-074）。**本次新增 ISS-057（处理中）**：D-14 章节片⑤新增 9 阻塞＋4 澄清＋1 存疑（报告＝评审记录**附 M**；**C2 不成立**，按「全量→处置→增量→全量」续跑）。**本次新增并即闭 ISS-056**（工作树行尾批量物质化事件：36 件 LF→CRLF 语义等价已验证、1 件 R-060 披露件、1 件占位删除；长基线按 ADR-4 例行折入，见 `doc/verify/05` R-062）。**本次新增并即闭 ISS-049~051**（口径漂移修正三处：未定义规则号 RA5 的残留活引用／机械判据与必报项的计数漂移／知识库流程文档版本口径过期——见 `doc/process/00` 各行与 `doc/verify/05` R-032）。**另新增并即闭 ISS-052**（`ra-token-defined` 扩面至"活引用面"：规则承载件＋工具＋队列全件、HANDOFF 仅最新记录的未完成/下一手；R0 选定 A、含双向验证，送审挂 Q-031）。**本次新增并即闭 ISS-048**（文档缺口两处：① **引用悬空的"第五条 RA 红线"**——知识库只定义 RA1~RA4（库内无该编号）⇒ 按 R0 指令**移除项目内全部引用、不新设编号**（实质由 RA1/RA3 与规则 23、§12.4 承载），并加机判 `ra-token-defined` 防回流；② **M0~M8 无权威表** ⇒ 本文件 §① 新增权威表，M2/M7 标"未使用"）。**第 12 轮处置（进行中）**：**ISS-043 关闭**（B-01：§8.1 核销行改可回查表述）、**ISS-047 关闭**（C-03：两条计数口径适用面）；**三份决策 AI ADR 已落**（ISS-039 选 A `xw_carry`／ISS-041 选 A·A′＋断言 A15~A18／ISS-045 删 `slot16_vld` 338→322）；R0 追认 S-01。 | `doc/process/00-问题记录.md` |
| 知识库精读与经验吸收 | ✅ 精读 CPU卷04（超标量设计空间）+ RISC-V 专项 5 卷 + 验证基础学习（12 维 / 34 法）+ AI验证工作流程 + `virtual_test经验` 13 卡；登记 8 条缺口并实测复现 ISS-026（6 处中文错字经落盘后字节校核全部修正） | `doc/process/00` ISS-023~031；记忆《IC验证知识库入口路由、语义仲裁优先级与只读约束》 |
| R8 编排角色与机械核销工具 | ✅ 已落地五项中的四项：`run_cmd/AutoQueue.yaml`（14 项，含可机判 done_when/landable）+ `script/gate.py`（check/dispatch/report/escalate，**以最近一次实跑末行为准**（如 `machine_r13_check.txt`＝22 PASS；D-5 起判据增至 24 PASS——D-14 纵片② 缺陷② 修正））+ `AI角色与职责.md` v1.1 + `项目开发流程.md` v1.1 + `AGENTS.md` 规则 23（并修好§1 过期状态与三处旧路径）；工具首跑即抓出**本人造成的两处缺陷**（看板计数镜像落后、错字“脉钩”（码点豁免：此处引错字本身作落实证据））。**待做第五项**：试跑队首 Q-002（`spec/04`） | `doc/process/00` ISS-030/033；四份升版件；`gate.py check` 输出 |
| 总体规格 | ✅ 含定位决策（§1.0）+ 参数总表 + 13 条取舍 + 接口冻结清单 M01~M20 | `doc/spec/00-总体规格.md` |
| 流水线与接口 | ✅ **v0.3 已收敛**（2026-09-24 R0 裁决 C2 成立、D-14 关闭；后随 Q-010 链 A13~A16）：17 级定义 + **28** 个 struct 成员位域 + 反压/恢复点 + **19 条断言（A1~A18 + A19 可选）**；第 12 轮新增 A15~A19（附 H/I/J）。**D-14（Q-004）进程**：章节片①~⑤＋纵片①②③全跑（附 M：9 阻塞/4 澄清/1 存疑，S-1 已裁域前缀）→ D1/D2 决策片（ADR-B1~B9，R-066/R-067）＋落文批 L1~L6（R-068~R-073）→ **增量复核 A1（附 N：4 阻塞/2 澄清/1 存疑）＋§12.5 审核 A2（附 O：17 行）**（ISS-058/059，C2 仍不成立）→ **D3 决策片已回收**（ADR-D3-①~⑤：B 保留在场/当拍覆盖胜出＋次拍起弹/吸收式再定向/A4 改 `##0`/活性界公式绑定；R-076）；**D4 决策片已回收**（ADR-D4-①~④，R-077）→ **落文批 L7 完成**（R-078，42/42 锚点）→ **A3 增量复核回收**（附 P）→ D5 决策片（R-080）→ L8（R-081）→ A4（附 Q）→ **A5 终局全量**（附 R：旧问题 38＝通过 37/部分 1/未改 0；新增 1＝BL-1）→ **BL-1 修（L9/R-083）＋CL-A/CL-B 处置** → **A6 收口确认（附 S/R-085）** → **R0 裁决：最严格口径再补一轮全量** → **A7 终局全量（附 T/R-086）：C2 未成立（1 载体回退＋2 文本级＋流程面）** → **即处置（`4afd5df`/`51531b6`）＋L10（R-087）** → **A8 收口窄确认（附 U/R-088）→ B-1/B-3 即修（`f314580`）→ A9 微确认（附 V/R-089）** → **R0 再裁：最严格口径再补一轮全量** → **A10 终局全量（附 W/R-090）：56/56 满足、新增 4 低烈度 → L11 全部处置（R-091）→ A11 微确认（附 X/R-092） → R0 续裁 → A12 第三轮全量（附 Y/R-093）：新增 0、旧问题 56/56、缺失型清单为空 → **R0 裁决：C2 成立（2026-09-24）→ Q-034/Q-004 置 done、台账五条关闭、D-14 关闭**。**Q-010 已闭（2026-09-24 R0 裁决「全 A」；R-108）**；下一手＝按《驱动计划》转下一件（`dispatch` 取队首；见 `run_cmd/AutoQueue.yaml`）（A16/E-1 刷新：原句「下一手＝落文批 L7…」系 D-14 期残留，与本格「C2 成立」自相矛盾） | `doc/spec/01-流水线与寄存器接口.md` |
| 环境清单 | ✅ 交用户执行 | `doc/环境搭建.md` |
| 规格↔实现一致性自查 | ✅ 修正 4 处：SRAM 位量（ROB/IQ/IBUF）、LQ 索引位宽、`csr_wr_t` 去重、取指返回双位图 | `spec/00` §4.8、`spec/01` §3.16/§3.19/§3.20/§8 |

### 下一批（按依赖顺序，无需用户输入即可推进）

0. **D-14 已关闭**（R0 裁决 2026-09-24，C2 成立；Q-034/Q-004 置 done）；**进行中＝Q-010（`spec/02` 逐指令互校收口，已派）**
1. `doc/spec/02`（逐指令行为 + μop 枚举 `isop_t` 值域）——**ISS 骨架已跑通，两边可开始互校**
2. `iss/tests/test_instr.py`：逐条定向覆盖 RV64I 剩余 ~25 条 + M 剩余 5 条（现在 README 已声明为未覆盖）
3. `doc/spec/04`（RAT/PRF 端口推导 + 不变式 `INV-1`~`INV-5`，**关闭 ISS-003**）
4. `doc/spec/03`（预测器表结构与算法，含 FTQ 读写指针四条维护规则）

---

### 计划快照（计划 AI 轮，2026-09-23；队列转写见 `run_cmd/AutoQueue.yaml`）

> 来源：vr1-planner 产出（只读，由 R8 转写为队列项与本节）；**计划→队列的转写是编排职责**，本节为快照，权威进度仍看队列与 §①。

| W | 任务 | 队列项 | 状态 |
|---|---|---|---|
| W-1 | Q-004 第 13 轮增量复核（判"新增 0"即安排终局全量轮） | Q-004 | 待基线提交后派发 |
| W-2 | Q-016 第 3 批：ISS-024/025/026/027/028（一项一片；**ISS-024 先呈人**——涉"监督通道自改须人批"） | Q-016 | 待派 |
| W-3~W-10 | spec/02（Q-010）→03（Q-006）→04（Q-002）→05（Q-017）→06（Q-018）→07（Q-019）→**10**（Q-020）→08（Q-021）→09（Q-022）→11（Q-023）→12（Q-024）→13（Q-025）→14（Q-026）；doc/verify/01（Q-007）／doc/verify/02 试点（Q-008）／ISS KNOWN_OPEN（Q-009）／平台骨架（Q-027） | 见各 ID | ready/blocked 见队列 |

> **Q-nn 映射纪律（L5 补）**：本节及全文的 Q-nn 与队列项映射以 `run_cmd/AutoQueue.yaml` 的 `id`/`task` 为唯一权威（权威在队列）；D-nn 两池对照与对账规则见 `doc/verify/03` §0 L5 注。

**篇序理由（计划 AI 原文要点）**：02 先（§3 值域引用＋ISS 互校）→03（`spec/01` 待建标注最密、ADR-1 收口）→04/07（G1 高风险）→**10 提前过 08/09**（`satp`/`mstatus`/异常入口是 08/09/11 的输入）→11/12/13 后置（需全库断言/参数源/延迟数据）→14 最后（冻结记录，G1 签署物）。**每篇独立队列项、独立走 §9.4 两档复核**（Q-004 的 `cap_waiver` 不外溢）。

**doc/verify/01 触发点**：Q-004 关闭＋ISS-020 落定 → 当日建篇（五节先铺序）。**doc/verify/02 触发点**：doc/verify/01 首版冻结（建议时点：02~07＋10 成稿）→ 起骨架，出口＝100% 覆盖 doc/verify/01 且无"待补充"。

**Q-004 可关闭出口（建议口径，待 R0 明示）**：①终局全量轮 C2 成立（新增 0＋旧问题全改＋档位声明＋取证范围自报）②报告与探针归档＋`doc/verify/05` 一行 ③R0 明示关闭。**跨篇待闭合清零是 G1 判据**，不叠进 Q-004 关闭条件（两账分开）。

**计划 AI 自报不确定项（待领队/人取舍）**：①**ISS-020 归属**——Q-002/Q-003 仍 `blocked_by: [ISS-020]` 注"需人定两个数"，而新口径下参数取值属决策 AI 自决面，二者未归一（建议：按 ISS-042 转决策 AI，或明示仍待人）②r13 diff 基线＝本次提交（已定）③`spec/11/12/13` 正式篇题待 §6 全表核对 ④smoke 激励是否需 riscv-gcc（决定 W-9 被 ISS-002 挡的比例）⑤**Q-007（doc/verify/01）现 `depends_on [Q-002, Q-006]` 与计划建议（"Q-004 关闭＋ISS-020 落定"）不一致**——未擅改，待取舍。

## ③ 回归 / 交付触发链（预置分级动作，每级带前置条件）

| 级 | 触发前置 | 动作 |
|---|---|---|
| T-A | M0 验收表 7/7 通过 | 编 Spike → `spike --log-commits` 跑通 hello ELF → 关闭 ISS-002 |
| T-B | `spec/02` 成稿 且 ISS 骨架完成 | 定向程序：ISS 自跑 → 出标准 CSV → 与 Spike CSV 逐指令比对（M1 起步） |
| T-C | UVM 平台可编译（`vlog` 0 error） | smoke 用例 → 0 UVM_FATAL → **宣布平台基线**（建 `doc/verify/10`，`rtl/` 转只读） |
| T-D | `spec/14` 接口冻结签署 | 启动 P3 RTL 实现；每模块 unit TB 先行 |
| T-E | 首条 riscv-test 通过（M5） | 采实测 IPC → 交回 ISS-012 决策 → 评估路径 A 抬档是否提前 |
| T-F | 回归全绿 | Step 7 一致性清单 → Step 8 `vcover merge` 批级判定 → Review G3 |

**回归纪律（已写入 `AGENTS.md` §5.2 规则 9/10）**：单用例 watchdog 2,000,000 拍 / 挂钟 20 min；
单轮自主仿真 ≤ 2 h；并发 ≤ 4；每套 runner 独占 `sim/run_<runner_id>/`；AB1/AB5/AB6 中止判据生效。

---

## ④ 评审状态

| Review | 对象 | 结论 | 遗留 | 签认 |
|---|---|---|---|---|
| **Review G1**（= Requirement Review，含接口冻结） | `doc/spec/00~14`，重点 `spec/10` CSR 表 / `spec/04` 端口数 / `spec/07` forwarding | ⏸ 未提交（等 14 篇齐） | — | 待用户逐篇签署 |
| Verification Point Review | `doc/verify/02` | ⬜ 未开始 | — | — |
| Sign-off Review | `doc/verify/07/08/09` + 台账闭环 | ⬜ 未开始 | — | — |
| **对抗评审轮**（`spec/01`） | `doc/spec/01` 全篇 | ⏳ **未收敛**（第 12 轮三片复核判 §6.1 C2 不成立；第 13 轮复核进行中） | **r12 三片**（附 K：A0 整轮版后到／A1 技术核销／A2 决策审核）：阻塞 3 **全部落修**（B-01 §8.1 指针、B-02 告警位名、B-03 err 归属句）；决策审核 11 条（去重 6 组）＝"需写死的边界"、**无一条主张推翻决策**；闭包 14/14 分流、真断链 0。**r13 在跑**（A=落修核销／B=七项新 ADR 审核，B 已回：决策审核 10 条＋doc/verify/04 存量 2 行）。四项决策已录入（ISS-007/017/003/012）；ISS-044/045 判"有条件关闭"（闸门＝r13） | 不适用（评审者只出问题清单；关闭与否归 R0） |

**AI 侧提交前自检义务**（《AI验证工作流》1.1 ①，G1 适用条目）：
① 规范原文已通读无遗漏章节 → 锚点可达性已恢复（2026-09-23）：`_tools/` 已补拷且经全库路径核查与 4 条原文复验（ISS-032 关闭），**本项降级注撤销**；但“通读无遗漏”仍未达成（Sv39 §4.4 待精读，ISS-005 保持处理中），且 **`spec/01` 尚处 v0.3 返工未收敛**（第 9 轮增量复核判 §6.1 C2 不成立，报告见评审记录**附 H**；ISS-034 仅关闭“轮次上限”一项，收敛本身未达成）；历史上曾因 `_tools/` 换机漏拷而不可达，
② 每条功能点标注规格出处 → ✅ **仅指“标注动作”已完成**（`spec/00` §4/§5 逐条带【超】页码 /【权】§号）；其中【超】页码可对着本地笔记复查（✅ 已做，出处 CPU卷/04）；**【权】§号的可验证性已于 2026-09-23 恢复**（`_tools/` 已补拷，抽 4 条回原文复验均命中，见 ISS-032 关闭记录），但**尚未逐条抽验**——剩余【权】引用的逐条回查归入 `spec/02/08/10` 写作时一并做（ISS-005 保持处理中）；
③ 存疑项单独列出而非臆测 → ✅（§10 未决表 + `doc/process/00`）；
④ 接口/寄存器/复位时钟三张假设表齐全 → ✅（`spec/01` §6 端口表、`spec/10` CSR 表待成稿、
`spec/00` §4.7 时钟复位行）。
