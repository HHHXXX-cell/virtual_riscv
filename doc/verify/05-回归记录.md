# 回归记录（doc/verify/05）

> 红线 R3：执行的命令、种子、结果全记于本文件；任何结论可回溯到具体 log 行。
> 本文件同时是 `doc/spec/01` §8.1 所指的“机判输出留档处”（B6：原“输出已留档”无落点，本文件即落点）。

| # | 日期 | 类型 | 命令 | 关键输出 | 结论 |
|---|---|---|---|---|---|
| R-001 | 2026-09-23 | 机械核销 | `python script/gate.py check` | 17 PASS / 1 WARN（低频字人工判）/ 0 FAIL | `spec/01` v0.2 重建后台账/看板/队列/交接/码点/表格结构一致。**（R-005 更正留痕，N17②/BLK-15：本行原多写“16 项不变式全过”无溯源、且与下方留档纪律自相矛盾，按纪律删除——项数以 check 输出末行为准）** |
| R-002 | 2026-09-23 | 位宽实算 | `python script/width_check.py` | 28 个 struct（含新增 `csrq_entry_t` 88 bit）：可机器核对 28 / 不符 0 / 未能核实 0 | 全文 struct 声明值与成员表逐项相加一致（脚本转入 `gate.py` 常驻门禁）。第 6 轮复核指出本行原写 27（漏计新增 struct），已修正 |
| R-003 | 2026-09-23 | 纯复核轮（第 5 轮） | 派 `vr1-auditor`（只读）复核 38 条落点 + 10 项专项 | 通过 33 / 部分 3 / 未落实 2；另阻塞 10（B1~B10）、待澄清 10（C1~C10）、存疑 1（S1） | **未收敛**；轮次用尽 → 置 fused 转 R0；报告全文见 `doc/review/01-评审记录.md` 附 B |
| R-004 | 2026-09-23 | 纯复核轮（第 6 轮） | 同上方法，核 B1~B10/C1~C10 + 9 项专项 | 已改 13 / 部分 7 / 未改 0；**另阻塞 17（BLK-01~17）**、待澄清 8（CLR-01~08）、存疑 1（SUS-01） | **未收敛**；R0 已批“继续直到收敛”；报告见附 D |
| R-005 | 2026-09-23 | 附 E 处置轮（第 8 轮）机判输出 | ①`python script/width_check.py`（工具已按 N16①②③ 修补：标题位宽数核对、NO-CLAIM 计入未能核实、转录基准行单列）②`python script/gate.py check` | ①28 个 struct：可机器核对 28 / 不符 0 / 未能核实 0 / 基准行 2，0 UNRESOLVED ②**18 PASS / 1 WARN（低频字人工判）/ 0 FAIL** | 附 E 全部 23 项处置（N1~N17、X1~X7、BLK-06/08/10/11/12/15 残留、CLR-05/06）已落 `spec/01` v0.3 与 `spec/00`；处置明细见评审记录**附 F**。附 E（第 7 轮）本身系评审者报告，其留档见该附录（评审者无执行工具，未实跑脚本——E-5 自报） |
| R-006 | 2026-09-23 | 第 8 轮纯复核 + 只读核查 + 第 9 轮处置机判 | ①派 vr1-auditor（只读子代理）②mtime 窗口扫描（T0=2026-09-23T10:39:34，规则 22）③处置后重跑 width_check / gate check | ①复核判定：旧问题 27/32 已改、3 部分（N10/N15/N17）、X5 部分、X6 通过；**新增 10 项（N-01~N-10，阻塞 2）**②**窗口命中 0**——评审者零写入③28/28 OK；18 PASS / 1 WARN / 0 FAIL | 第 8 轮复核：**仍未收敛**（报告入评审记录**附 G**）；第 9 轮处置（N-01~N-10 + N15③ + X5 残留）已落地；**待第 9 轮纯复核**，其输入须增列 `doc/process/00` ISS-013/ISS-037 两行（闭核验缺口） |
| R-007 | 2026-09-23 | 机制落地（§9.4 两档制）+ 台账/队列/交接回填 | ①`python script/gate.py changed`（基线 `a8a63f5`）②`python script/gate.py check`（在新增 `pending-list-stale`、`queue-full-round-cap(-waiver)` 两项判据之后跑） | ①改动集：`spec/01` +106/−65（56 hunk）、`spec/00` +19/−5、评审记录 +144/−0；引用闭包 **14 个标识符未展开**（`ckpt_held_vec`/`commit_fl_ret_vec`/`entry_offset`/`entry_pc`/`entry_vld2`/`exc_t`/`ptw_fill_valid`/`ptw_ftq_id`/`rd_is_new`/`rollback_fl_ret_vec`/`tlb_write_en`/`vr1_cexpand`/`vr1_exu_int`/`width_check`）②**21 PASS / 1 WARN（低频字人工判）/ 0 FAIL** | 人 2026-09-23 指令的三处落地：`doc/项目开发流程.md` §9.4（两档定义 + 复核面三者并集 + **每轮处置开工前提交 git 基线** + 全量轮上限 5 与 `cap_waiver` 口径）、`.qoder/agents/vr1-auditor.md`（机判已覆盖者抽查复算 ≥5、未覆盖者一律实算、取证一律文件+节号）、`script/gate.py`（新增 `changed` 子命令与两项判据）。同批回填：`doc/process/00` 待决策清单移除已闭环三行（ISS-001/030/032）、ISS-001 关闭、新增 ISS-038；`doc/verify/04` 镜像改 38 条；`AutoQueue.yaml`（Q-004 补 `full_rounds: 5` 并靠 `cap_waiver` 越权、Q-013 解 ISS-001、新增 Q-015）；`HANDOFF` 补录第 8/9 轮三条 + 本轮一条（此前 `handoff-exists` 因 Q-015 缺记录 FAIL，补录后转 PASS）。**本行不改 `spec/01` 正文**——第 9 轮纯复核的 diff 基线仍须为 `a8a63f5`，故本轮**不做任何 git 提交**。（复核面后续由 3 来源扩为 7 来源，见 R-008）**（同日更正注）**闭包数首测为 3，系 `changed` 的闭包语料把 `01-评审记录.md` 也算进命中数所致：评审记录是叙事件，其提及会把"只在被改处出现"的 token 抬到 hits≥2 而**漏报**（同时把 `cap_waiver`/`fused` 这类队列词误报成断链候选）。语料已收窄为**规格篇正文**（`NN-*.md`，排除评审记录/探针），同一基线重跑得 14 个未展开项——即前次报 3 是**工具自造假阴**，以 14 为准 |
| R-008 | 2026-09-23 | 机制落地第 2 批（复核面扩至七来源 + `changed` 输出瘦身） | ①`python script/gate.py changed`（默认）②`python script/gate.py changed --all` | ①默认 **124 行**＝14 个"闭包未展开" + 27 个枢纽项（命中 ≥10）+ 中间档 109 个折一行统计 + 0 命中的另列一行 ②`--all` **232 行**（全表）。**行数随改动文件数变化，可复现的是规则而非常数**：默认＝只列"未展开"与 ≥10 的枢纽项，中间档折一行 | 人 2026-09-23 追问"复核面三来源是不是只有这三个 / gate.py 的 diff 检查能否减弱"的落实：①`doc/项目开发流程.md` §9.4 复核面表 **3 行 → 7 行**（④机判未决项 ⑤上轮自报的诚实边界 ⑥转派/路由项 ⑦缺失型＝**无取法**、仅全量轮可覆盖），并补"④ 需分流后再定级"段；同步修本节一处自相矛盾（两档表原写"改动集＝作者声明的改动节 ∪ 机算闭包"，与后文"改动集由机器生成、不由作者自报"冲突 → 定案"机算 ＋ 作者只能加不能减"）②`changed` 默认只列真信号、`--all` 取全表；尾注加分流提示 ③`.qoder/agents/vr1-auditor.md` 工作流第 2~3 条同步（七来源 + ④ 分流义务）。**未减弱任何覆盖面**：`changed` 本不产 PASS/FAIL（是范围生成器而非判据），瘦身只减展示量——`--all` 仍给全表，闭包计算未动 |

| R-009 | 2026-09-23 | 第 9 轮增量复核轮（§9.4 新档，首次实跑）+ 只读核查 | ①派 `vr1-auditor`（模式 T 只读子代理），输入 = 七来源并集 + 机判三件（`doc/review/01-评审探针/machine_r9_check.txt` / `machine_r9_width.txt` / `machine_r9_changed_all.txt`）②mtime 窗口扫描（规则 22，**T0 = 2026-09-23T12:26:50**）③复核后重跑 `python script/gate.py check` | ①判 18 条（13 声称行 + N10/N15/N17 + X5/X6）：**已改 15 / 部分 2（N-04 walk 槽 15 跨窗、N17① 计数对账）/ 通过 1（X6）**；**新增阻塞 2（H-01 `rd_paddr` 上游断链、H-02 walk 槽 15 跨窗几何不可实现）+ 需澄清 5（H-03~H-07）**；闭包 14 项分流完（13 留痕/名称类 + `rd_is_new`→`new_alloc` 绑定 1，**无真断链**）；抽查复算 ≥5（含两转录基准行 `ftq_entry_t` 145 / `rmap_uop_t` 322、标题位宽数 `ret_ctrl_t` 49 与 `excp_t` 177），机判未覆盖部分逐项实算（`mem_req_t` 143、栈条目 27、`older(e,f)` 三边界、N-01 双重归还审计）②**窗口命中 0**——评审者零写入，探针未留忽略区③21 PASS / 1 WARN（低频字人工判）/ 0 FAIL；**归档后末次复跑 21 PASS / 0 WARN / 0 FAIL**（WARN 归零系备注④的字频效应，非判据放宽） | 第 9 轮增量复核：**仍未收敛**（§6.1 C2 不成立），报告全文入评审记录**附 H**（含复核者自报的未读范围，供下轮⑤）。**本档实测有效**：复核面收窄为"改动集及其闭包 + 历史未闭环 + 机判未决"后，仍抓出 2 个阻塞——且两个都属"改了下游、上游仍空 / 新判据自身无出口"，与 §9.4 所述"最大一类缺陷住在没改的那一侧"一致，说明**七来源并集未削弱覆盖面**（若只审 diff 会漏 H-01 的 §3.11 侧）。**⑦缺失型本轮未覆盖**（复核者已显式声明），由终局全量轮承担。第 10 轮处置输入 = 附 H 的 H-01~H-07 + N-04 部分 + N17① 残余；**本轮不改 `spec/01`/`spec/00` 正文**，diff 基线仍保持 `a8a63f5` |

④ 低频字 WARN 集合本轮变动：原 6 字（苗/狠/淡/隶/乙/帽）因附 H 正文**引述其判读**而使频次 ≥2 自动退出，仅余 `届`（`.qoder/handoff/HANDOFF.md` 轮次 9 条“届时”）——**人工判为正当词**，与复核者 H§1 的六字判读同口径 |

| R-010 | 2026-09-23 | 第 10 轮处置轮（§9.4 基线纪律首次走完一整圈）+ 机判 | ①处置开工前提交基线 `cf39eeb`（“Q-004 r10 基线：固化第 9 轮增量复核结果（附 H）”）②改正文（附 H 的 H-01~H-07 + N-04 部分 + N17① 残余）③处置后 `python script/width_check.py` 与 `python script/gate.py check` 重跑 ④`python script/gate.py changed cf39eeb --all` 生成下一轮复核面 ⑤`git status` 逐项审阅后按路径显式提交 | ①基线提交前 `git status` 逐项核对：4 处修改 + 9 处新文件全部为脚手架/探针/证据，无夹带 ②`H-01`：`disp_x_t` 补 `rd_paddr` 6 bit（详见 §3.11），322→**328 bit**；`spec/00` §4.8 **新增一行**（IQ→EU 分发寄存 1,968 b = `6 × 328`），原「级间 μop 寄存」行保持 2,244 b 不动 ③`H-03/H-04/H-05/H-06/H-07` 与 `N-04`（槽 15 情形标「待定（ISS-039）」）、`N17①`（§8.1 计数口径改为“以最近一次实跑末行为准”）均已落 `spec/01`；`doc/review/01-评审记录.md` 附 G-3 补**更正注（H-03）** ④`width_check` 28 个 struct 可机器核对 28 / 不符 0 / 未能核实 0；`gate.py check` **21 PASS / 0 WARN / 0 FAIL** ⑤`changed cf39eeb --all`：`spec/01` +28/−20（21 hunk）、`spec/00` +1、`doc/process/00` +3/−2、`doc/verify/04` +1/−1、评审记录 +1、队列 +1/−1、`gate.py` +4；**闭包未展开 7 项**（`cont`/`device`/`inside`/`no_prefetch`/`pma`/`slot16_vld`/`strong_order`，待复核者按来源①分流；计数历经 9→7→127→8→7 四代，末三代系探针扫描缺陷与记录文本污染所致，见 R-011） | 附 H 全部 7 项阻塞/澄清已处置：**H-01 真断链（上游缺目的号载体）已闭**、H-02 经 R0 选 **E（推迟到 spec/03 出`块布局限制`细则后收口，G1 前必须闭合）**、H-03~H-07 均已落正文；**N-04 按 R0 的 E 选项转为 `doc/process/00` ISS-039 处理中（带 G1 前闭合义务）**。**待第 10 轮增量复核判 §6.1 C2**，其 diff 基线固定为 `cf39eeb`（本轮处置输出即复核面，机判三件已生成：`machine_r10_{check,width,changed_all}.txt` + `snapshot_r10_spec0{0,1}.md`） |

**R-010 备注**：本轮起 §9.4 的基线纪律闭环可机械复现——“基线提交 → 动正文 → 机判 → `changed` 生成复核面 → 再提交”五步全部有落盘证据；复核轮的 diff 基线恒为**上一处置轮开工前**那次提交，故 R-009 与 R-010 之间的 `cf39eeb` **不得**再被后续提交覆盖或改写（若需改写须在评审记录留痕并重生成 `changed`）。

| R-011 | 2026-09-23 | 工具缺陷修复（复现→修→复测）+ 探针固化 | ①`python script/gate.py changed cf39eeb --all`（探针件入版本控制后重跑，复现）②修 `cmd_changed` 扫描侧 ③重跑（记录/台账侧仍污染）④修抽取侧（抽取来源与闭包语料同类：只取规格篇正文）⑤同基线第四次重跑（复测落定） | **四代计数轨迹**：**7**（探针未入版本时，真值）→ **127**（探针入版本后被当正文抽取，含 `clk_i`/`from`/`amo_op`/`fencei_ack_i`）→ **8**（只排除探针后，记录文本引述的示例词又添 4 项）→ **7**（抽取来源限规格篇正文后落定）。**旧代码可复跑**：第 2/3 代行为可用 `git show 05f759b:script/gate.py` 取出对同一基线重跑验证（R3 可回溯）。未展开 7 项全在正文侧：`cont`/`device`/`inside`/`no_prefetch`/`pma`/`slot16_vld`/`strong_order` | 立案 `doc/process/00` **ISS-040**（分类=平台，按红线 R2 直接修并登记、已闭环）。**与第 7 轮同类**：那轮修的是闭包**语料**（计数侧误含评审记录），本轮补上**抽取侧**（被改标识符的来源）与探针目录；两侧合上后进入闭包的只有规格篇正文。**未减弱覆盖面**：记录/台账/探针/脚本仍逐文件列在改动清单中并标注「不参与标识符闭包」，`--all` 语义未变，收窄的只是"哪些行算标识符来源"——且反向堵住了一处**假阴**（记录提及可把真孤儿抬到 hits≥2 而漏报） |

**R-011 备注**：本轮的 `changed` 输出共经四代（7 → 127 → 8 → 7），**以最终版 `machine_r10_changed_all.txt` 为准**（第 10 轮增量复核的机判输入即此版）；前三代不再另存文件——第 2/3 代可由 `git show 05f759b:script/gate.py` 对同一基线复跑复现。修复本身（`script/gate.py` 的 +19/−3）**留在复核面内**，复核者可审计该改动是否夹带放宽：判据函数未动，只改了标识符抽取的来源条件与显示标注。

**R-010 / R-011 对账更正注（第 11 轮处置补，I-05 处置；原文不改写）**：I-05 指出同一文件三种 diff 数字并存。本轮用 git 实测对账（`git show --numstat 05f759b` / `e502ba8`、`git diff --numstat cf39eeb..ec063fb`；git 2.52.0，命令可复跑）判明：三种数字各有"作用域"，**其中一处确为错误**——① R-010 ⑤ 的 `gate.py` +4 / `doc/process/00` +3/−2 / 队列 +1/−1 是 **05f759b 单提交**的 diff，就单提交作用域而言正确（缺陷在未标注作用域）；② R-011 备注的 `gate.py` **+19/−3 错**：e502ba8 单提交实为 **+16/−3**，连同 05f759b 的 +4/−0 合计 **+20/−3**（与机判件一致）；③ `machine_r10_changed_all.txt` 的逐文件数字是**其生成时刻**对 `cf39eeb..当时 HEAD` 的快照——机判件自身也是快照，本轮复测时 `cf39eeb..ec063fb` 的 `doc/process/00` 已为 +8/−4（机判生成后 doc/process/00 又添 ISS-041 登记行）。**权威口径（自本轮起）**：对账以**最新一代机判件**为准，引用时须连带其生成时刻的 HEAD 与基线 commit；手工记录的数字必须标注作用域（单提交/区间）。**轨迹口径统一为 7 → 127 → 8 → 7**（四代，终代即机判件真值）；R-010 ⑤ 首项"9"系领队笔误（HANDOFF 已注）。本条不影响任何实质判定（复核者 I§7 的 26 项核对与 8 项发现均据此判读）。**适用面与优先级（C-03 补，2026-09-23）**：本注口径只适用于**diff 数字对账**；**门禁/评审引用**取  §8.1 表头的"最新归档件"口径；两者相遇以归档件为权威，未落件的实跑只作过程记录引用并须注明（先例：r9 的 0 WARN 与 1 WARN 之差，见 B-01 更正）

| R-012 | 2026-09-23 | 第 10 轮增量复核轮（§9.4 第二档第 2 次实跑）+ 只读核查 | ①派 `vr1-auditor`（模式 T 只读子代理），输入 = 七来源并集 + 机判三件（`machine_r10_{check,width,changed_all}.txt`）+ 快照两件 ②mtime 窗口扫描（规则 22，**T0 = 2026-09-23T12:59:28 +0800**）③复核后重跑 `python script/gate.py check` | ①附 H 的 13 项处置判 **11 已改 / 开放 3（H-02、N-04、ISS-039，同一缺口）/ 未改 0**；闭包 7 项**全部分流完毕**（`inside`=SV 关键字、`pma`/`strong_order`/`no_prefetch`/`device`=`mmu_transl_rsp_t` 子域枚举、`cont`=ISS-039 机制名、`slot16_vld`=转 I-06）——**7/7 非断链**；**新增 8 项（I-01~I-08）：阻塞 2（I-01 §3.10「W 级停放」语义未定义可构造环等死锁、I-02 §3.19 规则 4「静默丢弃」缺时序前提与告警挂钩、可成假绿通道）、需澄清 4（I-03 §3.11 注与 §3.8 载荷字面冲突、I-04 §4.8 指针名错位、I-05 记录计数三口径并存、I-06 `slot16_vld` 恒真疑义）、存疑 2（I-07 归属裁决、I-08 状态事实）**；算术义务：抽查复算 6 项（含 2 转录基准行 + 2 标题位宽项）全符、机器未覆盖 4 项（`rd_paddr` 6 bit 边界、328×6、跨 struct 键、C 类两分析）已实算——**未触发"机判不可信"**；工具修复审计：`cmd_changed` 外无任何闭包抽取代码、七判据函数无放宽痕迹（局限：无 git 工具，diff 本体未逐行核，已入盲区）②**窗口命中 0**——复核者零写入，探针未留忽略区（同 R-009/R-010 例）③21 PASS / 0 WARN / 0 FAIL | 第 10 轮增量复核：**仍未收敛**（§6.1 C2 不成立），报告全文入评审记录**附 I**（含 I§8 盲区声明与 I§9 领队结论）。**档位有效性第二次实测**：本轮缺陷住在"**刚改的那一侧**"（H-05 新写语句不完备 → I-01；csrq 新写规则 4 → I-02），与第 9 轮"缺陷住在没改的那一侧"互为镜像——说明两种档位都不可替代、也不互相削弱。**I-01/I-02 属新机制语义分叉（含接口影响），按 §12.3 呈 R0**（登记 `doc/process/00` ISS-041）；I-03~I-06 为文本/对账类，作者可直接处置；I-07 领队判为按 §10 既有收录口径的机械补录（若 R0 认否即回退）；I-08 无动作。第 11 轮处置输入 = 附 I 的 I-01~I-08 + R0 对 ISS-041 的裁定 |

| R-013 | 2026-09-23 | 第 11 轮处置轮（附 I 的 I-01~I-08 + R0 对 ISS-041 裁定；§9.4 基线纪律第 2 圈）+ 机判 | ①R0 裁定先落台账（ISS-041→处理中、选 C；移出待决策清单；`doc/verify/04` 镜像同步）②改正文（见右列的逐条处置）③`python script/width_check.py` 与 `python script/gate.py check` 重跑 ④`python script/gate.py changed ec063fb --all` 生成第 11 轮复核面 ⑤`git status` 逐项审阅后按路径显式提交 | ①**R0 2026-09-23 两项裁定**：**ISS-041 选 C（I-01/I-02 两项都推迟）**、**I-07 补入 `spec/00` §10**。②**I-01 落文本**：§3.10 `src_kind` 行加「待定（ISS-041）」——停放槽容量与上界/是否阻断其他发射/重选语义/等待出口可判定性四项明列未定，`spec/04` 收口（G1 前必须闭合）；并锁 R0 的约束"停放路径不得形成环等死锁"（附死锁构造与"§7 A12 检不出"）。**I-02 落文本**：§3.19 规则 4 加「待定（ISS-041）」——迟到写前提（两写口最迟到达界 `k` 与出队先后约定）未给、"丢弃"无告警/断言挂钩，`spec/13`/`spec/04` 收口；锁约束"不得把丢弃作为合法 CSR 写路径的兜底"（附 `csr_old` 缺值→提交仍成功→只查提交接口即假绿的路径），闭合前实现至少须让丢弃可见（sticky 告警 + 计数 + 进 §7 断言集）。③**I-03**：§3.11 `rd_paddr` 注更正为"随 `iq_wr_t` 226 bit 载荷存进 IQ、发射选择拍可读出，但条目发射后即回收、下游无 IQ 回读口故须 `disp_x_t` 携带"（与 §3.8 载荷、§3.9 附加数组口径统一，H-01 结论不变）。**I-04**：§3.11 合计行指针改写为 `spec/00` §4.8 **IQ→EU 分发寄存（`disp_x_t`）1,968 b = `6 × 328`**（原误写"级间 μop 寄存"，该行 2,244 b 未动）。**I-05**：`doc/verify/05` 补 **R-010/R-011 对账更正注**——经 git 实测（`git show --numstat 05f759b`/`e502ba8`、`git diff --numstat cf39eeb..ec063fb`）：R-010 ⑤ 三数是 05f759b 单提交作用域（正确但未标作用域）、R-011 备注的 `gate.py` **+19/−3 错（实为 e502ba8 单提交 +16/−3，合计 +20/−3）**、机判件是其生成时刻快照；**定权威口径**"对账以最新一代机判件为准、手工数字须标作用域"，轨迹统一 **7→127→8→7**；§8.1 宽度/核销两行改引最新机判件（补引 r10 未引件 + 本轮 r11 件）。**I-06**：§3.16 `slot16_vld` 行加「待定」——恒真冗余疑义与不完整构型判据转 `spec/03`（消费方）/`spec/09` 定稿（或按设计选择删冗余字段）。**I-07**：`spec/00` §10 补 ISS-039 行（`spec/03` 收口、G1 前闭合）；并按同一收录口径补 ISS-041 行（`spec/04`/`spec/13` 收口、G1 前闭合）——ISS-041 行为作者按同口径的对称补录，随本轮复核面受审。④机判：`width_check` **28 个 struct 可机器核对 28 / 不符 0 / 未能核实 0 / 基准行 2**；`gate.py check` **21 PASS / 0 WARN / 0 FAIL**。⑤`changed ec063fb --all`：`spec/01` +7/−7（7 hunk）、`spec/00` +2、`doc/process/00`/`doc/verify/04`/`doc/verify/05` 记录侧只列不计；被改标识符 16 个、**闭包未展开 1 项**（`slot16_vld`——本轮刚转 `spec/03`/`09` 持有，属来源③非断链，待复核者按分流口径确认）；机判三件 + 快照两件已入 `doc/review/01-评审探针/`（`machine_r11_*`、`snapshot_r11_spec0{0,1}.md`） | **待第 11 轮增量复核判 §6.1 C2**，其 diff 基线固定为 `ec063fb`（本轮处置输出即复核面）。处置输入＝附 I 的 I-01~I-08 + R0 裁定，I-08（状态事实、无动作）亦已随本轮记录留痕 |

**R-013 备注**：本轮起机判件归档固定为 **UTF-8 转码**落盘（本轮首生成时误写为 GBK，已用 `iconv -f GBK -t UTF-8` 重生成并以 `file` 核对，与 r9/r10 件格式一致）——避免复核者按 UTF-8 读取时出现"工具审计局限"类假盲区。

| R-015 | 2026-09-23 | 第 11 轮增量复核轮（§9.4 第二档第 3 次实跑）+ 报告归档 + 只读核查 | ①派 `vr1-auditor`（模式 T 只读，无执行工具；**T0 = 2026-09-23T13:34:37 +0800**）②报告全文归档入评审记录**附 J**（J§1~J§6 复核者原文 + J§7 领队结论）③按规则 22 扫 mtime 窗口 ④复核后重跑 `gate.py check` | ①**仍未收敛**（§6.1 C2 不成立）：核对 21 项 → **通过 15 / 阻塞 2 / 需澄清 3 / 存疑 1**——阻塞＝**B-01**（`spec/01` §8.1 核销行的 r9"同值"引用与归档件不符：r9 件实为 21/1/0，0 WARN 出自未落件的复跑）、**B-02**（两项「待定」的闭合义务无验收物：§7 A1~A14 无任何写口/停放类断言，§10 行却写"断言集（A12/A14 面）随同补全"）；需澄清＝C-01（`slot16_vld` 义务无追踪载体）、C-02（`doc/verify/04` 存量行陈旧）、C-03（两条计数口径适用面未写明）；存疑＝S-01（§10 ISS-041 行"断言集"字样越 R0-C 字面）。**上轮 I-01~I-08 全数核销**（I-01/I-02 判真改、I-03~I-07 通过、I-08 留痕一致）；闭包 1 项 `slot16_vld` 分流＝**来源③**（未写篇目持有，非断链）；算术义务抽查 8 项全符（含 2 转录基准行 `ftq_entry_t` 145/`rmap_uop_t` 322 + 2 标题位宽项 `ret_ctrl_t` 49/`excp_t` 177）——**未触发"机判不可信"** ②**只读核查**：mtime 窗口（排除 `.git`/`sim`/`script/tmp`）命中 10 处＝9 个文件（**全部属领队同日口径变更提交 `4e23924`**，`git show --numstat 4e23924` 逐文件核对一致）+ 1 个 `script/__pycache__/gate.cpython-314.pyc`（领队导入 `gate.py` 的缓存）⇒ **复核者零写入**、探针未留忽略区 ③21 PASS / 0 WARN / 0 FAIL | **新增项的性质变化（记档，供 §9.4 档位有效性观察）**：第 9 轮缺陷住"没改的那一侧"、第 10 轮住"刚改的那一侧"（皆机制语义），**本轮两项阻塞均非机制语义缺口**——B-01 是记录/引用对账、B-02 是推迟项的验收物缺口 ⇒ 语义层趋收敛，余下两类分别可机械修（B-01/C-03）与可由决策 AI 补（B-02/C-01）。**登记**：ISS-043（B-01）、ISS-044（B-02，承接 S-01）、ISS-045（C-01）、ISS-046（C-02，**已即修即闭**）、ISS-047（C-03），台账 42→**47**。**下一手**：第 12 轮处置（B-01/C-03 文本修 + Q-016 的 ADR 落地）→ 第 12 轮增量复核 |

| R-016 | 2026-09-23 | 第 12 轮处置（第 1 批文本修；开工基线 `c6ac86a`） | ①按 S-01 追认记录裁定（`doc/process/00` ISS-044 + 附 J J§7）②`spec/01` §8.1 核销行改可回查表述（**B-01/ISS-043**）③§8.1 表头新增"两条计数口径的适用面与优先级"，`doc/verify/05` R-010/R-011 更正注同步（**C-03/ISS-047**）④机判重跑 | ①S-01 关闭为"已追认"②修正后表述：r9 归档件末行＝**21 PASS / 1 WARN / 0 FAIL**，其"0 WARN"出自归档后末次复跑、未落件（出处 R-009 备注④）——引用件与所述值从此可同时成立 ③口径适用面：**门禁引用取最新归档件／对账引用取最新机判件／相遇以归档件为权威**（未落件者只作过程记录并须注明）④机判：`gate.py check` **21 PASS / 0 WARN / 0 FAIL**；台账 47 条＝3 待决策 / **25 处理中** / **19 已解决**（ISS-043/047 关闭） | 第 12 轮处置**未完成**：ISS-044/ISS-045 待 **Q-016**（决策 AI，后台运行中）的 ADR 落地（断言编号＋SVA 草案、`slot16_vld` 取值域、ISS-039/041 机制选定），完成后才生成 r12 复核面。**S-01 已由 R0 追认，无遗留** |

| R-017 | 2026-09-23 | 第 12 轮处置（第 2 批）：Q-016 决策 AI 首批三份 ADR 落地 + `_tools` 事实更正 | ①派 `vr1-decider`（只读；**首批三项**＝ISS-039 取指跨窗机制 / ISS-041 两项机制语义（含 B-02 要求的断言草案）/ ISS-045 `slot16_vld` 取值域）②按 ADR 改 `spec/01`（§1.1 P5、§3.10、§3.16 ×3 处、§3.19 规则 4、§7 新增断言）与 `spec/00` §10 两行 ③回填 `doc/process/00` 四行（ISS-039/041/044/045）④复跑 `width_check` ⑤修 `AGENTS.md` §1 的过期警告 | ①**ADR-1（ISS-039）：选 A**——P5 增设 `xw_carry`（尾半字保持寄存器，≥57 bit）+ 次窗拼接；"禁跨 32B 边界拼接"改"**仅允许经该寄存器拼接**"；本窗 walk 止于槽 15、次窗先产出拼接指令再自槽 1 续 walk；基址不连续/冲刷 ⇒ carry 作废；**锚点＝规范"指令对齐于任意 16bit 边界"（本机提取件一级依据）＋ cv32e40p `aligner` 与 ibex `fetch_fifo` 双参考核同型拼接 ＋ 几何/吞吐实算（仅槽 15 跨窗、缺量恒 1 槽；D 选项覆盖不足的 11 条指令构型反例）**；取代 R0 的 E ②**ADR-2（ISS-041）：I-01 选 A**（门控并入 P9 发射选择拍、不选中等下拍重判、**取消 W 级停放状态**、不阻断，附死锁自由构造证明）、**I-02 选 A′**（**构造性上界**：写 ≤`E` 拍、出队 ≥ finish+1 ≥ 写口点火+1 ⇒ 合法写恒先于出队，"到达已出队"只属被冲刷截断条目 ⇒ 丢弃无损；**丢弃两分类** `drop_cnt_squash`/`sticky_drop_illegal`，`K` 暂定 2 由 `spec/13` 定值）；**取代 R0 的 C**（R0 锁定的两条 guard 保留为正文约束）；**§7 新增 A15/A16/A17/A18（+A19 可选）**——把 B-02 的"验收物缺失"闭合 ③**ADR-3（ISS-045/I-06）：删 `slot16_vld`**——`icache_rsp_t` **338→322 bit**（窗恒 32B 对齐 ⇒ 16 槽恒在窗内、恒 1 冗余，无"不完整构型"可定义）；替代＝`valid ∧ ¬err` + `err_pos` 截断；回退点＝亚 32B 取指粒度若出现 ④**事实更正（决策 AI 提出，采纳）**：派发词称"`_tools/` 缺失、规范原文不可达"**已过期**——ISS-032 已闭合、`_tools` 已恢复，本会话直读 `riscv_spec_full.txt` 与参考核源码（ADR 锚点因此为一级依据）；`AGENTS.md` §1 的过期警告同步撤销 ⑤机判：`width_check` 28 个 struct 可机器核对 28 / 不符 0 / **`icache_rsp_t` 322 OK**；`gate.py check` 21 PASS / 0 WARN / 0 FAIL | **三项 ADR 均属设计取舍（b 类）且含接口位宽变更（322）与新增断言 ⇒ 必须进 §9.5 评审 list ④ 段**（G1 前由人读）；**下游义务**已写入各 ADR 节与 `spec/00` §10 两行（`spec/03` 块布局细则、`spec/05` F2 mux 时序、`spec/13` `K` 值与"出队 ≥ finish+1"、`spec/04` csrq 状态面、`doc/verify/02` 验证点组×6）；**Q-016 余下 8 项**（ISS-003/007/012/017/024~028）未开工——按新口径属同批自决面，留待第二批派发 |

| R-018 | 2026-09-23 | 流程追加：决策审核（一次·只记录，`doc/项目开发流程.md` **§12.5**）+ Q-016 第 2 批派发 + r12 复核派发 | ①按人第二条指令新增 §12.5（四处落点：流程文件 / `vr1-auditor.md` 工作流第 0 步 / `vr1-decider.md` 决策审核节 / `doc/process/00` ISS-042 追加项 ⑤）②派 Q-016 第 2 批（ISS-003/007/012/017，后台）③生成 r12 复核面并派第 12 轮增量复核（后台，**T0 = 2026-09-23T14:48:38 +0800**） | ①§12.5 要点：审一次·只记录·**不作决定性反驳依据**（不自动回退/不阻断）；审五类（无锚点/无回退/下游义务缺失/正文自相矛盾/致死反例）；记 `doc/verify/05` + 评审附件（RA4 不得删除）；由决策 AI 在"剩余分歧"回应；仍保留：R6/R7 恒转人、人签署与批准权、无锚点项只能标"登记＋预测＋回退点"、记录/一致性缺陷照原流程 ②**r12 复核对 ADR-1~3 的检查即三项决策的首次审核**（非决定性）③r12 复核面固化：`machine_r12_*` + 快照两件（提交 `d04deb7`，基线 `c6ac86a`；闭包 14 项未展开待分流）④Q-016 第 2 批派发词含"RVFI 逐字段比对 + IPC 一阶定量模型 + PRF 最坏界实算 + 规范版本锁定"四项，并明确"`_tools` 已恢复、锚点用规范提取件一级依据" | **待两件后台返回**：①第 12 轮复核报告 → 归档附 K（含 §12.5 首次决策审核的记录）②Q-016 第 2 批 ADR → 落地并入 r12/r13 处置。**Q-016 第 3 批**（ISS-024~028 工具/平台类）未派 |

| R-019 | 2026-09-23 | 流程细化（§12.5 以风险为主轴）+ 两处文档缺口即办（ISS-048） | ①按人第二条指令后半"**对于决策 ai 的审核主要考虑的是风险**"改 §12.5 的"审什么"行为风险主轴五类（判错/不可逆/下游/自洽/致死），并同步 `.qoder/agents/vr1-auditor.md` 第 0 步与 `vr1-decider.md`（回应须按风险口径：接受/缓解/维持原判）②**RA5 登记**：项目侧定义落 `AGENTS.md` 规则 23 后（＝"评审未收敛不得默认通过、不得宣布关闭或进入下一阶段"；依据＝"门禁前停"边界，见 ISS-033；RA1~RA4 仍指库 §8）③**M0~M8 权威表**落 `doc/verify/04` §①（逐项：定义／验收判据／原散点出处／现状；**M2/M7 标"未使用（无定义）"**）④登记 `doc/process/00` **ISS-048**（已解决）⑤机判重跑 | ①§12.5 新增要点：每条风险须写 **触发条件＋影响面＋现有兜底＋残余风险结论**（`可接受`/`需进 §9.5 list ④段`/`需决策 AI 回应`）；**措辞/体例/编号类非风险发现不入决策审核清单**（转记录类原流程）②RA5 定义与四处引用对齐（此前知识库只定义 RA1~RA4，库内全文 grep 无 RA5 ⇒ 引用悬空）；**知识库保持只读**，如需库内补定义须人明确指令＋双确认＋留痕 ③M 表覆盖 M0（环境，7 项验收表）/M1（ISS-vs-ISS）/M3（平台 smoke）/M4（单元对拍 ≥1000）/M5（首条 riscv-test＋实测 IPC）/M6（合规套件）/M8（性能）；**关键节点仍只有 3 个**（Review G1/M5/G3），M 系列是过程验收点 ④台账 **48 条＝3 待决策 / 25 处理中 / 20 已解决** ⑤机判：`gate.py check` **21 PASS / 0 WARN / 0 FAIL** | 两处缺口均为"引用悬空"类，**不阻塞主线**；`doc/verify/04` §③ 触发链（T-A~T-F）与 M 表互补（触发链管"前置→动作"，M 表管"定义与判据"）。**留痕事故**：本条编辑期一次不完整替换误删 `ISS-042` 行首（已即时修复，台账连续性由 `gate ledger-contiguous` 复核通过）——按"声明与实态脱钩"教训记档 |

| R-020 | 2026-09-23 | RA 编号纠偏（R0 改判）：移除"第五条 RA"引用、不新设编号 + 新增机判防回流 | ①按 R0 指令"如果之前没有 RA5 那就去掉项目中的 RA5 引用；如果有必要增加新规则再增加 RA5"重判 ②移除规则承载件 7 处引用（`AGENTS.md` 规则 23 与 RA 记号说明、`doc/项目开发流程.md` §9.4/§12.4、`doc/AI角色与职责.md` R8 行、`vr1-auditor.md`/`vr1-decider.md`）③`AGENTS.md` RA 记号说明改写为"**RA 系列只有 RA1~RA4；本项目不新设额外编号**；草拟新红线先登记编号再引用" ④新增机判 `gate.py ra-token-defined` ⑤`doc/review/01-评审记录.md` 篇首加**记号注**（历史附件原文不改写）⑥台账 ISS-048 加"同日改判"追加注 ⑦机判重跑 | ①**判定"必要性"结论：无需新设编号**——被 RA5 指代的实质只有两句：**门禁权在人**＝RA1 已覆盖；**不得默认通过**＝RA3（不可回退事项必须等人明确选择）＋项目弃用 24h 默认策略（§12.4 尾注）＋规则 23 明文。挂一个不存在的编号不增加约束力，只制造"引用悬空"②**未削弱任何判据**：移除的是编号引用，同义条款原文都在（逐处替换为 RA1/RA3/规则 23/§12.4，非删除条款）③防回流判据：`ra-token-defined` 只扫**规则承载件**（AGENTS/流程/角色表/两个子章程），台账与评审记录允许出现该字样（那是"已移除"的事后记载）④机判：`gate.py check` **22 PASS / 0 WARN / 0 FAIL**（判据数 21→22） | **历史附件（评审记录附 B/E/I、旧 HANDOFF 条目）中的该字样按"原文不改写"保留**，由篇首记号注统一说明——若 R0 要求连历史记录也逐处标注，可另批执行。**"如有必要再加"的判定接口**：若将来确需新红线，按 `AGENTS.md` RA 记号说明"**先登记编号与条文、再引用**"，届时 `ra-token-defined` 的 `RA_DEFINED` 集合随登记同步扩展 |

**R-020 备注**：第 12 轮复核与 Q-016 第 2 批的**首次后台派发被 R0 手动停止、无产出**；且首次派发后复核面又并入了流程提交（§12.5 / RA 编号纠偏 / ISS-048 / M0~M8 表 / `gate.py` 新判据），故**按现 HEAD 重生成 `machine_r12_*` 与快照**（`spec/01` +14/−9 十处 hunk、`spec/00` +2/−2；闭包未展开仍 14 项），并以 **T0 = 2026-09-23T15:11:54 +0800** 重新派发两件（复核者派发词已增列"§12.5 决策审核（风险主轴·非决定性）"小节与模式 P 轻量项）。

| R-021 | 2026-09-23 | 远端同步纪律（R0 定）+ 推送链路诊断 + 后台任务分片重派 | ①按 R0 指令"commit 之前先 pull 操作，看下是否有冲突，然后 再 commit push"把三步顺序写入 `doc/项目开发流程.md` §9.4（基线纪律之后新增"**远端同步纪律**"）②实测推送链路（远端/网络/凭据三查）③**后台任务分片重派**（两件同批失败后） | ①**纪律**：**① `git pull`（或先 fetch 比对）查冲突 → ② `commit` → ③ `push`**；有冲突先解（按"保留双方语义"，**禁 `-X ours/theirs` 盲解、禁 `reset --hard`**）；**只 commit 不 push ＝ 远端与本地脱钩**——本次实测本地已积 **16 个未推送提交**（`origin/main` 停在 `a8a63f5`）②**链路诊断**：远端＝`https://github.com/HHHXXX-cell/virtual_riscv.git`（**私有**）；**直连 443 不通**（21 s 超时），本机 `127.0.0.1:7897` 代理可用（`curl -x` 得 200）；**Windows 凭据管理器无 github.com 条目** ⇒ pull/push 需**人先认证一次**（GitHub Desktop 自带 git 的 system 配置 `credential.helper=manager`＝GCM；Qoder 自带 git 为 `helper-selector`）③**分片重派**：第 12 轮复核与 Q-016 第 2 批的**整批派发两件均被模型服务拒绝**（各耗 ~9-12M token、53-60 次工具调用、~19-20 min 后失败）⇒ 改为**小片 + 硬性预算**（每片目标 10 min、工具调用 ≤15~18 次、禁止整篇通读/整文件读源码）：复核拆 **A1（技术核销+闭包+算术）** 与 **A2（§12.5 决策审核 + 模式 P 轻量项）**，决策拆 **B1（ISS-017 RVFI / ISS-007 规范版本）** 与 **B2（ISS-012 IPC / ISS-003 PRF，待跑）** | **待 R0 一次认证**（三选项）：(a) **GitHub Desktop 点 Push**（最快）；(b) 用 Desktop 自带 git 执行一次 `git push`，在 GCM 弹窗/浏览器完成 GitHub 授权 ⇒ 之后 CLI 免交互；(c) 配置 PAT（不建议在会话里贴 token，请自行 `git credential` 存入）。**达成 (a)/(b) 后**领队即按新纪律执行 `pull（查冲突）→ 补齐 commit → push`。**说明**：本行所属提交**先于 pull** 落盘——因无凭据时 pull 不可执行，属不得已的顺序偏离，待认证后立即补 pull 并处理任何冲突 | **同日追加（R0 建议）**：派发改为**按修改点/决策项分片**——规则已写入 `doc/项目开发流程.md` §9.4「派发分片规则」；本轮在跑的 A1/A2/B1 即按此收窄（A1＝技术核销片、A2＝决策审核＋模式P 片、B1＝ISS-017/ISS-007 两决策项），B2 起改**一项一片**。


| R-014（**补录注**：本行为当日口径变更轮，落盘时点在 R-013 之后、R-015 之前；表内位置上移系补录所致，编号序≠排版序） | 2026-09-23 | 流程口径变更轮（ISS-042，R0 定）+ 台账重构 + 机判 | ①按人本会话口径改写四处：`doc/项目开发流程.md`（**§9.5 新增**、§12.1 P1·P4、§12.2、§12.3、§12.4）、`.qoder/agents/vr1-decider.md`（分类/方法/ADR/硬约束）、`AGENTS.md`（规则 2/20/23）、`doc/AI角色与职责.md`（R1/R6/R8 行）②台账重构：登记 **ISS-042 即闭**、**ISS-008 关闭**、**ISS-003/007/012/017/024~028 九项转决策 AI**（待决策 11→3）③新增队列项 **Q-016**（决策 AI 批量自决，含 ISS-039/041）④机判重跑 | ①口径变更要点：**设计空间取舍＝日常技术决策（AI 自决）**、**spec＝设计 AI 的承诺**、**G1/G3 材料＝AI 列出的评审 list、人逐项签署**（§9.5 四段：取舍条目表/未决项/风险残余/抽查建议；签署语义＝"清单完整＋我接受其风险"）、**ADR 四要素**（选项/选定/**外部锚点**/下游义务）＋回退方式、**成本分级** a（唯一确定）/b（完整 ADR）/c（纯偏好）、**不可回退项禁走"超时默认"且必须进 list ④ 段**、原 §12.3 第 2 类（疑似设计/规格缺陷必报）**撤出**（保留三类：R6/R7 边界、无复核手段的✅陈述、环境权限）②台账：**42 条＝3 待决策 / 23 处理中 / 16 已解决**——待决策仅剩 ISS-002（环境安装）/ISS-023（放宽通道）/ISS-029（判定档位），待决策清单由 11 行瘦至 3 行 ③Q-016 派发语已写明 vr1-decider **无写权限**（落地由作者角色代执行）④机判：`gate.py check` **21 PASS / 0 WARN / 0 FAIL**、`width_check` 28 个 struct 可机器核对 28 / 不符 0 / 未能核实 0（本行落盘时值） | **未变**：P1~P4 的签署动作本身、R6/R7 边界、`rtl/` 不动（红线 R1）、规则 21（同模型盲点残余由人签署＋锚点兜底）、规则 22（只读评审不给执行工具）。**待复核对账**：§9.5 的"抽查建议"粒度与机判义务（判据待 `spec/14` 成形后落地，落地前由 R6 人核）——首版由下一轮审计/复核轮核 |

| R-022 | 2026-09-23 | 第 12 轮复核归档（附 K 三片）＋ 本批落修 ＋ 四项决策录入 ＋ 决策审核回应落文 | ①三片报告全文入评审记录**附 K**（K§1＝A1／K§2＝A2／**K§2b＝A0 整轮版（分片前重派、后到，读的是修复前的树）**／K§3＝领队结论三片整合版）②按修改点落修：B-01（§8.1 指针→r12）／B-02（A18 名合一）／**B-03（§3.16 ③ 补 err 归属句与优先级）**／C-01（§7 新增 H-06 监视器信号声明段）／C-02（A19 依据列改引规则 4）／C-03（M 表两处引位）／C-06（doc/process/00 两行加“已被 ADR 取代”）／C-07 前半（R-017 引述改“结论”）＋A1-C3（A19 可选性说明）＋A2-C1（两章程指向 §12.5 完整例外）③四项决策录入台账（ISS-017/007/003/012）＋`spec/01` §1.1 P9 行加 ADR-4 索引 ④**B3 审核回应落文**：§3.16 `xw_carry` **定宽 62 bit**（`{v, lo16, pc[39:0], err, err_code}`，删“上下文”字样）＋跨窗 err 合成规则（本窗槽 15 随行锁存、双源按位或）＋carry 生命周期/顺序守卫/作废补全（含 `xw_carry_overrun_sticky` 与候选断言 A20）＋回退点判定三条；§3.10 补**公平性前提**（有界跳过，禁“资源优先且无限越过队头”）＋统计项 `perf_csrq_head_stall_cycles` ⑤机判重跑 | ①三片原始统计：A0＝18/3/9/0＋决策审核 6；A1＝22/2/5/1；A2＝0/3/12＋决策审核 5 ⇒ **去重后阻塞 3 项全部落修（B-01/B-02/B-03）**；**决策审核 11 条（去重 6 组）全部为“需写死的边界”，无一条主张推翻决策**——与本轮 §12.5 设计意图相符；B3 六条回应：五条“缓解”、容量项“维持＋构造性理由” ②关键落文数字：`xw_carry` 57→**62 bit**（+1 err +4 err_code）；锚点＝**ibex `err_plus2`**（跨边界错误归属 32bit 指令，2026-09-23 实查源码）；公平性前提以“有界跳过/老化即可满足”措辞，不指定固定优先级序 ③四项决策要点：ISS-007 锁 1.13＋分期（4 组实现/8 组不实现/PMP 随 spec/08，未实现 CSR→illegal）／ISS-017 不采纳全量 RVFI（`rt_t` 保 543 bit＋预留附录；**R0 口径提示**：若 formal 属路径 A 则翻选）／ISS-003 保 64＋不变式 I1~I5＋扩容触发（建议直取 128）／ISS-012 保 3 拍＋T1（IPC<1.71）/T2（f>60%∧C∈[4,5]）触发 ④机判：`gate.py check` **22 PASS / 0 WARN / 0 FAIL**；`width_check` 28/28 OK（本批只改注释/说明，成员与合计未动） | **未完成**：①**B3b**（A0 独有两条：`spec/00` §10 是否并入 ADR-1 下游义务；`K` 与“写口在途界”确权）未派 ②记录类四条（C-04 `doc/verify/04` §② 阻塞表／C-05 与 A2-C2（§④ 与轮次/日期）／C-08 §4 IBUF 反压行／C-09 “届”字清除路径）③`spec/01` §3.21 **RVFI 映射附录**（ISS-017 落点，B1 已给三清单）④**A1-S1：ISS-044/045 是否关闭——呈 R0** ⑤`doc/verify/02` 的 A1-C1 条目（待建篇，先留痕） |

| R-023 | 2026-09-23 | 自动推进循环建制（R0 指令） | ①按 R0 指令"**请决策 AI 进行决策、计划 AI 给出后续计划、待完成后计划 AI 再给出计划、不停自动推进**"把循环写入 `doc/项目开发流程.md` §12.2（新 bullet：决策→计划→执行→再计划，人只在 P1~P4 与三类必报项介入；每圈仍须留痕、不得跳过复核或默认通过）②派 **Q-016 分片 B3b**（决策收口三项：`spec/00` §10 是否并入 ADR-1 下游义务／`K` 与"写口在途界"确权／**A1-S1＝ISS-044·045 是否关闭**）③派 **计划 AI**（`vr1-planner`）出后续推进计划（下一批工作项表＋spec/02~14 篇序与理由＋收敛节奏与出口条件＋`doc/verify/01`/`doc/verify/02` 建篇触发点＋人工干预点＋不确定项） | ①循环要点：**决策 AI 决策 → 计划 AI 计划 → 执行角色落地 → 再计划**；人只在门禁/必报项介入；**"自动"不等于免检**（复核、机判、留痕照旧）②两片均带硬性预算（≤8/10 min、≤12/15 次工具调用），依据＝§9.4 派发分片规则与 `doc/verify/05` R-021 的整批失败实测 ③**待返回后**：B3b 结论落正文/台账；计划 AI 的产出由领队转为 `AutoQueue.yaml` 队列项与 `doc/verify/04` 状态（计划→队列的转写是 R8 职责，不得只贴文本） | 计划落地后按循环自转；**人侧仍待办**：GitHub 一次认证（CLI push）／ISS-002 工具链安装（挡 M0/M1/M6/M8）／以及"formal 是否属路径 A"与 ISS-023/029 两条口径（可随时答，不阻主线） |

| R-024 | 2026-09-23 | 决策收口（B3b）＋计划轮落盘＋队列转写 | ①**B3b 三项决策落盘**：`spec/00` §10 ISS-039 行解除条件并入 ADR-1 下游义务（F2 mux／`doc/verify/02`×6，消"R-017 陈述与实态不符"）；**`K` 定值归属改判 `spec/04`**（原 `spec/13` 系"无推导"根源——性能模型无此源量），给 `K ≥ max(L_S, L_E)` 推导要求＋默认期三条锁定＋`K*≠2` 处置（§3.19／§7 A17／§10 ISS-041 三处同步改判）；**ISS-044/045 ＝有条件关闭**（闸门＝第 13 轮增量复核；条件明细已入台账行，**状态字待闸门通过后改**）②**计划轮落盘**：W-1~W-10 与 spec 篇序（02→03→04→05→06→07→**10**→08→09→11→12→13→14）写入 `doc/verify/04` §②"计划快照"；**转写为队列 Q-017~Q-027**（spec/05~14 十项＋平台骨架一项）③机判：`gate` **22 PASS / 0 WARN / 0 FAIL**（队列 28 项：ready 16/blocked 6/评审中 2/done 4）；`width` 28/28 OK | ①**收口三项性质**：均为设计/记录类（非放宽类、非 R6/R7 边界、非"无复核手段的通过陈述"）⇒ 决策 AI 自决成立；两片（B3b、计划）**零写入**自报 ②**计划的人工干预点（人不做则相应线停）**：ISS-002 安装（挡 M0/M1/M6/M8 与合规/性能线）／**push 认证**（挡远端同步）／ISS-023·ISS-029（待决策，ISS-029 挡回归 runner 判据）／G1 签署（挡冻结与 RTL 启动）／M2·M7 一句认领或废弃 ③**计划 AI 自报不确定项**已入 `doc/verify/04` 计划快照末段（含 **ISS-020 归属**与 **Q-007 依赖口径**两条待取舍）——**未擅改队列依赖**，原文呈报 | **下一手（循环继续）**：①本提交即 **r13 基线**（提交信息标"r13 基线"）②派第 13 轮增量复核（**按修改点分片**：A=落修核销（r12 全部落修＋B3/B3b 文本）／B=新决策审核（B1/B2a/B2b/B3b 的七项 ADR 首次审核·风险主轴·非决定性）＋模式 P）③Q-016 第 3 批（ISS-024~028，ISS-024 先呈人）④随后按计划快照推 spec/02 与平台骨架 |

| R-025 | 2026-09-23 | R0 三条裁定落盘＋决策片派发＋r13 记录项落修 | ①**R0 2026-09-23 三条裁定**：**ISS-024 放行**（原"监督通道自改须人批"获人批）并转决策 AI；**ISS-020 转决策 AI**（参数取值属自决面）；**Q-007（doc/verify/01）依赖按计划 AI 建议改**为 `depends_on: [Q-004]`＋`blocked_by: [ISS-020]` ②派两片：**S1＝ISS-024**（基线指纹判据）、**S2＝ISS-020**（四项关键参数口径——**闭合即解锁 Q-002/Q-003**）③r13 记录项/阻塞落修（doc/verify/04 两行、spec/00 §10 ISS-007 行、§4.3 注、D-5/D-7/D-8/D-10、R-014 补录注、BLK-1 改名 `K_stall`、CL-1 A20 候选不编号）④机判重跑 | ①三裁定按 R0 原话落盘（"1 放行／2 转决策AI／3按照建议进行"），未擅改范围 ②决策片带硬预算（6~8 min、≤10~12 次调用） ③r13 A 片：落修 **16/16 真改**、闭包 **7/7**（真断链 0）、算术 **10/10**；B 片：决策审核 10 条（①2/③4/④4，**⑤致死 0**）＋阻塞 2（记录类，已修）④机判：**22 PASS / 0 WARN / 0 FAIL** | **未完**：B4（r13 回应）与 S1/S2 在跑；**ISS-044/045 闸门待 B4 落文后重跑一小轮即过**；其后按计划快照启动 spec/02（Q-010）与平台骨架（Q-027） |

| R-026 备注 |

**R-026/R-027 备注（留痕事故）**：R-027 落文时对 `doc/process/00` ISS-041 行做"去粗体"修复，一次过宽的替换**截断了该表格行**（`gate md-integrity`/计数判据立即报 5 FAIL 抓出）——已从提交恢复并改用**最小替换**修复，现 **22 PASS / 0 WARN / 0 FAIL**。教训与 R-013/R-019 同型（长表格行的编辑必须最小化 old_string），记档不藏。

| R-026 | 2026-09-23 | S1（ISS-024）决策回收＋实施项立案 | ①收 S1 的 **ADR-4**（基线指纹判据：`md5:size:mtime` 三元组存储、内容侧计修改、mtime 列 INFO、新增/删除恒计；双用途分开——规则 22 窗口制 vs 换机/完整性长基线）②回填 `doc/process/00` ISS-024 决策列（含下游义务六条）③新增队列 **Q-028**（实施 `snapshot`/`rocheck`＋规则 22 措辞＋首跑记耗时＋§12.5 送审）④机判重跑 | ①**关键推演已答**：探针件每轮为新文件名 ⇒ 只落 ADDED、不误报 MODIFIED；"每轮必改件不入长基线"处置了台账假红风险 ②**如实声明**：现仓 `script/` 无任何 mtime/md5 实现——原"纯 mtime"是**人工命令级流程**（HANDOFF 各轮"全仓 mtime 窗口命中 0"），故本条实为**首次脚本化**、非改检查器 ③锚点＝知识库 `05-门禁完整性基线实务.md` §2.1/§4（一手，已读原文）④`gate` **22 PASS / 0 WARN / 0 FAIL**；队列 **29 项** | S2（ISS-020）与 B4（r13 回应）仍在跑；ISS-025/026/027/028 待派（第 3 批余项，一片一项） |

| R-027 | 2026-09-23 | B4（r13 决策审核回应）回收＋落文 | ①收 B4：**追认 3 条**（BLK-1 改名 `K_stall`／CL-1 A20 候选／D-5 §4.3 注）＋**六条"接受"**（D-1 双轨回锚＋抽法／D-2 §3.21 占位＋§10 条件行／D-3 定名 `INV-n`／D-4 两路归还句／D-6 ④段＋M5 实测锚点路径／D-9 六类重跑清单＋清账口径）②落文：`spec/00` §4.3（`INV-1~INV-5` 归一＋可粘贴句）与 §10（**新增 ISS-017 条件行**）；`spec/01` §3.22 前置**附录占位**；`doc/process/00` ISS-003/ISS-041 决策列回填（`INV` 定名、`K*≠2` 重跑清单）③机判重跑 | ①**无一条放宽/豁免判据，无一条推翻原 ADR**（与 §12.5 设计意图一致）②关键新件：`INV-n` 单一命名空间（防"同名传染换个皮复发"）、RVFI 附录占位＋"路径 A 含 formal 即触发翻选"的可判据条件、`K*≠2` 的 6 类重跑清单（避免假绿残留/假红不清） ③B4 自报："D-7/D-10 未定位原文，按记录类落修追认"——领队核实：两者确已随记录项落修（§1.1 触发口径衔接、§8.2 条件③登记）④`gate` **22 PASS / 0 WARN / 0 FAIL** | **未完**：S2（ISS-020）在跑；`spec/04`/`spec/10`/`doc/verify/02` 承接的登记义务（INV 总表、引入版本模板、归还唯一性与协议违例验证点）已写入各台账行，待其成篇时核销 |

| R-028 | 2026-09-23 | S2（ISS-020）ADR 落地＋解锁 Q-002/Q-003 | ①收 S2 的 **ADR-S2**（四项关键＋`HIST_DEPTH`/`ROLLBACK_MAX`）②落文：`spec/00` §4.3（`HIST_DEPTH`=`ROLLBACK_MAX`=**33**＋**ADR-S2-1 窗口可达性口径注**）、§4.6 两行（`FWD_MAX_SOURCES`＝保持 2 但**废"截断"改"更老源＋超限延迟重试"**；`FWD_CMP_BITS` **12→44** PA 全宽含 PPN）、§9（指标改 `fwd_blocked_replay`，不设截断次数）③`doc/process/00` ISS-020 决策列回填＋待落清单 ④机判重跑 | ①**决策要点**："窗口不可达"取**收窄宣称＋数值零改动**（容量上限口径；`IPC≥1.8` 附条件"平均驻留 ≲25 拍"，超此为登记性风险非缺陷）——与 ISS-003"保持 64"自洽；②原先"`FWD_MAX_SOURCES` 截断"实为**把死锁登记成指标**（第二轮评审原话），本次废除并给"无环/有界/无死锁"论证；③`FWD_CMP_BITS` 12→44 消"跳页同偏移误转发"与"VA 别名漏转发"；④`HIST_DEPTH`/`ROLLBACK_MAX`=33 由"每未提交含目的 μop 恰占一条栈项且持一 PRF 目的项"导出（随 PRF 联动：128⇒97/7 bit）⑤**待落**：§4.8 四行重算（TAGE/L1D/L2/合计，归 Q-003）、§8 LR/SC 行、`spec/01` §5.3 `SP_ck`=6、§7"等待集⊆更老"类新断言、`width_check` 扩件（乘法/加式校验）⑥机判：`gate` **22 PASS / 0 WARN / 0 FAIL**；`width` 28/28 OK | **解锁**：Q-002（spec/04）与 Q-003（spec/00 v0.2）的"ISS-020 阻塞"实质解除（待台账状态字与队列 `blocked_by` 复核后生效）；**ADR-S2 按 §12.5 需送 auditor 一次审核（非决定性）**；S1/SUS-01 维持转 `spec/04` |

| R-029 | 2026-09-23 | r13′ 确认轮回收＋BLK/记录类落修＋S3 录入＋spec/02 首片收讫 | ①r13′ 确认轮（auditor）判：**K 体系/S2 落文/附录与条件行/INV 定名注 = 真改**（17 通过），**2 阻塞**（BLK-1 `spec/00` §4.3 的 `HIST_DEPTH` 值"33"与说明列"待定"并存 ＋ §10 ISS-018 行；BLK-2 `spec/01` §5.2/§5.3/§8.2 三处未随 S2 更新、`SP_ck` 位宽仍悬）＋3 需澄清＋1 存疑（A20 行两句并存）②**已落修**：BLK-1/BLK-2（三处"待定"→"已定值=33"，`SP_ck` 回落 6 bit）＋SUS-1（A20 编号口径标明取代关系）；CL-2（§4.3 旧名处置口径）与 CL-3（doc/verify/04 B7 行承接字样）留待下一批 ③**S3 录入**：ISS-025/027/028 的 ADR-5/6/7 已入台账决策列（三档证据行／回显+反注／族级开关+三证据；方向均为"让 FAIL 更真"，零放宽）④**spec/02 首片收讫**：`doc/spec/02-指令集与逐指令行为.md` v0.1 骨架——值域 **20 条**＋`isop_t` 约束 3 条＋首批 **3 类**条目（x0／RVC 展开／CSR）＋不变式 I2-1~I2-10＋【权】锚点 **25 处**＋**待补 17 条**（T-1~T-17）；**设计师上报口径差异**（派发词写"一期含 F/D"，与 `spec/00` §2"一期 rv64imac、F/D 二期"矛盾）——**正文按 `spec/00` 口径写**，属派发词笔误，记档于此、不立缺陷 ⑤机判：`gate` **22 PASS / 0 WARN / 0 FAIL**、`width` 28/28 OK | **未完**：①CL-2/CL-3 落文本批 ②`spec/02` 后续（T-1 成员表＋T-17 RV64I 全量，建议按类分片）③ISS-026（码点扫描）与 ISS-044/045 过闸待判（r13′ 条件①行面已过、§3.19/§3.16 本体在本片盲区，需下一片补核）④ADR-4/5/6/7 与 ADR-S2 按 §12.5 各送审一次（非决定性） |

| R-030 | 2026-09-23 | 计划引领制落地（R0 定：AI 计划 agent 引领，非定时器）＋S4 回收 | ①按 R0 口径把循环驱动源从"会话判断"改为"**计划 AI 维护的驱动计划**"——`doc/项目开发流程.md` §12.2 增「**计划引领制**」：载体＝**`doc/verify/03-验证计划.md` §0 驱动计划**（有序工作项表；**计划 AI 唯一作者，R8 只执行不得改序跳项**）、周期协议＝check→执行队首→核销→**调计划 AI 重排**→下一项、`【待人】`项不得代做 ②新建 `doc/verify/03-验证计划.md` 并转录首版驱动计划（D-1~D-10＋H-1~H-3；与队列 Q-017~Q-028、`doc/verify/04` 计划快照对齐；**三者冲突以本表为准**）③回收 **S4＝ISS-026 ADR-8**（升为 G1 冻结材料必附件＋扫描面收口＋坏词表维护责任；与 ISS-018 事实合批）④机判重跑 | ①**机制要点**："下一项做什么"不取决于当前会话的判断或记性，而取决于计划 AI 的《驱动计划》——**任何会话（含新会话）只按队首执行**，故循环可跨会话接力；`【待人】`三项（push 认证／ISS-002 安装／ISS-023·029）独立成行、只呈报不代做 ②首版驱动计划含 **D-1~D-10 ＋ H-1~H-3**，其中 D-1/D-2/D-3 已在跑、D-4（ISS-044/045 过闸）/D-5（Q-028 实施）/D-6（spec/02 终片）/D-7（平台骨架）/D-8（spec/04 起稿）/D-9（§4.8 四行重算）待办 ③机判：`gate` **22 PASS / 0 WARN / 0 FAIL** | **未完**：D-1（二合一审核片）与 D-3（spec/02 续片）在跑；D-5/D-8/D-9 为下一批候选；**驱动计划的首次"计划 AI 重排"将在 D-1/D-3 回收时执行**（照 §12.2 周期协议第④步） |

| R-031 | 2026-09-23 | D-1/D-3 回收：附 L 归档＋轻级矛盾修＋spec/02 续片收讫 | ①**D-1 归档入评审记录附 L**（L§1 闸门补核 12 子项：在/自洽 10、轻级矛盾 2、盲区 2；L§2 五项 ADR 风险审核 11 条：**可接受 5／需进 §9.5 ④段 2／需决策 AI 回应 4**）②**轻级矛盾修**：§3.16 回退点双措辞归一（以三条件判定标准为准）＋A20 编号链路改引 §7 行口径 ③**D-3 spec/02 续片收讫**：§9 `isop_t` 成员表 **87 成员**（编码全列"待定（T-1 编码面）"、未自造一码；容量核验 `1024−87＝937`）＋§10 **RV64I 全量 63 条**＋新增锚点 5 处（AN-30 保留待锚点）＋**T-17 关闭**、新增 T-18/T-19 ④机判：`gate` **22 PASS / 0 WARN / 0 FAIL**（设计师自跑并自修 3 行结构缺陷） | **未决（勿当闭环）**：`isop_t` 编码面七项（T-18）与 T-19 待核；本片锚点均为节级/待锚点（设计师自报"未逐行回读"）⇒ **编码未定＝`spec/02`↔`spec/01` 编码面闭合缺一环**；四条"需决策 AI 回应"（R-4.1/R-4.2/R-6.1/R-7.1）与两条"需进 §9.5 ④段"（R-5.1/R-S2.1）已按协议派/登记。**下一手**：按 §12.2 计划引领制**第④步**调计划 AI 重排《驱动计划》，随后执行新队首 |

| R-032 | 2026-09-23 | 口径漂移修正（知识库同步轮回溯，R0 指令"修正项目里的版本口径和计数漂移"）＋队列/台账/交接回填 | ①清 `RA5` 残留活引用 3 处（`script/gate.py` 打印串 2 处、`run_cmd/AutoQueue.yaml` 协议头 1 处）②`AGENTS.md` 规则 23 计数改"以实跑输出末行为权威源"③"§12.3 四类必报项"改"三类"2 处（`AutoQueue.yaml` 协议头、`gate.py` 打印串）④版本口径 2 处改 **v3.0**（`AutoQueue.yaml` 协议头、`gate.py` 头注释；并注明 §6.1 系 v3.0 新建）⑤`doc/process/00` 新增 ISS-049~051（已解决）、`doc/verify/04` 镜像 48→**51**、`AutoQueue.yaml` 新增 **Q-029**（done）⑥机判重跑 | ①三项漂移均系**知识库同步轮（同日）回溯发现**：RA5 属 ISS-048 清理漏网（判据扫描面不含工具/队列）、"12 项"与实跑 22 项脱钩、"四类必报"与 §12.3 三类脱钩、v2.1 口径过期且 §6.1 曾为悬空引用（知识库 v3.0 已补正）②修法统一取"权威源＝工具实跑/文档正文"，不写死数字防再漂 ③历史记录（`HANDOFF`、`doc/verify/05` 旧行、评审附件）一律不改写——新记录更正 ④机判：`gate.py check` **22 PASS / 0 WARN / 0 FAIL**；台账 51 条＝3 待决策 / 25 处理中 / **23 已解决** | **未自决项**：`ra-token-defined` 的扫描面是否扩至工具/队列/交接件（本轮只登记 ISS-049 盲区，未改判据本身——属监督通道自身改动，按纪律须人批） | 

| R-037 | 2026-09-23 | 终局全量轮分片规范落地（R0 定）＋B5 四条回应落盘 | ①按 R0 提案"所有问题清空后再加一轮**完全复核**、**按章节切片**"落 **§9.4b「终局全量轮的分片规范」**——**章节片（横切）＋三条纵片（跨章一致性／声明—实态／机判全量）**；纵片三项**强制不得省**（跨章与声明类正是章节切片的盲区；先例＝附 L R-4.1 抓到"ADR-4 已落"而实件未建）②**驱动计划加 D-11**（终局全量复核，触发＝"增量连续新增 0 且台账无残留"；全过且新增 0 ⇒ C2 成立、**报 R0**）③**B5 四条回应入附 L L§2b**（均"接受"、全为收紧方向）＋**Q-028 done_when 扩充**（长基线刷新硬规则六条＋`capability-landed` 判据）④机判重跑 | ①**机制要点**：章节片管"章内完整/一致/锚点"；**纵片管章节管不到的**——术语与编号一物一名、`§` 指向真实存在、每条"已落"能找到实物、机判与算术全量复跑；②**出口**：全过且新增 0 ⇒ C2 成立但**只能报 R0**（评审者/工具/计划 AI 均不得宣布通过）；任一片出阻塞 ⇒ 按 `全量→处置→增量→全量` 再来一轮（`cap_waiver` 在效、每轮留痕）③**B5 收紧要点**：`capability-landed`（防"声明有件不见"复发，模式同 `ra-token-defined`）／基线刷新六条硬规则（**失败轮后禁刷**、只折已复核集）／反注两档机判（`echo` 降位为装载一致性证据）／白名单批指纹 `WHITELIST_TIMING`④机判：`gate` **22 PASS / 0 WARN / 0 FAIL** | **未完**：计划 AI 重排（首次实跑第④步）在跑；B5 的"落盘由 R8 承担"项已并入 Q-028 与平台项；D-11 待触发（依赖 D-4~D-10 全闭） |

| R-033 | 2026-09-23 | 判据扩面（R0 选定 A）：ra-token-defined 扩至活引用面 ＋ 双向验证 | ①`script/gate.py` 新增 `RA_LIVE_FILES`（规则承载件＋本工具＋`run_cmd/AutoQueue.yaml` 全件）与行级豁免标记 `记录不改写`；新增 `_ra_live_lines()`，HANDOFF 只取**最后一条记录**的"未完成/下一手" ②判据自述段去编号字样（不再自引已移除编号）③**双向验证**：基线 22 PASS → 注入未登记编号 → `ra-token-defined` **1 FAIL** → 加豁免标记 → PASS → 撤除 → PASS 且 sha256 前后一致 ④`doc/process/00` ISS-052（已解决）＋状态总览与镜像回填（52 条＝3 待决策 / 23 处理中 / 26 已解决）⑤队列新增 Q-030（done）与 Q-031（ready，送审）⑥机判重跑 | ①**扩面前实测的假阳面**：`gate.py` 判据自述 2 处、`HANDOFF` 历史记录 12 处 ⇒ 裸扩面不可行，故定"分层扩面＋行级豁免"（豁免标记沿用 codepoints 判据先例、须逐行显式标注理由）②**双向验证是本次唯一可证伪的验收物**：注入不得命中即形同虚设，豁免标记不得生效即误伤记录 ③R0 2026-09-23 选定 A（判据变更属监督通道自身改动、经人批）④机判：`gate.py check` **22 PASS / 0 WARN / 0 FAIL** | **未完成**：送审 **Q-031**（R6，非决定性；按规则 22 附仓库清单比对＋mtime 窗口）未跑。**撞号注**：并发会话另有同号 `R-032`（"终局全量轮分片规范落地"）行，按"记录不改写"未改其行，留待 R0 或该会话统一改号 | 

| R-038 | 2026-09-23 | **计划引领制第④步首次实跑**：驱动计划重排＋D-4 执行＋并发冲突处置 | ①**转录计划 AI 重排版 §0**（D-1/D-2/D-3/D-10 转已完成；**D-4 升队首**；D-5/D-8/D-9 解阻前移；新增 **D-11 编码面/D-12 B5 回应/D-13 §9.5 汇聚/D-14 终局全量轮/D-15 spec/03 起稿**）＋重排记录行 ②**执行队首 D-4**：抽查条件①两处修点在位（§3.16 `L§1 归一口径`＋`编号口径以 §7 A20 行为准`＝均 True）＋条件②③按 r13′ 与 D-10 登记核销 ⇒ **ISS-044/ISS-045 状态字改【已解决】＋时间 2026-09-23** ③**队列对齐**：解 Q-002/Q-003 的 `blocked_by: [ISS-020]`；为 D-11/D-13/D-14 建 **Q-031/Q-032/Q-033**（Q-033 blocked_by Q-031）④**并发冲突（如实申报）**：本轮发现**另一会话正在同一仓库写作**（新增 ISS-049~052 与队列 Q-029/Q-030，且一度出现 `handoff-exists: Q-030 无交接记录` 与看板镜像落后 2 项 FAIL）——领队据**表体实测**重写计数（3/23/26＝52）与看板镜像；两处 FAIL 随后由并行会话补齐后**已归零**⑤机判：`gate` **22 PASS / 0 WARN / 0 FAIL** | ①**机制首次实跑结论**：计划引领制走通（"计划 AI 重排 → R8 转录 → 执行队首 → 留痕"全链有落盘证据）；**唯一风险是并发写者**——两个会话同写 `doc/process/00`/队列时计数与镜像会互相踩（本轮实例）②**建议（待 R0 定）**：同一仓同一时刻**只保留一个活跃 R8 会话**；并行会话（如知识库同步）**先完成并提交，再交棒**；或在队列加"当前持有者"字段做软锁 ③D-4 完成后队首推进至 **D-5（Q-028 实施）** | **未完**：D-5 待派（下一步）；D-11/D-13/D-14 队列项已建待执行；**并发写者协调待 R0 一句** |

| R-034 | 2026-09-23 | D-5 实施（Q-028）：ADR-4 落件——`gate.py` 新增 `snapshot`/`rocheck` 两子命令＋`capability-landed` 判据＋`AGENTS.md` 规则 22 措辞同步 | ① `script/gate.py`：新增 `snapshot`（受控目录集逐文件 `md5:size:mtime` → `script/gate/integrity_baseline.json`；`--refresh` 强制随 `--reason <折入集出处>`，否则拒绝执行）与 `rocheck`（`--baseline` 长基线四分类、`--window <ISO>` 全仓窗口含 `script/tmp/` 等忽略区）＋ `check_capability_landed`（并入 check：规则承载件出现 `rocheck`/`integrity_baseline` 而实件缺失即 FAIL）② 基线实件首建：`script/gate/integrity_baseline.json`（88 文件，指纹 `sha256:77efa13c…`；每轮必改件 doc/process/00/04/05、HANDOFF、AutoQueue.yaml、评审记录不入长基线）③ `AGENTS.md` 规则 22：改为"仓库清单比对＋内容指纹（md5:size）窗口；纯 mtime 单列 INFO；新增/删除恒计入"，保留"探针禁入忽略区" | ① 首建快照 88 文件耗时 **0.158s**；`rocheck --baseline` **0 MODIFIED / 0 ADDED / 0 DELETED / 0 MTIME-ONLY**、rc=0（0.135s）② 窗口 INIT 119 文件 **0.149s**；反证实验（注入 `script/tmp/_falsify_rocheck.txt` → **ADDED=1 rc=1**；追加 `iss/tests/probes/_e.txt` → **MODIFIED=1 rc=1**；恢复后 **MTIME-ONLY=1 rc=0**——内容侧判据、mtime 不改判）③ `snapshot`/`snapshot --refresh`（缺 `--reason`）双路径拒绝执行（rc=2、不写盘）④ 收尾 `gate.py check` **23 PASS / 0 WARN / 0 FAIL**（含新判据） | **未完成**：① 本片未经评审（Q-028 送审由 R8 派，按规则 22 附 `rocheck --window` 输出）② `--window` 制在真评审轮的首次实战留痕未跑 ③ 基线刷新正路径（带 `--reason`）本轮未触发——按硬规则仅处置轮开工前可刷、评审轮与 FAIL 轮后禁刷。**校准说明**：首跑无格式/阈值改动；窗口制首次调用语义为"INIT 只记录起点"，同 ISO 二调才出四分类（已写入 `--help` 头注释） |

| R-035 | 2026-09-23 | D-9 处置：`spec/00` §4.8 四行重算（ADR-S2 判"未达标"项） | ①`python script/width_check.py` ②`python script/gate.py check` ③四行新算式复算（`python -c` 逐式实算）④`sed -n '258,276p' doc/spec/00` 内 `≈`/`…` 计数 | ①28 个 struct 可机器核对 **28 / 不符 0 / 未能核实 0**（基准行 2，未转正）②**23 PASS / 0 WARN / 0 FAIL** ③**TAGE** `4 × 2048 × (11 + 3 + 2)` = 131,072 ＋ `1024 × 2` = 2,048 ⇒ **133,120**（原 ≈136,000）；**L1D** 数据 `64×8×64×8` = 262,144 ＋ tag `512 × 32` = 16,384 ＋ 状态 `512 × (1+1)` = 1,024 ⇒ **279,552**（原 ≈300,000）；**L2** 数据 `256×16×64×8` = 2,097,152 ＋ tag `4096 × 30` = 122,880 ＋ 状态 `4096 × 2` = 8,192 ⇒ **2,228,224**（原 2,136,000，tag/状态由 38,848 改为 131,072＝`4096 × 32`，与标准式量级相符）；**合计** Σ12 行 = 4,224+1,472+17,664+2,320+16,192+1,216+2,244+1,968+133,120+280,576+279,552+2,228,224 = **2,968,772 b**（原 ≈2.9 Mb）④§4.8 全文 `≈`/`…` 计数 **0**；`git diff --stat` = 4 insertions / 4 deletions（只动四行） | ADR-S2 三行（TAGE/L1D/L2）＋合计行已改为显式算式，无 `≈`/无省略；字段溯源：TAGE←§4.2 参数、L1D/L2←§4.6 参数＋§4.1 `PA_BITS`（tag 32/30 由 `PA_BITS`−index−offset 导出）。**待锚点三项**（不得当已锚定）：①TAGE 条目完整位域表属 `spec/03`（未建），SC 权重/ITTAGE/RAS 是否计入本行未定；②L1D 替换位与字节有效/dirty 形态属 `spec/09`（未建）；③L2 替换位与 non-inclusive 目录位是否实现属 `spec/09`。合计行已注明**未计**BTB/RAS/TLB/MSHR/SC/ITTAGE ⇒ 为非全核总量的下界 |

| R-036 | 2026-09-23 | R0 两条运营裁定（并发写者 / CLI push 认证） | ①按 R0 指令"**并发写者软锁不需要，后面注意**"——**不加软锁**；R8 执行口径改为：**每轮开工与收尾各做一次"表体实测 vs 计数/镜像"对账**，发现并行会话写入的条目**只对齐、不代改**，并在 `doc/verify/05` 如实申报②R0 询问"CLI push 认证是啥"：**与知识库无关**——该认证指**把本地提交推到项目私有远端** `github.com/HHHXXX-cell/virtual_riscv`（知识库 `D:\IC验证知识库\` 不在此仓、只读、不参与 push）；R0 裁"**先不加**"，改用 **GitHub Desktop 按需手动推送**。③**H-1 出口判据需改**（由"CLI push 成功一次"→"R0 按需 Desktop 推送"）——按 §12.2 属计划内容，**由下一次计划 AI 重排提交**（D-11 回收时的第④步），R8 不擅改《驱动计划》 | ①两条均为**运营层裁定**，不触 §12.3 三类必报、不动任何判据与红线②对账口径与 `feedback-concurrent-writers` 一致：**用实测覆盖猜测**（本轮即按表体重写 3/23/26＝52 并同步镜像）③**每轮收尾须申报远端落后数**（本轮：待 Desktop Push） | **待人项不变**：H-1（push，改为按需手动）／H-2（ISS-002 安装）／H-3（ISS-023·029）；**软锁不加**，R8 以对账代替 |
| R-039 | 2026-09-23 | 驱动计划 D-6 队首执行：`spec/02` 终片——D-11 编码 ADR 落文本（`isop_t` 87 成员具值）＋余项清账 | ①内联填充脚本（`python -`，逐处断言唯一命中）：`spec/02` §9.1 编码列 87 处原地填充（0x000~0x39A）、`spec/01` 两处指针（行 86／行 161；位宽未动）②`spec/02` 新增 §9.3（位段＋编码规则与保留段）／§9.4（§9.2 七项裁定）／§9.5（T-19 四域派生视图）／§14（待补清单更新）／§15（自检）③`python script/gate.py check` ④`python script/width_check.py` | ①87/87 命中唯一；集合 87 值两两互异且 <0x400；14 例码 14/14（`AMOSWAP`=0x392 反查 `111.g1` 第 2 号成员相符）②T-1／T-18／T-19 关闭；T-2~T-16 逐条状态入 §14（未动 11 项＝T-3／T-4／T-7／T-8／T-10~T-16；部分 2 项＝T-2／T-9）③`gate.py check` = 24 PASS / 0 WARN / 0 FAIL ④`width_check` = 28/28（不符 0／未能核实 0；spec/01 只改 2 处指针文本） | 未完成：①本片未经评审（送审由 R8 派）②`spec/01` §3.8 行 277／§3.11 行 340 的 `isop` 行仍无 `spec/02` 指针（按派发词未改，留痕）③保留段 937 组合非法化路径归 `spec/10`（T-9） |

**编号更正注（2026-09-23）**：本表曾出现**重号 R-032 / R-033**（并行会话与领队先后追加所致）。处置：**后出现者后移**——"终局全量轮分片规范（R0 定）＋B5 回应落盘"→ **R-037**；"计划引领制第④步首次实跑"→ **R-038**；原 R-032（知识库同步轮回溯·口径漂移修正）与 R-033（判据扩面·`ra-token-defined` 活引用面）**编号不变**。引用面已同步（`doc/verify/03` §0 重排记录的"留痕"列已改指 R-038）。**防复发**：`gate.py` 新增判据 `doc05-numbering`（本表 R 编号不得重号；序号因补录/后移非严格升序，属已注明例外），随本注同批落地。

| R-040 | 2026-09-23 | **CLI push 认证尝试（R0 指令"现在进行"）＋失败归因** | ①环境：GitHub Desktop 自带 **GCM 2.9.0** 在位（`credential.helper=manager`）；远端为私有仓 ②**两次尝试**：经本地代理（端口 7897）跑 `github login --device` → **GCM 自身令牌交换在网络层失败**（日志原样："发送请求时出错／基础连接已经关闭／身份验证失败"）；撤代理直连重试 → 进程起了设备码轮询但**无控制台输出**（非交互 shell 接不住它的提示）③**连通性复测**：经代理访问 github 登录端点＝`200 Connection established`；**直连 `api.github.com`＝200 OK**（443 直连不通已恢复，原阻塞为瞬时/路由态） | **结论**：认证可行，**但需一次交互式完成**（GCM 的设备码/浏览器提示需有控制台或 UI 承载）。**给 R0 的一次性步骤**（任意终端）：① 进入项目目录 ② 把 Desktop 自带 git 的 `cmd` 目录临时加进 `PATH` ③ 执行 `git push origin main`——GCM 会弹浏览器/GUI 完成 GitHub 登录并**把凭据存入 Windows 凭据管理器** ④ 回一句"好了"；我随即复验：先 `git fetch` 再 `git push` 实测（**此后 CLI 免交互**）。**备选**：继续用 GitHub Desktop 点 Push（零配置，但凭据只在其内部）。**残余**：临时登录进程已终止、临时日志已删（`script/tmp/` 无残留） |

| R-041 | 2026-09-23 | ISS-053 实装（R0 指出 attempts 从未被写入、`queue-attempts-cap` 判据成死代码）：`gate.py` 新增 `attempts` 子命令＋`dispatch` 提示＋`check` 对账 WARN＋规则入文 | ① `python script/gate.py attempts Q-027` ×2：字段 1→2；`--reset`：→0；`parse_queue` 项数 32 不变 ② 剥掉 attempts 行后与原文件**逐字节一致**（混合行尾保持、其余 0 改动，脚本 `script/tmp/_test_iss053.py` 实跑） ③ 中途 `check`：WARN `queue-attempts-ready` 命中 Q-027、0 FAIL；复位后 WARN 归零 ④ 副本探针反证：第 3 次 ⇒ `state: blocked`＋`block_reason: "派发失败 3 次，转人"`（WARN 同步消隐、0 FAIL）；探针即删 ⑤ `dispatch` 尾行打印重派提示（现场 id=Q-006） ⑥ 坏 id `Q-999` rc=2 且未写盘 ⑦ 终态 `gate.py check` = ---- 24 PASS / 0 WARN / 0 FAIL ---- | **未完成**：① 本片未经评审（送审由 R8 派）② R0 指定的 `yaml.safe_load` 自检命令在**存量**文件上即失败（值以反引号起始，属既有非严格 YAML；`git show HEAD` 版同样失败，非本次引入）——已以"剥行后逐字节比对"替代完整性判据，是否统一修 YAML 建议单独立项 ③ attempts 仅人工入口（派发行为本身不在 `gate.py` 内） |

| R-042 | 2026-09-23 | **CLI push 免交互达成** ＋ D-14 缺陷处置 | ①**认证达成**：R0 在浏览器完成设备码授权（码 A9B2-45BC）后，`git ls-remote origin main` 即通、**`git push` 实测成功**：`a91d3ff..f2bd4f6 main -> main`，本地与远端同步（`## main...origin/main`）——**此后 CLI `pull`/`push` 免交互**（代理 7897 已随命令传入）。**如实申报**：我的一次性登录脚本其**轮询环节仍报 URLError**（Python 直连被 TLS 重置；改走代理后设备码取得、但轮询不稳），**最终凭据可用**（git 侧实测为准，机制以实测证据落定）；临时脚本与日志已删 ②**D-14 纵片② 缺陷处置**：**缺陷①（阻塞级）**——"Q-032/Q-033 无实件"属实（我漏建），已补建 **D-13/D-14 的队列项**（ID 取当时空闲值）；**缺陷②（轻级）**——`doc/verify/04` 的"实跑 12 PASS"已改为"以末行为准（并注 D-5 起 24 PASS）" ③**ISS-053 实装收讫**：`gate.py attempts <Q-id> [--reset]`（达 3 自动 blocked＋block_reason）＋`dispatch` 提示＋规则入文＋`WARN queue-attempts-ready` 对账；反证实验（递增/复位/第 3 次置 blocked/WARN 消隐）全过 ④机判：`gate` **24 PASS / 0 WARN / 0 FAIL**；队列 **35 项** | ①**H-1（push 认证）实质达成**——**《驱动计划》H-1 的出口判据应由"CLI push 成功一次"改为 R0 已授权的 Desktop/CLI 双通道**（计划内容，待下一次计划 AI 重排提交）②ISS-053 的实装偏差已如实报：R0 指定的 `yaml.safe_load` 自检在**存量**文件上即失败（值以反引号起始的行＝既有非严格 YAML，`git show HEAD` 版同样失败、非本次引入）——**是否统一修 YAML 语式建议单独立项**（未做） |

| R-043 | 2026-09-23 | **D-14 纵片③「机判全量＋算术抽查」执行（§9.4b）** | ①`width_check.py`：**28/28 OK**（不符 0／未能核实 0／**转录基准行 2**）②`gate.py check`：**24 PASS / 0 WARN / 0 FAIL**（含 `codepoints` 与新增 `capability-landed`/`doc05-numbering`/`queue-attempts-ready`）③**闭包/引用链**（对 r13 基线 `b29dd4b`）：**11 条未展开**——参考核符号 5（`aligner`/`err_plus2`/`fetch_fifo`/`r_instr_h`/`rdata_unaligned`）／改名留痕 1（`csrq_drop_illegal_sticky`）／新信号 2（`drop_cnt_squash`/`fwd_blocked_replay`）／`spec/02` 助记符 3（`hardwired`/`jr`/`sfence`）——**分流按既往口径**（无真断链），其中 `spec/02` 三条为新引入、留章节片确认 ④**算术抽查（独立复算：自表格行逐成员相加，不采信 width_check 内部）**：**6/7 一致**——`wb_t` 80／`disp_x_t` 328／`csrq_entry_t` 152／**`ftq_entry_t` 145（转录基准行）**／**`ret_ctrl_t` 49（标题位宽项）**／`icache_rsp_t` 322；**`excp_t` 未解析**（其表格式与我的一次性解析器不兼容＝**本片工具局限，非缺陷**；历史轮次已验 177） ⑤**结论（片级）**：纵片③四项全跑、**无 FAIL/WARN**、算术抽查未触发"机判不可信" | **知会**：本片由**领队亲自执行**（机判类不需另派子代理，省一片预算）；`excp_t` 建议在**章节片**补一次人工复算；闭包 11 条中 `spec/02` 三条请章节片一并确认语义归属 |

| R-044 | 2026-09-23 | **D-14 纵片①（跨章一致性）回收＋批量落修** | 纵片①结果：**核对 42 条＝通过 26 / 缺陷 15（中 5・轻 7・需澄清 3）/ 不可判 1（Q 系）**。**本轮已修 8 条**：F1（`doc/process/00` 总览"处理中"枚举漏 ISS-053 → 补）／F3（`spec/02` 悬空 `I2-x` → `I2-4`）／F4（`spec/01` §5.3 `PRF_ENTRIES` → `PRF_INT_ENTRIES`）／F8（§5.3 一处残留"待定"→ 已定值 33；**另两处锚点未命中，留 F8-余**）／F9（`doc/process/00` ISS-039 行 ≥57/上下文 → **62 bit＋err/err_code**）／F10（§8.1"最新件"→"以末行为准"）／F11（`spec/02` 篇首引述 §3→§9）／F13（`doc/verify/04` 篇首时间戳 2026-09-22→09-23）＋**F7**（`doc/verify/03` 补 D-12 行）＋**F14**（状态回填：D-6→部分完成、H-1→已完成/判据待重排） | **未修（留后续片）**：**F2**（`I1/I2` 三义并存，`spec/01` §4/§8.2 与 `doc/verify/04` §② 仍写旧名——按 ISS-003 裁定改引 `INV-n`，派设计片）／**F5**（`T1/T2` 一名多物：批中止判据 vs 翻选触发——需分域命名，派决策片）／**F6**（`D-nn` 两编号池无对照表）／**F12**（`doc/verify/05` R-033 撞号注与更正注两说，需同步句）／**F15**（Q 系映射不自洽，权威在队列）／F8-余（两处锚点）。**所有修点均带 F-n 标记**（可回查） | ①**本片价值实证**：15 条缺陷**全部是"章节片看不见"的跨章/编号/引述类**（正合 §9.4b 设纵片的理由）；其中 F8/F9/F11 均为**同一根因**——"值改了、引述没跟上"（改一处漏一处），与 R-042 缺陷②同型②闭包 11 条中 `spec/02` 三条助记符由**章节片①**确认；③机判 `24 PASS / 0 WARN / 0 FAIL` |

| R-045 | 2026-09-23 | **D-14 章节片①（`spec/01` §1~§3）回收＋分流** | 核对 66 条＝**通过 55 / 缺陷 11（缺 9・矛盾 2）/ 未证实 1**。**A 类缺失型 9 条**（章节片的正菜）：A1 `iq_entry_t`（写者/读者缺）／A2 `rob_entry_t`（无方向・未声明"存储体豁免"）／A3 `wb_t`（产生级与消费方未落列）／A4 `mshr_wake_t`（方向・产生者・消费者全缺）／A5 `excp_t`（产生点未署名）／A6 `x_complete_t`/`ls_complete_t`（六项缺四）／A7 `mmu_transl_req_t.rw`（**2 bit 值域未给**：R/W/X→2bit 映射缺）／A8 §1.1 职责表无"读哪些结构"列／**A9「后读」无单一定义**（四处引用互不对齐——**直接决定 §3.11 成员语义与 §3.8 存/不存划分**）。**B 类矛盾 2 条**：B1 `raw32`"随流携带"（§3.5）vs"不含 `raw32`"（§3.8）／B2 `isop_t` 引用链跨文件错位（§3.4 指向 vs `spec/02` §9.0 前注"不产生任何编码"——前注系历史、D-6 已落值，属**引述未随落值更新**）。**通过面**：**28/28 合计与例化复算全对**（含 `excp_t` 177 人工复算 —— 补上纵片③的解析盲区）、六项清单 21/28 块齐备、锚点抽查 4/5 可回查（`FDIP` 缩写本轮未证实）、`disp_x_t.rd_paddr` 全链闭合 | **分流**：①**A1~A8＋B1/B2** 派**设计片**补全（六项缺口按 §3 各节惯例落文；B2 修前注）②**A9＋A7** 属设计语义分叉（读值拍/值域映射）→ 派**决策片**（A9 还牵 `spec/04`——D-8 正在起稿，需并入）③F5（`T1/T2` 分域命名）与 F2（INV 改引）随同两片；④`FDIP` 锚点留**待锚**（不勾销） |

| R-047 | 2026-09-23 | **D-15 回收：`spec/03` 首片成稿** | 产物＝`doc/spec/03-分支预测与取指前端.md` v0.1：篇首三段声明＋**§1 FTQ 四规则**（F1 读写指针/F2 分配释放/F3 满空判据/F4 冲刷交互，各附依据＋I3-1/I3-2）＋§2.1 参数引用表/§2.3 块布局 B-1~B-4/**§2.4 与 `spec/01` §3.16 逐句对账 12 行**＋§3.1 消费侧 C1~C5＋**§3.2 IBUF 满停工语义**（补 D-14 纵片② 指出的缺行）＋§4~§7；**ISS-039 收口条款齐**（任意 2B 槽／loop 终止＝末指令字节取全／**66 B 上界含几何复算**／nib-字节关系），**仍差**＝对账表评审核对＋T3-1（>66 B 块处置）；机判 `gate` 24 PASS / 0 WARN / 0 FAIL、`width` 28/28、**S1 参数 33 名全命中 `spec/00` §4**（未自造值）；**注意**：B-3 的 66 B 为几何复算读法，篇内已声明与 ISS-039 决策列回函不一致时的更正路径 | **待补 T3-1~T3-8 共 8 条**（含 `SC_INPUTS`=24 构成、位域面）；本篇**未经评审**（按篇序，spec/03 首轮评审待其成稿） |

| R-048 | 2026-09-23 | **D-8 回收：`spec/04` 首片成稿（两项硬交付齐）** | 产物＝`doc/spec/04-重命名与乱序引擎.md` v0.1；**硬交付 A｜`INV-1~INV-5` 五列对照表**（新名/旧名/语义/定值篇/验证方式；**旧编号只在该表出现**；每条给可写 SVA 形式并逐条列缺：`alloc_cnt` 观测口／组计数展开／free-list 写口采样／聚合就绪信号／快照向量采样）；**硬交付 B｜`K` 的推导与定值**：`K ≥ max(L_S,L_E)`，`L_x = d_x + q_x + s_x`（q/s 项为 0 均附依据；d_S=1、d_E=2）⇒ **本篇定值 `K = 2`**（机制不动、值来源改判）＋同时钟域前提＋紧致性（每 +1 拍扩 A17 假绿面）＋回退点＋**三处回写义务**（`spec/01` §3.19／§7 A17／`spec/00` §10——由后续片执行）；**PRF 联动公式**：`HIST_DEPTH = ROLLBACK_MAX = PRF_INT_ENTRIES − 31`（复算：栈 891 bit、快照 816 bit；128 档 2619/952） | **S1/SUS-01 部分闭合**：闭合 2 项（提交读 4 口＝N9；CSR 值载体链 S 读出→`csrq.src_val`→提交/`rt_t`）；**列缺 4 项（T-04-5a~d）**：12 读口 4×3 与 6×2 二读法对账／2 口"恢复读"承载方／同组前递拍序／§3.11 措辞对账——需 `spec/01`/`spec/00` 升版对账（建议登记转办）；待补 **T-04-1~8**（含 5a~5d）；`K_stall`（与 `K` 两个量）仍待定值。机判：`gate` **24 PASS / 0 WARN / 0 FAIL**；本篇无位宽改动故未跑 `width_check`（已在交接声明） |

| R-046 | 2026-09-23 | **D-14 章节片① 缺陷收口·设计片（A1~A8＋B1/B2 落文）** | ①一次性补丁 `script/tmp/d14_patch.py`（逐锚点唯一命中断言；spec/01 30 处操作＋spec/02 1 处；两处自断言计数笔误由脚本自行拦截后修正；收尾删除）②`python script/width_check.py` ③`python script/gate.py check` | ①A1~A8 全补：§1.1 加“读哪些结构”列×14 行（17 级）＋补注、§3.5/§3.8 两链分工澄清（B1）、§3.7 存储体豁免＋端口方向/速率、§3.9 方向/速率/逐成员写者读者、§3.10 标题改接口式＋产消方清单、§3.15 `mshr_wake_t` 方向＋产消者＋`ok=0` 路径、§3.18 `excp_t` 产生点署名、§3.20 两结构方向/速率/产消者；B2 改 `spec/02` §9.0 前注一句（“（历史）编码已落 §9.1；本前注保留为沿革”，占位串计数＝2 不破）②`width_check` = 28 可核对 / 不符 0 / 未能核实 0 / 转录基准行 2 ③`gate.py check` = **24 PASS / 0 WARN / 0 FAIL** | **未完成**：①A9（读值拍）与 A7（`rw` 值域映射）不在本片——归决策片 ②`spec/02` 仅改 §9.0 一句、其余未动 ③本片未经评审（送审由 R8 派） |

| R-049 | 2026-09-23 | **D-14 决策片回收：A9／A7／F5／F2 四条 ADR** | ①**A9（关键）**：**锁定唯一读值拍＝`P9`/`W` 发射选择拍读 PRF**；`S`/P8 只做就绪初值判定、**不读源值**；§3.8（226）/§3.9（27）**不存值不变**，值唯一载体＝§3.11 `disp_x_t`（328 含 `rs1_val/rs2_val` 128 bit）；**根因找到**：书内"发射/分发"与本工程**方向相反**（书：发射＝入队、分发＝送出；工程：P8 分发＝入队、P9 发射＝选择），"后读在分发级读"被按工程词读成"S 拍读"⇒四处互不对齐；算账：甲（S 读）须条目 +128 bit ⇒ IQ 14,464→22,656（+56.6%）且未就绪源仍须二读 ⇒ **不成立**；乙（W 读）**0 bit 变更**。回写 7 处＋`spec/00` gloss＋**须通知 D-8**（其"S 读出"句与 T-04-5a/5d）②**A7**：`rw`＝00/01/10/11=R/W/X/保留（保留码发出即违例、接收端按 access-fault 兜底）；分工＝`kind` 类别/路由、`rw` 权限检查、**`fetch` 冗余（≡kind==2）首选删除**（合计 67→66）③**F5**：三族两两撞名、**任改一族必留 ≥1 对撞**⇒最少改两族——批中止判据 `T1/T5/T6`→**`AB1/AB5/AB6`**、翻选触发 `T1/T2`→**`PT1/PT2`**、`spec/02` 的 `T-1..T-19` **不动** ④**F2**：三义归一——`spec/01` §4 旧 `I1/I2` **均折入 `INV-4`**（勿误引 `INV-2`）、`spec/00` 旧址 `I1`→`INV-1`、`spec/02` 的 `I2-n`→**`SI-n`**；改引清单 6 处（spec/01 L733/734/901、doc/verify/04 L62/87、spec/00 L168-170） | **均判可自决**（设计取舍/编号归一；无放宽、无不可回退）；**A9/A7 含接口语义与位宽变更 ⇒ 进 §9.5 ④段**；剩余分歧：书-工术语映射为**笔记级**（原书页未回读）、`SI-n` 落名前须 grep 无冲突、kind=11 的 rw 口径归 `spec/08`。**下一手**：派设计片落文（先 git 基线、逐锚点断言、重跑 width/gate）；F5 的规则件改名随同片 |

| R-050 | 2026-09-23 | **D-14 决策片·落文（A9／A7／F2／F5 四份 ADR 落盘）** | ①**A9**：`doc/spec/01` 七处——§1.1 `P8` 行改“就绪初值判定（源已完成或与本拍 `WB` 广播命中；不读源值）”＋读列改“`PRF`（仅 P9 后读）”、`P9` 行加“后读读 PRF（6×2 口）→ `disp_x_t`”＋读列补 PRF、表后新增“后读＝出队（选择）拍读 PRF”定义句、§3.5 产生点改“`W` 后读拍（`P9`）”、§3.9 改“`S` 分配拍就绪初值判定”、§3.11 改“`W`（发射选择）拍自 PRF 读出”、§3.19 改“`W` 后读拍” ②**A7**：§3.14 `rw` 值域 00=R／01=W／10=X／11=保留（保留码发出即违例、接收端按 access-fault 兜底）＋`kind=11` 携“触发 walk 的原始访问类型”（细化归 `spec/08`）；删 `fetch` 行；合计 67→**66** ③**F2**：§4 BP1/BP2 旧 `I1`/`I2` 均折入 `INV-4`、§8.2 同改；`doc/verify/04` 两行（L62 行内双命中＋L87）、`spec/00` §4.3（L168-170）归一 ④**F5**：`AGENTS.md` 规则 10 → `AB1`/`AB5`/`AB6`＋旧名对照注；`spec/01` §1.1 翻选触发 → `PT1`/`PT2`（2 句 4 处）＋定义句；`spec/02` 未动。一次性补丁＝逐锚点断言 30 op／4 文件（CRLF 感知、先验后写） | 机判：`width_check` 28 个 struct 可机器核对 28／不符 0／未能核实 0／转录基准行 2；`gate check` **24 PASS / 0 WARN / 0 FAIL**。基线＝`b3b3e5b`「Q-004 r15 基线」（开工前提交；工作树开工前已净，其前为 `75fae56` 决策片回收点）。**残项（未动，grep 逐一列出）**：`doc/verify/04` L65／L122、`run_cmd/AutoQueue.yaml` L348 仍引旧 `T1/T2` 族；“须通知 D-8”（`spec/04` “S 读出”句与 T-04-5a/5d）与 `spec/02` `I2-n`→`SI-n` 不在本片范围 |

| R-052 | 2026-09-23 | **D-14 落文遗留收口（小片）：旧 `T1/T2` 残引 3 处 ＋ `spec/02` `I2-n`→`SI-n` ＋ `spec/04` A9 更正**（**编号注**：原拟 R-051，与并行会话「`spec/03`＋`spec/04` 章节片②回收」行撞号 ⇒ 按“后出现者后移”改 **R-052**） | ①全仓对账（正则 `\bT[1-6]\b`，`python` 扫描工作区；改前命中 17 文件）②逐锚点断言补丁（`python` 脚本，先验后写）③`python script/width_check.py` ④`python script/gate.py check` | ①本片改动 3 处＝`doc/verify/04` L65（`T1/T2`→`PT1/PT2`，翻选触发类）、`doc/verify/04` L122（`T1/T5/T6`→`AB1/AB5/AB6`，批中止判据类）、`run_cmd/AutoQueue.yaml` L348（`T1/T2`→`PT1/PT2`）；保留＝旧名对照注（`AGENTS.md` L176、`spec/01` L43）与历史记录（`doc/verify/05`／HANDOFF／评审探针，按记录不改写）；转派＝`doc/process/00` L31/L34/L51、`iss/README.md` L55、`iss/tools/trace_compare.py` L10/L64（非本片写面）；他族＝`spec/00` `T1..T4`（TAGE 表名）、`spec/03` `T3-n`、`spec/02` `T-n`；改后复查命中 15 文件（余者＝保留/转派/他族/本片新记录） ②`spec/02` `I2-`→`SI-` 31 处（改前 `SI-` 于 `doc/spec` 面 0 占用，记录面仅作改名目标名出现；改后 `spec/02` 内 `I2-` 残余 0）；自引用抽查 3 处过 ③`spec/04` A9 更正：3 句改「`S 级后读拍`→`W`（发射选择）拍读出」＋「由 A9 闭合」标注 4 处（T-04-5a／6×2 读法；与 `spec/01` §3.11/§3.19 现文一致，`S 写口` 命名按 `spec/01` 保留）④`width_check` 28/28（不符 0／未能核实 0／转录基准行 2）；`gate check` 收尾复跑＝---- 24 PASS / 1 WARN / 0 FAIL ---- | T-04-5b（余 `spec/00` §4.4 行注落文）、5c/5d 维持缺项；`S 写口`／`E 写口` 是否随 A9 更名未自裁（`spec/01` 现文亦保留原名）；本片未经评审（送审由 R8 派）。**收尾复核注**：末次 `gate check`＝---- 24 PASS / 1 WARN / 0 FAIL ----（FAIL＝无；WARN＝rare-chars(人工判)        低频字 1 个：（低频字，见 gate 输出）） |

| R-051 | 2026-09-23 | **D-14 章节片②（`spec/03`＋`spec/04`）回收** | 核对 44 条＝**通过 32 / 阻塞 5 / 需澄清 7**（另存疑 3 呈 R0）。**阻塞 5 条**：①**阻塞-1** `spec/04` 与 **A9 新裁定冲突**（其 §4.1/§3.3 仍写"S 级后读拍读出"；`spec/01` 已改 W 拍）——**已知冲突，正在由落文遗留收口片修** ②**阻塞-2（最重）** `spec/03` B-3 的"**66 B 上界**"**可构造反例**：朴素续走 68 B、拼接续走 98 B（均无分支长串型合法块）⇒ 作为块跨度普适硬界**不成立**，须**限定适用范围或改名**（否则一条合法 >66 B 块触发假红/截断）③**阻塞-3** `spec/03` §7-2 两式**不能同真**（槽 15 地址 W+30 时末字节 W+33；W+65 要求槽 15 在 W+62＝差一窗"基址"未限域）④**阻塞-4** I3-3 引 `§7-5` **悬空**（该行是 nib/字节反例，无关）⑤**阻塞-5** `older()` **位宽失配**（FTQ 5 bit 域直接用 `spec/01` 的 8 bit 专属定义 mod 256/半量 128；FTQ 应为 32/16）。**需澄清 7 条**含：L_x"条目可截断点"未定义（K 紧上界不可独立复算）／B-3 更正路径不可执行（基准件·落点·批准人全缺）＋"经评审轮核对"与"首片未评审"相抵／INV-2·INV-3 的 SVA 半覆盖（缺面未列缺项）／INV-1 与 INV-4 的"旧 `I1`"溯源双记／`spec/03` F2"起始槽"集合歧义／§6.1③ 依赖的"待定（ISS-039）标记"位置待给。**通过面亮点**：**K 推导逐步复算全符**（L_S=1、L_E=2、K=2）＋§5.2 复算 891/816/2619/952 全符＋**锚点抽 5 处全命中**（【超】p.157-160/p.140/p.141-143/p.121-123/p.126-128）＋§2.4 对账表 11/12 行命中 | **下一手**：派 `spec/03` 收口片（阻塞-2/3/4/5＋澄清 6）＋`spec/04` 澄清 1/3/4/5 随落文片；存疑 3 条（S-1 66 B 原始适用范围／S-2 A9 是否连 `spec/04` §3.3 一并改／S-3 T-04-5a 存废）**呈 R0**——其中 **S-1/S-3 需你一句**（我建议：S-1 按"两窗+2B 路径界"限定措辞；S-3 撤销重复登记） |

| R-053 | 2026-09-23 | **D-14 章节片② `spec/03` 收口片（阻塞-2/3/4/5＋B-3 更正路径/F2 起始槽/§6.1③ 三项澄清）**（**编号注**：派发词拟 R-052，与并行会话「D-14 落文遗留收口」行撞号 ⇒ 按"后出现者后移"顺延 **R-053**） | ①一次性补丁 `script/tmp/patch_d14b_spec03.py`（逐锚点正则唯一命中断言、CRLF 感知、先验后写；10 op）＋行边界修复 `script/tmp/fix_v3_spec03.py`（两脚本收尾删除）②`python script/width_check.py` ③`python script/gate.py check` ④槽映射枚举算式（`python -c`） | ①处置——**阻塞-2**＝B-3 改"两窗内取全路径"限域（68 B/98 B 反例明列域外，>66 B 块处置归 T3-1/`spec/04`；"该 2 B 只由 `xw_carry` 消费"同句限域）；**阻塞-3**＝槽 15 属 **W+32 窗**、槽地址 **W+62**、末字节 **W+65**（B-3 与 §7-2 双处更正，原"基址 + 30"读法作废）；**阻塞-4**＝I3-3 改引 **§7-9**（新增算式行：槽号 = `pc[4:1]`、16 槽 × 2 B = 32 B/窗、`pc[0] = 0`）；**阻塞-5**＝F1/F4 改 FTQ 参数化式 **`older_5`（mod 32/半量 16）**＋显式声明 `spec/01` §3.19 规则 1 为 `rob_id` 8 bit 专用、FTQ 不直接引用；**B-3 更正路径三件**＝基准件（`doc/process/00` ISS-039 决策列＋`spec/01` 现行文本）/落点（§2.3 行内更正注，涉接口回写 `spec/01`）/批准人（R0，经 R8/领队按规则 23 呈报）＋删"该读法经评审轮核对"相抵句；**F2 起始槽**＝澄清为**块内各指令起始槽**（非块首单槽；附语义一致性论证）；**§6.1③**＝改述（`spec/01` §3.16「待定（ISS-039）」标记已不存在，§1.1 P5 行现为「ISS-039 定案 A」；条件改为按现行文本核对后回写台账）②`width_check` = 28 可核对／不符 0／未能核实 0／转录基准行 2 ③`gate.py check` = **24 PASS / 0 WARN / 0 FAIL**（首跑曾 2 FAIL：本片版本表行边界事故＋并行会话 R-052 改号前瞬时重号，均已消解；以末行为准）④16/16 OK | **未完成**：①本片未经评审（首片亦未评审；收口片不构成评审证据）②R-051 澄清面其余项（L_x"条目可截断点"、INV-2/3 SVA 半覆盖、INV-1/4"旧 `I1`"溯源双记）不在本篇写面，随 `spec/04` 落文片 ③ISS-039/ISS-045 闭合闸门不因本片解除（仍待评审核对＋T3-1 闭合）④一次行边界事故如实留痕（v1 补丁插入行未含行尾 ⇒ `md-integrity` 报 `spec/03` 行 5/6 列数 2/8；已由 `fix_v3` 修复并复验） |

| R-054 | 2026-09-23 | **D-7 平台骨架片（ModelSim-only）**：`filelist/tb.f`＋`tb/vr1_uvm/` 7 件骨架＋`run_cmd/` 模板＋vlog 实跑取证 | ① `filelist/tb.f`＝3 编译单元（`vr1_pkg.sv` 经 `include` 聚合 agent/scoreboard/env/smoke_test → `vr1_if.sv` → `vr1_top_tb.sv`），**不含 `rtl/`**（RTL 尚未存在）② `tb/vr1_uvm/` 新增 7 件：`vr1_pkg.sv`／`vr1_if.sv`（按 `spec/01` §3.13~§3.21 接口族建占位信号表，仅声明不驱动、位宽标 [回填] 不自造）／`vr1_top_tb.sv`（10 ns 占位时钟＋复位占位＋`vr1_if` 例化＋`run_test()`，不例化 DUT）／`vr1_env.sv`／`vr1_agent.sv`／`vr1_scoreboard.sv`（build/connect 空实现）／`vr1_smoke_test.sv`（仅 `uvm_info` 与 objection 收放）③ `run_cmd/vr1_flow.do`（§3.1 位形；vsim 行改 `-do "source run_cmd/vr1_smoke.tcl"` 正斜杠；头注 ADR-5：退出码不作判据、判据＝产物证据行）＋`run_cmd/vr1_smoke.tcl`（`run -all`＋`quit -f`）④ 实跑（仓库根；沙箱 `sim/run_r4_d7/`）：`vlib`＋`vmap work`＋`vlog -64 -sv -cover bcesf +incdir+D:/modeltech64_2020.4/verilog_src/uvm-1.2/src -f filelist/tb.f`；证据＝`-- Compiling` 3 行（vr1_pkg／vr1_if／vr1_top_tb）＋`-- Importing package uvm_pkg (uvm-1.2 Built-in)`＋段末 `Errors: 0, Warnings: 1`（warn=vlog-159，-64 被忽略属良性）；另跑 UVM 1.2 源码编译 `uvm_pkg.sv`＝1 单元 `Errors: 0`（`vlog_uvm_src.log`） | **未决（勿当闭环）**：① `vopt`／`vsim` 两段**未实跑**（无 DUT）——`+UVM_TESTNAME=vr1_smoke_test` 运行路径未验证 ② `vr1_if` 信号名/位宽为占位，待 §3 冻结后回填 ③ 本片无 scoreboard／断言／覆盖率证据（红线 R8 仿真侧判据未触及），未经评审 ④ **本机实测修正**：vlog **不支持 `-ntb_opts`**（vlog-1902）——`AGENTS.md` §3.1 该选项与本机 SE 2020.4 不符，需 R8 登记 `doc/process/00`（平台类）并修订命令位形（实测可用＝`+incdir+<uvm src>`，预编译 UVM 经 built-in 解析）；`vmap mtiUvm` 首试 EACCES 未复现、最终流程未依赖 ⑤ 首跑在仓库根生成 `modelsim.ini`（工具行为）——已删除并确认不在仓；`sim/run_r4_d7/` 留 INI 副本与全部日志（可清理沙箱，R10 独占） ⑥ 编号说明：原拟 R-050 已被并行会话（D-14 决策片·落文）占用，本行按派发词顺延为 R-054。 |

| R-055 | 2026-09-23 | **R0 裁定：S-3 撤销**（`spec/04` 重复登记项 T-04-5a） | 按 R0 一句"**应该可以撤销**"落定：`spec/04` 的 T-04-5a 标**撤销**（理由：与 `spec/01` §3.11「原"分发级已读出"措辞作废」重复登记，不得再作缺项计入）；D-14 章节片② 的 S-1（66 B 限域）已按领队建议由 `spec/03` 收口片落定（"两窗内取全路径"上界＋反例域外）；**存疑三条至此全清**（S-1 落、S-2 已由 A9 落文片连带改 `spec/04` §3.3、S-3 撤销） | **未完**：D-14 余 **`spec/00`／`spec/02` 两个章节片**＋纵片① 的 F6/F12/F15 记录同步项——已派（见 HANDOFF 下一手）；其后即判 §9.4b 出口与 §6.1 C2 |

| R-056 | 2026-09-23 | **D-14 章节片④（`spec/02`）回收** | 核对 23 条＝**通过 15 / 阻塞 3 / 需澄清 5**。**阻塞**：**B-1** §14 合计行与表体计数不符（枚举 11 项漏 T-5/T-6、实际 13）＋**T-17 无状态载体**（其"关闭"仅在 §12，而 §14 自称"原表不改以本表为准"）；**B-2（语义）** §10.9 的 CSRRW/CSRRS/CSRRC "立即数规则"列**一律填 10**，与 §3.3（10＝CSR uimm[4:0]）／§4.3（仅 CSRRWI/RSI/RCI 标 10）／§9.5（CSR 寄存器 3 归 00 PRF）冲突 ⇒ 两读法并存、下游可任选（若按 `imm_kind==10` 走 SI-9 取 uimm，CSRRW 类会**用 uimm 而非 rs1 写 CSR，csr_new 与 trace 全错**）；**B-3（真缺陷，有锚）** §10.2 JALR 写"目标非对齐（bit1≠0）→ instruction-address-misaligned"，而**一期含 Zca ⇒ IALIGN=16**，提取件明文"**IALIGN=16 下不可能有 instruction-address-misaligned 异常**"（2717-2720/11869-11870）⇒ 照抄即对合法 C 边界目标误报异常、与 Spike 分叉；且该行**缺"目标非规范地址→access fault"**（JAL 行有，同族不对称）。**需澄清 5**：C-1 §9.5 `src_sel` 派生判据不可复现（三组反例）／C-2 C-3"复核通过"无判据／C-3 `spec/01` §3.8/§3.11 的 `isop` 行**无指针**（2/4 缺口）／C-4 AN-12 复现关键词字面不命中（间隔符非 ASCII 空格）／C-5 §9.0/§15 引 `spec/01` **行号已漂移**（161/277/340 → 实测 167/285/352）。**通过亮点**：87 编码**两两互异（19 段全无重叠）**、全落 10 bit（max 0x39A）、**位段一致抽样 31/31**、**例码 14/14**（AMOSWAP=0x392 ✓）、§9.2 计数式 87 ✓、§9.5 派生 87/11/15 ✓、**`SI-` 改名零残留且 5 处自引用一致**（含原悬空项已指 SI-4）、§10 63 行齐、锚点字面 8/9 | **下一手**：派 `spec/02` 收口片（B-1/B-2/B-3＋C-3/C-5）；C-1/C-2/C-4 需判据或登记；其后 `spec/00` 章节片回收即 D-14 全片型齐 |

| R-057 | 2026-09-23 | **D-14 章节片③（`spec/00`）回收** | 核对 26 条＝**通过 14 / 阻塞 2 / 需澄清 8 / 存疑 2（呈 R0）**。**阻塞**：**B-1** §1.1「G1 行」字串写"乱序超标量**单发射**核"，与 §3 微架构图（4 μop/拍译码、6 路分发）、§4.3 `DECODE_WIDTH=4`、§4.4 `ISSUE_WIDTH=6`、§1.0 定位段**正面冲突**（已二次定点复核非误读）⇒ 冻结面存在"发射宽度"两读法；**B-2（缺失型）** §4 参数表**缺"消费篇"维**——107 行仅约 **14 行**能指到"谁在用"（§4.2 2/25、§4.5 0/7 最甚），与"§4 是所有数值唯一权威源"的定位不匹配（须补列或显式声明不在本表范畴）。**需澄清 8**：C-1 **ISS-018 家系三处口径互斥**（§10 写"X4 关闭"／`doc/process/00` 仍「处理中」／`spec/01` 同篇并存"已定值 33"与"待 `spec/04` 定"两句）／C-2 §10 混排已闭环项且无状态列／C-3 §4.8 **L1I 行三项口径不齐**（数据无算式、tag 宽无推导、状态"+4"无分解）／C-4 `FWD_CMP_BITS=44` 跨篇闭环不可验证（`spec/01` 0 命中）／C-5 `ASID_BITS_IMPL` 的【权】§4.1.11 锚点未闭环／C-6 §4.8 三行"待锚点"与"未计"清单（登记态，待 `spec/03`/`09` 闭合）／C-7 `PRF_FP_ENTRIES`/`FRF_RD_PORTS` 在 §7 而 §4 总表外／C-8 §4.3 注"旧名仅存 `spec/04`"与 `spec/01` §4 仍有旧名括注轻微不符。**通过亮点**：**§4.8 合计 2,968,772 与 12 加数全对**、大数组四行全对、§4.3 注 93/33/97-7bit 全对、**派生位宽 8 项全对**、抽 5 参数跨篇一致、锚点 3/3 命中、§6/§9/§10 列齐、§10 与 `doc/process/00` 方向一致（6 行） | **D-14 覆盖进度**：章节片①（`spec/01` §1+§3）②（`spec/03`+`04`）③（`spec/00`）④（`spec/02`）已跑；**唯余 `spec/01` §2/§4/§5/§7/§8**（已派章节片⑤）；纵片①②③已跑。**收口片**：`spec/02` 收口（B-1/B-2/B-3）与 `spec/00` 收口（B-1/B-2/C-1/C-3）待派/在派 |

**留档纪律**：本表每行必须能被第三人按“命令”列重跑复现；不可复现的结论不得入表。表中“不变式项数”以 `gate.py check` 末行 PASS/WARN/FAIL 明细为准（第 6 轮 CLR-07 指出原写“16 项”无溯源，已改为不写固定项数）。
| R-058 | 2026-09-23 | 机械核销 / 结构改造 | `git mv` 四件（00→`doc/process/`，03/04/05→`doc/verify/`；`01-评审记录.md`＋`01-评审探针/`→`doc/review/`）；`python script/tmp/sweep_doc.py --apply`（703 处）＋`sweep_review.py --apply`（37 处）＋`patch_resid.py`（4 处）；`python script/path_audit.py --fix`；`python script/gate.py check` | 清扫 744 处 / 涉 40 余文件；残留复扫 **0 处**；`path_audit` = 缺失路径 0 / 骨架缺失 0 / 无占位 0；`gate check` = **24 PASS / 0 WARN / 0 FAIL**（迁移首跑一度多出 1 WARN，查明为 `char_freq()` 语料根随 `dirname(LEDGER)` 缩成 `doc/process/` 所致，已把语料根显式钉回 `doc/`，WARN 复零）| **doc/ 四类落位完成**（verify / design / review / process，治理四件留顶层）。同时修两处判据面缺陷：① `scanned_docs()` 原只扫 doc/ 一层，新子目录会**掉出** codepoints/错词/表格结构判据面 ⇒ 改递归（仍跳过冻结的评审探针）；② `BASE_GLOBS`/`scan_dirs` 改指 `doc/review/*-评审探针/`。冻结证据（`01-评审探针/` 内 30 件）内容**零改动**；`doc/review/01-评审记录.md` 内 3 处历史引文（旧绝对路径）保留原文不追改。台账 ISS-055 登记；长基线刷新另见后续行。 |
| R-059 | 2026-09-23 | 长基线重折（ADR-4/B5） | `python script/gate.py rocheck --baseline`（先取差异取证）→ `python script/gate.py snapshot --refresh --reason "5dce8c0 doc 分类改造（ISS-055；本文件 R-058）…"` → 复跑 `rocheck --baseline` | 折前差异**逐项可解释**：MODIFIED 8（`iss/` 内 doc 路径清扫）、ADDED 40（探针新路径 30 件 ＋ D-7 平台产物 10 件：`filelist/tb.f`、`run_cmd/vr1_flow.do`、`run_cmd/vr1_smoke.tcl`、`tb/vr1_uvm/*.sv` 7 件）、DELETED 30（探针旧路径）、MTIME-ONLY 1（`iss/tests/probes/_e.txt`，INFO）；旧→新指纹 `sha256:77efa13c…b63ad7` → `sha256:aabdd271…f87ff21`，文件数 88→98；折后复跑 = **内容侧 0 差异**（退出码 0）| 执行者＝R8；折入集出处＝提交 `5dce8c0`（ISS-055／本文件 R-058）＋D-7 产物（`doc/verify/05` 既有行、HANDOFF D-7 条）。**披露**：本轮一并**补折**了 D-7 平台 10 件与 `iss/` 8 件——旧基线（17:28 生成）早于 D-7 产物落地，它们此前已进复核面却从未被折入，属补账而非抹差异；探针 30 件为同内容换路径键（md5 未变）。**取证件归档**：清扫/补丁脚本 5 件＋折前差异取证＋终局判据输出见 `doc/review/00-doc分类改造探针/`（含第三人复跑入口）。 |
| R-060 | 2026-09-23 | 机械核销 / 收尾补漏 | `python doc/review/00-doc分类改造探针/resid_all.py`（全仓残留扫描，不设目录跳过）＋两文件定点清扫；`python script/gate.py rocheck --baseline` | 首扫暴露**两处漏网**：`script/tmp/README.md`（2 处）、`sim/covdb/README.md`（5 处）——二者是**受控文档**却被上轮清扫的 `SKIP_DIRS`（`tmp`、`sim`）跳过，属清扫器口径漏洞（`sim/covdb` 在长基线扫描面内、`script/tmp/README.md` 已入库）。补扫后全仓残留＝**3 文件 19 处，全部落在豁免面**（本探针目录自身 15 处＝改造记录与补丁脚本、工具状态 `window_state.json` 4 处）；`gate check` 复跑 = 24 PASS / 0 WARN / 0 FAIL | **披露（长基线已知差异）**：本轮 `≤1 次/轮` 的刷新（R-059）已用满，其后才补修的 `sim/covdb/README.md` 恰在扫描面内 ⇒ 下次 `rocheck --baseline` 将**预期报 MODIFIED 1**，属本改造收尾件，下轮处置开工时随例行 ≤1 次刷新折入；**不因消除此差异而重刷基线**。取证件：`doc/review/00-doc分类改造探针/`（脚本 5 件＋折前取证＋终局判据输出＋豁免面清单）。 |
| R-061 | 2026-09-23 | 调度留痕（R0 停机指令） | 人指令（无命令）；`python script/gate.py check` 复核 | R0：「计划AI 停止派发任务；这个任务结束后 上传 git」。在飞的 **D-14 章节片⑤**（`doc/spec/01` §2/§4/§5/§7/§8 只读审计）被 R0 **手动停止 ⇒ 未产出回收件**；工作树干净（被停片为只读角色，未留散件） | **停机处置**：① `run_cmd/AutoQueue.yaml` Q-034 → `state: blocked` ＋ `block_reason` 记停止原因与「R0 明示恢复后由队首续派」② 驱动计划 D-14 状态列回填「进行中（R0 暂停）」③ `doc/verify/04` 停机注 ④ 本行＋HANDOFF 条。**覆盖缺口如实声明**：D-14 章节片①（§1+§3）/②（spec/03+04）/③（spec/00）/④（spec/02）与纵片①②③已跑，**唯余章节片⑤（§2/§4/§5/§7/§8）**与 spec/02、spec/00 两收口片未完成 ⇒ **C2「新增 0」判定无从作出**，D-14 不得视为收敛（§12.2 执行断点纪律：暂停须写停止原因＋恢复条件）。 |
| R-062 | 2026-09-23 | **调度留痕（R0 明示恢复自动推进循环）＋ r17 基线立 ＋ 长基线例行折入** | ① R0 两条指令（原话）：「先关联下git cli」「根据自动推进循环进行项目」② git CLI 实测：`git --version`＝2.52.0.windows.1；`git fetch origin` 通（origin＝`github.com/HHHXXX-cell/virtual_riscv.git`）；`git status` 与 `origin/main` 同步（无 ahead/behind；工作树净，仅未跟踪 `.qoder/settings.json`）③ `python script/gate.py check`＝**24 PASS / 0 WARN / 0 FAIL**（队列 36 项：ready 19 / blocked 7 / in_progress 2 / done 8）④ `python script/gate.py rocheck --baseline` 取证 → `snapshot --refresh --reason …`（见右列②）⑤ 章节片⑤重派（auditor 只读；其 T0 入回收行） | ①**恢复条件满足**（R-061 载「恢复条件＝R0 明示恢复」）：Q-034 `blocked`→`in_progress`、`block_reason` 解除；`doc/verify/04` 停机注后追加恢复注；驱动计划 D-14 状态列回填。**R0 停机指令中"上传 git"一项**：实测本地与 `origin/main` 已同步（含停机提交 `46bbc45`），无需补推 ②**长基线差异全解释后折入**（旧→新 `sha256:aabdd271…f87ff21` → `sha256:098fca46…8bb75`，文件数 98→97）：MODIFIED 37＝**36 件纯 LF→CRLF 工作树物质化**（mtime 全集 2026-09-23T22:33:06 同秒＝单次批量事件；逐件「CRLF 归一后 md5＝基线 md5」36/36 全等＋抽样 `git show 46bbc45:<f>` blob md5 与基线相等 ⇒ 语义零变更；来源未确证、**非项目角色写入**）＋1 件 `sim/covdb/README.md`（R-060 已披露补修）；DELETED 1＝`sim/covdb/.gitkeep`（未入库占位件，R-060 收尾被 README.md 取代）；MTIME-ONLY 60＝同批量事件。**R-060「预期 MODIFIED 1」更正为实况 37＋1**；登记 **ISS-056**（平台，已解决） ③**r17 基线已立**：r16 基线（`78f9773`）之后被 R0 指令的 doc 分类改造三提交（`5dce8c0`/`38a33c0`/`46bbc45`）穿插、处置未开工即环境已变 ⇒ 按 §9.4 重立基线（提交标「Q-004 r17 基线」，本条即理由留痕） | **下一手**：① **章节片⑤**（`spec/01` §2/§4/§5/§7/§8，auditor 只读，已重派）② **`spec/02` 收口片**（B-1/B-2/B-3＋C-5；**C-3 的两处 `spec/01` 指针增补转下一片**——避免与章节片⑤同轮改 `spec/01`）③ 其后 `spec/00` 收口片 → D-14 出口判定（C2） |

| R-063 | 2026-09-23 | **D-14 章节片④ `spec/02` 收口片（处置 R-056 的 B-1/B-2/B-3＋C-5）** | ①先验后写：改前逐一实读锚点区（§14 表体＋合计行逐行清点；§10.9 三行；§10.2 JALR 行；§9.0/§15 行号引用）；提取件 `riscv_spec_full.txt` 行 2717／11870 grep 复核命中（B-3 一级依据）②Edit 逐锚点补丁 6 处（每处命中恰 1 次；未建 tmp 脚本——守写面约束）③`python script/width_check.py` ④`python script/gate.py check` | ①**B-1 落**：§14 插入 T-17 行「**关闭（承接 §12）**｜§10 共 63 条（RV64I 全量）；后续仅随评审修订」＋合计行改「未动 13 项＝T-3／T-4／T-5／T-6／T-7／T-8／T-10~T-16；部分推进 2 项＝T-2／T-9；关闭 4 项＝T-1／T-17／T-18／T-19；13＋2＋4＝19＝T-1~T-19 全集」——枚举与表体 19 行逐行一致 ②**B-2 落**：§10.9 CSRRW/CSRRS/CSRRC 立即数规则列「10」→「不适用（寄存器型，src_val＝rs1）」（§3.3：10 仅 CSR 立即数型；§4.3/SI-9：寄存器型取 rs1）；CSRRWI/RSI/RCI 维持「10」；该表其余 6 行逐行核对无同类 ③**B-3 落**：§10.2 JALR 行补齐「目标非法（非规范地址）→ 取指侧 access fault（同 JAL 行）」＋「bit1≠0 **不产生** instruction-address-misaligned——`IALIGN=16`（一期含 Zca）下该异常**不可能发生**（行级锚＝提取件行 2717-2720／11869-11870，一级依据）」 ④**C-5 落**：§9.0 三处（161/277/340→§3.4 `uop_t`／§3.8 `iq_wr_t`／§3.11 `disp_x_t`）＋§15 两处（277/340→节号）改引节号；「行 86 包含件声明」经核未漂移保留 ⑤机判：`width_check`＝28 个 struct 可机器核对 28／不符 0／未能核实 0／转录基准行 2；`gate.py check`＝**24 PASS / 0 WARN / 0 FAIL**（末行为权威源） | 未完成：①C-1（§9.5 `src_sel` 判据不可复现）/C-2（“复核通过”无判据）/C-4（AN-12 关键词字面不命中）需判据或登记、不在本片 ②C-3（`spec/01` §3.8/§3.11 `isop` 行指针增补）转下一片——本片未改 `spec/01` ③本片未经评审（送审由 R8 派） |

| R-064 | 2026-09-23 | **D-14 章节片⑤回收（`spec/01` §2/§4/§5/§7/§8 审计，auditor 只读）＋ 规则 22 窗口核查 ＋ 附录 M 归档** | ①派 `vr1-auditor`（Read/Grep/Glob 只读；**T0＝2026-09-23T23:04:42 +0800**），输入＝冻结快照 `snapshot_r17_spec01.md`＋机判件三件 ②`python script/gate.py rocheck --window 2026-09-23T23:04:42+08:00`（二调四分类，输出归档 `doc/review/01-评审探针/rocheck_r17_window.txt`）③报告全文归档入评审记录**附 M**（M§1~M§6 复核者原文＋M§7 领队结论） | ①**核对 28 ＝ 通过 14 / 阻塞 9（B-1~B-9）/ 需澄清 4（C-1~C-4）/ 存疑 1（S-1，呈 R0）**——阻塞含 3 项 C 类（回滚终止规则不覆盖非分支 B／冲刷当拍 P8 档无撤销载体／回滚路径 P7 在途物理号归还缺失）、1 项与已闭合 H-01 同型的载体链断链（`disp_x_t.lq_id/sq_id` 上游缺域）、及 `HIST_DEPTH` 待定残留／BP1 门控三口径／§8.2 标注不符／A4 不可判定／ckpt 槽生命周期两边界；抽查复算 ≥5（含转录基准行 `ftq_entry_t` 145／`rmap_uop_t` 322）全符 ⇒ 机判可信 ②**窗口核查**：MODIFIED 3（`.qoder/handoff/HANDOFF.md`／`doc/spec/02-…`／`doc/verify/05-…`——全部属同轮 `spec/02` 收口片**已申报写面**）＋ADDED 1（`rocheck_r17_window.txt`＝本证据件自指）＋DELETED 0 ⇒ **审计者零写入** ③`gate.py check`＝24 PASS / 0 WARN / 0 FAIL | **C2 判定：不成立**（新增 9 阻塞）⇒ 按「全量→处置→增量→全量」续跑。**分流**：B-1/B-2/B-3/B-8（回滚/检查点生命周期）＋B-4/B-6/B-9（载体链/门控/断言）＝机制取舍 → 派决策片 D1/D2；B-5/B-7/C-1~C-4＝文本类 → 随决策落文批处置；S-1 呈 R0。**登记** `doc/process/00` **ISS-057**（处理中）。**长基线待折**：r17 探针 5 件＋`rocheck_r17_window.txt` 为本轮新增、尚未折入——下轮处置开工时随例行 ≤1 次折入（同 R-060 先例） |

| R-065 | 2026-09-23 | **R0 裁决：S-1——加域前缀区分（评审发现编号 vs §7 断言编号）** | 人指令（原话）；依据＝评审记录**附 M §3** 存疑项 S-1（`doc/verify/05` R-064） | **约定（由 D-14 落文批执行）**：① §7 断言 `A1~A20` 为篇内一等编号，**不加前缀**；② **评审/复核发现的编号在正文引用处加来源域前缀**——D-14 章节片 N → `片N-`（如 `片①-A8`／`片①-A9`／`片①-B1`）；附件条目 → `附X-`（如 `附K-B3b-2`）；③ 已自带域记号者（`BLK-`／`CLR-`／`I-`／`H-`／`N13`／`K1`／`R-5.1`／`SUS-01`）**保持不动**；④ 随同归一 **C-4**：`SUS-1`→`SUS-01`；⑤ 落文范围＝`doc/spec/*.md` 全库同域引用逐处处理（逐锚点唯一命中断言） | **转办**：并入 D-14 落文批（与 B-5/B-7/C-1~C-4 同批）；附 M §3 已加裁决注；ISS-057 的 S-1 由"呈 R0"改记"已裁、待落文" |

| R-066 | 2026-09-23 | **D-14 决策片 D2 回收：ADR-B4／ADR-B6／ADR-B9（B-4/B-6/B-9 机制决策）** | ①派 `vr1-decider`（只读，无执行工具；实耗约 3.5 min／14 次调用，未超预算）②输入＝评审记录附 M §1（B-4/B-6/B-9）＋`spec/01` §1.1/§3.8/§3.9/§3.11/§3.13/§4/§5.3/§7＋`spec/00` §4.3/§4.5/§4.8＋先例 `doc/verify/05` R-010/R-013/R-017 ③产出＝三份 ADR（四要素＋回退方式＋成本分级）；领队已用全仓 grep 复核其引用数字的落点 | **ADR-B4（选①：增 `iq_wr_t` 载荷域）**：补 `lq_id` 6＋`sq_id` 5 ＝ **+11 bit，226→237**；IQ 例化 `64×237＝15,168`；`spec/00` §4.8 三路发射队列行 `16,192→16,896`（+704＝64×11）、合计 `2,968,772→2,969,476`；`disp_x_t` 1,968 b **不动**。否决②旁路数组（位量代价同为 +704，且 I-03 已把 §3.9 定死为"就绪附加区"，语义面变差）与③ROB 回读口（§6 M09/M11 已声明无回读口，代价更大）。**变体**（若 `sq_id` 按 §2 `sqptr_t` 取 6 bit）：238／16,960／2,969,540。下游义务＝`spec/01` §3.8（成员表+2、合计、例化、备注）／§3.11（`lq_id/sq_id` 行补上游绑定句）／§3.9（加澄清句防 I-03 同型误读）／§3.13（补源句）／§1.1 P8（可选）／§8.2（"接口链无断点"加闭合句）；`spec/00` §4.8；`spec/04` 两处"226 bit"引用（grep 实测命中；落文时全仓兜底）。**ADR-B6（唯一布尔式）**：`rob_near_full ≜ (ROB_ENTRIES − rob_free) ≥ ROB_NEAR_FULL`（占用 ≥120 ⇔ free ≤8）；**停止＝`rob_near_full`、出口＝其补（free ≥ 9）**，严格互补、无状态（free∈[4,8] 同真消除）；原出口"free ≥ `DECODE_WIDTH`"降为**推论**（最坏界实算：放行时 occ ≤119，单拍 +4 ⇒ occ ≤123、free ≥5 恒成立）；§7 A1 改写为 `rob_near_full \|-> !rob_alloc_grant`；第三口径 `rob_full` **删除**。否决滞回带状态（与 §5.3"组合重算"相抵、无锚点）与"停＝free<4"（使 `ROB_NEAR_FULL` 失去挂钩）。锚＝`spec/00` §4.5/§4.3＋§4 BP1 括注＋§5.3 句（《CPU卷04》p.378-387 笔记级标注）。**ADR-B9（择②为主、①之信号定义作前置）**：删 `rob_near_full_d`；`rob_tail_rollback` 定义为**回退生效拍脉冲**；A4 改为**重算等价**：`rob_tail_rollback \|-> ##1 (rob_near_full == ((ROB_ENTRIES − rob_free) ≥ ROB_NEAR_FULL))`＋H-06 同款内部信号声明（`rob_tail_rollback`/`rob_free` 为监视器信号）。否决"给 `_d` 补延迟定义"（仍与 §5.3 不等价）与"删 A4"（缩验证面） | **三项均为设计类自决**（无 P1~P4／放宽／环境类要素，无需强制转人）；但含接口位宽/门控/断言语义 ⇒ **须进 §9.5 评审 list ④ 段**（D-13 汇集），并按 §12.5 各送审一次（并入下一轮审核片）。**决策 AI 自报未决**：①`sq_id` 5/6 对账未闭合（本 ADR 按 5 bit，变体已备算——落文时给口径或转下一决策）②B-6 停顿率影响归 `spec/13`（登记）③全仓 `226`／`14,464`／`16,192`／`2,968,772` 残引落文时 grep 兜底（探针/评审记录按"记录不改写"保留）。**下一手**：随 D1 一并进落文批（`spec/01`＋`spec/00` §4.8＋`spec/04`）；落文后复跑 `width_check`/`gate.py check` |

| R-067 | 2026-09-23 | **D-14 决策片 D1 回收：ADR-B1／ADR-B2／ADR-B3／ADR-B8（回滚/检查点生命周期四决策）** | ①派 `vr1-decider`（只读；实耗约 5 min／14 次调用）②输入＝附 M §1（B-1/B-2/B-3/B-8）＋`spec/01` §1.1/§2/§3.19/§4/§5.1~5.3/§7/§8＋`spec/00` §4.3/§10＋先例 ISS-041 ADR-2 ③产出＝四份 ADR；**锚点口径先行声明**：`D:\IC验证知识库\_tools` 内无 BOOM 类乱序核（检索 0 命中），四项以**构造性论证＋既有正文内锚**记"已定"，**外部实现锚点缺失**如实标注（规范逐行回锚下放 `spec/02`/`spec/10`/`spec/11`） | **ADR-B1（选 A：终止键改「非严格老于 B」）**：`pop ⇔ !older(e_top, B)`（`older` 同 §3.19 规则 1 口径）；空栈＝正常完成；致命路径收窄为"序违例"（`(B−e) mod 256 == 128` 不可达）；**`br_mark` 保留但降级**（不再作终止键）⇒ **栈条目 27 bit 不变、struct 位宽零变更**；构造性论证：栈深 33 覆盖在途 ≤33、弹出 8/拍 ≥ 压入 ≤4/拍、回绕安全；回退＝显式深度计数器。**ADR-B2（选 A：冲杀 lane「并入压栈」）**：撤销 N3 子句"被冲刷作废的 lane 不得压栈"——冲杀载荷与常态压栈**同源复用**（S 级组合可得，零新增存储/端口）；入栈序＝lane 序、≤4 条/拍；checkpoint 路径 `SP := SP_ck` **取覆盖**（不弹、由 `FL_ck` 托管，防双归还）、rollback 路径由 B-1 新终止自动弹出；否决第二载体（+144 bit 与跨档仲裁）与排空（冲刷延迟 5→6 拍全情形）。**ADR-B3（选 A：分配-回滚互斥）**：R 级 `r_alloc_grant`/RAT 写由 `!flush_apply` 门控 ⇒ **冲刷当拍 R 级零副作用、泄漏界＝0**；否决"扩展归还路径"（覆盖已从构造上消灭的窗口）与"有界泄漏"（实算 33/4≈8.25 次冲刷即耗尽且**单调不可恢复**）；在途寄存器 `{arch, old_paddr}` **可删或按等价变体扩展——落文时二择一冻结**。**ADR-B8（①耗尽放行**：槽不可得 ⇒ 该分支 `needs_ckpt=0` 走逐条回滚路径（与 `spec/00` §4.3 `ROLLBACK_RATE` 行既有定价一致；否决反压等槽〔新增等待类+性能悬崖〕与复用最老槽〔破坏 A6 唯一性〕）；**②在途槽释放＝三源闭包＋冲刷当拍统一释放**：新增 **4 bit 内部寄存器 `ckpt_pend_v`**（非接口、非 struct、不入 `width_check`）；释放事件＝①提交②`flush_apply` 当拍（两路径统一，checkpoint 路径"回滚完成拍"问题消除）③保留"回滚完成拍"为兜底等价时刻；A6 扩第三源） | **无强制人裁决项**（均 §12.2 自决面）；四项（尤其 B-1/B-2 **首度改动 N3 既有冻结句**）**须进 §9.5 ④段**（D-13 汇集），并按 §12.5 各送审一次（并入下一轮审核片）。**一致性（关键）**：B-8① 使「needs_ckpt=0 分支」从稀有升为**常态可达** ⇒ **B-1 修正是热路径前置，两片必须同批落文**；联合死锁自由＝「单一撤销流＋零侧漏＋无等待态」，≤10 拍恢复；与 N-01/N2/N3 的 checkpoint 与提交弹栈机制零改动。**未决/盲区（自报）**：①外部实现锚缺失（已声明，不得沿用【权】级引用）②P7 在途寄存器去留与 B-8② 措辞冻结归落文轮 ③`ROLLBACK_MAX=33` 与 §5.2"上界＝`ROB_ENTRIES`"两口径并存（B-5 范畴残留，落文时同步复核）④**建议新增断言两条**（回滚活性 ≤6 拍；`flush_apply \|=> ckpt_pend_v==0`）——**编号待 `spec/11` 定稿，不得自编号冻结**。**下一手**：随 L1 落文片（`spec/01` §5.2／§5.3／§4 BP2 句／§7＋`spec/00` §4.3 注） |
