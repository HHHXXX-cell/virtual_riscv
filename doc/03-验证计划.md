# doc/03 — 验证计划

> 本篇为**计划 AI（vr1-planner）的产物边界**：§0《驱动计划》由计划 AI 维护，R8 只执行。
> 验证策略/环境/覆盖度等正式内容（Step 3 本体）随 spec 篇目推进在此篇内续写；
> 队列与状态看 `run_cmd/AutoQueue.yaml` 与 `doc/04`，**本表的"序"是唯一权威的工作顺序**。

---

## §0 驱动计划（计划 AI 唯一作者；R8 只执行、不得改序或跳项）

**维护规则**：①每次重排必须留痕（本表"重排记录"行 + `doc/05`）；②项状态只许 `待办/进行中/已完成/【待人】/【阻塞】`；③出口判据必须可核（机判优先）；④派发词要点须自足（含预算：目标时长与工具调用上限）。

| 序 | 工作项 | 角色 | 产物落点 | 出口判据 | 派发词要点 | 依赖/阻塞 | 状态 |
|---|---|---|---|---|---|---|---|
| D-1 | **第 13″ 轮确认**（r13′ 盲区补核 + 五项 ADR 送审） | auditor | 评审记录附件 | 条件①三处本体"在/缺/矛盾"给全；ADR-4/5/6/7/S2 各 ≥1 条风险记录 | 已派（二合一审核片） | — | 进行中 |
| D-2 | **ISS-026**（码点扫描是否列交付前置） | 决策 AI | `doc/00` 决策列 | ADR 四要素齐 | 已派（S4） | — | 进行中 |
| D-3 | **spec/02 续片**：`isop_t` 成员表 + RV64I 全量条目 | 设计 AI | `doc/spec/02-*.md` | 成员表行数 ≥20；RV64I 条目覆盖全部一类指令；新锚点带行号区间 | 已派 | — | 进行中 |
| D-4 | **ISS-044/045 过闸判定** → 关闭（若 D-1 条件①②③全过） | R8 | `doc/00` 状态字 | 闸门三条件全过 + 机判 22 PASS | 领队执行（不派新片） | D-1 | 待办 |
| D-5 | **Q-028 实施**：`gate.py snapshot/rocheck`（ADR-4 落地） | R8/R4 | `script/gate.py`＋`script/gate/integrity_baseline.json` | 四分类输出可用；首跑记耗时；规则 22 措辞同步；送审一次 | 单片；含"首跑校准"步骤 | D-2（ISS-024 已决，可直接做） | 待办 |
| D-6 | **spec/02 终片**：T-1~T-17 清账（按类分片） | 设计 AI | `doc/spec/02-*.md` | 待补清单归零或逐条转 `待锚点` 并登记 | 按类分片（每片 ≤3 类） | D-3 | 待办 |
| D-7 | **平台骨架**（ModelSim-only：tb 骨架 + filelist + do/tcl 模板） | R4 | `tb/vr1_uvm/`、`filelist/tb.f` | `vlog` 0 error（证据行口径＝ADR-5）；smoke 0 UVM_FATAL | 可分两片（骨架／smoke） | — | 待办 |
| D-8 | **spec/04 起稿**（含 `INV-1~INV-5` 权威对照表、`K` 推导、`HIST/ROLLBACK` 联动公式、S1/SUS-01 收口） | 设计 AI | `doc/spec/04-*.md` | 对照表含"新名｜旧名｜语义｜定值篇"；`K ≥ max(L_S,L_E)` 算式在；S1/SUS-01 给结论 | 分两片（总分表／推导） | Q-002 就绪 | 待办 |
| D-9 | **spec/00 v0.2**（§4.8 四行重算：TAGE/L1D/L2/合计，禁 `≈`） | 设计 AI | `doc/spec/00-*.md` | 四行给显式加式且 `width_check` 认；`≈` 归零 | 单片（算术为主） | Q-003 就绪 | 待办 |
| D-10 | **Q-016 第 3 批余项**（ISS-024 已决；余 026 见 D-2） | 决策 AI | `doc/00` 决策列 | 全批决策列回填 | — | — | 进行中 |
| H-1 | **【待人】push 认证**（CLI 免交互） | R0 | — | CLI `push` 成功一次 | 三选项已呈 | 人 | 【待人】 |
| H-2 | **【待人】ISS-002 工具链安装**（挡 M0/M1/M6/M8） | R0 | `doc/环境搭建.md` §7 M0 验收表 | 7/7 通过 | 按 `doc/环境搭建.md` | 人 | 【待人】 |
| H-3 | **【待人】ISS-023 / ISS-029**（放宽通道 / 判定档位） | R0 | `doc/00` | 两行状态变更 | — | 人 | 【待人】 |

**重排记录**

| 日期 | 触发 | 变更摘要 | 留痕 |
|---|---|---|---|
| 2026-09-23 | 首版（R8 依计划轮 W-1~W-10 与 §12.2 计划引领制转录） | 建 D-1~D-10 + H-1~H-3；与 `doc/04` §② 计划快照、队列 Q-017~Q-028 对齐 | `doc/05` R-030 |

> **与既有产物关系**：`doc/04` §② 计划快照＝只读快照；队列＝执行记录；**本表＝唯一权威顺序**。三者冲突时以本表为准并回填另两处。
