# doc/当前目标卡（流程 A 的人类可读镜像）

> **权威源是机器件**：`state/flow.json` + `python script/flow.py next`。本卡是它的易读镜像，
> 两者不一致时以 `flow.py next` 为准；本卡在里程碑切换时手工刷新（≤30 行）。
> 流程定义：`doc/process/03-自动化流程重构-三方案.md`（A 为骨架 / B 判据按模块增量 / C 里程碑边界启用）。

## 当前里程碑

**M3 覆盖率与回归收敛** — **已达成（2026-09-26 心跳第十六轮；`flow.py check` 末行 PASS，记账 R-272）**。
三条 exit 全过（口径＝**只核证据＋时效**、不重跑重活；阈值取 `AGENTS.md` §4 原文，未达标如实 FAIL、不放宽）。

| exit 判据 | 结论与证据（2026-09-26 心跳第十六轮刷新） |
|---|---|
| `m3-code-cov` | ✅ **PASS**：Statements **98.56%**（1025/1040）／Branches **100.00%**（447/447；原 447/463 扣列缺豁免 16）／Conditions **100.00%**（71/71；原 71/79 扣豁免 8）／FSM Transitions **100.00%**（6/6；原 6/8 扣豁免 2）；证据 `doc/verify/07-m3cov_r5.txt`（batch=m3cov_r5，rtl.f md5=10f20a51 == 现盘，54/54 用例 PASS）。**本轮新增豁免机制**（R0 授权，判据本体 `script/flow.py:chk_m3_code_cov` ＋ `doc/verify/02` §7.2 机器可读豁免集）：只扣清单内、依据可查的条目，并用报告现值设**反向上界**（报告没报缺的项豁免不了）；Statements 的 15 处未命中**不在**清单内 ⇒ 照旧计入分母 |
| `m3-func-cov` | ✅ **PASS**：TOTAL 97.61%（工具自报行）／逐 CG＝`cg_fetch` **8/8**／`cg_lsu_fsm` **12/12**／`cg_retire` **34/34**（原 34/35，扣列缺豁免 1＝`priv_u`，`doc/verify/02` §6 NU-1）；豁免只对「清单候选 ∩ 报告当前 missing bin」生效 |
| `m3-regress-stab` | ✅ **PASS**：本版本同配置 r5/r6/r7 三轮全过、轮间 0 新 FAIL、R9/R10 在环（各 total=50 pass=50 fail=0）；证据时效口径＝**只锚 RTL 面**（TB 面不进版本校验） |

- **下一里程碑＝M-B**（旧编号体系的 M3「平台 smoke」⇒ **人的决策点／基线宣布**，`doc/verify/04` §①）。
- 第三条 M3 exit（`m3-mutation-audit`／错误注入保真度抽检）**机制尚无落点** ⇒ 保留待注册（TODO(M3-3)）。

## 本批（心跳第十六轮，R-273）：R0 两裁定落地

1. **① 列缺豁免机制（改判据本体，已获授权）**：两条 M3 判据新增「解析 `doc/verify/02` 列缺清单 →
   从分母扣除 → 消息逐条回显（含清单位置行号与依据／归属）」通道；四条硬边界写进判据 docstring 与消息
   （仅限清单内且依据可查／不改 bin·采样条件·阈值·检查器／**反向上界**防凑达标／**豁免集为空时与落地前
   逐字一致**，而「清单本体解析不到」一律 FAIL）。自证伪：4 种造假注入（超上界／回指「已纳入」行／凭空行号／
   对全命中单元灌豁免）**全被拦**。Conditions「差的 1 项」按指示先试对账／激励：8 项逐项源码复核
   （`doc/verify/02` §7.3）全为结构性／无产生者／死路径／无中断源且非 ISA 行为面 ⇒ **啃不掉**，
   留在豁免集内并写明。落点＝`doc/verify/02` §7.2／§7.3。
2. **② ISS-129 口径（R0 裁定②）**：先试「原版 Spike 不改源码」⇒ 实测跑不了项目 ELF（两条 rc=255 留证
   `sim/run_iss129_upstream/`）⇒ 补丁固化 `iss/tools/patches/spike-vr1-platform-map.patch`（两处常量＋应用说明）
   ＋版本指纹 `iss/tools/spike_oracles.json`（`iss/tools/spike_run.py` 每次核 md5、不符即拒跑）；
   口径**明写「改过的 oracle，非原版」**并约定「两侧不一致先核补丁本身」；落点＝`doc/verify/02` §8。
   口径下重跑：ISS-vs-ISS 五用例 **0 mismatch**（41/470/237/115/435 条；`sim/run_iss_vs_iss/run.log`）。

## 已达成（回归复核仍常跑）

`M0.5` 流水线自证 + 参数单源（2026-09-25 15:15）｜`M1a` 接口类型单源（15:24）｜
`M1-I` G1-I 机判面（22:47，11 条 exit）｜`M1` RTL-vs-ISS 首条闭环（2026-09-26 02:24；
`rtl-compile`＋`rtl-vs-iss` 全过，步骤 P7 已由 `flow.py run` 收口置 done）｜
**`M2` rv64ui 定向子集闭环（2026-09-26 08:43；第 12 轮判定面扩到 52/52 全过**，
`m2-regress`／`m2-cov` 随激励面变更复验仍 PASS）。

## 阻塞与需人项

1. `[P5]` **需人**：**G1-F 全量冻结**待材料（G1-I 清单 §6.1 段 3：`spec/00` 过 RR；`04~13` 余项
   T-xx 清零或列缺带归属；`spec/10` T-10-1 CSR 全表闭合）。
2. ModelSim license 的 OS 侧 rehost（ISS-104 残余，可选——会话内注入已够用）。
3. （**已闭合**，2026-09-26 心跳第 13 轮）NU-2 通道缺口：R0 裁定①落地＝受控复位注入通道（见上），
   两 bin 实测命中。
4. （**已闭合**，2026-09-26 心跳第十六轮）**(a) 的 Conditions／FSM 两项＝口径裁定项**：R0 已**授权**
   「列缺豁免」机制（判据本体改动）⇒ 两项不再挂人工决策；机制与四条硬边界见 `doc/verify/02` §7.2
   （机器可读豁免集）、Conditions 逐项不可达对账见 §7.3；FSM 2 弧（CC-12＝vcover 不计异步复位弧，
   功能面 12/12 已命中）同在该豁免集内。
5. 观察项（**已闭合**）：`window` 基线件数不一致的病灶＝08:29 折入的快照缺件 ＋ `snapshot_files()`
   的 `os.walk(...)` 默认 `onerror=None` 静默吞错（`doc/process/ledger.json` R-264 查清）；加固已由
   心跳第 11 轮落地（R-266：`onerror` 记账、有枚举错即 fail-closed、`--take` 件数下限校验＋`--force`）。
