# VR1 指令集与逐指令行为（doc/spec/02）

| 版本 | 日期 | 状态 | 变更摘要 |
|---|---|---|---|
| v0.1 | 2026-09-23 | 草稿（**骨架·首片**；未评审、未定稿） | 首片：篇首三段式声明；`spec/01` §3 反查的枚举值域登记（20 条）＋ `isop_t` 三条编码约束；首批 3 类逐指令条目（x0 类 / RVC（Zca）展开 / CSR 类）；不变式 I2-1~I2-10；【权】锚点台账 25 条；待补清单 17 条 |

> **本篇声明（三段式，照 `doc/spec/00` 惯例）**
> **① 范围**：一期 ISA ＝ `rv64imac_zicsr_zifencei`（`spec/00` §2 权威表）。本篇定义**指令级语义、μop 分解与分类枚举值域**及逐指令行为；F/D 属二期（`spec/00` §2/§7.5；一期任何 FP 指令报 illegal-instruction）。**本片只完成骨架与首批 3 类条目，不构成全篇定稿**；未覆盖的指令类逐条列于 §6。
> **② 边界**：流水线/寄存器接口的 struct、位宽、反压与恢复点以 `spec/01` 为准——本篇引用其 §2/§3.x，只读不改位宽；CSR 地址表、复位值、位段、WARL、特权与 trap 入口归 `spec/10`——本篇只定义"指令看到的行为"；取指 walk 细则与 `xw_carry` 生命周期归 `spec/03`（`spec/01` §3.16 已授权转派）。模块归属：M04 `vr1_cexpand`、M05 `vr1_decode` 属本篇（`spec/00` §6 索引表）。
> **③ 值域权威源**：`isop_t[9:0]` 及本篇 §3 登记的分类枚举（`type[6:0]` 位序、`dst_kind`、`imm_kind`、`src_kind`、`is_w`、`csr_wr_type`、`op`、`wdata_sel`）**以本篇为唯一权威**（`spec/01` §2 已声明"完整枚举表只在 `spec/02` §3 定义"）；数值参数仍以 `spec/00` §4 为唯一权威，本篇不另赋值。
>
> **【差异登记·待裁，未自行选择解释】** 本片派发词写"RV64GC 一期：RV64I/M/A/F/D/C + Zicsr/Zifencei"；`spec/00` §2 权威表写一期＝`rv64imac_zicsr_zifencei`、"二期 +f+d"。两处对"F/D 是否进一期"冲突。按"`spec/00` 为权威、遇冲突停在冲突点"的纪律：**本篇正文一律按 `spec/00` §2 口径编写**（F/D 一期报 illegal）；差异请领队裁定后登 `doc/00`（设计角色的写边界只有 `doc/spec/`，无法自登台账）。

---

## 1. 指令子集与分期（前提）

| 扩展 | misa 表示 | 一期 | 依据 |
|---|---|---|---|
| I（RV64I 基线） | `misa.I` | 进 | `spec/00` §2 |
| M | `misa.M` | 进 | 同上 |
| A | `misa.A` | 进（Zama16 对齐原子保证） | `spec/00` §2/§8 |
| C | `misa.C` | 进 | 同上 |
| Zicsr | 无 misa 位（待核，见 T-12） | 进（6 条 CSR 指令，AN-03） | 行 1952、行 4986 |
| Zifencei | 无 misa 位（待核，见 T-12） | 进（FENCE.I） | 行 4884 |
| Zicntr（CY/IR/TM 子集） | 无 misa 位（同 T-12） | 进（附带） | `spec/00` §2 |
| F / D | `misa.F` / `misa.D` | 不进（二期） | `spec/00` §2/§7.5；一期报 illegal |
| G 缩写 | = IMAFD_Zicsr_Zifencei | 二期才完整 | 行 36709–36710 |

**本篇对 F/D 的引用口径**：`dst_kind=10`（FPR）一期**禁止产生**（`spec/00` §7.2）；FP 指令的 illegal 提升路径与 CSR 侧 `mstatus.FS` 归 `spec/10`（T-13）。

---

## 2. 指令格式与展开口径（骨架）

### 2.1 基本格式与 `imm_kind` 归属（骨架口径，随逐指令表定稿复核，见 T-6）

| 格式 | 字段骨架 | 一期出现类 | `imm_kind` 归属（本篇口径） |
|---|---|---|---|
| R | funct7/rs2/rs1/rd | ALU 寄存器型、MUL/DIV | 不适用（无立即数） |
| I | imm[11:0]/rs1/rd | ALU 立即数、载入、JALR、移位（shamt）、CSR（uimm） | 01（ALU）/ 00（载入、JALR 偏移）/ 11（shamt）/ 10（CSR uimm） |
| S | imm/rs2/rs1 | 存储 | 00 |
| B | imm/rs2/rs1 | 条件分支 | 00 |
| U | imm[31:12]/rd | LUI、AUIPC | 01 |
| J | imm/rd | JAL | 00 |

> 六个基本格式的行级锚点本片未取（骨架）；`imm_kind` 取值域出自 `spec/01` §3.4，映射列的"哪个指令用哪个值"属 T-6。

### 2.2 C 族格式与展开级

- C 族格式（实读佐证）：CI（行 12427、12580、12834）、CR（行 12378）、CB（行 12418、12623）、CIW（行 12540）、CJ（行 12344）、CA（行 12755）；其余（CSS/CL/CS 等）待补。
- **展开级**：C→32 bit 展开在 M04 `vr1_cexpand` 完成（`spec/00` §4.3 `C_EXPAND_STAGES=1`、§6 M04）；展开后为 32 bit 流进 IBUF 出口。
- **长度判定的唯一位置**：16/32 bit 判定只在 P5 walk（`spec/01` §3.16：槽半字低 2 bit ≠`11`→16 bit，下一起始＝当前+1；=`11`→32 bit，下一起始＝当前+2；起始可落任意槽号；槽 15 起始 32 bit 走 `xw_carry` 拼接，ADR-1）。**M04 不重判长度**，只消费 `fetch_raw_t.is_c`（`spec/01` §3.3）。
- `is_c` 生命周期（五级）：`fetch_raw_t.is_c`（§3.3）→ `uop_t.is_c`（§3.4）→ `rob.type[0]`（§3.6/§3.7）→ `excp_t.is_c`（§3.18）→ `rt_t.is_c`（§3.21）。

---

## 3. 枚举与值域登记表（本篇为权威源）

### 3.1 `type[6:0]` — μop 分类位（位序照 `spec/01` §3.6 定死，与 §3.7 直连）

| 位 | 符号 | 值域 | 含义（本篇） | `spec/01` 生产/消费点 | 本篇状态 |
|---|---|---|---|---|---|
| bit0 | `is_c` | {0,1} | 来源指令为 16 bit 压缩形式（展开后仍标记） | 生产 §3.3/§3.4；消费 §3.18、§3.21 | 已登记；逐指令绑定（T-5） |
| bit1 | `is_br` | {0,1} | 控制流分支/跳转类 | 生产 §3.5/§3.6；消费 §5.2 checkpoint | 已登记；逐指令绑定（T-5） |
| bit2 | `is_comp` | {0,1} | "C 展开的第二半"（§3.6 原文；细化见 T-4） | 生产 §3.6 | 已登记；语义细化（T-4） |
| bit3 | `is_wfi` | {0,1} | WFI 指令标记（语义见 §3.17） | 生产 §3.6 | 已登记；逐指令绑定（T-5、T-16） |
| bit4 | `is_fence` | {0,1} | FENCE/FENCE.I 标记 | 生产 §3.6 | 已登记；逐指令绑定（T-5、T-16） |
| bit5 | `is_mem` | {0,1} | 访存类（LD/ST/AMO；BP3 按此门控，§4） | 生产 §3.6 | 已登记；逐指令绑定（T-5） |
| bit6 | `is_csr` | {0,1} | CSR 类；提交级 CSR 判据（§3.17 `csr_vld`） | 生产 §3.6；消费 §3.17/§3.19 | 已登记；逐指令绑定（T-5） |

### 3.2 `dst_kind[1:0]`（`spec/01` §3.4；与 `spec/00` §7.2 一致）

| 值 | 含义 | 一期 |
|---|---|---|
| 00 | 无目的（含 rd=x0、store、无链接分支） | 用（ISSUE 见 §4.1） |
| 01 | GPR 目的 | 用 |
| 10 | FPR 目的 | **禁止产生**（二期） |
| 11 | CSR 旧值→GPR | 用（CSR 类，§4.3） |

### 3.3 `imm_kind[1:0]`（`spec/01` §3.4）

| 值 | 含义 | 一期出现类 |
|---|---|---|
| 00 | 地址偏移 | 载入/存储/分支/JAL/JALR |
| 01 | ALU 立即数 | 算术/逻辑立即数、LUI/AUIPC |
| 10 | CSR uimm[4:0] | CSR 立即数型（§4.3） |
| 11 | shamt[5:0] | 移位立即数 |

### 3.4 `src_kind`（`spec/01` §3.10 `wb_t`，1 bit）

| 值 | 含义 | 生产点 |
|---|---|---|
| 0 | 执行结果 | 各 EU 写回 |
| 1 | CSR 旧值 | E 级投机读（门控＝`csrq` 队头，§3.10 N-02/I-01 定案） |

### 3.5 CSR 侧编码域（`spec/01` §3.11/§3.19）

| 符号 | 值域 | 含义 | 生产点 | 本篇状态 |
|---|---|---|---|---|
| `csr_wr_type[1:0]` | 2 bit | CSR 写类型（`disp_x_t`，§3.11） | §3.11 | 与 `op` 的编码空间关系**待补（T-2）** |
| `op[1:0]` | 2 bit | 00 RW / 01 RS / 10 RC / 11 读使能-only | §3.19 | 已登记（照录） |
| `wdata_sel[1:0]` | 2 bit | 选择 `rs1_val`/`imm`/`old` | §3.19 | 编码归 `spec/10`（§3.19 原文）；本片只登记（T-3） |

### 3.6 `isop_t[9:0]` — 枚举本体（`spec/01` §2 声明的唯一枚举表位置）

本片登记**编码约束 3 条**，成员编码表**待补（T-1）**：

- C-1：位宽 10 bit（`spec/01` §2：`logic [9:0]`，本篇不再赋值）。
- C-2：C 展开在 M04 完成（`spec/00` §4.3 `C_EXPAND_STAGES=1`），`isop_t` **不含 C 变体维度**——同一语义的 16/32 bit 形式共享同一 `isop`；形式标记单独由 `is_c` 承载（§2.2/§3.1）。
- C-3：`isop` 与 `type[6:0]` 不得互为冗余（同一分类信息单一载体）；`type` 位在 D 级由译码生成（位序由 `spec/01` §3.6 定）。本约束随成员表定稿复核。

> 撤销/回退点：**成员表定稿前，任何下游（`spec/04`/`spec/05`/`spec/11`）不得按臆测编码实现或断言**；`spec/01` §2 的引用点在表定稿前保持悬空状态（非缺陷，属本片边界）。

### 3.7 反查：`spec/01` §3.x 引用符号 → 本篇映射状态（缺者逐条列待补）

| `spec/01` 引用符号 | 引用点 | 本篇对应 |
|---|---|---|
| `is_c` | §3.3/§3.4/§3.6/§3.18/§3.21 | §3.1 bit0（已登记） |
| `is_br` | §3.5/§3.6/§3.21 | §3.1 bit1（已登记） |
| `is_comp` | §3.6 | §3.1 bit2；细化待补 T-4 |
| `is_wfi` | §3.6 | §3.1 bit3；绑定待补 T-5/T-16 |
| `is_fence` | §3.6 | §3.1 bit4；绑定待补 T-5/T-16 |
| `is_mem` | §3.6/§4（BP3） | §3.1 bit5；绑定待补 T-5 |
| `is_csr` | §3.6/§3.17/§3.19 | §3.1 bit6；绑定待补 T-5 |
| `dst_kind` | §3.4/§3.6/§3.7/§3.8 | §3.2（已登记） |
| `imm_kind` | §3.4/§3.8/§3.19 规则 2 | §3.3（已登记）；逐指令分派 T-6 |
| `src_kind` | §3.10 | §3.4（已登记） |
| `is_w` | §3.4/§3.8/§3.11 | 已登记（W 型 32 bit 运算标记；逐指令绑定 T-15） |
| `csr_wr_type` | §3.11 | §3.5；关系待补 T-2 |
| `isop` | §3.4/§3.8/§3.11 | §3.6（约束已登记；成员表 T-1） |
| `is_jalr_ret` | §3.12 | 接口局部属性，不并入 `isop_t`；产生规则待补 T-7 |

---

## 4. 首批逐指令条目（3 类，每类给格式样板）

### 4.1 类①：x0（硬连线零）相关

**前提**：x0 硬连线为全零（AN-01，行 1964）；`spec/00` §4.3 窗口可达性口径：融合式 PRF 之 31 项常驻架构态（x1~x31）⇒ 投机在途含目的 μop ≤ `64−31 = 33`。

| 条目（样板） | misa 扩展位 | μop 分解（数＋类型） | 目的/源操作数规则 | 异常条件 | 与 ISS 的对应点 |
|---|---|---|---|---|---|
| JAL/JALR 且 rd=x0（伪指令 `j`/`jr`） | I | 1 个；分支类（`type.bit1`） | rd=x0 ⇒ `dst_kind=00`：不分配 PRF 目的、不消耗 free-list、不进映射栈（`spec/01` §5.3 仅 `dst_kind≠00` 弹栈，A14）；源＝pc+imm 或 rs1+imm | 目标非规范地址 → instruction access fault（`spec/01` §3.12 `bad_va`；异常码表归 `spec/10`） | trace 无 rd 写；`rt_t.is_br=1` |
| 载入 rd=x0 | I | 1 个；访存类（`type.bit5`） | 结果丢弃（AN-01）；**访存照常发起**；"rd=x0 是否豁免异常/副作用"本片未取证，不预设（T-8） | 地址非对齐（`MISALIGNED_EN=0` 报 fault）、page/access fault | trace 记该 load 退休、无 rd 写 |
| ALU/移位/乘法 rd=x0 | I/M | 1 个；`dst_kind=00` | 同上；源依赖照常建立（分发读口照常参与） | 无（除零不异常） | 无 rd 写 |
| CSRRS/CSRRC 且 rs1=x0 | Zicsr | 1 个；`type.bit6`，`dst_kind=11` | **不写 CSR**（AN-07）：无写副作用、不因只读 CSR 报 illegal；rd≠x0 时读旧值写 rd | 读侧照常（AN-07：恒读） | `csr_old` 有值、`csr_new = old`（§3.21 合成） |
| CSRRW 且 rs1=x0 | Zicsr | 1 个；同上 | **写 0 到目标 CSR**（AN-08，与 CSRRS/CSRRC 相反）；rd=x0 时同时"不读"（AN-06） | 只读 CSR → illegal（AN-11） | `csr_new` 按 wdata=0 合成（可写位清 0、只读位不变） |

**不变式**
- **I2-1**：任何 `rd=x0` 的 μop ⇒ `dst_kind=00`；不分配 PRF 目的、不消耗 free-list、回滚不归还（`rd_is_new=0`）。依据：AN-01 ＋ `spec/01` §5.3/A14。
- **I2-2**：x0 作源的读值恒 0；31 项常驻架构态口径（x1~x31）与在途上界 33 引用 `spec/00` §4.3，本篇不赋值。
- **I2-3**：x0 目的不产生任何架构状态变化与 trace 写字段（`rt_t` 无 x0 写，§3.21）。

### 4.2 类②：RVC（Zca）展开

**前提**：长度判定唯一在 P5（§2.2/I2-x），M04 只展开不重判；展开后进 `uop_t` 的 `isop` 与 32 bit 形式相同（§3.6 C-2）。

| 条目（样板） | misa 扩展位 | μop 分解（数＋类型；展开形式） | 目的/源操作数规则（含保留点/HINT） | 异常条件 | 与 ISS 的对应点 |
|---|---|---|---|---|---|
| C.NOP | C | 1；展开 `addi x0,x0,0`（=NOP） | 不改任何用户可见状态（AN-21）；imm≠0 = HINT | 无 | `is_c=1`；pc 递增 2 |
| C.ADDI | C | 1；展开 `addi rd,rd,imm` | rd=x0 ⇒ C.NOP；rd=x0 且 imm≠0 = HINT；rd≠0 且 imm=0 = HINT（AN-17） | 无 | 同上 |
| C.LI | C | 1；展开 `addi rd,x0,imm` | rd=x0 = HINT（AN-16） | 无 | 同上 |
| C.LUI | C | 1；展开 `lui rd,imm` | 仅 rd≠x2 且 imm≠0 合法；imm=0 保留；rd=x2 且 imm≠0 ⇒ C.ADDI16SP；rd=x0 且 imm≠0 = HINT（AN-16、AN-23） | 保留编码点 → illegal（化归 T-9） | 同上 |
| C.ADDI16SP | C | 1；展开 `addi x2,x2,nzimm[9:4]` | 目的固定 x2；nzimm=0 保留（AN-17） | 保留点 → illegal（T-9） | 同上 |
| C.ADDI4SPN | C | 1；展开 `addi rd′,x2,nzuimm[9:2]` | 立即数零扩展、非零、按 4 缩放；nzuimm=0 保留（AN-18） | 保留点 → illegal（T-9） | 同上 |
| C.ADDIW（RV64 专有） | C | 1；展开 `addiw rd,rd,imm` | 仅 rd≠x0 有效；rd=x0 保留（AN-17、AN-23） | 保留点 → illegal（T-9） | 同上 |
| C.J | C | 1；展开 `jal x0,offset`（±2 KiB） | 无链接写（rd=x0，走 §4.1 规则） | 无 | `is_c=1`、`is_br=1`；pc 递增 2 |
| C.JAL（RV32 专有） | C | （RV64 不实现） | RV64 同位编码点 = C.ADDIW（AN-13、AN-23）；RV32 语义：link=pc+2 写 x1（AN-12） | — | 不出现（RV64） |
| C.JR / C.JALR | C | 1；展开 `jalr x0,0(rs1)` / `jalr x1,0(rs1)` | C.JR 仅 rs1≠x0（rs1=x0 保留）；C.JALR 的 rs1=x0 编码点 = C.EBREAK（AN-14、AN-22）；link=pc+2 | 保留点 → illegal（T-9） | `is_jalr_ret` 绑定待补（T-7） |
| 全零 16 bit（0x0000） | C | — | 规范永久保留为非法指令（AN-20；全 1 同理） | illegal（T-9） | trace 侧须有异常条目 |

**不变式**
- **I2-4**：16/32 bit 判定**唯一**在 P5 walk（`spec/01` §3.16）；M04 展开级不得重判（否则双判定点会产生"同一字节流两种切分"的假绿）。
- **I2-5**：C 展开为 **1:1**（一个 C 指令产出一个 μop）——`UOP_MAX_SPLIT=2` 仅 DIV 与 AMO（`spec/00` §4.3）；C 类不拆包。
- **I2-6**：`is_c` 五级链（§2.2）逐级传递，任何一级丢失都会使 trace/ISS 的 pc 递增与异常现场口径分叉。
- **I2-7**：C.JALR 的 link 值（pc+2，AN-15）与 base 指令 JALR（pc+4）不同——RTL 与 ISS 两侧必须同口径，落实方式见 T-10（本片不定实现）。

### 4.3 类③：CSR（Zicsr）

**前提**：6 条 CSR 指令（AN-03）；读写条件三要素（rd、rs1、指令）见规范 Table 21（AN-10）；uimm 为 5 bit 无符号、零扩展（AN-09）。

| 条目（样板） | misa 扩展位 | μop 分解（数＋类型） | 目的/源操作数规则 | 异常条件 | 与 ISS 的对应点 |
|---|---|---|---|---|---|
| CSRRW | Zicsr | 1 个；`type.bit6`，`dst_kind=11`，寄存器型 | rd=x0：不读、无读副作用（AN-06）；rs1=x0：仍写 0（AN-08）；否则 rd←old、CSR←rs1 | 写只读 CSR → illegal-instruction（AN-11） | `csr_old`=old；`csr_new` 按 `op(old,src_val)` 合成（§3.21） |
| CSRRS / CSRRC | Zicsr | 1 个；同上 | rs1=x0：不写（且不因只读报 illegal）；恒读并置位/清位（AN-07）；rd 仅决定是否写回 GPR | rs1≠x0 且写只读 CSR → illegal（AN-11） | `csr_new` ＝ old 按位或 mask，或 old 按位与 ~mask |
| CSRRWI | Zicsr | 1 个；`imm_kind=10` | uimm 零扩展后写（AN-09）；rd=x0：不读（AN-09） | 写只读 CSR → illegal（AN-11） | `csr_new = zext(uimm)` |
| CSRRSI / CSRRCI | Zicsr | 1 个；`imm_kind=10` | uimm=0：不写、不因只读报 illegal；恒读（AN-09） | 写只读 CSR（uimm≠0）→ illegal（AN-11） | `csr_new` ＝ old 按位或 zext(uimm)，或 old 按位与 ~zext(uimm) |
| （类级）读写条件 | Zicsr | — | 三条件表（rd/rs1/指令）＝规范 Table 21（AN-10） | CSR 地址不存在（一期仅实现 `CSR_TRAP_SET`）→ 行为归 `spec/10`（T-9） | ISS 按同表实现，逐条 trace 比对 |

**不变式**
- **I2-8**：CSR 类为**单 μop、不拆包**（`spec/00` §4.3 `UOP_MAX_SPLIT` 仅 DIV/AMO）。
- **I2-9**：`src_val` 解析责任在 `R` 级——寄存器型取 `rs1_val`；立即数型取 `imm[4:0]` 零扩展（AN-09；`spec/01` §3.19 规则 2 同口径）。
- **I2-10**：CSR 写只经 `csr_wr_t`（`spec/01` §3.17/§3.19）；同包多 CSR 只提交最老一条（`spec/01` §3.19 规则 3）；写队列命中/丢弃语义（§3.19 规则 4）属 `spec/01`，本篇不复述。

---

## 5. 【权】锚点台账（引用纪律）

引用件（一级依据）：`D:\IC验证知识库\_tools\extracted\riscv_spec\riscv_spec_full.txt`；
"行"＝该文件行号；复现＝对同文件 grep 下表关键词。未回读上下文者标"待核"。

| 编号 | 锚点结论 | 出处（§＋行） | 复现关键词 |
|---|---|---|---|
| AN-01 | x0 硬连线为全零 | 行 1964 | `hardwired` |
| AN-02 | G ＝ IMAFD_Zicsr_Zifencei | 行 36709–36710 | `IMAFDZicsr_Zifencei` |
| AN-03 | Zicsr 含 6 条 CSR 指令 | 行 1952 | `6 CSR instructions` |
| AN-04 | Zifencei 章 §4.1 | 行 4884（页眉 4922/4982） | `4.1. Zifencei` |
| AN-05 | Zicsr 章 §4.2 | 行 4986（页眉 5074） | `4.2. Zicsr` |
| AN-06 | CSRRW：rd=x0 不读、无读副作用 | 行 5049–5052 | `shall not read` |
| AN-07 | CSRRS/CSRRC：rs1=x0 不写、不报只读 illegal；恒读 | 行 5067–5069 | `will not write to the CSR` |
| AN-08 | CSRRW 且 rs1=x0：仍尝试写 0 | 行 5073 | `write zero` |
| AN-09 | 立即数型零扩展 5 bit uimm；RSI/RCI 的 uimm=0 不写 | 行 5078–5085 | `zero-extending a 5-bit unsigned` |
| AN-10 | CSR 读写条件三要素表（Table 21） | 行 5086–5094 | `Table 21` |
| AN-11 | 写只读 CSR 地址 → illegal（【权】卷 II） | 行 42980–42981 | `read-only CSR address raise` |
| AN-12 | C.J 展开 JAL x0（±2 KiB）；C.JAL 写 x1（pc+2） | 行 12345–12349 | `expands to JAL x0` |
| AN-13 | C.JAL 仅 XLEN=32 | 行 12347–12349 | `XLEN=32-only` |
| AN-14 | C.JR→JALR x0,0(rs1)（rs1≠x0）；C.JALR→JALR x1；rs1=x0⇒C.EBREAK | 行 12379–12383 | `expands to JALR x0, 0` |
| AN-15 | C.JALR link 偏移 2（≠4）的规范注意条款 | 行 12385–12387 | `2 rather than 4` |
| AN-16 | C.LI（rd=x0 HINT）；C.LUI 合法域与 C.ADDI16SP 对应 | 行 12456–12468 | `C.ADDI16SP` |
| AN-17 | C.ADDI（rd=x0⇒C.NOP）；C.ADDIW 仅 rd≠x0；C.ADDI16SP nzimm≠0 | 行 12506–12517 | `C.ADDIW is valid only` |
| AN-18 | C.ADDI4SPN nzuimm≠0；C.SLLI shamt=0 或 rd=x0 为 HINT | 行 12540–12583 | `C.ADDI4SPN is valid only` |
| AN-19 | C.MV/C.ADD 展开与保留点（rs2=x0 对应 C.JR / C.JALR、C.EBREAK） | 行 12690–12700 | `C.MV copies` |
| AN-20 | 全零 16 bit 永久保留为非法（全 1 同理） | 行 12798（12800–12804） | `permanently reserved` |
| AN-21 | C.NOP 展开 NOP；imm≠0 为 HINT | 行 12834–12836 | `C.NOP expands to NOP` |
| AN-22 | C.EBREAK 展开 EBREAK | 行 12853–12855 | `C.EBREAK shares` |
| AN-23 | RV64/RV32 同位编码表区（C.JAL(RV32) / C.ADDIW(RV64, rd=0 保留) / C.LUI 保留与 HINT） | 行 13109–13143 | `C.JAL(RV32)` |
| AN-24 | 压缩指令章号 §7.1（Zca） | 行 12350、12462 | `7.1. Zca Extension` |
| AN-25 | 「Zicsr, Zifencei 被排除（excluded）」字样——**上下文未回读，仅登记不作结论** | 行 32874 | `excluded` |

---

## 6. 本篇待补清单（本片未覆盖者）

| # | 待补项 | 归属/出口 |
|---|---|---|
| T-1 | `isop_t` 全量成员编码表（现仅 3 条约束；覆盖 RV64I/M/A/C 展开/CSR/系统后一次定稿） | 本篇后续片；`spec/01` §2 引用点等待 |
| T-2 | `csr_wr_type`（`disp_x_t`）与 `csr_wr_t.op` 的编码空间关系（同编码/直连/映射表） | 本篇＋`spec/10` |
| T-3 | `wdata_sel` 编码（§3.19 已声明归 `spec/10`） | `spec/10` |
| T-4 | `is_comp` 语义细化（"C 展开的第二半"具体构型；与 `UOP_MAX_SPLIT` 关系） | 本篇＋`spec/01` 复核 |
| T-5 | `type[6:0]` 七位的逐指令绑定表 | 本篇后续片 |
| T-6 | `imm_kind` 逐指令分派全表 | 本篇后续片 |
| T-7 | `is_jalr_ret` 产生规则（RAS 更新归 `spec/03`） | 本篇＋`spec/03` |
| T-8 | 载入 rd=x0 是否豁免异常/副作用——规范未取证，不预设 | 本篇后续片（回锚后） |
| T-9 | 保留编码点（RES）与非法 CSR 地址的非法化路径、异常码与路由 | `spec/10` |
| T-10 | C.JALR link 值（pc+2）在 RTL/ISS 两侧的落实口径 | 本篇＋`iss/` 对比口径 |
| T-11 | 提取件版本次元：Zca（§7.1）命名与一期 `C` 的映射；`spec/01` §3.19 引【用】§2.8 与本机 §4.2 的 §号差异 | 待核（登记不裁定） |
| T-12 | misa 位段（含 Zicsr/Zifencei/Zicntr 的 misa 表示；行 32874 上下文未回读） | `spec/10` |
| T-13 | F/D 一期报 illegal 的路径与 `mstatus.FS` WARL | `spec/10`（本期兼容口径见 §1 注） |
| T-14 | A 类（LR/SC/AMO）μop 分解与 `amo_op` 值域（11 值，`spec/01` §3.13） | 本篇第二批 |
| T-15 | M 类（MUL/DIV）拆分口径（`UOP_MAX_SPLIT=2`）与 `is_w` 交互 | 本篇第二批 |
| T-16 | 特权/系统指令（ECALL/EBREAK/MRET/SRET/WFI/SFENCE.VMA/FENCE.I）逐指令条目 | 本篇后续片 |
| T-17 | RV64I 基础全量逐指令表（首片只 3 类样板） | 本篇后续片（优先） |

## 7. 下游义务（应回引本篇的篇目）

| 篇目 | 回引点 |
|---|---|
| `spec/01` | §2 的 `isop_t` 枚举表位置（T-1 定稿前该域不可冻结）；§3.6 `type` 位序与本篇 §3.1 双向核对 |
| `spec/03` | §2.2/I2-4 的 walk 对齐声明；T-7 `is_jalr_ret` |
| `spec/04` | §4.1 I2-2 引用的 31 常驻态与在途上界 33（`spec/00` §4.3） |
| `spec/05` | F2 装载路径（与 C 展开/`xw_carry` 相关的 mux 时序） |
| `spec/10` | §1 表 F/D 行、§4.3 全类的"归 `spec/10`"条目（T-2/T-3/T-9/T-11/T-12/T-13） |
| `spec/11` | 断言汇总时不新增本篇未定语义（A17 窗口 `K` 等引 `spec/04`） |
| `doc/02` | 候选验证点输入：x0 不写/不占资源（I2-1）、CSR rs1=x0 不写与 CSRRW rs1=x0 写 0（AN-07/08）、C.JALR link=pc+2（I2-7）、RES 编码点非法（T-9）、`is_c` 五级链（I2-6） |

## 8. 出口自检表（本片，每项附复核手段）

| # | 检查项 | 复核手段（本轮实做/待做） |
|---|---|---|
| 1 | 全部【权】锚点行号可复现 | §5 的 25 条每条给 grep 关键词；本轮实做：对提取件逐条 grep/Read 命中（AN-25 除上下文外） |
| 2 | 枚举值域与 `spec/01` 逐字一致 | 本片抄录自对 `spec/01` §3.4/§3.6/§3.10/§3.11/§3.19 的实读行；复核：`grep -n "dst_kind\|imm_kind\|src_kind\|csr_wr_type" doc/spec/01-流水线与寄存器接口.md` |
| 3 | 本篇未引入任何数值/位宽（不越权赋值） | 全篇可算量均为引用（10 bit/64/33 等带 `spec/00`/`spec/01` 节号）；复核：逐条核对引用节号 |
| 4 | 表格完整性（markdown 行列一致） | `python script/gate.py check`（`md-integrity` 判据）；本轮实跑输出见 `.qoder/handoff/HANDOFF.md` Q-010 记录 |
| 5 | 与 `spec/00` §4 参数名一致 | 本篇仅出现 `XLEN`、`C_EXPAND_STAGES`、`UOP_MAX_SPLIT`、`MISALIGNED_EN` 四个参数名；逐一在 `spec/00` §2/§4 核对（本轮实做） |
| 6 | 差异登记已显式（F/D 口径、T-11 两处 §号） | 篇首差异块＋T-11；未自裁（停在冲突点，未选解释） |
