# VR1 前端规格：分支预测与取指（doc/spec/03）

| 版本 | 日期 | 状态 | 变更 |
|---|---|---|---|
| v0.1 | 2026-09-23 | 草稿（骨架·首片；未评审、未定稿） | 首片：篇首声明块；§1 FTQ 四规则成文（Q-006 `done_when`）；§2 预测器结构参数引用＋§2.3 ISS-039 块布局收口条款（B-1 至 B-4、I3-3 至 I3-5）＋§2.4 与 `spec/01` §3.16 逐句对账 12 行；§3 取指消费侧（`valid` 且非 `err`＋`err_pos` 截断）与 IBUF 满停工语义；§4 待补 8 条；§5 下游义务；§6 ISS-039/ISS-045 闭合声明；§7 自检表 8 项 |
| v0.1 续 | 2026-09-23 | 草稿（首片＋收口片；未评审、未定稿） | 收口片（D-14 章节片② 的 BLK-2~5 与 CL-2/6/7）：B-3 限域为"两窗内取全路径"跨度上界（>66 B 块归 T3-1/`spec/04`）；槽 15 几何更正（属 W+32 窗、地址 W+62、末字节 W+65）；I3-3 算式落点改 §7-9（新增行）；F1/F4 `older()` 改 FTQ 参数化式 `older_5`（mod 32/半量 16）；F2「起始槽」集合澄清＝块内各指令起始槽；§6.1③ 改述（`spec/01` 已为「定案 A」） |

> **① 范围**：前端＝分支预测（`P0`~`P2`，BP1~BP3）＋ FTQ ＋ 取指（`P3`~`P5`，F0~F2）＋ IBUF 的**机制与时序**。模块清单与接口冻结对象见 `spec/00` §6；本篇不重述模块目录与 struct 位域。
> **② 与 `spec/01` 的边界**：`spec/01` 定**接口与级号**（级号表、struct 位宽、端口、反压/恢复点的接口面）；本篇定**机制与时序**（FTQ 规则、预测器结构、块布局、取指遍历与停工语义）。位宽与端口一律引用不重述；凡机制需要改接口/位宽的，一律停下登记并退回领队（G1 前走接口升版回写 `spec/01`），本篇不自行裁定。
> **③ 值域权威源**：参数值唯一源＝`spec/00` §4（本篇只引用、不另赋值）；指令与枚举值域＝`spec/02`；规范锚点＝本机提取件 `riscv_spec_full.txt`（一级）与参考核（cv32e40p / ibex）；设计空间页码＝【超】。本篇机制句的锚点类型逐条标注；仅"本篇推导"且无算式/引文锚点者标 `待锚点`（评审面）。`_tools/` 已恢复，规范类引用按一级依据直引（ISS-032 已闭）。

## 1. FTQ 与预测结构

### 1.1 生产与消费点（引用面）

| 角色 | 级 | 动作 | 出处 |
|---|---|---|---|
| 生产者 | `P2`（BP3） | 每拍至多 1 项：打包 `ftq_req_t` 写入 FTQ，置 `ftq_id` | `spec/01` §1.1 |
| 消费者 | `P3`（F0） | FTQ 队头出队；L1I TLB 查表 | `spec/01` §1.1 |
| 深度参数 | — | `FTQ_ENTRIES` = 16；约束 ≥ `BP_STAGES` ＋ `IF_STAGES` = 6（在途重定向窗口） | `spec/00` §4.2 |
| 标签宽度 | — | `ftq_id` 5 bit（4 bit 索引 ＋ 1 bit 回绕位） | `spec/01` §3.3 |
| 解耦依据 | — | 预测与取指解耦（FDIP），取指 miss 时预测器继续领先 | 【超】p.157-160 |

### 1.2 FTQ 四规则（Q-006 `done_when` 要求的成文条款）

| # | 规则 | 条款 | 依据 |
|---|---|---|---|
| F1 | 读写指针 | `wptr` / `rptr` 各 5 bit = 4 bit 索引 ＋ 1 bit 回绕位；`wptr` 仅由分配推进（每写一项 +1），`rptr` 仅由出队推进（每出队一项 +1）；出队严格队头序，禁止乱序出队；一切跨回绕的大小比较用参数化谓词 `older_W(x, y)`（语义：x 比 y 老，当且仅当 `((y − x) mod 2^W) < 2^(W−1)`）——FTQ 取 W = 5：`older_5(x, y)`（当且仅当 `((y − x) mod 32) < 16`，**mod 32 / 半量 16**；BLK-5 处置）。**显式声明**：`spec/01` §3.19 规则 1 的 `older(e, f)` 定义为 `((f − e) mod 256) < 128`，系 `rob_id` **8 bit 专用**形式，**FTQ 不直接引用该式**；两者只共用同一参数化定义、W 各取实体回绕宽度（FTQ 5 bit，本条） | `spec/01` §1.1、§3.3；`older` 参数化形式＝§3.19 规则 1 按 W 归一推广（W = 5 代入；锚点类型＝可证伪算式）；指针推进与回绕式为推导句，锚点类型＝可证伪算式（§7-3） |
| F2 | 分配与释放 | 分配＝在 `wptr` 处写入项、置有效与 `ftq_id`，随后 `wptr` +1；**出队（读）不是释放**——释放条件＝「本块末条指令字节取全」（§2.3 B-2）且「**本块内各指令的起始槽**（walk 自块首槽起产出的每条指令起始槽的集合，含经 `xw_carry` 拼接产出的指令）**均已写入 IBUF 或已按 lane 粒度作废**」；**集合澄清（CL-6）**：该集合＝**块内各指令起始槽**（通常多条；与 §3.1 C1 的 walk 产出、§3.2 的"起始槽"同义），**不是块首单槽**——只认块首会使释放先于块内其余指令的 IBUF 落位，与 B-2 的"取全"判据及 §3.2 停工语义（未写成不得丢弃）相抵；作废来源＝冲刷（F4）与 err 截断（§3.1 C1/C2）两类；未取全而需跨窗续取时该项保持"在取"状态，其上下文在 `P5` 的 `xw_carry` 中 | `spec/01` §1.1（P2/P3）、§3.16 ②、§3.3（IBUF lane 语义）；释放条件为机制句（CL-6 澄清），锚点类型＝§3.16/§3.2 引用＋语义一致性论证（评审面） |
| F3 | 满/空判定 | 空：`wptr == rptr`（全 5 bit 相等）；满：`wptr[3:0] == rptr[3:0]` 且 `wptr[4] != rptr[4]`（同索引、异回绕位，可用满 16 项）；满在**分配拍前**组合判定，满时 `P2` 停写（BP 级反压）；空时 `P3` 不出队；两谓词互斥 | `spec/00` §4.2（`FTQ_ENTRIES` = 16）；`spec/01` §3.3（回绕位 = `ftq_id` 最高位）；互斥性为可证伪算式（§7-3） |
| F4 | 冲刷交互 | 重定向/恢复携带边界 `ftq_id`（＝发起指令所属项）；边界项**之后**的所有项作废（`older_5(x, boundary)` 为假且 x 非 boundary 的项；`older_5` 见 F1），`wptr` 回退至边界 +1、被作废项有效位当拍清零；`xw_carry` 随冲刷作废（§3.16 ③）；**冲刷优先于满/空与停工**，可无条件打断 `P5` 停工态；恢复点＝重定向目标 PC 作为新分配项的块首（不对已作废项重放）；边界项自身的保留/作废口径引用 `spec/01` 的冲刷定义，本篇不另规定 | `spec/01` §3.16 ③（冲刷/重定向作废）、§7（lane 粒度口径）；边界项处置＝引用句（对账表 T-08） |

不变式：

- **I3-1**：FTQ 的 `wptr` / `rptr` 只由「分配」与「出队」两种事件推进；任何停工/冲刷/重定向不改变指针推进规则本身（冲刷按 F4 回退 `wptr`）。空与满两谓词不同真。
- **I3-2**：FTQ 项在其释放条件（F2）满足前，其 `ftq_id` 值不得被重新分配给新项。

## 2. 分支预测器

### 2.1 结构与参数引用（`P0`~`P2`）

| 级 | 结构（机制） | 引用参数（值见 `spec/00` §4.2） | 依据（同 §4.2 行） |
|---|---|---|---|
| `P0`（BP1） | L1BTB 命中路径（预测第 1 级、零气泡）；L2BTB 并行查；读 GHR/RAS 快照 | `L1BTB_ENTRIES`、`L2BTB_ENTRIES`、`GHR_BITS`、`RAS_ENTRIES` | 【超】p.140（多级 BTB）、p.141-143（RAS） |
| `P1`（BP2） | TAGE 多表并行读，最长历史命中仲裁，SC-lite 校正，ITTAGE-lite 目标选择 | `TAGE_BASE_ENTRIES`、`TAGE_TABLES`、`TAGE_HIST_LEN`、`TAGE_ENTRIES_EA`、`TAGE_TAG_W`、`TAGE_CTR_W`、`TAGE_USEFUL_W`、`SC_ENTRIES`、`SC_INPUTS`、`SC_WEIGHT_W`、`ITTAGE_TABLES`、`ITTAGE_HIST_LEN`、`ITTAGE_ENTRIES_EA`、`ITTAGE_TARGET_W`、`BP_ALT_HIST` | 【超】p.121-123、p.126-128、p.149-151 |
| `P2`（BP3） | 打包 `ftq_req_t` 写 FTQ、置 `ftq_id`；GHR/RAS 更新 | `BP_STAGES`、`IUM_EN`、`SARAS_FIX_QUEUE` | `spec/01` §1.1；【超】p.126-128（IUM）、p.141-143（SARAS） |

机制骨架（本片成文部分）：

- 方向仲裁：最长历史命中优先；无标签命中（T0 基础表）为回退层；`BP_ALT_HIST` 使能时以次长命中作替补（alt-pred）。
- 块终结：被预测 taken 的分支即块末指令；其目标作为下一条 FTQ 项的块首。
- 条件分支预测经三级对齐（`BP_STAGES` = 3），与取指三级（`IF_STAGES` = 3）解耦【超】p.157-160。

**未成文（本片不覆盖，清单见 §4）**：条目与权重阵列的完整位域、u 位分配/递减规则、`SC_INPUTS` = 24 的逐位构成（`spec/00` §4.2 转派"构成见 `spec/03`"）、RAS/SARAS 算法细则、两级 BTB 的替换与重填、预测表复位初始态。

### 2.3 块布局限制（ISS-039 收口条款）

> 本节为 `doc/process/00` ISS-039 决策列（ADR-1，选 A：`xw_carry` 尾半字保持＋次窗拼接）交办 `spec/03` 的"块布局细则"落文；与 `spec/01` §3.16 的逐句对账见 §2.4。

- **B-1 块可起任意 2B 槽**：块首指令起始地址允许落在任意 2 字节槽（无奇偶槽限制）；块首槽 = 块起始 `pc[4:1]`，`win_base_pc[1:0]` 恒 00、不作定位键。**I3-3**：块首 `pc[0]` 恒 0，`pc[4:1]` 唯一确定块首槽（存在性与唯一性均为可证伪算式，§7-9；BLK-4 处置：原引 §7-5 系 nib/字节反例行、改引本行）。
- **B-2 fetch loop 终止 = 末条指令字节取全**：取指 loop 的终止判据是"末条指令的全部字节已到手"，不是 nib 计数完成、不是窗槽耗尽。收尾两情形：①末条指令完全落在当前窗内，本轮 walk 产出到它即止；②末条指令为槽 15 起的 32 位指令（或字节跨窗），必须经 `xw_carry` 与次窗槽 0 拼接后才算"取全"（`spec/01` §3.16 ②）。
- **B-3 "两窗内取全路径"的块字节跨度上界 66 B（限域；BLK-2 处置取①限域，不取②改名）**：术语——块跨度 = 末条指令末字节地址 − 块首字节地址 + 1。**适用域**：本上界只对"两窗内取全路径"成立——块首字节所在 32B 窗基址为 W 时，块的全部内容（含经 `xw_carry` 拼接的尾 2 B）在"W、W+32 两完整窗 + 第三窗 W+64 前 2 B"内取全，此时末条指令末字节地址 < W + 66。等号场景几何：块首在 W 偏移 0（槽 0）、块内容覆盖 W、W+32 两个完整窗（64 B）＋第三窗 W+64 的前 2 B（64 + 2 = 66）；该 2 B（W+64、W+65）在本适用域内只由 `xw_carry` 消费——它们是 **W+32 窗槽 15** 起的 32 位指令的尾 2 B：该槽地址 = (W+32) + 30 = **W+62**（**BLK-3/CL-2 更正**：槽 15 属 W+32 窗；原句"槽 15 地址 = 基址 + 30"未对基址限域，按字面读作 W+30 则末字节 W+33、与 W+65 恰差一窗，该读法已作废），该指令占字节 **W+62 至 W+65**、末字节 W+65 = W + 66 − 1。**限域边界**：`xw_carry` 拼接按 §3.16 ② 的 walk 语义发生（本窗槽 15 起 32 位指令装载 carry、次窗先产出拼接再自槽 1 续走）；**>66 B 块**（无分支长串型合法块——本章节片评审件构造的朴素续走 68 B、拼接续走 98 B 反例）**不在本上界覆盖范围**，其处置 ⇒ §4 待补 **T3-1**（与 `spec/04` 联片定稿）；闭合前实现不得以截断或静默丢弃处置。依据＝`doc/process/00` ISS-039 决策列的声明值（66 B）＋本篇几何复算（§7-2，限域后）。**更正路径（CL-2）**：基准件＝`doc/process/00` ISS-039 决策列原文（ADR-1 选 A）＋`spec/01` 现行文本（§1.1 P5 行「ISS-039 定案 A」、§3.16）；落点＝本篇 §2.3 行内更正注（涉接口/位宽者按篇首②回写 `spec/01` 接口升版）；批准人＝R0（经 R8/领队按规则 23 呈报，本篇不代行）。该读法尚未经评审轮核对（首片未评审，见 §6.1①）——原句"该读法经评审轮核对"已删（与 §6.1 口径相抵）。
- **B-4 块内 nib 计数与字节完成的关系**：1 槽 = 2 B = 4 nib，1 窗 = 16 槽 = 64 nib；nib 计数是**位置记账**（`slot = nib >> 2`，由槽号派生），不参与终止判定；"字节完成"才是终止判据（B-2）。两向反例：①nib 未满 64 而字节已取全（块末指令止于窗中，其后字节属下一项块）；②nib 达 64 而字节未取全（槽 15 起的 32 位指令还差次窗槽 0 的 2 B = 4 nib）。**I3-4**：任一时刻的"取指完成"与任何 nib/槽计数不互为充要。
- **I3-5**：`xw_carry` 是跨窗拼接的唯一载体（位宽与字段 = `spec/01` §3.16 ② 原文，62 bit = 1 + 16 + 40 + 1 + 4；载荷/消费/作废/粘滞违例规则逐句引用 §3.16 ②④，本篇不复述、不另赋值）。

### 2.4 与 `spec/01` §3.16 的逐句对账表

| # | `spec/01` §3.16 原句（`icache_rsp_t` 注与 walk 规则段） | 本篇对应 | 核对 |
|---|---|---|---|
| T-01 | 块首槽 = 块起始 `pc[4:1]`；`win_base_pc[1:0]` 恒 00，不作定位键 | B-1、I3-3 | 一致（本篇补"任意 2B 槽"的显式句） |
| T-02 | 逐槽 walk：半字低 2 bit 非 11 则 16 位，为 11 则 32 位占两槽 | B-2、B-4 | 一致（本篇只补终止判据，不改 walk） |
| T-03 | 起始可落任意槽号，无"只落偶数槽"约束 | B-1 | 一致 |
| T-04 | 槽 15 为 32 位起始则装载 `xw_carry`、本窗 walk 止于槽 15 | B-2、I3-5 | 一致（引用不重定义） |
| T-05 | 次窗基址连续则先产出拼接指令，再自槽 1 续 walk | B-3（2 B 消费路径） | 一致 |
| T-06 | 基址不连续/冲刷/重定向/同基址重取则 carry 作废 | F4 | 一致（本篇补"冲刷优先于停工"） |
| T-07 | `err_pos` 命中槽处截断；err 展开为按 lane 的 `err`/`err_code` | §3.1（C1、C2） | 一致 |
| T-08 | 边界项（重定向指令所属项）的保留/作废口径 | F4 引用 `spec/01` 冲刷定义 | 本篇不另规定（引用句） |
| T-09 | `v=1` 再装载为协议违例，置粘滞位（候选断言 A20） | I3-5（引用） | 一致 |
| T-10 | `v=1` 未消费期间全字段在停顿/反压期保持稳定 | §3.2（停工语义） | 一致 |
| T-11 | 回退点三条件（请求/响应/可用性粒度） | §6.2（闭合声明） | 一致（随 `spec/09`/`spec/10` 对照） |
| T-12 | 细则转派：carry 逐拍、块跨度 66 B、loop 终止、nib 计数 | B-2 至 B-4（本片成文） | 本篇承接（本片） |

## 3. 取指与 IBUF

### 3.1 消费侧：`valid` 且非 `err` ＋ `err_pos` 截断（`slot16_vld` 已删）

字段面（`spec/01` §3.16；ADR-3 / ISS-045）：`icache_rsp_t` 322 bit；`slot16_vld` 已删除（338 → 322，历史变更值）。半字可用性表达 = `valid` 且非 `err` ⇒ 全 16 槽可用；`err=1` ⇒ 按 `err_pos` 截断。消费者一览：

| # | 消费者 | 消费的语义 | 动作与口径 |
|---|---|---|---|
| C1 | `P5` walk（`vr1_ifetch` 内） | 起始位图 | 自块首槽起按 §3.16 ① walk；`err_pos` 命中槽处截断（其后槽不再产出起始）；其字节跨度含 `err_pos` 槽的指令必须带 err（含跨槽/跨窗情形） |
| C2 | `P5` 写 IBUF 时 | err 展开 | 按 `err_pos` 把异常打到对应 lane 的 `err`/`err_code`（§3.3）；与 C1 是同一异常码表的两级表达（N12 口径） |
| C3 | `D` 级 | lane err | 只读 `fetch_raw_t.err`/`err_code`；`err_pos` 不出 `P5` |
| C4 | 跨窗拼接拍 | err 合成 | 本窗槽 15 或次窗槽 0 命中 ⇒ 拼接指令带 err；`err`/`err_code` 随行锁存于 `xw_carry`，拼接拍按位或；双源同时置位取指令首字节所在窗侧（`spec/10` 另有定序则从其） |
| C5 | 断言/覆盖面 | A20 等（候选编号） | 编号口径以 `spec/01` §7 汇总为准；接口名随 `spec/11` 核对 |

- **I3-6**：上述消费者共享同一异常码表（`spec/10`）；ADR-3 之后不存在第二种"部分槽可用"表达。`spec/01` §3.16 的回退点三条件任一成立 ⇒ 按接口升版恢复按槽可用性位图（或等效字段）。
- **I3-7**：`valid=0` 的响应不产生任何 lane 写入、不传播任何 err；`err=1` 且 `err_pos=0` 时该窗自槽 0 起即截断（无起始产出）；拼接路径的 err 处理按 C4。

### 3.2 IBUF 满时的停工语义（F2 与 `P5`）

结构（引用）：IBUF 16 项 × 92 bit（`spec/01` §3.3）；`D` 每拍组合读出全部 16 槽并选取 ≤ `DECODE_WIDTH` = 4 个起始槽（`spec/01` §1.3）；槽清零粒度为 lane。

- **满判据**：无空 lane（16 lane 全部占用）⇒ 本拍不可写入。
- **停工（stop）语义**：`P5`（F2）本拍不写 IBUF、不推进 walk、不消费 FTQ 项；`xw_carry` 全字段保持稳定（§3.16 ④）；上游逐级反压——`P4` 不发新请求、`P3` 不出队（`rptr` 不前进）、FTQ 满则 `P2` 停写、BP 级停（§1.2 F3）。
- **正确性约束**：停工不置任何 err、不丢弃已 walk 结果、不改变任何已写入 lane；恢复 = `D` 消费释放 lane 后可当拍恢复（背压气泡上限与握手细化见 §5 下游义务）。
- **优先序**：冲刷优先于停工（F4 末句）；停工期间到达冲刷 ⇒ lane 按粒度作废、`xw_carry` 作废，恢复从重定向目标的新块重新开始。

## 4. 本篇待补清单（首片未成文节）

| # | 待补项 | 闭合/转派 |
|---|---|---|
| T3-1 | 跨度上界（66 B）等号场景确认，与"大于 66 B 块"（预测远分支/无分支长串；本章节片评审件反例 68 B/98 B）处置 | G1 前闭合（ISS-039 收口剩余项）；>66 B 块处置与 `spec/04` 联片定稿（B-3 限域后） |
| T3-2 | TAGE/ITTAGE 条目与 SC 权重阵列完整位域（含 u 位分配/递减、CTR 饱和规则） | 承 `spec/00` §4.2"分配/递减规则见 `spec/03`"与 D-9 未完成项① |
| T3-3 | `SC_INPUTS` = 24 的逐位构成（逐项列来源） | `spec/00` §4.2 转派 |
| T3-4 | GHR 更新/恢复与 checkpoint 的 `HIST_DEPTH` 交互；`IUM_EN` 逐拍更新时序 | `HIST_DEPTH` 已定值 = 33（`spec/00` §4.3，ADR-S2）；本篇只引用，机制交互面待补 |
| T3-5 | RAS/SARAS 推入/弹出/校正提交细则，与 `spec/02` T-7（`is_jalr_ret` 产生规则）合并定稿 | 与 `spec/02` 联片 |
| T3-6 | 两级 BTB 条目位域、L2BTB 替换策略、L1 至 L2 重填路径；`ITTAGE_TARGET_W` = 40 与 `VA_STORE_W` = 40 位域核对 | 本片续 |
| T3-7 | FTQ 条目/请求 struct 与块布局信息的字段级对应 | 引用 `spec/01` §1.1 P2；字段核对留评审面 |
| T3-8 | 预测表复位初始态与冲刷清表口径 | `spec/13` 性能模型需要 |

## 5. 下游义务

- `spec/05`：F2 mux 时序、停工/反压握手与冲刷仲裁细化（ISS-039 交办）。
- `spec/09` / `spec/10`：回退点三条件逐条对照（ISS-045 交办）＋异常码表定序（C4 双源口径）。
- `spec/04`：`HIST_DEPTH`（已定值 = 33，`spec/00` §4.3）与 checkpoint、GHR/RAS 恢复的机制交互（T3-4 依赖）。
- `spec/13`：预测器规模与 IUM 的性能模型口径；`K` 不属本篇。
- `doc/verify/02`：验证点组共 6 组（块布局 / 跨窗拼接 / err 截断 / 停工反压 / 冲刷作废 / 满空判据）——ISS-039 交办。
- `spec/11`：断言接口名与 A20 编号汇总核对（`spec/01` §8.2 待同步清单口径）。

## 6. ISS-039 / ISS-045 闭合声明

### 6.1 ISS-039（"块布局限制"收口）

- 本篇已成文：B-1 至 B-4、I3-3 至 I3-5、§2.4 对账表 12 行（本片）。
- **仍属开放（不可据此关闭）**，还差：①本片与对账表经评审轮逐句核对（首片未经任何评审，不得作收敛证据）；②T3-1 闭合（"大于 66 B 块"处置）；③闭合后由 R8/领队回写 `doc/process/00` ISS-039 行（本文件不代行台账动作）。**改述（CL-7）**：原③所述 `spec/01` §3.16 槽 15 情形的「待定（ISS-039）」标记**现已不存在**——ADR-1 落「定案 A」后 `spec/01` 已按定案表述（§1.1 P5 行明记「ISS-039 定案 A」）；本条件改为：按 `spec/01` 现行文本（ADR-1 定案 A）与本节 B-1~B-4、T3-1 结论核对无冲突后，由 R8/领队回写台账。

### 6.2 ISS-045（`slot16_vld` 删除）

- 本篇侧义务已成文：§3.1 消费侧语义（`valid` 且非 `err` ＋ `err_pos` 截断、C1 至 C5 消费者表、I3-6/I3-7）；全篇 `slot16_vld` 命中均为历史说明与自检引用，无把它当现行字段使用的句子（§7-6 实跑）。
- **仍属开放（附K-B3b 有条件关闭的闸门）**，还差：①第 13 轮增量复核确认 `spec/01` 正文无残留引用、且 `width_check` 的 `icache_rsp_t` = 322 预置锚点相符；②回退点三条件随 `spec/09`/`spec/10` 定稿对照；③闸门通过后方可改状态字，不得前置外推。

## 7. 出口自检表（每项附复核手段）

| # | 检查项 | 复核手段 | 本轮结果 |
|---|---|---|---|
| 1 | 参数名与 `spec/00` §4 一致（引用不另赋值） | 命令 S1：抽取本篇反引号大写名去重，逐名核 `spec/00` §4（§4.1/§4.2/§4.3） | 实跑：33 名＝级名 `P0`~`P5`（6）＋`VA_STORE_W`（§4.1）＋`DECODE_WIDTH`/`HIST_DEPTH`（§4.3）＋24 个前端参数（§4.2），无表外名（S1） |
| 2 | 66 B 上界几何复算（限域，见 B-3） | 算式：64 + 2 = 66；末条指令＝**W+32 窗**槽 15 起的 32 位指令——该槽地址 = (W + 32) + 30 = **W + 62**，末字节 = W + 62 + 3 = **W + 65**（= W + 66 − 1） | 两式自洽（S2 手算复核）；BLK-3 更正：原式"槽 15 地址 = W + 30"未对基址限域、按字面得末字节 W+33、与 W+65 差一窗，已改为 W+32 窗基址 |
| 3 | 指针宽与满/空谓词 | 算式：log2(16) = 4，加 1 回绕位 = 5 = `ftq_id` 宽；满/空谓词互斥枚举 32 × 32 = 1024 组合 | 实跑：互斥性 True（S3） |
| 4 | 不变式编号不与他篇冲突 | 命令 S4：在 `spec/02` 中检索 `I3-`，期望 0 命中 | 实跑 0 命中（S4） |
| 5 | nib/字节关系反例 | 算式：16 × 4 = 64 nib；反例②：64 − 4 = 60 nib 时仍差次窗 4 nib；反例①：块末止于窗中（nib 小于 64 且字节已全） | 两式自洽（S2 手算复核） |
| 6 | `slot16_vld` 残留（不得作现行字段依赖） | 命令 S5：本篇全文检索 | 实跑 6 行命中：均为历史说明/节标题/自检引用，无字段使用句（S5） |
| 7 | 机判回归 | 命令 S6：`python script/gate.py check` 与 `python script/width_check.py` | 实跑（末行见 HANDOFF 本轮记录） |
| 8 | 对账表覆盖 | 行数 12，覆盖 §3.16 转派细则 4 项与 walk 主体；逐句对照原文 | 评审面义务（首片未评审） |
| 9 | 块首槽存在与唯一（I3-3 的算式落点；BLK-4 补行） | 算式：槽号 = `pc[4:1]`、取值域 {0..15}；16 槽 × 2 B = 32 B/窗；`pc[0] = 0`（块起始 2B 对齐）⇒ 任一块首唯一落窗（`pc[31:5]`）、窗内偏移与槽号一一对应 | 枚举 16 槽映射（槽 0..15 对偏移 0, 2, ..., 30）复算一致（本轮实跑，见 `doc/verify/05` R-052） |

复核命令（本轮实跑，输出末行记入 HANDOFF）：

```bash
python script/gate.py check
python script/width_check.py
grep -oE '`[A-Z][A-Z0-9_]+`' doc/spec/03-*.md | sort -u   # S1
python -c 'print(all(not((w==r) and ((w&15)==(r&15)) and ((w>>4)!=(r>>4))) for w in range(32) for r in range(32)))'   # S3
grep -c 'I3-' doc/spec/02-*.md   # S4
grep -n 'slot16_vld' doc/spec/03-*.md   # S5
```
