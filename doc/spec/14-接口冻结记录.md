# VR1 接口冻结记录（doc/spec/14）

| 版本 | 日期 | 状态 | 变更摘要 |
|---|---|---|---|
| v0.1 | 2026-09-25 | 草稿（**骨架·首片**；未评审、**未签署**） | 首片：§1 冻结范围与判据；§2 冻结前置状态表（逐篇收口状态）；§3 **§9.5 评审 list 四段（G1 材料；④段＝汇聚清单）**；§4 冻结动作与签署（模板；**待人**）；§5 冻结后纪律；§6 出口自检 |

> **① 范围**：本篇是 **Review G1（接口冻结）** 的记录载体：冻结范围、前置状态、评审 list（§9.5 四段）、签署动作与冻结后纪律。
> **② 边界**：接口细节以 `spec/01` 为准（本篇只做索引与状态）；评审 list 的**条目来源**在 `doc/verify/05` 与评审记录（逐条双指针）；**冻结判据**引 `spec/00` §6。
> **③ 引用纪律**：本篇**不定义任何接口**；状态词只用「已收口／起稿／待评审／待人」。

---

## 1. 冻结范围与判据

| 项 | 内容 | 引 |
|---|---|---|
| 冻结对象 | **接口名、方向、位宽与语义**（不是内部实现） | `spec/00` §6 |
| 冻结判据 | 每个 `*_t` 有**成员级位域定义与合法值域**，能直接生成 SV struct 与端口表 | `spec/00` §6 |
| 机判锚 | `width_check`（28 struct 成员和 vs 声明值＝28/28）＋`python script/flow.py check`（判据项数与结论以实跑末行为准，不写死） | `doc/verify/05` R-160~R-162 |
| 模块边界索引 | `spec/00` §6 表（M01~M20：上游/下游接口＋规格篇） | `spec/00` §6 |

## 2. 冻结前置状态表（截至 2026-09-25）

| 篇 | 状态 | 备注 |
|---|---|---|
| `spec/00` 总体规格 | **v0.1 待评审**（含 §4 参数总表＝唯一权威源） | 冻结前须过 Requirement Review（人） |
| `spec/01` 流水线与接口 | **已收口**（经 D-14 全链＋R0 裁断 C2 成立） | 冻结主对象（§2/§3 全 struct） |
| `spec/02` 指令集与逐指令行为 | **已收口**（经 Q-010；§17/§18/§19 后续片闭合） | — |
| `spec/03` 分支预测与取指前端 | **已收口**（经 Q-006，R-132） | — |
| `spec/04` 重命名与乱序引擎 | 起稿（S-08 收口：清单④⑤ 落文、T-04-5b~d 归属） | 余项 T-04-1~4/6~8（各自承接） |
| `spec/05` 时序与流水级细节 | 起稿（S-15；F2 `xw_carry` mux 专项落文） | 余项 T-05-1~5 |
| `spec/06` 访存与 cache 层次 | 起稿（S-16） | 余项 T-06-1~6 |
| `spec/07` 转发与旁路 | 起稿（S-17；单拍可实现性＝登记＋预测＋回退点） | 余项 T-07-1~4 |
| `spec/08` 访存一致性与虚拟内存 | 起稿（S-18；【权】行号锚待补） | 余项 T-08-1~6 |
| `spec/09` L1I/L1D 与取指粒度 | 起稿（S-19；`slot16_vld` 回退点对照） | 余项 T-09-1~6 |
| `spec/10` 特权与 CSR | 起稿（S-20；版本声明三段式；五处【权】锚） | 余项 T-10-1~8（**2026-09-25 校正**：原写 T-10-1~6，与 `spec/10` §6 现行族号 T-10-1…T-10-8 不符——只改范围表述，状态不动）：**T-10-1 已闭合（R-181）**；其余项状态见 `spec/10` §6 |
| `spec/11` SVA 与 lint | 起稿（S-21） | 余项 T-11-1~5（A20 编号定稿） |
| `spec/12` 参数与配置区间 | 起稿（S-22） | 余项 T-12-1~4 |
| `spec/13` 周期模型与性能口径 | 起稿（S-23） | 余项 T-13-1~5 |
| `spec/14` 本篇 | 首版（本文件） | 签署栏待人 |

## 3. §9.5 评审 list（四段；G1 材料，由人读）

**① 段——AI 自决已定（ADR 全链，供过目）**：ADR-1（`xw_carry`）／ADR-2（csrq I-01/I-02）／ADR-4（完整性基线）／ADR-5（三子命令）／ADR-8（码点扫描）／ADR-K3（版本行体例）／ADR-S1（`bp_upd_id` 身份键面）／ADR-S2（窗口口径/FWD 双项/`HIST_DEPTH`=33）／ADR-S04-B2·B3／ADR-S05-B1（RAS `11`@`sp==0`）／ADR-S06-A（`Zama16` 作废）·B（LR/SC＝1 μop）／ADR-S08（`PRF_RD_PORTS`=16）／ADR-S12（组内前递拍序）／ADR-B1~B9、ADR-D3-①~⑤、ADR-D4-①~④、ADR-D5-①~③（逐条见 `doc/verify/05` R-066/R-067/R-076/R-077/R-080/R-081）。

**② 段——待人拍板**：`doc/process/00` 待决策清单（现行 3 条：ISS-002／ISS-023／ISS-029）；H-2/H-3 行（工具链安装／环境面）。

**③ 段——需人读的判定口径类**：无开放项（历史项均已裁并留痕：`Zama16`→ADR-S06-A、`IPC≥1.8` 附条件→ADR-S2-1、`FWD_MAX_SOURCES` 死锁→ADR-S2-2 等）。

**④ 段——汇聚清单（原 D-13；G1 前由人读）**：条目数与各轮"需进 ④段"标注一致（可对账），逐条**双指针**（来源条目 → `doc/verify/05` 行）：

| 条目 | 来源 | `doc/verify/05` 行 |
|---|---|---|
| B-1／B-2／B-3 | 评审记录附 M §1 | R-067／R-068 |
| B-4 | 附 M §1 | R-066／R-069 |
| B-5 | 附 M §1 | R-068 |
| B-6 | 附 M §1 | R-066／R-069 |
| B-7 | 附 M §1 | R-069 |
| B-8／B-9 | 附 M §1 | R-067／R-068（B-9 另 R-066／R-068） |
| ADR-D3-①（附 N 阻-1/存-1） | 附 N／附 O | R-076 |
| ADR-D3-②（附 N 阻-2＋附 O B2-①/B1-②） | 附 N／附 O | R-076 |
| ADR-D3-③（附 O B1-③） | 附 O | R-076 |
| ADR-D3-④（附 N 阻-3＋附 O B9-①） | 附 N／附 O | R-076 |
| ADR-D3-⑤（附 O B1-①） | 附 O | R-076 |
| ADR-D4-①（附 N 澄-1＋附 O B4-①） | 附 N／附 O | R-077 |
| ADR-D4-②（附 O B8-①） | 附 O | R-077 |
| ADR-D4-③（附 O B6-①） | 附 O | R-077 |
| ADR-D4-④（附 O B6-②/B8-②） | 附 O | R-077 |
| D5-①（附 P 阻-L7-1） | 附 P | R-080（落文留痕 R-081） |
| D5-②（附 P 澄-L7-1） | 附 P | R-080（落文留痕 R-081） |
| D5-③（附 P 澄-L7-2） | 附 P | R-080（落文留痕 R-081） |

> **对账口径（附 AK C2 更正：行/项口径统一）**：上表**表体 18 行**（B-1~B-9 合并占 6 行＋ADR-D3 系 5＋ADR-D4 系 4＋D5 系 3）；**条目数 21 项**（B 系 9＋D3 系 5＋D4 系 4＋D5 系 3）——与 `doc/verify/03` §0 D-13 行所载明细**逐项一致**（该件**已退役留档**；对账明细以本表为准，现行工作顺序见 `state/flow.json`／`python script/flow.py next`）；新增条目随轮次在本表追加（只增）。

## 4. 冻结动作与签署（模板；**待人**）

| 项 | 内容 |
|---|---|
| 签署人 | **R0（人）**——唯一门禁权（`doc/项目开发流程.md` §12.1 P1） |
| 触发条件 | ①本表 §2 前置状态达"可冻"（`spec/00` 过 RR；01~13 余项（T-xx）清零或逐项列缺带归属；~~`spec/10` T-10-1 CSR 全表~~ **已闭合（R-181）**）②§3 四段材料人读无异议 |
| 冻结动作 | ①本篇记**签署时间戳**与冻结清单（`spec/00` §6 全表 M01~M20）②`rtl/` 随即转**只读**（红线 R1；后续任何 `rtl/`/`tb/` 改动走 R5 先批后改）③基线宣布（如触发）按 `AGENTS.md` §1 基线状态 |
| 当前状态 | **未达**（前置余项在册；CSR 全表已于 R-181 闭合）——**本行由 R0 签署后改写** |

## 5. 冻结后纪律（预告，随签署生效）

- `rtl/` 只读；疑似设计缺陷只登记不改（红线 R1）。
- 平台/tb 改动先批后改（红线 R5）；检查器改动照红线 R6/R14。
- 接口变更＝升版＋RR＋G1 重签（`spec/00` §6 同口径）。

## 6. 出口自检（每项附复核手段）

| # | 检查项 | 复核手段 | 本轮结果 |
|---|---|---|---|
| 1 | 冻结判据与 `spec/00` §6 一致 | §1 表行 2 逐字对照 | 一致 |
| 2 | 状态表逐篇可回查 | §2 表 15 行均带 R 行/状态来源 | 一致 |
| 3 | ④段条目与 D-13 明细对账 | 逐条双指针（来源＋`doc/verify/05` 行）；口径＝表体 **18 行**／条目 **21 项**（附 AK C2） | 18 行／21 项全对齐 |
| 4 | 签署栏明示"待人" | §4 末行（未达＋由 R0 改写） | 一致 |
| 5 | 结构完整（表列数/码点） | `python script/flow.py check` 的 `md-integrity`/`codepoints` | 待本轮实跑 |

---

## 7. G1-I 分段签署记录（R0，2026-09-25）

> **① 依据**：`doc/decisions/G1-I-最小冻结清单.md` §6.1 段 0（G1-I 冻签）＋`doc/decisions/G1-I-待裁三点-简报.md`（三处待过目点的可作答面）。
> **② 语义**：本节记的是**分段签**（G1-I 最小冻结面）；G1-F 全量冻结仍按本节 §4 的模板与 `spec/00` §6 全表执行（清单 §6.1 段 3）。
> **③ 边界**：本节**不定义任何接口**；接口细节以 `spec/01`、`spec/10` 与清单 §2 为准。
> **④ 与 §4 的关系**：§4 表「当前状态」行所载「未达」＝**本节签署前**的状态快照（该行自述「由 R0 签署后改写」）；签署后的权威状态以本节与 `state/freeze.json` 为准，本节只**追加**、不改写既有各行。

### 7.1 签署对象（＝人签的「清单完整」面）

| 面 | 数量 | 落点 |
|---|---|---|
| ① 接口 struct | **23 项**（`spec/01` §3 已定义型） | 清单 §2.1；排除的 5 型（MMU/cache 缺失面）见清单 §3 |
| ② CSR | **6 个**（`mtvec`／`mscratch`／`mepc`／`mcause`／`mtval`／`mstatus`） | 清单 §2.2；行文见 `spec/10` §4.1 |
| ③ 异常 | **4 项**码值（6／4／1·5·7／2）＋ **1 行** trap 进场与返回动作口径 | 清单 §2.3（表体 5 行）；码表见 `spec/10` §2.1 |
| ④ trace 口径 | **6 行** | 清单 §2.4；`spec/01` §3.21＋`iss/README.md` §3 |
| ⑤ 参数 | **16 行** | 清单 §2.5；值源见 `spec/00` §4 |

### 7.2 三处待过目点的裁定（R0 2026-09-25 裁定，三点全取推荐项 A）

| # | 待裁点 | 裁定 | 依据与后果（出处＝简报同号项） |
|---|---|---|---|
| ① | `rt_t` 是否以「补 `spec/01` §3.21 附录（RVFI 三清单）」为 G1-I 前置 | **取推荐项 A：由 R0 明示豁免**该节自述句「附录未填前，本节不得作为 G1 冻结依据」，`rt_t` **照冻** | 附录降为**下游义务**（G1-F 前补）；G1-I 范围不变（简报 ①） |
| ② | `mtval` 非对齐取值口径（`spec/10` §4.1 未成文） | **取推荐项 A：以规范（非特权卷）＋`iss/vriss` 实现为准**（cause 6 ⇒ `mtval`＝出错 VA），`spec/10` §4.1 **后补一行** | 本节**显式标注该口径为「过渡态临时裁定」**：依 `AGENTS.md` §5 权威原则补充，差异未决过渡期内可实现为行为真值，但**不得上升为通则**（简报 ②） |
| ③ | `ibuf_wr_t`／`lq_alloc_t`／`sq_alloc_t` 的「折入既有型／恒等旁路」 | **取推荐项 A：认可**——首版不定义、不例化；D 级直读 `spec/01` §3.3 的 `fetch_raw_t`，LQ/SQ 分配折入 `spec/01` §3.6 的 `rob_alloc_t`／`spec/01` §3.8 的 `iq_wr_t` | 三型**留 G1-F**（清单 §3 第 1 行的 12 个零定义型之三）；本口径为 G1-I 期口径，按清单 §5 的 v5／v6 升版补型（简报 ③） |

### 7.3 清单完整性声明（照知识库 §6.2 的签署语义）

- 人签的是「**清单完整 ＋ 我接受其风险**」；**不要求**签署人重算或复核全部细节——细节核验由锚点（`spec`／ISS／机判）、独立只读复核与判据承担。
- 已核机判面：`python script/flow.py check` 0 FAIL（类型／参数／CSR／码表／指令集／ISS 冒烟／镜像／trace 自比诸判据在环，结论以实跑末行为准）。
- 四段材料：清单 §6.2 的取舍条目 7 条／未决项 0／风险 6 条／抽查建议 4 处**已齐**；清单 §4 的 14 条可签判据按「现有判据覆盖情况」列如实标注（未覆盖者已登记待材料，见 §7.5）。

### 7.4 签署范围与不含项

- **本次签的是 G1-I 最小冻结面**（§7.1 五面），签署结论＝**通过**。
- **不含**（逐条理由与 G1-F 触发源见清单 §3）：12 个零定义的 `*_t`、复位值与 WARL 全面、延迟数字、PMA／PMP 判决面、CLINT／PLIC 与中断面、Sv39／PTW／TLB、C 展开（含 `xw_carry` 跨窗拼接）、原子与内存序（RVWMO）、性能与度量口径、`spec/01` §3 排除的 5 型。
- **不含项一律延后到 G1-F**（清单 §5 的缺口驱动增量），**不得**在未升版的批次里实现其机制（清单 §4 的负判据）。

### 7.5 风险与残余（签署时已知）

| # | 残余 | 现状 | 出口 |
|---|---|---|---|
| 1 | 待材料 5 条（清单 §4 未覆盖率，均已在台账） | **ISS-116**（`mtval` 口径待落文）／**ISS-117**（参数「值＋出处」行级对账缺）／**ISS-118**（`spec/01` §3.21 附录未填）／**ISS-119**（TB 侧 `$readmemh` 同口径留证缺）／**ISS-120**（TB 侧写出器／停机／watchdog 缺） | 随 M1 实施批或 spec 落文批逐条闭合 |
| 2 | 首版仿真器不可用 | **ISS-104**：ModelSim `vsim` license HOSTID 不符（`C:\flexlm\LICENSE.TXT` 为陈旧绑定；`vlog` 编译不受影响）——**2026-09-25 更正：`vsim` 已可用**（`flow.py` 注入 license 环境变量，R-224）；Spike 1.1.1-dev 亦自建就位（R-235，调用带 `--dtb=`，限制见 ISS-123） | 本条**已不阻塞 M1**；OS 侧 rehost 降为可选（既有 `flow.py` 注入即够用）；原「或走 ISS-002（WSL2＋iverilog/verilator）」随 R0「永不装 Linux」作废 |
| 3 | 同模型多角色共享盲点（红线 R21） | 清单 §6.2 ③ 的 `GR-6`：锚点以内部锚（`spec`／ISS／机判）为主，外锚少 | 由本节签署（R0 接受风险）＋后续增补留痕兜底 |

### 7.6 时间戳与签署人

| 项 | 内容 |
|---|---|
| 签署时间 | **2026-09-25** |
| 签署人 | **R0（人）**——唯一门禁权（`doc/项目开发流程.md` §12.1 P1） |
| 签署结论 | **G1-I 通过**：清单完整、风险已接受；范围＝§7.1，不含项＝§7.4，裁定＝§7.2 |
| 状态件 | `state/freeze.json`（机械面：G1-I 已签 ⇒ `rtl/**` 进实现期；基线宣布后转「先批后改」红线 R5） |
| 台账 | `doc/process/ledger.json`（本轮登记；编号由脚本自增，禁手写） |
