# VR1 访存一致性、虚拟内存与保护（doc/spec/08）

| 版本 | 日期 | 状态 | 变更摘要 |
|---|---|---|---|
| v0.1 | 2026-09-24 | 草稿（**骨架·首片**；未评审、未定稿） | 首片：篇首三段式；§1 地址空间与分页（Sv39）；§2 TLB 与 PTW；§3 PMP/PMA；§4 fault 分流与 A/D 位；§5 一致性/序纪律（单核面）；§6 待补清单；§7 出口自检 |

> **① 范围**：本篇给出**虚拟内存与保护**的系统口径：分页结构、TLB/PTW 机制、PMP/PMA 判定面、fault 分流，以及**单核下的一致性/序纪律**（`sfence.vma` 语义面）。
> **② 边界**：接口位域以 `spec/01` §3.14 为准（`mmu_transl_req_t`/`rsp`）；**`ptw_mem_req_t`＝待闭合型**（`spec/01` 现文未定义；定义落点按 `spec/00` §6 X5 注＝其「规格篇」列所指篇目，M12→本篇；G1 前闭合；见 §2 与 ISS-092）；访存路径与 cache 层次归 `spec/06`；**CSR 全表（`satp`/`mstatus`/`pmpcfg*` 等地址/复位值/位段/WARL）归 `spec/10`**；拍序归 `spec/05`；**数值一律引 `spec/00` §4**。
> **③ 引用纪律**：规范锚以**【权】特权卷 §4.4/§3.6/§3.7**（本机提取件）为准，**行号待本篇深化片补**（登记 T-08-1）；无锚点项按规则 20 降级「登记＋预测＋回退点」。

---

## 1. 地址空间与分页（Sv39）

| 项 | 口径 | 引 |
|---|---|---|
| 虚拟地址 | `VA_BITS=39`（符号扩展至 64）；非法高半区 ⇒ page-fault | `spec/00` §4.1；【权】§4.4 |
| 物理地址 | `PA_BITS=44`（决定 PMP/PMA/AXI 宽度） | `spec/00` §4.1 |
| 页表 | 三级；页大小 4K/2M/1G（`rsp.super` 3 值）；PTE 格式与位域（V/R/W/X/U/G/A/D/RSW/PPN）| 【权】§4.4；`spec/01` §3.14 |
| 模式 | 一期仅 **Sv39**（`satp.MODE` 只支持 39；其余值 WARL 归 `spec/10`） | `spec/00` §2 |
| 翻译时机 | 取指与访存**统一经 MMU**（两路径共用 `mem_req_t`/翻译接口） | `spec/06` §1；`spec/01` §3.13/§3.14 |

## 2. TLB 与 PTW

- **TLB**：L1I 侧 `L1I TLB16`（`spec/00` §4.6）；L1D 侧条目数以 `spec/00` §4.6 为准（**条目位域与替换态归 `spec/09`**——登记 T-08-2）。
- **ASID**：`ASID_BITS_IMPL`（`spec/00` §4.6 行；【权】§4.1.11 锚点闭环状态见 `spec/00` §10）。
- **PTW**：单 walker（一期）；walk 请求的载体＝**`ptw_mem_req_t`（`spec/00` §6 X5 注所列待闭合型；`spec/01` 现文未定义 ⇒ 定义落点按 X5 注＝「规格篇」列所指篇目（**M12→本篇**），G1 前闭合；登记 ISS-092）**，经 L1D 发出；**walk 与数据请求的仲裁口径**归本篇深化片（登记 T-08-3）。
- **翻译结果**：`mmu_transl_rsp_t`（含 `pa`/`fault`/`rw`/`super`，`spec/01` §3.14）；`rw` 保留码发出即违例（access-fault 兜底）。
- **TLB 填入纪律**：填入须与 `satp`/`sfence.vma` 序一致；**翻译一致性口径**（`satp` 写与 `sfence.vma` 的失效面）归 `spec/10` 定稿（登记 T-08-4）。

## 3. PMP / PMA

| 面 | 口径 | 引 |
|---|---|---|
| PMP | **PMP 8 项**（`spec/00` §2）；读/写/执行权限按项匹配；判定先于/后于翻译的次序以【权】§3.7 为准 | `spec/00` §2；【权】§3.7 |
| PMA | 静态区域表：`non-cacheable`/`strong-order`/`no-prefetch` 三类标记；**MAG PMA 不配置**（不实现 `Zama16b`，ADR-S06-A） | `spec/00` §2/§4.7/§8 |
| MMIO | non-cacheable 区单拍访问（`MMIO_BURST`：len=0，size=2..8B） | `spec/00` §4.7；`spec/06` §4 |
| 取指面 | PMP 的 X 权限对取指生效；I 侧 fault 经 `ifetch` 通路 | `spec/01` §3.3/§3.14 |

## 4. fault 分流与 A/D 位

- **分流**（本篇口径）：权限/属性类 ⇒ **access-fault**（含 PMA 违规、保留码）；翻译缺失/权限位不满足 ⇒ **page-fault**；具体优先级次序待深化片与【权】§3.7 对账（登记 T-08-5）。
- **A/D 位**：**硬件置**（`spec/00` §5 D8）——PTE 的 A/D 由硬件在翻译命中时写入（PTW 已有 RMW 通路）；由此避免首 store 变 trap。
- **非对齐**：与 MMU 无关——非对齐一律 address-misaligned（`spec/00` §2；`spec/02` §18.2）。
- **异常提交抑制写**：翻译/权限 fault 属异常提交面 ⇒ 内存写不发生（`spec/02` §19 对照表行 3/5）。

## 5. 一致性/序纪律（单核面）

- **单核假设**：无多核一致性需求；L2 **non-inclusive**（`spec/00` §5 D10）；`LR/SC` 不在总线层实现独占（`spec/00` §8）。
- **fence / fence.i**：`fence` 处排空 SQ；`fence.i` 排空前端（IBUF/FTQ）并无效化 L1I 行（`spec/00` §8；`spec/02` §10.8）。
- **`sfence.vma`**：语义面（虚拟地址/ASID 失效粒度）归 `spec/10`；本篇锁：**执行后须使后续翻译不复用失效前 TLB 结果**（纪律句，拍序由 `spec/05` 引用）。
- **PMP/PMA 变更**：属 CSR 写面 ⇒ 随 `fence`/串行化口径（归 `spec/10`）。

## 6. 待补清单（T-08-x）

| # | 项 | 出口 |
|---|---|---|
| T-08-1 | 【权】行号锚（§4.4/§3.6/§3.7 逐节行号） | 本篇深化片（提取件行号实取） |
| T-08-2 | TLB 条目位域/替换态/ASID 域宽 | `spec/09`＋`spec/00` §4.6 |
| T-08-3 | PTW 与 L1D 数据请求的仲裁（含 `req_id` 3 bit 分配） | `spec/06` T-06-5 同源 |
| T-08-4 | `satp`/`sfence.vma` 的翻译一致性细则（含 ASID 复用） | `spec/10` |
| T-08-5 | page-fault 与 access-fault 的判定次序与 `tval` 内容 | 【权】§3.7 对账＋`spec/10` |
| T-08-6 | 大页对齐检查与 super 页错误的 fault 类型 | 【权】§4.4；深化片 |

## 7. 出口自检（每项附复核手段）

| # | 检查项 | 复核手段 | 本轮结果 |
|---|---|---|---|
| 1 | 数值零自造 | 数字 token 全量清点（39/44/4/2/1/8/3/16/0/2..8 均带篇号节号） | 本片实读 |
| 2 | 接口零定义 | 本篇无 struct 成员表（`width_check` 28/28 不变） | 待本轮实跑 |
| 3 | 无锚点项已降级登记 | T-08-1~6 逐项有出口；§3 行号锚明标「待补」 | 一致 |
| 4 | 结构完整（表列数/码点） | `gate.py check` 的 `md-integrity`/`codepoints` | 待本轮实跑 |
