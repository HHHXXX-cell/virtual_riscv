# run_cmd/自动循环定时任务.md — 定时自动化留档（2026-09-24 建／同日取消；2026-09-25 重建）

> 用途：本文件记录「VR1 自动循环推进」定时任务的完整定义，**任务已由 R0 于 2026-09-24 取消**；
> 如需重建，按下方参数用 `qoder_cron`（local 模式）`add` 即可，或口头让 AI 重建。

## 任务参数（留档）

| 项 | 值 |
|---|---|
| 名称 | VR1 自动循环推进 |
| 位置 | local（目录 `D:\virtual_riscv`） |
| 周期 | every 600000 ms（10 分钟；期间曾用 30→15→10 分钟） |
| 输出模式 | merged（各轮复用同一执行会话 → 连续循环） |
| 权限 | Full Access（`bypassPermissions`） |
| 模型 | 继承创建时会话（`dfmodel`） |
| 运行记录 | 首轮 succeeded（约 10.5 分钟）；取消前 `lastRunStatus: succeeded`、`consecutiveFailures: 0` |
| 状态 | **已停止（2026-09-25 14:27，R0 指示「停止自动任务设置」）**：本轮于 14:06 按 R0 指示重建（新 id `a8dfef52-a57b-423f-a46c-55171233e737`，interval 10 min，`target: newQuest`）、14:17 首轮起跑（运行中不可改/删），**14:27 删除成功（`status: cancelled`，`tasks: []`）**；旧 id `2d8b4b97…` 的定义在 14:06 前已不存在。现行口径＝**循环回归「人发消息驱动」**（同 R-159）；prompt 逐字见下（含 3b R0 闸门） |

## 开工 prompt（逐字留档）

```
你是 VR1 项目（D:\virtual_riscv）的领队 AI。按 AGENTS.md 规则 24「开工即自动推进」执行：
1) 读 AGENTS.md 与 doc/自动推进说明.md（§0 开工协议＋§7 角色作业守则 SOP）；
2) 跑 `python script/gate.py check`（以实跑末行为权威；有 FAIL 先修或按规则 R2 分级处置并登记，不得在 FAIL 时宣称已落实）；
3) 读 doc/verify/03 §0/§1 与 .qoder/handoff/HANDOFF.md 最后一条，取 §1 下一个「待派」切片，按该行「目标/写面/验收判据/预算」执行——审计类派 vr1-auditor、决策类派 vr1-decider（独立子代理）；计划/开发/验证类由本体直做；短派发词（run_cmd/派发词模板.md）；
4) 归档与核销：评审记录附 X（如为审计报告）／doc/verify/05 R 行／doc/process/00 ISS 行／doc/verify/04 看板镜像／AGENTS.md §1 计数／HANDOFF 五字段条；切片状态改「已核销」并带 R 行 token；然后继续取下一片；
5) 停机条件（仅四条）：硬停机四类（doc/项目开发流程.md §12.1 P1~P4／§12.3 三类必报／评审 5 轮上限／红线 R9-R10 资源上限）／§1 无「待派」且 §0 无「待办」／队列空／R0 叫停。撞停机即停，并在 HANDOFF 留痕；需人裁的用 `python script/gate.py escalate <ISS-id>` 出 ≤5 选项决策包，不自决、不默认通过。
6) 【不得中途停（R0 2026-09-24 强化）】凡未撞第 5 条的四条停机条件之一，就必须连续执行循环五步（一片接一片），不得停轮、不得以"汇报进展/询问是否继续"结束本轮；仅在撞停机条件或需人裁呈报时才可停下，且停下时必须在 HANDOFF.md 明写"因撞【哪条停机条件】而停"。
纪律：只读复核必须独立子代理（不得本体自审）；行尾不变量（doc/verify/05=LF，其余=CRLF）；写盘后扫控制字符；每次提交用 git（远端推送失败可用 -c http.proxy=http://127.0.0.1:7897）；禁止任何口头「已落实」，一律留痕。
```

## 备注

- 取消后：循环回归"人发消息驱动"（用户说「继续」即按规则 24 连续推进）；规则 24 与摘要 §0/§7 的固化**不受影响**。
- 重建时的注意：任务 id 会变；本地任务不继承本会话上下文（prompt 已自包含）；如需"停等 R0 签署"类闸门，可在 prompt 的第 3/5 条间加一条"遇 `run_cmd/AutoQueue.yaml` 中标注『待 R0 签署』的项即停"。
## 重建记录

| 日期 | 动作 | 留痕 |
|---|---|---|
| 2026-09-25 | R0 指示「能不能不交还/进行下一次循环」→ R8 按本留档逐字重建（local／every 600000 ms／merged／Full Access；新 id 见上表） | `doc/verify/05` **R-158**；本条＋`run_cmd/自动循环定时任务.md` 状态行 |

> 说明：跨轮「不交还」的机制＝本定时任务（每 10 分钟起一轮新会话执行同一开工 prompt，`merged` 复用执行会话以求连续）。单轮内的「不交还」由规则 24 ⑥ 承载（一片接一片，撞停机条件才停）。
| 2026-09-25 | R0 指示「先不要开启定时任务」→ R8 `disable`（定义保留） | `doc/verify/05` **R-159**；状态行同上 |
| 2026-09-25 | **R0 指示「恢复到之前的状态」→ R8 按留档重建**（新 id `a8dfef52…`；首轮 14:17 起跑） | 状态行；HANDOFF 追加条（待补） |
| 2026-09-25 | **R0 指示「停止自动任务设置」→ R8 删除**（删除曾因 run active 被拒，等首轮收工后重试成功；`status: cancelled`） | 状态行；本条；HANDOFF 追加条（待补） |
