# doc/process/00 — 问题记录台账（红线 R4）

> 分类口径：**平台** / **代码**（DUT 设计，AI 只记录不改）/ **文档** / **流程** / **验证代码**。
> 发现即登记（状态=待决策/处理中）；解决后在**原条目**回填【已解决】+ 时间 + 方案摘要。
> Sign-off Review 前本表必须无「待决策/处理中」残留。

## 状态总览

| 状态 | 计数 | 条目 |
|---|---|---|
| 待决策 | 3 | ISS-002 / ISS-023 / ISS-029 |
| 处理中 | 24 | ISS-003 / ISS-004 / ISS-005 / ISS-007 / ISS-009 / ISS-010 / ISS-012 / ISS-016 / ISS-017 / ISS-018 / ISS-019 / ISS-021 / ISS-022 / ISS-024 / ISS-025 / ISS-026 / ISS-027 / ISS-028 / ISS-033 / ISS-037 / ISS-041 / ISS-060 / **ISS-073**（spec/03 续写片退回的接口缺口，2026-09-24） / **ISS-079**（2026-09-24 R0 指令：开发/验证角色并入本体，仅审计保留独立；R-115） / **ISS-080**（2026-09-24 R0 指令：新增决策 AI 角色 RD，独立派发；R-116） / **ISS-082**（`bp_upd_id` 提交侧载体，ADR-S1 转出，2026-09-24） / **ISS-094**（「（该篇待建）」残留 20 处（`spec/01`×19＋`spec/03`×1），已收口篇专片待扫；2026-09-25，R-169） |
| 已解决 | 67 | **ISS-091**（2026-09-24 收尾巡检：4 个失败写残留＋0 字节整句式散件清理；R-151） / **ISS-090**（2026-09-24 S-11 送审 10 条全处置：即修 2＋Q-038 七项（A-2／A-3 部分／A-4／A-5／B-2／B-4／B-5），R-143／R-145／R-146／R-147） / ISS-001 / ISS-006 / ISS-008 / ISS-011 / ISS-013 / ISS-014 / ISS-015 / ISS-030 / ISS-031 / ISS-032 / ISS-035 / ISS-036 / ISS-034 / ISS-038 / ISS-047 / ISS-046 / ISS-045 / ISS-044 / ISS-043 / ISS-048 / ISS-042 / ISS-040 / ISS-049 / ISS-050 / ISS-051 / ISS-052 / ISS-055 / ISS-056 / ISS-054（CL-A 补枚举，2026-09-24） / **ISS-053**（2026-09-24 实核关闭，CLR-4） / **ISS-057／ISS-058／ISS-059／ISS-061／ISS-062**（2026-09-24 R0 裁决 C2 成立、D-14 全链闭环） / **ISS-063／ISS-064／ISS-065**（Q-010 互校发现，2026-09-24 随修闭环，R-097） / **ISS-066／ISS-067**（Q-010 处置批闭环，2026-09-24，R-102~R-104／附 AA） / **ISS-069／ISS-070**（2026-09-24 A14 轮回收入库，R-105／附 AB） / **ISS-071**（2026-09-24 A15 轮回收入库，R-106／附 AC） / **ISS-072**（2026-09-24 A16 轮回收入库，R-107／附 AD） / **ISS-068**（2026-09-24 R0 裁决「全 A」：认可替代核验 → Q-010 关闭批，R-108） / **ISS-074**（2026-09-24 Q-036 轮机械修复入库，见该行） / **ISS-075**（2026-09-24 R0 指令：计划角色并入 R8／派发词模板／δ 复核面收窄，R-110） / **ISS-076**（2026-09-24 HANDOFF 截断事故：已自 git 恢复＋丢失条重建，R-110 备注） / **ISS-077**（2026-09-24 本地知识库写库：计划分层与角色合并入 `virtual_riscv经验`，R-111） / **ISS-073**（2026-09-24 随 ADR-S1 批闭环） / **ISS-078**（2026-09-24 L13＋S-01b 闭环） / **ISS-083**（2026-09-24 R0 指令：自动循环固化——AGENTS 规则 24＋摘要 §0 开工协议，R-121） / **ISS-084**（2026-09-24 R0 指令：角色作业守则 SOP 固化，R-122） / **ISS-085**（2026-09-24 B1 即修＋B2/B3 经 ADR-S04 裁定并落文，R-123／R-125／R-126） / **ISS-087**（2026-09-24 R0 指令："不得中途停"固化——规则 24 ⑤＋摘要 §0 第 7 条＋定时任务 prompt，R-129） / **ISS-086**（2026-09-24 B-1 经 ADR-S05-B1 裁定并落文（S-05b），R-128／R-130） / **ISS-088**（2026-09-24 S-05c 两阻塞即修：EOL 畸形＋§1 行粘连，R-131） / **ISS-020**（2026-09-24 S-09 批残余清点关闭：§4.8 四行重算已落并升级为 `width_check` 机判（乘法/加式＋合计＋禁 ≈）、§8 LR/SC 行与 `spec/01` §5.3 `SP_ck` 已落、余两项归属 Q-037/S-11；R-140） / **ISS-089**（2026-09-24 S-06 段②：三项文内更正（LR/SC μop 计数／行号漂移改引节号／§8 保留键＝PA 全比对）＋`Zama16` 声称经 ADR-S06-A / ADR-S06-B 自决并落文，R-136／R-137） / **ISS-039**（2026-09-25 闭合：ADR-1 下游义务三件全落——`spec/05` §3 F2 mux 专项／`doc/verify/02` VP-X06a~f／`spec/00` §10 回标；R-169） / **ISS-092**（2026-09-25 余项全清（C1/C4/C5/C6）＋K1 落地，R-169） / **ISS-093**（2026-09-25 δ 轮 2 新增 N4/N5 即修（fail-closed＋§3 节内作用域），R-169） |
| **合计** | **94** | ISS-054 为 D-7 首跑实测的 vlog `-ntb_opts` 不支持项（已修 AGENTS §3.1）；ISS-053 为 attempts 死代码（已实装）；ISS-049~052 为并行会话同批登记项；ISS-043~048 见各行；ISS-055 为 doc/ 分类改造（R0 指令）；ISS-056 为工作树行尾批量物质化（语义等价已验、长基线按 ADR-4 折入，见 `doc/verify/05` R-062）；ISS-057 为 D-14 章节片⑤新增 9 阻塞/4 澄清/1 存疑（附 M；C2 不成立，处置批续跑）；**ISS-058** 为 D-14 增量复核片 A1 复核发现（4 阻塞/2 澄清/1 存疑，报告＝附 N；C2 仍不成立）；**ISS-059** 为 §12.5 决策审核（七 ADR 17 行风险发现，报告＝附 O）；**ISS-060** 为 `spec/13` 性能建模输入登记载体缺失（ADR-D4-④，载体随落文批 L7 落地）；**ISS-061** 为 L7 增量复核片 A3 发现（1 阻塞＋4 澄清；D5/L8 续处置）；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环，ISS-057/058/059/061/062 同批关闭**；**ISS-062** 为 A7 终局全量（R0 指定）发现（BLK-1~4/CLR-1~4/SUS-1；BLK-1/2 与 CLR-3/4 已即处置，BLK-3/4/CLR-1/2 归 L10，SUS-1 确证＝R8 收口编辑所致）；**ISS-067／ISS-068** 为 Q-010 处置批 A13 增量复核（附 AA；六项同批闭环＋存疑-1 待裁，见各行）；**ISS-069** 为规则 22 窗口顺序缺失（A13 窗口未隔离评审者；A14 轮按纠正顺序取干净窗口后闭合）；**ISS-070** 为 A14 终局 δ 复核发现（B-1 r25 窗口段与证据件相抵／C-1 r26 基线两说／C-2 Q-010 队列字段滞后／C-3 工具措辞误标；同批闭环）；**ISS-071** 为 A15 δ 复核发现（C-14 landable 过期／C-15「删后复跑」时序失真＋留痕缺口／C-16 单槽窗口语义误述／C-17 跨篇清单①未编入 done_when／C-18 长基线 R-095 后未折入；同批闭环）；**ISS-072** 为 A16 δ 终局确认轮发现（D-1 ISS-070 行旧表述残留／D-2 HANDOFF 跨轮行内改写违头注约定／D-3 C-16 注未分槽态两支／D-4 landable 链缺 A14 段；＋存量 E-1 看板行陈旧；同批闭环）；**ISS-068** 经 R0 2026-09-24 裁决「全 A」认可 md5 替代核验足够 → 随 Q-010 关闭批闭环（R-108） |

---

## 台账

| ID | 分类 | 发现时间 | 发现步骤/用例 | 问题描述 | 证据 | 状态 | 决策人/决策 | 解决方案 | 解决时间 |
|---|---|---|---|---|---|---|---|---|---|
| **ISS-054** | 平台 | 2026-09-23 | D-7 平台骨架首跑（ModelSim SE-64 2020.4 实跑） | **`AGENTS.md` §3.1 的 vlog 模板含 `-ntb_opts uvm-1.2`，本机 vlog 不支持（vlog-1902）**——照模板会直接报错；D-7 实跑改用 `+incdir+…/uvm-1.2/src` 得 `-- Compiling` 3 行＋`Errors: 0, Warnings: 1`（vlog-159 良性） | `AGENTS.md` §3.1；`sim/run_r4_d7/vlog_compile.log` | 已解决 | 平台类，R2 直接修并登记 | §3.1 已改 `+incdir` 形式＋实测更正注；遗留 vopt/vsim 待 DUT 接入补跑 | 2026-09-23 |
| **ISS-053** | 验证代码 | 2026-09-23 | R0 复核（"attempts 字段尚未实际使用"） | **队列 `attempts` 字段从未被写入**：`gate.py` 的 `queue-attempts-cap` 判据（≥3 且 state 非 blocked/fused ⇒ FAIL）**永不触发**——`run_cmd/AutoQueue.yaml` 全表 **0 处 `attempts:`**；"派发失败可自动重派但不超过 2 次、第 3 次必须转人"的规则**无执行路径**（本会话已发生 2 次派发失败——模型服务拒绝，均只靠口头重派、未计数）。同 `gate.py` 第 58 行自警的"判据变死代码"反模式 | `run_cmd/AutoQueue.yaml`（0 处 attempts）；`script/gate.py` 的 `queue-attempts-cap`；本会话两次派发失败的处置（无计数留痕） | 已解决 | R0 2026-09-23 指出；按 R2 平台/工具类**直接修并登记** | **修法（待实装）**：①`gate.py` 新增 `attempts <Q-id> [--reset]`（递增并写回；达 3 自动置 `state: blocked`）②`dispatch` 打印"派发失败请递增"提示 ③`AutoQueue.yaml` 头注与 `doc/项目开发流程.md` §12.2 写明"派发失败（空返回/超时/被拒）⇒ R8 必须递增 attempts，第 3 次转人" ④机判补：`attempts>0` 且 state=ready 的不一致项告警。**2026-09-24 领队实核：已实装**（`script/gate.py` 的 `cmd_attempts` 子命令与 `queue-attempts-cap` 判据、`dispatch` 提示均在位）——行内原『待实装』字样按实况更正（A7-CLR-4 关闭） | 2026-09-24 |
| **ISS-001** | 平台 | 2026-09-22 | 开工环境探测 | 本机**未安装 git**（`Get-Command git` 空），方案第 11 节第 1 步的 `git init` 无法执行。当前仓库无版本管理，RTL/UVM 代码将无法 diff 追溯，违背 R3「可回溯」的落地便利性 | 探测输出：`git --`（未找到）；无 `scoop`/`choco`。**2026-09-23 更正注**（原文不改写，§6.2）：换机后实测 `git version 2.52.0.windows.1` 可用、仓库已有 3 次提交、`.gitattributes` 已在——"未安装"前提失效。**2026-09-23 二次更正注（本会话实测）**：`git` **不在系统 PATH**——新开 cmd/脚本直接调 `git` 失败，`gate.py changed` 首测即因此报"git 不可调用"；实际可用的是 **GitHub Desktop 自带 git**（`%LOCALAPPDATA%\GitHubDesktop\app-3.6.4\resources\app\git\cmd\git.exe`，实测版本 `2.53.0.windows.4`，另有 `C:\ProgramData\HHHXXX\GitHubDesktop\` 下 app-3.6.4 与 app-3.6.5 两份拷贝；上方"2.52.0"记法与本次实测不一致，以实测为准）。**可用口径**：调 git 的命令前先 `set "PATH=%PATH%;C:\Users\HHHXXX\AppData\Local\GitHubDesktop\app-3.6.4\resources\app\git\cmd"`（本会话已按此实测 `changed` 出全量改动集）；根治须由人把该目录加入系统/用户 PATH | 已解决 | R0 2026-09-23 定：取方案 ②（沿用本机 Git for Windows 纳管）；并定"每一处置轮开工前先提交一次 git 基线"，写入 `doc/项目开发流程.md` §9.4「基线纪律」 | 沿用本机 git 纳管全仓；基线纪律见 `doc/项目开发流程.md` §9.4；驱动工具 `script/gate.py changed`（实测以 HEAD 为基线输出 v0.3 的改动节与被改标识符引用闭包） | 2026-09-23 |
| **ISS-002** | 平台 | 2026-09-22 | 开工环境探测 | **WSL 无发行版**（`wsl -l -v` 返回"未安装适用于 Linux 的 Windows 子系统"），因此无 riscv64 GCC、无 Spike、无 Verilator；本机亦无 gcc/g++/make/cmake/JDK。测试程序的编译环节与 ISS-vs-ISS 互检当前全部阻塞 | `wsl -l -v` 报错并提示 `wsl --install`；`riscv64-unknown-elf-gcc --` / `spike --` / `verilator --` | 待决策 | **需人执行**：安装动作需管理员权限与一次重启，AI 不能代做。清单见 `doc/环境搭建.md` | 按 `doc/环境搭建.md` §1~§5 执行后，回填 M0 各命令版本号 | — |
| **ISS-003** | 文档 | 2026-09-22 | Step 1 规格（`doc/spec/00` 参数表自检） | **PRF 容量与书式最坏界冲突**：【超标量】p.187-188 给 `inflight_max = wdw + nEU + nLQ + nSQ`。按 VR1 基线代入 `4 + 9 + 48 + 32 = 93`，而参数表定的整数 PRF 只有 **64 项**，小于该界。经验关系 `wdw ≤ nr ≤ nROB`（`4 ≤ 64 ≤ 128`）是满足的，但**必须给出"PRF 耗尽只反压、不死锁"的论证**，否则是个真 bug 苗子 | `doc/spec/00-总体规格.md` §4 参数表；出处 `Verilog与CPU设计知识库/CPU卷/04` 第 4 章「数量」维度 | 处理中 | **2026-09-23 决策 AI ADR（B2b）：一期保持 `PRF_INT_ENTRIES=64`**，以 `spec/04` 可机判不变式 **I1~I5** 为成立条件（I1 守恒 `alloc_cnt+free_cnt==64` 逐拍／I2 分配前提"free<n 时整组停顿、禁部分分配"／I3 仅提交拍释放被覆写旧条目／I4 提交不阻塞（依赖闭包不含 free-list ＋ 有界活性）／I5 冲刷归还 `alloc_ptr==FL_ck`）；**书式 93 是"无停顿满窗口"界、非正确性缺陷**（反压为本设计一级机制）。算账：`4+9+48+32=93` 缺口 29；常驻钉住 x1~x31 共 31 ⇒ 可投机 **33**（<48/<128 ⇒ LQ/ROB/IQ 深度不可达，与 ISS-020 独立结论一致）。**回退触发**（阈值初值、由 `spec/13` 校准）：反压停顿占比 >15% 或 裸机均值 IPC <1.62 且归因 PRF；M5 实测 IPC <1.5 且 PRF 主瓶颈；或 G1 前 I1~I5 未成可机判件。扩容建议**直取 128**（=nROB、2 的幂）。下游义务：`spec/04` 落 **`INV-1~INV-5`** SVA（**B4 定名**；旧 `I1/I2` 名作废，`spec/04` 建唯一权威总表，`spec/01` §4/§8 改引）＋`doc/verify/02` 4 覆盖项＋**备选扩容切换清单**（B4 纠正：**触发判据须写死**，`paddr 6→7 bit` 全链列为附带项）；`spec/13` 加"free 空反压停顿占比"必报量。原决策列保留：原决策列保留：待定：PRF 扩到 96 / 保持 64 并补回收时机论证 / 缩小 LQ-SQ | 在 `doc/spec/04-重命名与PRF.md` 写清：①条目回收时机=提交（融合 PRF 只改状态不搬数据）；②PRF 空 → 反压分发而非停顿提交通路；③形式化不变式 `Σ未提交目的寄存器数 ≤ nPRF`，并列为 SVA 与功能覆盖项；④给出端口数推导（读 ≥ wdw×3=12、写 ≥ 发射宽度 6，面积按端口平方律） | — |
| **ISS-004** | 平台 | 2026-09-22 | Step 3 计划（激励选型） | **riscv-dv 的 `yaml/simulator.yaml` 无 ModelSim 条目**，官方只列 vcs / ius / questa / dsim / qrun；本机唯一 HDL 仿真器是 ModelSim SE-64 2020.4，直接 `--simulator questa` 会因 `vmap mtiUvm $QUESTA_HOME/uvm-1.2` 与 `qrun`/`-outdir` 语法差异失败 | `D:\IC验证知识库\_tools\riscv-dv-master\yaml\simulator.yaml`（grep 确认无 msim/modelsim 条） | 处理中 | 两条腿走路（已写进 AGENTS.md §3.3）：主用 **`pygen` 纯 Python 生成路线**规避"生成器本身要仿真器"；同时自加 `modelsim` 条目（参照 `questa` 条改写为 `vlog -64`/`vopt`/`vsim -64 -c`） | 自加条目后在 `run_cmd/riscv_dv_target/` 留下可用记录，并以 `+ntb_random_seed` 打通种子复现 | — |
| **ISS-005** | 文档 | 2026-09-22 | Step 1 规格（Sv39 章节可用性核查） | 知识库《RISC-V专项/03-特权架构与机器模式速览》明示"**§4.4+ Sv39/48/57 仅综述**"，即本地笔记**没有** Sv39 细节；VR1 要做 Sv39，`doc/spec/08` 不能只依赖该笔记 | `RISC-V专项/03` 第 24 行「本速览覆盖声明」 | 处理中 | 直接以规范全文为准：`_tools\extracted\riscv_spec\riscv_spec_full.txt` 定位 §4.4 Sv39 原文 + `_tools\riscv-spec.pdf` 卷 II 复核 | 写 `doc/spec/08` 时逐条带 §号引用，出处注明"规范原文（非笔记）"；同步在知识库《RISC-V专项》补一篇 Sv39 精读（由人决定是否入库）。**2026-09-22 本方案不可执行**：所依赖的提取件在本机已不存在（见 **ISS-032**），`spec/08` 的 Sv39 逐条原文核对暂停，相关结论一律标 `待锚点` | — |
| **ISS-006** | 平台 | 2026-09-22 | Step 3 计划（ISS 耦合方式） | ModelSim SE 在 Windows 侧做 **DPI-C 需宿主 C++ 编译器**，本机无 gcc/MSVC/msys2 → golden ISS 不能以 DPI 方式挂进仿真器 | `gcc/g++ --`、无 `C:\msys64`、无 VS 目录（探测记录） | 已解决 | 设计决策：改为**文件级解耦** | RTL 提交级 trace 由 tb 内 monitor 落文件 → `iss/tools/` 归一化为标准 CSV → 独立 Python 比对脚本；避免 DPI 依赖，且比对逻辑可复用于 Verilator 侧。已写入 `AGENTS.md` §3.4 坑位 1 与方案第 1 节 | 2026-09-22 |
| **ISS-007** | 文档 | 2026-09-22 | Step 1 规格（规范版本口径） | 本地规范为**特权 1.13**（含 `menvcfg`/`mseccfg`/双 trap 16 号等新增项），而参考实现口径不一：ibex 按 1.11、笔记提到 picorv32 中断**完全不对应特权规范任何一节**。照抄参考实现会引入版本混杂 | `RISC-V专项/03` 第 5 行（1.13）与第 124 行（picorv32/ibex 两套词汇表） | 处理中 | **2026-09-23 决策 AI ADR（B1）：锁特权 1.13 ＋ 分期实现**——本地提取件为唯一一级依据；`spec/10` 开篇立"三段式"（基准声明／CSR 总表含"**引入版本(锚点)**"列／实现对账锚），**未回锚前一律"待锚点"**；一期边界＝**实现 4 组**（M 基础/MIS/mcycle·minstret/Zicsr·Zifencei 指令面）、**不实现 8 组**（HPM 与 mcountinhibit、menvcfg/mseccfg、S/H 域、Smstateen/Sscofpmf/Smcntrpmf、双 trap、调试 CSR、mconfigptr）、**PMP 随 `spec/08`**；未实现 CSR 统一 **illegal instruction（cause=2）**（不选"读 0"以免双侧口径放宽度）；锚点＝提取件 §3.1/§3.1.12/§6.1/§6.4/§8.9 与变更记录区（一手）；残余风险＝引入版本未逐条回锚、1.11→1.13 无语义差属推断。原决策列保留：原决策列保留：待定：VR1 锁 1.13 全量 / 锁 1.11 子集（便于对照 ibex） | 在 `doc/spec/10-特权与中断异常.md` 开头立"规范版本声明"表：逐 CSR 标注引入版本与是否实现；`doc/verify/01` 功能点全部回指该表 | — |
| **ISS-008** | 流程 | 2026-09-22 | Step 1 规格（评审门语义） | 九步流程假设"规格是外部输入"，本项目 spec 由 AI 自产 → **Requirement Review 存在作者=被评审人**的自证风险，红线 R6/R8 的诚实性前提被削弱 | 方案第 7 节阶段图；《AI验证工作流》1.1 节角色边界 | 已解决 | **R0 2026-09-23 口径变更定案（ISS-042）**：Review G1 仍由人签署，但**材料＝AI 列出的评审 list**（`doc/项目开发流程.md` §9.5），人**按 list 逐项签**并**必读三处高风险**（`spec/04` 端口数、`spec/07` forwarding、`doc/verify/02` 覆盖度）；“作者=被评审人”风险改由 ①list 摊开全部取舍与依据链 ②R6/R7 对抗评审 ③锚点与机判 三条补偿（§9.2 补偿措施 1~5 继续有效） | 评审结论按"通过/有条件通过/不通过/暂停"四级落盘到各 spec 篇首 + `doc/process/00`；冻结签署写 `doc/spec/14`。**2026-09-22 缓解方案升级**：除人逐篇签署外，新增（a）R0~R7 角色读写边界与 RACI（b）信息隔离的对抗评审子代理 R7（c）关键结论必绑外部锚点——详见 `doc/AI角色与职责.md`；已写入流程文档 §9.2 第 5 条与 `AGENTS.md` 规则 19~21 | 2026-09-23 |
| **ISS-009** | 平台 | 2026-09-22 | Step 4 平台（构建入口） | 本机**无 make/cmake**（Windows 侧），而知识库模板与方案均以 `make regress` 为回归入口；直接写 Makefile 在 PowerShell 下不可用 | `make --`、`cmake --` | 处理中 | 入口改为 **Python driver**（`script/regress.py`，Python 3.14 已在）+ `run_cmd/*.ps1` 薄封装；Makefile 仅在 WSL 侧用于 Spike/Verilator 构建 | 平台搭建时交付 `script/regress.py`（含 R9 watchdog、R10 并发与独占沙箱、T1/T5/T6 终止判据），并在 `AGENTS.md` §3.1 命令区同步 | — |
| **ISS-010** | 文档 | 2026-09-22 | Step 1 规格（`doc/spec/00` §2/§8） | **非对齐访问两侧口径与方案示例命令不一致**：方案第 1 节给出的 Spike 命令带 `--misaligned`，而 `spec/00` 定下的口径是一期**不支持非对齐**（报 address-misaligned）。若 ISS 开了 `--misaligned` 而 RTL 不开，非对齐激励会产生“ISS 能跑 / DUT 报 fault”的**假 DUT bug**。现改口径：一期 `MISALIGNED_EN=0`，两侧**均不开** `--misaligned` | `spec/00` §2 口径统一段、§4.7 `MISALIGNED_EN=0`；与 `AGENTS.md` §3.2 原示例命令对照 | 处理中 | 技术决策已定（以 spec 为权威源，同步修订 AGENTS.md 示例）；若三期要支持硬件非对齐拆包，需重过 Requirement Review | 已同步修订 `AGENTS.md` §3.2 的 Spike 行（删除 `--misaligned`）；`iss/` 与回归脚本的 Spike 模板一律不带该选项 | — |
| **ISS-011** | 验证代码 | 2026-09-22 | Step 1 规格（`spec/01` §3.20 草稿自查） | `spec/01` 初稿把 `ls_complete_t.lqid` 写成 5 bit，但 `spec/00` 定 `LQ_ENTRIES=48`（非 2 的幂）→ 索引需 **6 bit**（环形指针 7 bit，见 `spec/01` §2 `lqptr_t`）。位宽不足会造成 LQ 回抄错项一类的隐蔽 bug | `spec/01` §2 类型表 vs §3.20 字段表 | 已解决 | 自查发现，属规格文档自身位宽错误，直接修正 | 已改 `lqid[5:0]` 并在 `spec/01` §3.20 写明依据；`spec/01` §8 自检表同步 | 2026-09-22 |
| **ISS-012** | 文档 | 2026-09-22 | Step 1 规格（`spec/01` §1.1 时序推论） | **ALU→ALU 最短依赖间距 3 拍与 IPC ≥ 1.8 目标存在张力**：`spec/01` 采用“后读 + 唤醒/选择分离”（WB 拍广播唤醒、次拍选择发射，依据【超】p.221-223），代价是串行依赖链每拍只能推进一步；若乱序窗口填不满空洞，IPC 会系统性低于目标 | `spec/01` §1.1“关键时序结论”段；`spec/00` §9 IPC 目标 | 处理中 | **2026-09-23 决策 AI ADR（B2a）：保持 ALU→ALU 依赖间距 3 拍**（候选②2 拍降级为带量化触发的回退/抬档项；候选③"前读＋加宽读口"否——与 §4.3 已定"后读"依据正面冲突且不压缩链）。一阶 IPC 模型（参数全取 `spec/00` §4）：**达标线**＝C≥6（任意 f）／f≤86%(C=5)／f≤58%(C=4)；纯链段需 C≥5.4（2 拍仅需 3.6）；**触发**：T1＝`spec/13` 模型或 M5 实测均值 IPC < **1.71** → 翻；T2＝f>60% 且 C∈[4,5] → 翻；命中即 G1 前升版翻选（不动位宽/端口，可逆）。残余：C/f 无实锚点（已绑定 T1/T2 兜底）、2 拍时序风险无 STA 证伪。下游义务：`spec/13` 含 C/f 测量与两方案对比；`spec/01` §1.1 加 ADR 索引与回退注；`spec/00` §1 抬档项②注明"与回退同路径"。原决策列保留：原决策列保留：待 `spec/13` 周期模型给出定量 IPC 再定；候选回炉：① 唤醒同拍完成选择（2 拍，时序风险）② 改前读 + 加宽 PRF 读口（面积代价，与 ISS-003 相互牵连）| 已写入 `spec/00` §10 风险表与 `spec/01` §1.1；`spec/13` 交付时必须给出两方案 IPC 对比数据供人裁决 | — |
| **ISS-013** | 文档 | 2026-09-22 | Step 1 规格（ISS 实现时发现） | `doc/spec/01` §3.21 的 `rt_t` 只有 `csr_old`，而 Spike commit log 与 riscv-dv trace CSV 的 `csr` 字段记的是**写后新值**。只给旧值时 CSR 行为无法直接对查，只能靠后续读回间接推，会漏掉 csrrw 写错值这类 bug | `iss/vriss/trace.py` 与 spike_log_to_trace_csv.py 对照 | 已解决 | 以规格为修正对象（不是改检查器迁就） | `spec/01` §3.21 新增 `csr_new`（总宽 490→554 bit；**2026-09-23 更正留痕，N17③：此为当时值，后补 `seq[11:0]` 12 bit，现值 543**，见评审记录 Q4 落点与附 E）；`trace.py` 的 `csr_field()` 改报新值；`trace_compare.py` 默认比对列含 `csr` | 2026-09-22 |
| **ISS-014** | 验证代码 | 2026-09-22 | ISS 冒烟首跑 | `machine.py` 把 I/S/B/J 四类立即数统一用“按 bit63 判符号”的 `_s()` 扩展，**没有按各自位宽（12/12/13/21）扩展** → 所有负立即数（`ADDI rd,rs1,-1`、负偏移访存/分支）全错；实测 `x9=0xfff` 而非 `0xffff_ffff_ffff_ffff` | 冒烟输出 `x9 = 0xfff，期望 0xffffffffffffffff` | 已解决 | 自查发现，属 ISS 自身错误，直接修正 | 改用 `consts.sext(v,bits)` 逐类扩展；`ADDI(9,0,-1)`、`BEQ`、`JAL` 均转为正确 | 2026-09-22 |
| **ISS-015** | 验证代码 | 2026-09-22 | ISS 冒烟二跑 | `encode.py` 对超范围立即数做 `& mask` **静默截断**：`ADDI(rd,rs1,0x1000)` 编成 `+0`（十进制 12 位字段装不下）。表现为“程序不停机”这种极难归因的假故障；后续手写定向用例时必再踩 | 冒烟：改 TOHOST=0x4000_1000 后仍不停机；另 `0xDEAD` 也被静默截断 | 已解决 | 技术决策：宁可报错不可静默 | 加 `_chk(val,bits,name)` 范围检查（I/S 12、B 13、J 21）+ B/J 偶数偏移检查；`LUI/AUIPC` 统一改为接 20 位高位立即数（消除量纲歧义）；检查当场拦下两处自身用例错误 | 2026-09-22 |
| **ISS-016** | 文档 | 2026-09-22 | ISS 冒烟三跑 | **tohost/signature 窗无后级支撑**：`consts.PMA_TABLE` 把 `0x2000_0000` 窗标为 non-cacheable 且未挂设备，对其做普通 store 会正确报 store-access-fault（模型行为合规，但会让所有用 `tohost` 收班的 bare-metal 用例全部进 trap）。需在 `doc/spec/09` 明确：该窗必须挂 signature sink 设备，且 `sw/env` 的 tohost 段必须落在有支撑区 | 冒烟首轮 `mcause=1, mtval=0x100` 死循环 trap 的排查记录 | 处理中 | 暂时把 tohost 改到 DRAM 区（`0x4000_0010`）跑通；正式方案待写进规格 | `doc/spec/09` 补“signature/tohost 窗设备挂载 + PMA 属性”一节，`sw/env/link.ld` 的 tohost 段位置与之绑定 | — |
| **ISS-017** | 文档 | 2026-09-22 | 学习知识库（《开源IP导读/03-ibex架构导读》+ `_tools\ibex-master\rtl\ibex_top_tracing.sv` 实测） | **退休接口选型：自定 `rt_t` 还是 RVFI 兼容接口**。本地 ibex 源码已确认采 RVFI 标准集（`rvfi_valid/order/insn/trap/halt/intr/mode/ixl/rs1-3_addr+rdata/rd_addr/rd_wdata/pc_rdata+wdata/mem_addr/rmask/wmask/rdata` + `csr_*`）。`doc/spec/01` §3.21 目前的 `rt_t` 是自定名，字段语义与 RVFI 高度重叠但不完全对齐 | `ibex_top_tracing.sv` L143-L169 信号声明；`doc/spec/01` §3.21 | 处理中 | **2026-09-23 决策 AI ADR（B1）：不采纳"全量 RVFI 兼容"、采纳"兼容性预留"**——**`rt_t` 保持 543 bit 不变**；`spec/01` §3.21 增**映射附录**（缺失/多余/语义不符三清单＋触发条件＋二期路径）。成本账：RVFI 经典行宽 647 bit、`rt_t` 缺 285 bit（其中 82 bit 可由 tb 监视器吸收）⇒ **核内净增 ≈203 bit**（操作数快照须自读口携至提交点），而一期无 RVFI 主消费方（工具链清单内无 yosys/sby 类 formal）⇒ 不扩接口。**触发条件**：formal 或第三方 RVFI 工具立项时重评（**R0 口径提示**：若认定 formal 在 M5 后"路径 A"范围内，则"无消费方"前提失效、应翻选全量）。锚点局限：riscv-formal `rvfi.md` 网络抓取失败（记 URL+日期），字段全集按"经典 21 信号"为基线并逐条标 **[待复核]**。**待写 §3.21 附录**（下一批落）。原决策列保留：原决策列保留：**建议采纳 RVFI 兼容超集**（保留 `rt_t` 名字与现有字段，另按 RVFI 命名对外映射一层），理由：① 可直接喂 riscv-formal（《AI验证工作流》不适用清单里的“形式验证”短板得以补齐）；② 复用现成 RVFI→CSV/比对工具；③ ibex 已验证该接口在 RTL 侧可落地。代价：`spec/01` §3.21 需升版并重过 Requirement Review | 已写入 `doc/项目开发流程.md` §2.4、§10 第 1 项；等人拍板后改 `spec/01` | — |
| **ISS-018** | 文档 | 2026-09-22 | 流程试点：R7 信息隔离对抗评审首轮（针对 `doc/spec/01` v0.1） | **作者在 `spec/01` §8 出口自检表写下「合计值已逐项相加核对 ✅」等四项 ✅，但当时从未执行过相加**。独立评审实测：13 个 struct 里 12 个合计与成员位宽之和不符（差 1~34 bit 不等，作者已独立复算确认评审者全对：`uop_t` 实 238/写 251、`rob_alloc_t` 实 140/写 125、`rob_entry_t` 实 135/写 122、`rt_t` 实 531/写 554 而 §6 又写 490）。同时共报 **21 项阻塞 / 15 项需澄清 / 3 项建议**，含三类真漏洞：回滚与 ROB 释放互相拆台（Q17）、CSR 写数据通路在冻结接口上断开（Q3）、trace 只有 VA 导致翻译/PA 错误两侧都看不见（Q19） | `doc/review/01-评审记录.md` 全文；作者复算记录于本条 | 处理中 | 技术决定：① `spec/01` 降为 v0.2 返工中，**不再是 G1 冻结候选**；② §8 自检表整体作废并保留更正痕迹；③ 新增自检纪律——**无复核手段不得打 ✅**，可机器判定的事（位宽相加/参数名一致/值域引用）一律写脚本；④ 下一轮对抗评审**先查 C 类（死锁/假绿/漏测）**，算术错反而不是最危险的 | 返工完成 + 位宽自检脚本输出留档后转【已解决】（登记于 `doc/spec/01` §8）。**2026-09-23 第 3 轮纯复核轮（r7-adversarial-reviewer）实测：本条声称的返工未发生**——评审记录 38 条仅 Q21 一条落实，**未落实 36 条**，v0.2 头部变更摘要声称的 6 项核心改动（`csr_wr_t` 补 wdata、`disp_x_t` 补 4 域、映射历史栈、NMI 移出、trace `exc_v` 规则、PA 盲区）在正文**无一处落点**；并新增算错 13 项（`ftq_entry_t` 写 149 实 151、`wb_t` 写 79 实 80、`disp_x_t` 写 293 实 265、`icache_rsp` 写 427 实 393 等）+ 5 个死锁/假绿/漏测场景（最重：DIV 冲刷"取消在途迭代并释放 busy"与"不可中途取消"直接矛盾 → 可致全停）。复核数值与本条第一轮已记录的 251/238、554/531/490、125/140 逐项一致 ⇒ 结论可信，**正文仍停在 v0.1 状态**。**已触 §6.1 熔断（3 轮未闭），转人裁决返工方式（队列 Q-004 置 fused）**。**处置收窄（R8，2026-09-23，见 `doc/verify/04` 与 D-14 链）**：本条两项闭合要素已分别由后续链承接——①「返工完成」＝`spec/01` v0.2/v0.3 重建＋D-14 全量轮修复链（**C2 成立即满足**）；②「位宽自检脚本输出留档」＝`script/width_check.py` 常驻门禁＋历轮输出已入 `doc/verify/05`（R-002/R-005 等）。**余项＝C2（D-14 出口判定）＋读点口径（SUS-01）待 `spec/04` 收口**；口径互斥（R-057 的 C-1）其 spec 侧两处已由 L1（`spec/01`）与 L4（`spec/00` §10 行）对齐，**本条与该两处自此相互一致**。**C2 成立时本条转【已解决】。** | — |
| **ISS-019** | 验证代码 | 2026-09-22 | 第二轮 R7 对抗评审（目标：golden ISS 语义），评审者实跑探针脚本取证 | **golden ISS 共 31 项问题（会掩盖 bug 15 / 会导致错判 16）**。最致命三项：① `riscv-tests` 的 PASS/FAIL **判定方向相反**（`halt_code=data>>1` 与 `==1` 判通过 ⇒ 写 1 报失败、写 3（=test 1 失败）报成功）；② `MRET/SRET/WFI` 三条编码与官方 `encoding.h` 不一致 ⇒ 工具链编的 `mret` 被当成 SRET 抛异常，而自造编码能跑（所冒烟“假绿”的真成因）；③ trace CSV **完全没有 `exc_v/exc_cause/mem_*` 列** ⇒ 异常类别在整个比对链上不可见。另：SRA 算成 SRL、SRAI/SRAIW 判非法、LBU/LHU 读 16/32 字节、MULW 族全判非法、`jal x0,0` 自旋被误推一条 | 评审探针输出（逐条带实跑值）；锚点 `_tools\ibex-master\vendor\riscv-test-env\encoding.h` 与规范提取件 | 处理中 | 技术决定：本轮只修“会反转红绿灯”的 8 类，其余按整批返工，不做零敲碎修 | **已修并回归**：tohost 判据方向、MRET/SRET/WFI 编码、SRA/SRAI/SRAIW、LBU/LHU/LWU 宽度与对齐、MULW 族、BRANCH 保留编码 KeyError、`jal x0,0` 自旋、MPP 保留值崩溃、`link()` 空洞改填非法字。**新建 `iss/tests/test_isa_semantics.py`：23 项断言全过（官方指令字写死为常量，避开“自编自解”自洽）**；余 13 类列为 `KNOWN_OPEN` 并随每次运行打印，**关闭前 ISS 不得作为 golden model 参与签核** | — |
| **ISS-020** | 文档 | 2026-09-22 | 第二轮 R7 对抗评审（目标：`doc/spec/00` 参数表与资源约束），共 35 项 | **参数不自洽 / 不可达类 16 项阻塞**，关键四条：① 融合式 PRF 64 项中 **31 项被架构常驻值钉住** ⇒ 可投机写回只剩 **33** 项 ⇒ `LQ=48`、`ROB=128` **不可达**，且 IPC≥1.8 与参数量级矛盾；② `FWD_MAX_SOURCES=2` 在“store 只在提交后写 cache”下是**真死锁**，而 §9 把它当成“截断次数”统计量（评审原话：把死锁登记成指标）；③ `FWD_CMP_BITS=12` 不比页号/PPN ⇒跳页同偏移误转发，LR/SC 同病；④ §4.8 SRAM 表 9 行中 6 行不符或不可复算，§6 冻结清单里 **14 个类型在 spec/01 无任何定义** | 评审逐项重算；本地规范提取件实测版本为 **20260922 Intermediate**，非“卷 II 1.13” ⇒ 全库【权】§号锚点版本口径不实 | 已解决 | **R0 2026-09-23 裁定：转决策 AI**（参数取值属 §12.2 自决面；原挂"需人定两个数"作废）→ 已派 Q-016/S2（四项关键：PRF 常驻钉住下的窗口口径／`FWD_MAX_SOURCES` 死锁／`FWD_CMP_BITS` 跳页同偏移／§4.8 SRAM 重算口径）；**本条闭合即解锁 Q-002（spec/04）与 Q-003（spec/00 v0.2）**。**【ADR-S2 已出（2026-09-23）】**：①窗口不可达→**收窄宣称、数值零改动**（容量上限口径＋`IPC≥1.8` 附条件"驻留≲25 拍"）②`FWD_MAX_SOURCES` 保持 2 但**废"截断"改"仅更老源＋超限延迟重试"**（消死锁，§9 指标改 `fwd_blocked_replay`）③`FWD_CMP_BITS` **12→44**（PA 全宽含 PPN；LR/SC 同键）④§4.8 **禁 `≈`/`…`**、行归篇复算，**未达标 4 行（TAGE/L1D/L2/合计）**；另 **`HIST_DEPTH`=`ROLLBACK_MAX`=33**（随 PRF 联动：128⇒97/7 bit；`SP_ck` 6 bit 落 `spec/01` §5.3）；**S1/SUS-01 维持转 `spec/04`**（本轮不裁）。**落地进度**：§4.3（含 ADR-S2-1 注）／§4.6 两行／§9 行已落；**待落**＝§4.8 四行重算（归 Q-003）、§8 LR/SC 行、`spec/01` §5.3 `SP_ck`、`spec/01` §7 新断言（等待集⊆更老）、`width_check` 扩件（R8 面）。**另按 §12.5 需送 auditor 一次审核（非决定性）**。原决策列：需人定两个数：PRF 扩到多少（建议 ≥ `ROB − 架构常驻 31 + 裕量` ≈ 100）与 forwarding 源数（建议按最大重叠 8 或改为字节拆分归约）；同时定【权】锚点版本基线| **2026-09-24 S-09 批残余清点（`doc/verify/05` R-140）→ 关闭**：①§4.8 四行重算＝**已落并升级为机判**（`width_check.py` 扩件：逐行乘法/加式＋合计＋禁 `≈`/`…`；双向验证过）②**§8 LR/SC 行**＝已落（S-06：保留键 PA 全比对更正注）③`spec/01` §5.3 `SP_ck`＝已落（6 bit，该篇 H-04 段）④`spec/01` §7 新断言（等待集⊆更老）＝**列缺归属 Q-037**（spec/01 后续对账片）⑤`width_check` 扩件＝**本批实装**（分式/加式＋总计三重判据）；⑥「§6 冻结清单 14 类型缺定义」＝**覆盖面核销**（原 14 项枚举记录不可回查；依据 D-14 章节片①⑤＋A12 三轮全量：缺失型清单为空；如后续轮再列具体项按新发现处理）⑦§12.5 送审（非决定性）＝转 S-11 送审清单（随 Q-028/Q-031 一并派 auditor 一次）⑧参数四项（PRF 窗口/FWD 源数/`FWD_CMP_BITS`/§4.8 口径）＝ADR-S2 已落并于本批复核确认。**全项有终态 ⇒ 关行** | 2026-09-24 |
| **ISS-021** | 流程 | 2026-09-22 | 同上（对第一轮处置的回头核查） | **第一轮评审记录的“处置”列本身不可信**：已批的三个修正（`VA_STORE_W`、`rob_entry_t.fdirty`、ROB 位量 15,616→17,408）**一个都没落到正文**；且两条处置自身是错的：写“`exc_v=1` 条目仍逐条比”但 CSV 压根没有异常列（不可执行）；写“IALIGN=32 下 `mepc[1]` 同理”但一期使能 C 使 IALIGN=16，该前提不成立 | `spec/00` §4.8 与 `spec/01` §3.7 正文 vs `doc/review/01-评审记录.md` Q8/Q22 处置行 | 处理中 | 技术决定：处置列也要有“落实证据”才能算闭环 | 新增硬规则：**处置分为“已接受”与“已落实”，只有附落点（文件+节号/脚本输出）的才算闭环**；`doc/verify/09` 一致性清单增加“评审处置落实率”一行；本条与 ISS-018 同源（无手段的自证） | — |
| **ISS-022** | 流程 | 2026-09-22 | 多 AI 角色可执行性验证轮（R7 两轮评审后的边界核查） | **子代理的“只读”约束仅靠提示词承诺，无机械保障**：两名 R7 评审者在仓库 `script/tmp/` 新建了 **34 个探针与输出文件**（提示词明写“不得修改仓库文件”）。更危险的是 `script/tmp/` 被 `.gitignore` 排除 ⇒ **评审证据（ISS-019/020 的全部实跑凭据）会随清理丢失**，直接违背红线 R3「结论可回溯到具体 log/代码行」 | 目录清单比对（登记 23 个文件 vs 实际 57 个，多出 34 个全在 `script/tmp/`）；mtime 窗口比对证明**无任何既有文件被改** | 处理中 | 技术决定：写禁令必须配机械验收，不能只写在提示词里 | ① 证据升格：29 个迁 `iss/tests/probes/`、5 个迁 `doc/review/01-评审探针/`（移出忽略区）；② `AGENTS.md` 新增规则 22：**每轮评审后跑“仓库清单比对 + mtime 窗口”核查并记 `doc/verify/05`**；③ `doc/AI角色与职责.md` §8.3 已将该障碍写为可执行性结论的一部分 | — |
| **ISS-023** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/02`） | **R1 缺「受控放宽」第三条路**：`AGENTS.md` §5.1 现口径为「G1 冻结后 `rtl/` 绝对只读、修 RTL 永远是人在人的明确指令下的单独动作」，无任何受控放宽模式——一旦接口冻结后遇到「缺陷必须改 RTL 才能继续验证」，流水线只能停等。知识库给出可复制的**五约束协议**：① 任务限定给单一角色 ② 依据前置（无规格/台账依据不改）③ 两道客观门＝编译 0 错 + `not bound` 计数 0，且须与基线对照（实测形态「mismatch 4→0」），**没过门不得宣布完成** ④ diff + 时间留痕 ⑤ 只落本地副本、不回写不自动提交。配套工程点：`rtl/` 必须是**独立实体拷贝**（若为 junction 会污染设计方仓库），解联要守卫式（先验 REPARSE 位 + 目标路径、非递归删除、复验文件数不变），拷贝后逐文件 md5 留证 | 出处 `D:\IC验证知识库\virtual_test经验\02-R1受控放宽-RTL修改协议.md`；对照 `AGENTS.md` §5.1（全文无该模式） | 待决策 | 待定：VR1 是否采纳。一期 G1 未达，暂不紧迫；但基线机制一旦触发就需先定好 | 若采纳：写入 `doc/项目开发流程.md` 门禁节 + `AGENTS.md` §5.1 细化；**必须与 R5（基线后先批后改）合并口径**，避免两条规则互相打架 | — |
| **ISS-024** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/05`） | **基线指纹判据过粗，会被纯 mtime 漂移刷红**：规则 22 只规定「仓库清单比对 + mtime 窗口」核查。知识库实测：拷贝/同步/junction 改实体只动 mtime，整体比较三元组会触发假「修改 448 项」（另 same 49，**内容实际零变更**）。给出**三方对比定性法**：now-vs-base 全为 mtime-only 且 src-vs-base 全为 same ⇒ 假告警；一旦出现 content-diff ⇒ 真变更，停报。**禁止报红就刷基线**；刷基线正当程序＝定性→备份→init-baseline→check 复验（目标告警消失且其余计数不变）→登记。VR1 当前「未基线」，正是定义基线指纹判据的窗口期 | 出处 `virtual_test经验\05-门禁完整性基线实务.md`；对照 `AGENTS.md` 规则 22 与本表 ISS-022 | 处理中 | **2026-09-23 决策 AI ADR-4（R0 已放行）**：判据＝指纹逐文件存 `md5:size:mtime` 三元组，**只看内容侧（md5/size）计"修改"，纯 mtime 差单列 INFO**，**新增/删除恒计入**（防 ISS-022 型"只治旧改漏新建"）。落地：`script/gate.py` 新增 `snapshot`/`rocheck` 子命令＋快照 `script/gate/integrity_baseline.json`（受版本控制）；**双用途分开**——规则 22 只读核查＝窗口制（含忽略区，仅排 `.git/`、`__pycache__/`）；换机/完整性校验＝受控目录集长基线（**每轮必改件如 `doc/process/00/04/05`、HANDOFF、队列不入长基线**，防假红）。关键推演：探针件每轮为新文件名 ⇒ 只落 ADDED、**不误报 MODIFIED**；权威锚点＝知识库 `virtual_test经验/05-门禁完整性基线实务.md` §2.1/§4。**下游义务（待实施）**：①`script/gate.py` 两子命令＋首轮记实测耗时 ②规则 22 措辞改"内容指纹窗口＋mtime 列 INFO＋新增/删除恒计" ③`doc/verify/10` 模板（建立时）④`doc/交接说明.md` 换机校验段 ⑤`doc/verify/05` 留痕格式加四分类计数 ⑥**按 §12.5 送 auditor 一次审核**。排入 **Q-028**；实施并首跑后回填【已解决】。原决策列：**2026-09-23 R0 裁定：放行（人批已给）＋转决策 AI 自决**——本条原受"监督通道自身改动须人批"约束，R0 已明确放行；按 ISS-042 转决策 AI 出 ADR（含 md5:size 判据的落地形态与回退）。原决策列保留：待定：基线指纹是否改为「`md5:size` 才计修改、纯 mtime 单列 INFO」（知识库自身亦标注该改动属「监督通道自身改动，须人批」）| 若采纳：写入 `doc/verify/10-基线记录.md` 模板与规则 22 核查条款；换机后（`doc/交接说明.md` 校验命令）首跑必做一次三方对比定性 | — |
| **ISS-025** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/13`） | **`vsim -do` 三种静默假成功，EXIT 码不可作判据**：① `-do "..\x.do"`（双引号+反斜杠）→ 反斜杠被 Tcl 吞 → `invalid command name`，0 条命令执行；② `-do "../x.do; quit -f"`（裸路径）→ 被当外部程序 → `couldn't execute...no such file`。两种情况 **EXIT 均为 0**，日志仅 137/163/247 字节、无 `-- Compiling` 行。若以退出码判「编译成功」，会把环境故障送进归因链伪装成 DUT bug（正是 R8 要防的「跑完没 error = 通过」）。可靠写法：`-do "source ../script/build/x.do; quit -f"`（**显式 source + Tcl 系内一律正斜杠**）；判据改用**产物证据行**（`-- Compiling` 行数 + 段末 `Errors: N`）；`-quiet` 与取证互斥（吞掉逐单元回显）。VR1 §3.1 命令模板与 `script/regress.py`（ISS-009）尚未建立，属**必须先吸收再写脚本**的前置项 | 出处 `virtual_test经验\13-vsim批处理do加载姿势与假成功.md`（该库最新一张）；`AGENTS.md` §3.1 现模板未涉及 | 处理中 | **2026-09-23 口径变更：转决策 AI 自决**（ISS-042；纯技术判据，明确不触 R6/R7）。**ADR-5（S3，已定）：三档证据行＋退出码仅旁证**（编译档 `-- Compiling` 计数＋`Errors: N`；装载/执行档须正向证据，缺则 FAIL_ENV＝假成功；禁 `-quiet`；判据唯一实现＝`script/regress.py`；`run_cmd/*.do` 改 source 式）。原建议保留：**建议采纳**（纯技术判据，不放宽任何检查口径，不触 R6/R7） | 建 runner 时写入 `AGENTS.md` §3.1 与 `doc/项目开发流程.md` 平台交付判据；`regress.py` 的 PASS 判定必须含「产物证据行」正则，且与 ISS-009 交付同批 | — |
| **ISS-026** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/08`） | **编辑工具会静默替换中文形近字并注入零宽字符，且 diff 视图不可信**：实测形近字替换（U+9AA8「骨」→ U+9AB8/9AB7）与伪空格注入（U+200B/200C/200D/FEFF/00A0/3000），且 **diff 里「新旧都错」**——靠看 diff 自检发现不了。VR1 主体产出是 14 篇中文规格 + `doc/verify/01~10`，而 ISS-018（作者自检打 ✅ 但从未相加）与 ISS-021（处置列自身不可信）已两度发生「无手段的自证」危机，同属一类。防线：批量写中文后**扫磁盘字节**（形近字表 + 零宽 + CJK 扩展区），归零才算完成；关键标识用 `\uXXXX` 码点直写；**独立回读不信 diff 视图**；跨库同步前后各扫一遍 | 出处 `virtual_test经验\08-AI中文文档码点防线.md`；本项目同源风险见 ISS-018 / ISS-021。**2026-09-22 登记本条时实测复现**：写完 ISS-023~030 后扫磁盘字节，发现 **4 处静默错字**（3 处「扫」U+626B →「扇」U+6247，1 处「环境」→「环环境」多出字符），而 **diff 视图未暴露**；同时 `✅` 在 PowerShell 控制台显示为 `?` 但文件内 5 处 U+2705 完好——**反向坐实“必须扫字节、不信视图”**；零宽字符扫描为 0。后续追加 ISS-031 时**再犯 2 处**（「脱钩」误作「脉钩」、「缺陷」误作「脉陷」），经码点复核 **至少这 2 处系产出方生成时选错码点，不能归因于工具静默替换**——因此本条结论修正为：**无论成因是工具替换还是作者选错码点，人眼与 diff 视图都拦不住，只有落盘后的字节扫描能兜**（“建议采纳”的理由由「工具会替换」改为「产出方无法自证无误」，论证更强而非更弱）**2026-09-22 同步 AI索引时再犯 1 处**：总览行把「经验」U+9A8C 写成「经**骍**」U+9A8D（**相邻码点**，同样出在本人生成的 `\uXXXX` 转义里），且这次靠人工回读才发现。**同时方法层得到两个修正**：① **单字级形近字表不可用**——本轮用「扇/脉」单字表扫全库时，「**脉冲**」的「脉」被误报为正词冲突，证明必须降到词组级；② 可行实现改为「**新增文本 × 全库字频基线**」：以知识库 130+ 篇 md 统计字频，新增段落中**全库出现次数 ≤ 3 的汉字**一律列为嫌疑（`骍` 频次 1 会被自动抓住；`一把梭` 的「梭」频次 2 也会被列，人工判一次即放过）。建议采纳时以此法为首选实现，形近字表降为辅助 | 处理中 | **2026-09-23 口径变更：转决策 AI 自决**（ISS-042；输出物＝自检脚本，属工具/流程类）。**ADR-8（S4，已定）：升为 G1 冻结材料必附件（不新设门禁）**——①冻结包必附：`codepoints` 0 FAIL ＋ **`rare-chars` WARN 逐条处置表**（看回/豁免/理由）②扩三处：**扫描面收口**（补 `doc/review/*-评审探针/` 与 `debug/`）／**坏词表维护责任**（R8；加行须绑当次事故 ID、随该 doc/process/00 行同批提交、变更记 `doc/verify/05`）／冻结时 WARN 逐条处置义务 ③与 ISS-018 **已事实合批**，关闭口径同挂 G1 冻结包同一验收事件（硬纪律改写点名 `gate.py codepoints`＋`width_check.py` 两脚本）。**不建议**升格：低频字作 FAIL／单字形近字表／bigram 门禁（三层已被实测否证）。原建议保留：**建议采纳**（成本≈一个自检脚本，收益覆盖全部中文产出） | 若采纳：脚本进 `script/`，与 ISS-018 处置项③的位宽自检脚本**同批交付**；规则并入「打 ✅ 前必跑脚本」硬纪律（ISS-018 ④）。**检测器设计据本条 7 次实测收敛为三层**：① **字频法**（全库建字频表，新增文本中全库频次 ≤ 2 的字列嫌疑）——精度最高，实测能自动抓住 `骍`；② **关键术语定点断言**（预先声明本篇必含词及其次数，如 `锚点`/`扫描`/`已落实`/`评审`）——能抓住 `锚→锈` 这类**字常见而词错**的情况（`锈点` 全文零次即报）；③ **bigram 语料比对经实测不可作门禁**：本条自查对 27 行报出 **318** 个候选（中文无词边界、术语不在语料内），噪声率不可接受，只可当人工扫读辅助。至于原防线里的**单字级形近字表**：因「脉冲」误报已证明不可独立作判据，降为②的词表素材 | — |
| **ISS-027** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/07`） | **ModelSim 泛参覆盖存在两种完全无感的失效形态**：`-g<name>`（裸名）只覆盖「未显式 map 传值」的实例；`-G<路径>/<name>` 才连显式 map 一并覆盖、可穿透深链。失效侧：路径写错仅一条警告 `vopt-3040` 后继续；**类型不匹配完全静默，0 警告 0 报错**。生效验证法＝正向微探针三态 + **反向注入错误值 → 预期成批 `not bound`**（实测 `-G/.../TECH=0` → 1753 条 `vsim-3473`）。VR1 将以 `doc/spec/00` §4 参数表做配置扫描（`MISALIGNED_EN`、`LQ_ENTRIES` 等），不配这套验证 = 扫的是「以为的配置」而非实际配置 | 出处 `virtual_test经验\07-ModelSim泛参命令行覆盖.md` | 处理中 | **2026-09-23 口径变更：转决策 AI 自决**（ISS-042；平台纪律属工具/流程类，不放宽判据）。**ADR-6（S3，已定）：回显判据＋反向注入双要件**（tb 新增 `vr1_cfg_echo` 读 DUT 例化后实际值，time-0 回显供逐字段比对，不一致⇒`FAIL_ENV_CFG` 且该轮扫描作废；变更时必做反向注入——"没报错≠生效"；覆盖固化进 `.do` 单一真源）。原建议保留：**建议采纳为平台纪律**：每加一个可覆盖参数配一次反向注入；覆盖项固化进 `.do` 作单一真源 | 随 ISS-025 一并写入 §3.1；参数扫描矩阵在 `doc/verify/03-验证计划.md` 中列出 | — |
| **ISS-028** | 平台 | 2026-09-22 | 学习知识库（`virtual_test经验/11`） | **「行为一致但超时」须先查日志量，勿先怀疑 DUT**：根因为 `numeric_std` metavalue 警告洪流——每轮 113,200 行 / 19.3 MB；在 `vsim -do` 序列**最前**加 `set NumericStdNoWarnings 1;` 后实测单模式 74.2→30.6 s、日志 19.3→0.65 MB、批 208.9→97.8 s、6/6 PASS。定性：这是**日志开关而非检查器放宽**（不触 R6/R7），但抑制后必须复核证据行 / bins / 结论不变。关联风险：R9 规定 ModelSim 挂钟上限 20 分钟/用例，警告洪流会把它伪装成 watchdog 超时并按 R2 停报，属可避免的空转 | 出处 `virtual_test经验\11-仿真日志噪声治理.md`；对照 `AGENTS.md` 规则 9 | 处理中 | **2026-09-23 口径变更：转决策 AI 自决**（ISS-042；日志噪声治理属工具类，且须"抑制后复核证据不变"防退化为降噪过关）。**ADR-7（S3，已定）：族级开关×单一真源×逐条登记（禁大类正则白名单）**；抑制后**三证据**——开/关日志逐字 diff 非空即 FAIL_ENV／同轮 ucdb 逐 bin hits 不变／判定与超时不动；**白名单变更不得发生于失败判定轮内**。原建议保留：**建议采纳**，附「抑制后复核证据不变」的自检要求，防止退化为变相降噪过关 | 建平台时写入 `AGENTS.md` §3.1 仿真命令行；`doc/verify/05` 记抑制前后各一例的日志体积与结论对照 | — |
| **ISS-029** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/06`） | **R10 终止判据缺「环境故障」档，许可/加载失败会被算成用例 FAIL**：VR1 现有 T1/T5/T6 覆盖编译加载失败计数、FATAL 占比、watchdog 超时，**全部计入用例结果**。知识库做法：**两张词表故意不交叉**——许可专串表（`License Issue` / `Failure to obtain...simulation license` / `Cannot find license file` / `Invalid host.`）vs 通用表（含 `Error loading design`）；`Error loading design` **故意只入通用表**（许可失效与编译错误都打这条，入许可表必误判），判定顺序先许可后通用；批级 `ENV_BLOCKED` 优先级高于 `ABORTED`，退出码 5。另：开跑前做隔离库许可预检（`sim/_licprobe/`），**预检自身不可执行时打印 UNVERIFIED 放行而不阻塞全批**；唯一可靠判据是「实跑能否正常 `$finish`」，禁用 hostid 静态判据；重试 1 小时 1 次限量、运行器不 sleep（交一次性任务承载） | 出处 `virtual_test经验\06-仿真许可与环境级阻塞判据.md`；对照 `AGENTS.md` 规则 10 判据表 | 待决策 | **建议采纳**（ISS-001/002 未决期环境本就不稳，误判成本高） | 新增 `ENV_BLOCKED` 档与退出码写入规则 10 判据表；两词表与预检逻辑由 `script/regress.py` 实现 | — |
| **ISS-030** | 流程 | 2026-09-22 | 学习知识库（`virtual_test经验/03` + `/12`） | **规则 19~22 只有角色声明，无机器门禁通道**：知识库把「AI 说做完了不可采信」做成工具——`agent_gate.py` 四子命令 `init-baseline / check / dispatch / report`；缺口须过四道校验才转指令，未过的进「需人裁决队列」（只报不做）；**熔断＝同一指令累计 >3 轮未闭则置 FUSED 挂人**；核销只认机器判据。防假阳性细节：标注即豁免（词形清单）、`check` 与复核器**共用同一判据函数**（防两套逻辑漂移）、裸数字加边界 `(?<!\d)1200(?!\d)`、台账状态列 ≤60 字符。审计方硬约束：只采信文件原文、每条结论带 `文件:行号` 逐字命中、**出现模糊词即判审计无效**、与被审方无共同上下文。另有 Auto Queue（每项＝ID / 工作项 / 归属角色 / 依据(文件:行号) / 可机判完成判据 / 状态）与修复 V 型环（发现→判定+最小修复→换角色独立复验→断言转正→基线刷新→台账闭环）；涉评审门 / waiver / 基线签核的结构改动**不入队列**，转「批量批准包」 | 出处 `virtual_test经验\03-多agent监督与门禁通道.md`、`\12-多agent自动流水线.md`；对照 `doc/AI角色与职责.md` §8.3 与 ISS-022（已实测发生过一次只读违反） | 已解决 | 人已选：R8 新角色 + 门禁前停 + 先落地后试跑（2026-09-23，详见 ISS-033） | **已落地**：`script/gate.py` 四子命令 check/dispatch/report/escalate + `run_cmd/AutoQueue.yaml` 14 项（字段含可机判 done_when 与 landable）；核销与报告共用同一判据函数，不养第二套口径。首跑 `check` 即抓出**一处真实缺陷**（看板台账计数镜像落后一版）；与 ISS-026 码点扫描、ISS-018 位宽自检合为**「机械验收三件套」** | 2026-09-23 |
| **ISS-031** | 文档 | 2026-09-22 | 登记 ISS-023~030 后的三同步核查 | **看板与台账计数脱钩**：`doc/verify/04` §②「近期进度」行写「问题台账 已记 **12** 条（4 待决策 / 6 处理中 / 2 已解决）」，而 `doc/process/00` 实际已有 **22** 条（5 / 12 / 5）——差额 10 条正是上一会话 ISS-018~022 评审轮期间新增的条目，当时未回写看板。违背规则 11「状态变更即记」与三同步纪律；危害是换机接手人（走 `doc/交接说明.md`）会按看板**低估开放问题量**，尤其低估 13 条待决策 | `doc/verify/04` L55 vs `doc/process/00` 状态总览（本次登记前 22 / 后 31） | 已解决 | 自查发现，属文档同步缺陷，直接修正 | ① 本条登记（分类=文档，当场闭环）；② `doc/verify/04` L55 计数回填至与台账一致；③ **本条是 ISS-030「机械验收三件套」的第 3 个实证**（前有 ISS-021 处置未落实、ISS-022 只读未核查）——建议将「台账计数 / ID 连续性 / 看板一致性」纳入机器判据脚本 | 2026-09-22 |
| **ISS-032** | 平台 | 2026-09-22 | 同步知识库 README 时的目录实测（顺带核查 AGENTS.md 声明的锚点路径） | **`AGENTS.md` §1 声明的「规范口径唯一来源」路径在本机不存在**：`D:\IC验证知识库\_tools\` 整目录缺失，全库（130+ 篇 md + `参考书/`）搜不到 `riscv-spec.pdf` 与 `extracted\riscv_spec\riscv_spec_full.txt`，也不见 `riscv-dv-master` / `ibex-master` / `cv32e40p-master` / `picorv32-main` / `axi-master` 源码包。**影响面（均动到 R20 外部锚点）**：① `doc/spec/` 已引【权】§号的全部结论无法回查原文；② **ISS-005** 的“直读规范提取件写 Sv39”方案不可执行；③ **ISS-019** 的 `MRET/SRET/WFI` 编码修正所据锚点 `_tools\ibex-master\vendor\riscv-test-env\encoding.h` 不可复验；④ **ISS-020** “本地规范实测为 20260922 Intermediate、非卷 II 1.13”的判定来源亦在该目录内；⑤ **ISS-004** riscv-dv 激励生成无生成器可用。关键线索：知识库根 `README.md` 目录树仍列 `_tools/`，且《更新记录》2026-09-22 四条均写“源材料存 `_tools/`” ⇒ 该目录**确实存在过**，现为缺失。最可能成因：本项目正处换机交接期（本文件 §1 与 `doc/交接说明.md`），md 笔记已同步到新机而 `_tools/`（PDF 与源码包，体量大）**未拷贝** | 实测：`os.path.isdir(_tools)=False`；全库 `os.walk` 搜 `riscv-spec` / `riscv_spec_full.txt` 命中 **0**；反查知识库根 README 目录树行与《更新记录》2026-09-22 四条 | 已解决 | **需人确认两件事**：① 本机是否即新机、`_tools/` 是否仍在原机（若是，拷贝即可，**无需重新下载**）；② 若无原机副本，是否授权按《更新记录》清单重新拉取（riscv-spec 为官方公开 PDF，riscv-dv/ibex/cv32e40p/picorv32/axi 为开源仓库）。**恢复前的临时口径**：`doc/spec/` 内所有【权】§号引用一律降级标注为 `待锚点`，**不得作为已锚定结论参与 Review G1 冻结判定**。**2026-09-22 成因已由人确认：换机时未拷贝**（非结构调整、非误删）。**关键旁证：`参考书/` 在本机完好且达 86 个文件 / 2.7 GB** ⇒ 不是“大目录被排除”，而是 `_tools/` 整目录漏拷 ⇒ **原机上大概率仍在，定向拷一个目录即可** | ① 恢复 `_tools/` 并跑一次全库路径核查（含 AGENTS.md §1/§3.3 全量路径）→ 抽 3 条【权】结论回原文复验 → 关闭本条与 ISS-005 阻塞；② 若确认无副本，则先改 AGENTS.md §1 锚点路径声明为实际可用位置（文档类改动，待批）。**已取证重建待拷清单（15 项，来源＝《知识库更新记录》2026-09-22 五条 + `AGENTS.md` §1/§3.3/§4 + 本项目引用处）**：`riscv-spec.pdf`、`extracted\riscv_spec\riscv_spec_full.txt`（属 `extracted/` 整目录）、`riscv-dv-master`、`ibex-master`、`cv32e40p-master`、`picorv32-master`、`verilog-ethernet-master`、`verilog-axis-master`、`verilog-pcie-master`、`axi-master`、`axi_cheatsheet.pdf`、`riscv_reader_ch.pdf`、`cummings_pdf`、`zipcpu_pdf`。**优先选 A（原机定向拷贝）而非重新下载**，理由：ISS-020 已实测本地规范文本为 **20260922 Intermediate 快照**，重下会得到另一个版本，使 spec/00~01 已写的【权】§号 **再漂移一次**。**附带发现**：`AGENTS.md` §4 写 `_tools\picorv32-main` 而下载留痕写 `picorv32-master` ⇒ 项目自有路径声明与实际名不一致，恢复时一并校正。**2026-09-23 关闭**：人已从原机补拷 `_tools/`；本条要求的两项验收已跑——全库路径核查（`script/path_audit.py`：19 条声明路径 0 缺失）+ 抽 4 条【权】锚点回原文复验**全部命中**（版本行 `Version 20260922: Intermediate Release`；IALIGN=16 与取指非对齐不可发生句；PMP 默认拒绝两句（`[spec p733]`）；官方 MRET/SRET/WFI/SFENCE_VMA = 0x30200073 / 0x10200073 / 0x10500073 / 0x12000073）⇒ 同时解除 **ISS-005** 的“不可执行”附注与队列 Q-009 / Q-010 的 `blocked_by`。另：本行附带发现的 `picorv32` 名字问题已查清——本机实际目录名为 `picorv32-main`（包名才是 `picorv32-master.zip`），即 `AGENTS.md` 的声明是**对**的 | 2026-09-23 |
| **ISS-035** | 平台 | 2026-09-23 | 换机（平台更新）后的环境核查 | 三项实态与声明脱钩：① `README.md` 第 5 行仍指已改名的旧路径 `AI验证工作流\AI验证工作流.md`（知识库现为 `AI验证工作流程\`，并新增总框架《AI进行IC开发验证工作流程》v2.0）；② **换机拷贝丢 16 个空骨架目录**（`rtl/vr1/*`、`tb/*`、`sw/*`、`filelist`、`sim/covdb`）——空目录不随常规拷贝传递，而 `AGENTS.md` §2 与队列 Q-012 都依赖它们；③ 首版核查脚本用内联正则被 PowerShell 转义吃掉，**只扫到 1 条路径就报“ 0 缺失”**（假阴性） | 实测：`python script/path_audit.py` 修复前报 3 缺失 + 16 骨架缺失；另更正一处**本人测量错误**：初步统计的“旧路径 15 处”系 `工作流程` 内含 `工作流` 子串造成的假阳，真实失效仅 1 处 | 已解决 | 技术缺陷，自查即修 | ① README 路径已重指向，并补登新框架文档为引用源；② `script/path_audit.py` 固化（`--fix` 自复骨架 + 抓取自检：候选 token < 20 则自报“结果不可信”）+ 假阳性过滤（通配符 / 代码栏示例）；③ 重建 16 个骨架目录，现 19 条声明路径 0 缺失、骨架 0 缺失、`gate.py check` 18 PASS / 0 FAIL；④ 残留 3 处《AI验证工作流》为正文简称非路径，已在 `doc/项目开发流程.md` §0 记录改名映射，不改写 | 2026-09-23 |
| **ISS-036** | 平台 | 2026-09-23 | 人指出 ISS-035 的真实根因（我原判定“拷贝丢失”有误） | **空目录不入 git——这是目录消失的根因，而非“换机拷贝漏拷”**：`rtl/ tb/ sw/ filelist/ sim/` 等目录内无任何文件时，git 不跟踪空目录，因此**任何一次经仓库/压缩包的传递都会丢**（人已确认：github 不能上传空文件夹）。危害不止“目录不见”：`AGENTS.md` §2 的目录约定、队列 Q-012 的 `tb/unit/` 落点、`sim/covdb/` 作为**签核证据受保护区**的规则、`script/tmp/` 的临时区纪律，全部会因目录消失而**失去落点**——规则写在文档里但无处落地 | 人指令：“在文件夹中增加临时文件”；实测：修复前 20 个空目录（含 `.qoder`，平台目录不计），其中 17 个为项目骨架叶目录；`.gitignore` 对 `script/tmp/*` 与 `sim/covdb/*` 有排除 + 单文件白名单 ⇒ 这两处 `.gitkeep` 仍会被忽略 | 已解决 | 技术缺陷，按人指令即刻修复 | ① 17 个叶目录各放 `.gitkeep`（含一行说明指向本条），两处被忽略区按既有白名单放 `README.md`（`script/tmp/README.md` 写临时区纪律与 ISS-022 教训；`sim/covdb/README.md` 写证据保护区规则与当前空态声明）；② `script/path_audit.py` 新增**占位判据**（目录内一个文件都没有 ⇒ FAIL），并纳入 `--fix` 自动补占位——同类问题不再依赖人记得；③ 实测：`path_audit` 输出【缺失路径 0 / 骨架缺失 0 / 无占位 0】。**更正留痕**：ISS-035 证据列写的“空目录不随常规拷贝传递”属推测，真实机制见本条（不追改原描述列） | 2026-09-23 |
| **ISS-034** | 文档 | 2026-09-23 | `spec/01` v0.2 重建后的**纯复核轮（§6.1 C2）** | **复核未收敛，且轮次用尽**。领队重建 38 条落点后的独立复核结果：落点抽查通过 33 / 部分 3（Q18/Q19/Q32）/ **未落实 2**（Q3：提交级无 CSR 判定源且 `csr_wr_t` 无来源；Q10：A6 断言未改 `needs_ckpt` 且该信号未进 ROB/commit 链）；另记**阻塞 10（B1~B10）**、待澄清 10（C1~C10）、存疑 1（S1：操作数“后读”读点口径）。其中 **6 项系本次重建新引入**（B5 `entry_pc` 无基址载体、B6 “已登记/已留档”无落点、B7 回滚栈三处缺口、B8 §6 `len=0` 与 `spec/00` `len=1` 打架、B9 `rollback_busy` 门控范围歧义→条件性死锁、B10 `exc_v` 清零粒度漏测）。正面证据：算术类已被机器全数枚平（27/27 struct OK，复核者逐项复算通过），且 C2 轮成功抓出“落点列写得像已完成、正文实为断链”——纯复核轮的价值得到第二次实证 | `doc/review/01-评审记录.md` 附 B（含全部证据行号）；`doc/review/01-评审记录.md` Q3/Q10 行；`script/width_check.py` 与 `gate.py check` 输出 | 已解决 | R0 已裁决（2026-09-23）：**批准"继续直到收敛"**——轮次上限豁免（`AutoQueue.yaml` Q-004 `cap_waiver` 为凭，`gate.py queue-review-cap-waiver` 核销），每轮仍须留痕且不得默认通过。原选项 A/B/C 的择定：按 A 执行（全量修复 + 新开确认轮），S1/SUS-01 与 `HIST_DEPTH`/`ROLLBACK_MAX` 转 `spec/04` 设计评审定案 | Q-004 已置 fused（`AutoQueue.yaml` 含 `escalate` 说明）；获批后先修再跑确认轮。**已解决（2026-09-23，附 E 处置轮执行完毕）**：附 E 全部 23 项（N1~N17、X1~X7、BLK-06/08/10/11/12/15 残留、CLR-05/06）处置落地——`spec/01` v0.3 + `spec/00` 同步（§4.3/§4.8/§7.3/§9/§6）+ `width_check.py` N16 盲区修补；机判 28/28 OK、gate 18 PASS / 0 FAIL（`doc/verify/05` R-005）；待第 8 轮纯复核确认 §6.1 C2 | 2026-09-23 |
| **ISS-033** | 流程 | 2026-09-23 | 人指令“设置一名 AI 项目经理，按《AI进行IC开发验证工作流程》带领各角色自动推进各阶段” | **新增 R8「项目编排 AI（PM）」需撞本项目两条自判条款**：`doc/AI角色与职责.md` §6.1“角色数封顶 8 个，加角色必须先说明它堵哪个**已发生**的失效”与 §6.3“去掉该角色后若无任何产物边界变化即为表演”。按 §6.1 举证四条已发生失效：① **无“开工即自动捡任务”的执行体**——§10 有依赖序、§1.1 有出口判据，但无人读取并推动（本会话全程靠人逐条下指令）；② **B1~B8 八个阻塞项无人跟踪催办**，从未按 §5 格式生成“≤5 选项决策包”；③ **三次“声明与实态脱钩”缺逐轮核销触发者**（ISS-021 处置未落实 / ISS-022 只读被违反 34 个文件 / ISS-031 看板落后 10 条）；④ **14 条待决策无催办**（§10 第 1 项 ISS-017 至今未拍）。按 §6.3 检验：R8 **新增三处产物边界**（`run_cmd/AutoQueue.yaml`、`doc/verify/04` 状态列与镜像行、`doc/verify/05` 调度留痕），故非表演。**R8 不接管任何角色的 A**，遇 §12.3 四类必报项一律转决策包 | `doc/AI角色与职责.md` §6.1/§6.3 条文；`doc/项目开发流程.md` §12.1~12.4；`script/gate.py check` 实跑输出（7 PASS / 4 FAIL，其中含本条要堵的看板镜像落后） | 处理中 | 人已定三项：形态＝**R8 新角色**；自治边界＝**门禁前停**（不用 24h 默认策略）；节奏＝**先落地后试跑** | ① 建队列与核销工具（已完成，见 ISS-030）；② 角色表加 R8 行与 RACI“编排与核销”行、§6.1 封顶 8→9 并附上述四条失效；③ 流程文档 §12 写入编排与催办条款；④ `AGENTS.md` 加规则 23 并顺手修 §1 过期状态与两处失效知识库路径；⑤ 试跑队首 Q-002（`spec/04`，顺带关 ISS-003）。本条转【已解决】的条件：五项全部落地且 `gate.py check` 无 FAIL。**2026-09-23 进展**：①②③④ 已落实（四份升版件 + 工具实跑 12 PASS / 0 FAIL）；**⑤ 待做**——试跑队首 Q-002（`spec/04` 重命名与 PRF，关 ISS-003），未跑成前本条不得提前转已解决 | — |
| **ISS-037** | 文档 | 2026-09-23 | `spec/01` 附 E 处置轮（X7/C2/C3 合并登记） | **`iss/README.md` 三处与 `spec/01` §7 已裁定口径不一致**：① §3:49 异常类写"改比 `--final-only`"，与 `spec/01` §7"异常条目 `exc_v=1` 仍逐条比 `pc/binary/mode`（清零按 lane 粒度）"方向相反；② §3 映射表缺 Q19① 的 lane 粒度清零规则（C3：以 spec/01 为准）；③ §4 签名表未扩 `WRITE_MEM`（C2/Q19② 内存镜像比对点）。`iss/` 属角色 R2 的产物边界（`AI角色与职责.md` §2），R1 不得代改，故登记台账并路由给 R2，同步完成前 ISS 侧若参与比对会产生假红/假绿 | `iss/README.md` §3/§4 vs `doc/spec/01-流水线与寄存器接口.md` §7 异常条目三条规定；评审记录附 E X7 | 处理中 | 技术决定（R1 已裁定"以 `spec/01` §7 为准"，C3）；同步动作归 R2 | R2 按本条三处逐一同步 `iss/README.md`；完成后在原条目回填并附 diff 摘要；`spec/01` §8.2 待同步清单已同路径跟踪 | — |
| **ISS-038** | 流程 | 2026-09-23 | `spec/01` 连跑 8 轮不收敛后的机制定案 | **原复核机制无出口**：收敛判据按硬约束要求"新增 0"，但新增项多为上一轮修复在接口链上又接一段（下游义务）＋跨篇未闭合项记在本篇账上；且复核者无执行工具时按"数值一律实算"把 28 个 struct 全量手算，实测贡献 0 个发现，而该轮 12 个阻塞全来自语义推演——慢且不收敛 | 阻塞项计数 21→10→17→15（不单调）；`spec/01` 附 E 的 12 项阻塞全属时序/链断类；`width_check.py` 实跑 0.4 s 覆盖同一批量 | 已解决 | R0 2026-09-23 定案（本会话）：两档制 + 全量轮上限 5 + 每轮 git 基线；AI 按此写三处，不自行放宽判据 | 三处落地：`doc/项目开发流程.md` §9.4（两档定义/复核面三来源/基线纪律/上限与出口）、`.qoder/agents/vr1-auditor.md`（算术义务按机判边界切分、禁行号取证）、`script/gate.py`（新增 `changed` 子命令 + `queue-full-round-cap` 判据 + `loop` 按档出动作）；机判 `gate.py check` 18 PASS / 1 WARN / 0 FAIL、`changed` 输出改动节与闭包。**2026-09-23 同日更正注**（原文不改写）：①机判计数已升至 **21 PASS / 1 WARN / 0 FAIL**——落地后又补两项判据 `pending-list-stale`（待决策清单不得留已闭环项）与 `queue-full-round-cap-waiver`，并修 `changed` 闭包语料（原误含评审记录，致未展开项由 14 假报为 3），见 `doc/verify/05` R-007；②`pending-list-stale` 首次实打即抓出清单里三行已闭环项（ISS-001/030/032），已移出；③复核面表**当天再由 3 行扩为 7 行**——补 ④机判未决项 ⑤上轮自报的诚实边界 ⑥转派/路由项 ⑦缺失型（⑦**无取法**，仅终局全量轮可覆盖），并修本节改动集口径的自相矛盾（定案"机算 ＋ 作者只能加不能减"）；`changed` 输出同批瘦身（默认 231→123 行，`--all` 取全表）——**本节前文所述"三来源"以 §9.4 现文（七来源）为准**，见 `doc/verify/05` R-008 | 2026-09-23 |
| **ISS-039** | 文档 | 2026-09-23 | `spec/01` 第 9 轮增量复核（评审记录附 H 的 H-02，阻塞） | **取指 walk 的「槽 15 跨窗」缺可行恢复机制**：块起始 PC 允许 2 字节对齐后，32bit 指令可起始于 `pc[4:1]==15`，其字节跨 32B 窗偏移 30..33；而 `win_base_pc = VA[39:5]<<5` 使窗口 32B 对齐、§3.16 又明禁跨 32B 边界拼接 ⇒“随下一窗口重取”要么把后半个字误当新指令起始（误译），要么原地重取（死循环）。修复必改接口（新增携带位或寄存器），属**设计空间取舍**，R8 不自决 | `doc/spec/01` §3.16 walk ② 与 `win_base_pc` 行、§1.1 P4；评审记录**附 H** 的 H-02 与 H§4 存疑① | 已解决 | **2026-09-23 决策 AI ADR-1（ISS-042 新口径）：选 A**——P5 增设「尾半字保持寄存器 `xw_carry`」（`{v, lo16, pc[39:0], err, err_code}` **62 bit**（F9 回填：删"上下文"、补 err/err_code））+ 次窗拼接，§3.16"禁跨 32B 边界拼接"改为"**仅允许经该寄存器拼接**"；锚点＝规范"指令对齐于任意 16bit 边界"（本机提取件 `riscv_spec_full.txt`）＋ cv32e40p `aligner` / ibex `fetch_fifo` 双参考核同型拼接 ＋ 几何与吞吐等价性算式；**取代 R0 的 E（推迟）**；下游义务＝`spec/03` 块布局细则（块可起任意 2B 槽、loop 终止＝字节取全、跨度上界 66 B、carry 冲刷作废）＋`spec/05` F2 mux 时序＋`doc/verify/02` 验证点组×6；回退＝C（34B 双窗）。原决策列保留：**R0 2026-09-23 选定 E（推迟）**——选项列表见右列，A/B/C/D 未选 | **（已被决策列 ADR-1 取代——本节保留为历史）** **R0 已选 E（2026-09-23）**：由 `spec/03`「块布局限制」细则收口，G1 前必须闭合、届时回写本行；成稿前 `spec/01` §3.16 的槽 15 情形标「待定（ISS-039）」。**未选选项备查**（≤5，按改动面递增）：**A（推荐）** 增“上一窗尾 2 字节”寄存器、下一拍拼接——+1 寄存器与末 2 字节 mux，须把 §3.16“禁跨窗拼接”改为“**仅允许经该寄存器拼接**”；**B** FTQ 条目增 `cont` 位、允许一个 32bit 指令由两次请求拼成——接口 +1 bit，walk 须处理“首槽为前半字”；**C** 取指请求改 34B/双窗（第二窗只取前 2 字节）——+1 次 tag 查或 +1 拍延迟；**D** 收窄适用面：要求 FTQ 块首 PC 4 字节对齐——**与 N-03 冲突、会退回第 8 轮缺陷，不建议**；**E** 推迟：待 `spec/03` 出“块布局限制”细则后再定——须写明影响面（G1 前必须闭合） | **2026-09-25 闭合（R-169）**：①`spec/03` §2.4 细则在文＋`spec/01` §3.16 定稿态；②下游义务三件全落——`spec/05` §3 F2 mux 专项／`doc/verify/02` VP-X06a~f（回标 `spec/00` §10 行） |
| **ISS-047** | 文档 | 2026-09-23 | `spec/01` 第 11 轮增量复核（附 J 的 C-03） | **两条计数权威口径并存且适用面未写明**：`spec/01` §8.1 表头＝“[check 值]以最近一次实跑末行为准”；`doc/verify/05` R-010/R-011 对账更正注＝“[对账]以最新一代机判件为准，引用时须连带生成时刻 HEAD 与基线 commit”。两者对象可能不同（check 末行 vs diff 数字对账）但未写明适用范围与相遇优先级；r9 实例中“最近一次实跑”（0 WARN，**未落件**）与“归档件”（1 WARN）取值不同，仅凭文本无法判权威 | `doc/spec/01` §8.1 表头；`doc/verify/05` R-010/R-011 更正注；`machine_r9_check.txt` 末行 vs `doc/verify/05` R-009 复跑记录 | 已解决 | 文本类，作者直接处置（§12.2） | **已修（第 12 轮处置）**：`spec/01` §8.1 表头新增"两条计数口径的适用面与优先级"（门禁引用取**最新归档件**／对账引用取**最新机判件**／相遇以归档件为权威），`doc/verify/05` R-010/R-011 更正注同步补适用面说明 | 2026-09-23 |
| **ISS-046** | 流程 | 2026-09-23 | `spec/01` 第 11 轮增量复核（附 J 的 C-02） | **`doc/verify/04` 近期进度表“流水线与接口”行停留第 9 轮措辞**（载"…第 9 轮增量复核新增阻塞 2（H-01/H-02），见附 H"）：H-01 已于第 10 轮闭合、第 10/11 轮新增项均未反映；同文件“④ 评审状态”行已更新至第 11 轮，两行口径不一 | `doc/verify/04` 该行 vs ④ 评审状态行；评审记录附 J C-02 | 已解决 | 存量镜像陈旧，属 R8 看板维护范围（规则 11 事件驱动更新） | 该行已改为随轮更新（第 11 轮判 §6.1 C2 不成立 + 附 H/I/J 索引 + ISS-044/045 指引） | 2026-09-23 |
| **ISS-045** | 文档 | 2026-09-23 | `spec/01` 第 11 轮增量复核（附 J 的 C-01，承 I-06） | **`slot16_vld`（§3.16）“G1 前定稿”义务无追踪载体**：行内标「待定（I-06）」+ `doc/verify/05` R-013 + HANDOFF 未完成栏，但 `doc/process/00` 无对应台账行、`spec/00` §10 无行、队列无工作项——对照先例（I-01/I-02→ISS-041＋§10 行；I-07→ISS-039）缺登记 | 评审记录附 J C-01；`doc/spec/01` §3.16 行内标注 | 已解决 | R8 2026-09-23 路由：取值域/不完整构型属设计取舍 → 随 **Q-016** 由决策 AI 出 ADR（派发词已含该问）；本台账行即其追踪载体 | **已落（第 12 轮处置，ADR-3）：删字段**——`icache_rsp_t` 338→**322 bit**（窗恒 32B 对齐 ⇒ 16 槽恒在窗内、字段恒 1 冗余，无"不完整构型"可定义）；替代＝`valid ∧ ¬err` ⇒ 全 16 槽可用 + §3.16 规则③ `err_pos` 截断；回退点＝`spec/09`/`spec/10` 若引入亚 32B 取指粒度则按接口升版恢复。**B3b 判定＝有条件关闭（2026-09-23，决策 AI）**，闸门＝**第 13 轮增量复核**：① 确认 §3.16 删字段后无残留引用（`width_check` 322 为预置机判锚点）② 回退点（亚 32B 粒度出现即恢复字段）随 `spec/09`/`spec/10` 定稿对照——已写入正文，转该两篇下游。**状态字待闸门通过后改** | 2026-09-23 |
| **ISS-044** | 文档 | 2026-09-23 | `spec/01` 第 11 轮增量复核（附 J 的 B-02，阻塞） | **两项「待定」的闭合义务无可用验收物**：§3.19 自认“丢弃无告警/断言挂钩”、§3.10 自认“A12 检不出”，而 §7 A1~A14 **无任何一条**覆盖 csrq 写口（`S` 写 `src_val`／`E` 写 `old_val` 的命中失败与丢弃）或 `W` 级停放/发射门控活性；§10 ISS-041 行却把补全面写成“断言集（A12/A14 面）随同补全”——A12/A14 面按自认无法承载该两类新检查，且未分配编号、未定验收物 ⇒ 两个已识别失效模式（环等死锁、写口静默丢弃致假绿）在回归面不可见 | 评审记录附 J B-02（含两条触发序列：I-01 csrq=[A,B] 阻断读法环等；I-02 写口竞争致 `csr_old` 缺值而提交成功）；`spec/01` §7 全表、§3.10/§3.19 新句 | 已解决 | R8 2026-09-23 路由：属设计取舍（断言面）→ 随 **Q-016** 由决策 AI 出 ADR，**派发词已要求“建议断言编号 + SVA 语句草案 + 命中/不命中语义”**；同时承接 **S-01**：§10 行内“断言集（A12/A14 面）”字样以本条为落点（**R0 2026-09-23 追认：维持该字样**，以本条为落点） | **已落（第 12 轮处置）**：`spec/01` §7 新增 **A15/A16**（CSR 投机读/发射门控在队头）与 **A17/A18**（未命中丢弃只在冲刷截断覆盖内合法；`sticky_drop_illegal` 恒假）＋ **A19 可选**（写落点匹配），`spec/00` §10 ISS-041 行解除条件同步改写；**B3b 判定＝有条件关闭（2026-09-23，决策 AI）**，闸门＝**第 13 轮增量复核**将本批全部落修（R-022 批＋B3 回应批）纳入来源①并判无阻塞；未过即重开。条件明细：① A15~A19（+A20）与 §3.10/§3.16/§3.19 的 B3 回应文本无阻塞 ② A20 编号随 §7 汇总定稿或明示"候选不编号" ③ "断言接口名随 `spec/11` 成稿核对"转 §8.2/待同步清单登记（登记义务，不阻本条）。**状态字待闸门通过后改**（不得前置外推为已解决） | 2026-09-23 |
| **ISS-043** | 文档 | 2026-09-23 | `spec/01` 第 11 轮增量复核（附 J 的 B-01，阻塞） | **§8.1 核销行的第 9 轮“同值”引用与归档件不符**：该行称“第 9/10 轮复核期同值（21 PASS / 0 WARN / 0 FAIL），分别见 `machine_r9_check.txt`/`machine_r10_check.txt`”；实核 r9 归档件末行为 **21 PASS / 1 WARN / 0 FAIL**（WARN＝rare-chars 低频字 6 个，人工判类），0 WARN 的真正出处是 `doc/verify/05` R-009 的“归档后末次复跑”（**该次未落归档件**）⇒ 引用件与所述值不可同时成立 | 评审记录附 J B-01；`machine_r9_check.txt` 末行、`machine_r10_check.txt` 末行、`doc/verify/05` R-009 | 已解决 | 文本/对账类，作者直接处置（§12.2） | **已修（第 12 轮处置）**：§8.1 核销行改为可回查表述——r9 归档件实值 **21/1/0**、0 WARN 出自未落件的复跑（出处 `doc/verify/05` R-009 备注④），并引本节表头新增的“两条口径适用面”第 2 条 | 2026-09-23 |
| **ISS-048** | 文档 | 2026-09-23 | 领队体检（会话中提问触发：两个"引用悬空"缺口） | **两处文档缺口**：① **RA5 无权威定义**——项目四处引用"红线 RA1/RA5"（`AGENTS.md` 规则 23、`doc/AI角色与职责.md` R8 行、`vr1-auditor.md`/`vr1-decider.md` 硬约束），但知识库《AI进行IC开发验证工作流程》§8 只定义 **RA1~RA4**（库内全文 grep 无 RA5），引用悬空；② **M0~M8 无权威表**——M 系列散见于 5 处（`doc/环境搭建.md` §0/§5/§6/§7、`doc/项目开发流程.md` §4/§8、`AGENTS.md` §1、`doc/verify/04` §③），且 **M2/M7 全仓零出现**（原开发方案未入仓） | 知识库 §8（仅 RA1~RA4）；`AGENTS.md:204` 等四处 RA5 引用；M 系列散点清单（见右列落点） | 已解决 | R0 2026-09-23 指令"把 RA5 登记与 M0~M8 权威表这两件小事先办了" | ① **RA5 项目侧定义落 `AGENTS.md` 规则 23 后**（定义＝"评审未收敛不得默认通过、不得宣布关闭或进入下一阶段"；依据＝人选定的"门禁前停"边界，见 ISS-033），并声明"项目内一切 RA5 引用一律按此定义"；RA1~RA4 仍指库 §8。**知识库保持只读**（如需在库内补定义，须人明确指令＋双确认＋留痕）② **`doc/verify/04` §① 新增「里程碑（M0~M8）权威表」**——逐项给"定义／验收判据／原散点出处／现状"，M2/M7 标"**未使用**（无定义）"并注明"如有人认领请补定义，否则明确废弃"。**2026-09-23 同日改判（R0 指令"之前没有该编号就去掉项目中的引用；有必要再加"）**：**不新设 RA 编号**——项目内 7 处引用**全部移除**（`AGENTS.md` 规则 23 与其后 RA 记号说明、`doc/项目开发流程.md` §9.4/§12.4、`doc/AI角色与职责.md` R8 行、两个子代理章程）；其实质由 **RA1、RA3** 与规则 23"不得默认通过"、§12.4 尾注承载（**未削弱判据**：同义条款原文都在，只是不再挂一个不存在的编号）。**防回流**：新增机判 `gate.py ra-token-defined`——规则承载件（AGENTS/流程/角色表/两个子代理章程）不得引用未定义 RA 编号（台账与评审记录允许出现该字样，因那是"已移除"的事后记载）。历史附件按"原文不改写"保留，`doc/review/01-评审记录.md` 篇首加**记号注**统一说明 | 2026-09-23 |

| **ISS-042** | 流程 | 2026-09-23 | 人指令（本会话）：口径变更——"设计空间取舍＝日常技术决策归 AI；spec 是设计 AI 的承诺；疑似设计/规格缺陷由决策 AI 按 L1+L2 决策；人在里程碑按 AI 列出的评审 list 签署" | **原口径把 spec 的设计取舍列为必报项、要人逐条裁决**（§12.3 第 2 类原逻辑："R1：AI 只记录不改；且 spec 是人的承诺"），导致 ① 人成为技术裁决瓶颈（每篇 spec 的机制分叉都要呈报——ISS-039/041 两次惊动即例）② G1 前"未决项"越积越多 ③ 项目口径**严于知识库原模型**——库《AI进行IC开发验证工作流程》§0/§4/§5 明写"人只把关关键节点""除关键节点与升级协议外任何人不得也无须介入 AI 的日常技术决策""Decider 判可自决/需转人" | 人本会话指令；`doc/项目开发流程.md` §12.2/§12.3 原文；库 §0/§4/§5 | 已解决 | **R0 2026-09-23 定案**（四处落点见右列；两类边界保留见右列末） | 四处落点：① `doc/项目开发流程.md`——§12.2 新增"设计取舍自决＋2~3 方案评估＋**ADR 四要素**（选项/选定/锚点/下游义务）＋成本分级＋不可回退项禁超时默认"；§12.3 原第 2 类**撤出**（改为三类必报）＋口径变更注；§12.4 清单收录范围收窄；§12.1 P1/P4 材料＝评审 list；**新增 §9.5 评审 list 规范**（四段：取舍条目表/未决项/风险残余/抽查建议；签署语义＝"清单完整＋我接受其风险"；机判义务与 `spec/14` 落地物）② `.qoder/agents/vr1-decider.md`——分类改写（设计类直接生效、四类第 0 次转人、不可回退且无锚点的特例）、ADR 输出格式、允许对设计类下"已定" ③ `AGENTS.md`——规则 2（设计类不停线）、规则 20（锚点＝自决的交换条件）、规则 23 括注（四类→三类）④ `doc/AI角色与职责.md`——R1 行加"领域第一负责人＋ADR"、R6 行加"ADR 完备性审计"、R8 行括注。**⑤（同日追加，人第二条指令）`doc/项目开发流程.md` §12.5「决策审核（一次·只记录）」**：决策 AI 的每项设计决策可被审核 AI **审核一次**，结果记入 `doc/verify/05` 与评审附件（RA4：不得删除）；**审核结果不作决定性反驳依据**——不自动回退、不阻断生效，由决策 AI 在"剩余分歧"逐条回应（可修可维持）；例外：R6/R7 类恒转人、人对 G1/G3/P3 的权利不变、无锚点项仍只能标"登记＋预测＋回退点"、记录/一致性缺陷仍按原流程处置；同步 `.qoder/agents/vr1-auditor.md`（工作流第 0 步）与 `vr1-decider.md`（决策审核节）。**本轮 r12 复核对 ADR-1~3 的检查即这三项决策的首次审核**（非决定性，发现将在附 K 记录）。**连带**：ISS-008 关闭（G1 签署改为"按 list 逐项签"）；ISS-003/007/012/017/024~028 共九项转决策 AI 自决（台账→处理中，决策列注明）；ISS-002/023/029 保持待决策（环境安装/放宽通道/判定档位）。**未变**：P1~P4 签署动作、R6/R7 边界、`rtl/` 不动（红线 R1）、规则 21（同模型盲点残余由人签署＋锚点兜底）、规则 22（只读评审不给执行工具） | 2026-09-23 |
| **ISS-041** | 文档 | 2026-09-23 | `spec/01` 第 10 轮增量复核（评审记录附 I 的 I-01/I-02，两项阻塞） | **第 10 轮新写文本的两处机制语义未定，且都可能致环等死锁或丢数据**：① **I-01「`W` 级停放」**——§3.10 只写"门控不成立时该 μop 停在 `W` 级等待，不消耗 `E` 级拍"，未定停放槽容量/是否阻断其他 μop 发射/重选语义/等待出口；若按"停放＝阻断"读法则**可构造环等死锁**（年轻 CSR μop 占住 W 等比它更老的 CSR 提交，而更老 CSR 依赖尚未发射的更老 μop），且现有断言 A12 只覆盖提交侧、检不出。② **I-02 csrq 规则 4「未命中即静默丢弃」**——§3.19 把"写到已出队项"列为容许情形，但"`E` 级 `old_val` 写必先于出队到达"的前提未给（两写口最迟到达界/仲裁未定义），也无告警/断言挂钩 ⇒ 合法迟到写被静默丢 ⇒ `csr_old`/CSRRW 的 `rd_data` 载体缺值，若回归只查提交接口即**假绿**。两项修复都含**接口/时序取舍**，R8 不自决 | `doc/spec/01` §3.10 `src_kind` 行、§3.19 规则 4 与 `csrq_entry_t.old_val` 行、§7 A12；评审记录**附 I** 的 I§3 与 I§9 | 处理中 | **2026-09-23 决策 AI ADR-2（ISS-042 新口径）：I-01 选 A、I-02 选 A′**（**取代 R0 的 C**，R0 锁定的两条 guard 保留为正文约束）——I-01：门控**并入 P9 发射选择拍**（不满足即不选中、条目留 IQ 每拍重判），**取消一切"W 级停放"状态**（无停放槽/不阻断/出口为队头直比），附死锁自由构造证明；I-02：**构造性上界**（写 ≤ `E` 拍、出队 ≥ finish+1 ≥ 写口点火+1）+ **丢弃两分类**（`drop_cnt_squash` 合法 / `sticky_drop_illegal` 非法，`K` 暂定 2 **（B4 追加：`K*≠2` 时按"6 类重跑清单＋清账口径"执行（符号索引机扫 `K`/`K_stall`/`sticky`/`$past`/在途界；依赖 K=2 的历史结论标"K 变更后作废、待重判"、不得闭环；重判轮并入 `spec/04` 落文轮固定项）B3b-2 改判：定值归 `spec/04`**，`spec/13` 仅引用不改值）；**断言已落 §7：A15/A16（队头门控）＋ A17/A18（丢弃 legality）＋ A19 可选＋A20 候选**（消化 ISS-044）；下游义务＝`spec/13` 定 `K` 与时序约束、`spec/04` 登记 csrq 状态面、`doc/verify/02` 验证点/覆盖；回退＝I-01 退 B（停放寄存器）、I-02 退 B′（`old_val_vld` 握手位，接口 +1）。原决策列保留：**R0 2026-09-23 选定 C（两项都推迟）**——选项 A/B/D/E 见右列备查 | **（已被决策列 ADR-2 取代——本节保留为历史）** **R0 已选 C（2026-09-23）**：`spec/01` §3.10 停放语义与 §3.19 规则 4 迟到写语义成稿前标「待定（ISS-041）」，只锁**实现不得违反的约束**（I-01 不得形成环等死锁；I-02 不得静默丢合法写）；G1 前由 `spec/04`/`spec/13` 收口并回写本行。**未选选项备查**（≤5，按改动面递增）：**A（推荐，最小改动面）** I-01：门控判定**前移到发射选择拍**（IQ 仲裁时不满足即不选中该 μop、继续选其他），`W` 级只携带授权结果——无新增寄存器、无新接口，死锁面直接消除；I-02：**构造性上界**（正文给出 `E` 级写口最迟到达拍数 k 与 csrq 出队不早于 k 的约定，`k` 由写口仲裁最坏界导出）+ **丢弃即置 sticky 告警位并入断言 A14**。**B** I-01：保留 `W` 级判定并增设**停放保持寄存器**（1~2 组）且明确规定"不阻断其他人的 `W→E`"；I-02：csrq 队头提交前须等 `old_val` 有效（**接口 +1 位**握手）。**D** 只加告警与断言（I-02 的降级版：语义仍写"丢弃"但必须可见、可计数），I-01 语义留待 RTL 实现自定但锁断言。**E** 你另给方案 | — |
| **ISS-040** | 平台 | 2026-09-23 | Q-004 第 10 轮处置后重生成复核面（`gate.py changed cf39eeb --all`） | **`changed` 的标识符闭包两头都被非正文文本污染**：① **扫描侧**——探针件（`doc/review/01-评审探针/` 下 `snapshot_r10_spec0{0,1}.md` 是规格篇全文冻结拷贝、`machine_r10_*.txt` 是机判输出）在第 9 轮入版本控制后，其新增行被当作文档正文抽取，"闭包未展开"由真值 **7 项暴涨到 127 项**（含 `clk_i`/`from`/`amo_op` 之类），复核面下界被噪声淹没、机器文件自我指涉；② **抽取侧**——记录/台账类文件（`doc/process/00`/`doc/verify/04`/`doc/verify/05`）里**引述的示例词**同样被当成"被改标识符"（本轮记录缺陷描述时写到的 `clk_i`/`from` 又制造出 4 项假未展开），且更致命的是反向：记录里提过一次的标识符会把规格正文中**只有 1 处**的真孤儿抬到 hits≥2 而**漏报**。②与第 7 轮"闭包语料误含评审记录致假阴"同根因（该轮只收窄了计数侧） | 四代计数轨迹 7 → **127** → 8 → **7**（`doc/verify/05` R-011）；旧代码可从 `05f759b:script/gate.py` 取出复跑，轨迹可复现 | 已解决 | 自查发现（属验证代码/工具缺陷，按红线 R2 直接修并登记） | `cmd_changed` 两侧对齐：**抽取来源与闭包语料同类**——只取规格篇正文（`doc/spec/NN-*.md`，排除评审记录/探针）；记录/台账/探针/脚本/队列的文件**只列不计**（改动清单照列并标「不参与标识符闭包」）。修后同基线重跑 "未展开" = **7 项**（正文侧真值），留档 `doc/verify/05` R-011 | 2026-09-23 |
| **ISS-049** | 文档 | 2026-09-23 | 知识库同步轮后的回头核查（领队） | **ISS-048 的 RA5 移除有漏网**：清理面只覆盖"规则承载件 7 处"，未覆盖**工具打印串与队列协议头**——`script/gate.py` 的 `cmd_loop` 与 `cmd_escalate` 提示串 2 处、`run_cmd/AutoQueue.yaml` 头部领队循环协议 1 处，仍在按**未定义编号 RA5** 下指令（"不得默认通过"）；`.qoder/handoff/HANDOFF.md` 两条历史记录中的同字样按"记录不改写"保留、由新记录更正 | `gate.py` 打印串 2 处、`AutoQueue.yaml` 协议头 1 处；`gate.py ra-token-defined` 的扫描面＝`RULE_FILES` 五件，**不含工具自身、队列与交接件** ⇒ 判据盲区的实证 | 已解决 | R0 2026-09-23 指令"修正项目里的版本口径和计数漂移"；引用实体应为 RA1/RA3（**不新设编号**，同 ISS-048 裁定） | 3 处活引用改指**红线 RA1/RA3**（`gate.py` 两处 print 串、`AutoQueue.yaml` 协议头）；判据盲区记档——`ra-token-defined` 扫描面维持"规则承载件"口径，工具/队列/交接件的活引用由人工核查＋本条登记兜底（是否扩判据面属另一决策项、未自决） | 2026-09-23 |
| **ISS-050** | 文档 | 2026-09-23 | 知识库同步轮后的回头核查（领队） | **两处计数漂移**：① `AGENTS.md` 规则 23 写"12 项机械不变式"，而 `gate.py check` 实跑为 **22 PASS**（判据自建立期持续增长，两个数字间无同步机制）② "§12.3 必报项"计数写作"**四类**"（`AutoQueue.yaml` 协议头、`gate.py` 的 `cmd_escalate` 提示串），而 `doc/项目开发流程.md` §12.3 与知识库 v3.0 §10 均为**三类**（表 3 行；"P3 恒归人"是另一段注、不构成第 4 类） | `AGENTS.md` 规则 23、`AutoQueue.yaml` 协议头、`gate.py` 打印串 vs `gate.py check` 实跑末行（22 PASS / 0 WARN / 0 FAIL）与 `doc/项目开发流程.md` §12.3 | 已解决 | R0 2026-09-23 指令；修法取"**权威源＝工具实跑**"，不写死数字以防再漂 | ① `AGENTS.md` 规则 23 改为"判据以实跑输出末行为权威源、不写死项数（建立期 12 项、2026-09-23 已长到 22 项）"② 2 处"四类"改"三类"。`AutoQueue.yaml` Q-000 的 `landable` 内"12 项不变式"系当时快照，按"不改写历史"保留 | 2026-09-23 |
| **ISS-051** | 文档 | 2026-09-23 | 知识库同步轮后的回头核查（领队） | **知识库流程文档版本口径过期**：知识库《AI进行IC开发验证工作流程》已于 2026-09-23 升至 **v3.0**（新增 §1.4 角色独立性、§6.1 评审收敛两档制、§6.2 评审 list、§10 P1~P4），项目内 2 处活引用仍按 **v2.1** 指向它（`run_cmd/AutoQueue.yaml` 协议头、`script/gate.py` 文件头注释）；**且两处引用的 §6.1 在该库 v2.1 时代并不存在**（悬空引用），v3.0 才补正——即项目曾长期引用一个未定义的节号 | `AutoQueue.yaml` 头部与 `gate.py` 头注释 vs 知识库 v3.0 §6.1 实存；知识库《更新记录》同日三条 | 已解决 | R0 2026-09-23 指令；与知识库同步面（`更新记录/知识库更新记录.md` 同日三条）对齐 | 2 处版本号改 **v3.0** 并注明"§6.1 系 v3.0 新建"；`HANDOFF.md` 与 `doc/verify/05` 的历史记录按"记录不改写"保留（新记录更正）。**连带闭合**：项目对 §6.1 的引用自此转为有效 | 2026-09-23 |
| **ISS-052** | 流程 | 2026-09-23 | R0 裁定"选 A"（针对 ISS-049 遗留的判据盲区扩面方案） | **`ra-token-defined` 扫描面盲区**（ISS-049 实证）：旧扫描面仅 `RULE_FILES` 五件，不含工具自身、队列与交接件 ⇒ 未定义编号的活引用可在这些文件里回流而判据不拦；**裸扩面不可行**——实测会引入 14 处假阳（`gate.py` 判据自述 2 处、`HANDOFF` 历史记录 12 处），须同时定豁免口径 | 扩面前实测计数（`gate.py` 2 / `HANDOFF` 12 / `AutoQueue` 0）；本条目附双向验证输出 | 已解决 | **R0 2026-09-23 选定 A**（分层扩面、零假阳）；属**监督通道自身改动**，经人批后实施 | **已实施**：① `script/gate.py` 新增 `RA_LIVE_FILES`（规则承载件＋本工具＋`run_cmd/AutoQueue.yaml` 全件）与行级豁免标记 `记录不改写`；`_ra_live_lines()` 对 HANDOFF 只取**最后一条记录**的"未完成/下一手"（最新交接面，其下均为快照）② 判据自述段去编号字样（指向本条与 ISS-048）③ **双向验证**：基线 22 PASS → 注入 RA7 → `ra-token-defined` **1 FAIL** → 加豁免标记 → PASS → 撤除 → PASS 且 sha256 前后一致 ④ 送审挂 **Q-031**（R6，非决定性，按规则 22 附清单比对与 mtime 窗口） | 2026-09-23 |
| **ISS-055** | 流程 | 2026-09-23 | 人指令（本会话三连：验证→`doc/verify`、设计→`doc/design`、spec 原地 → 评审文件入 `doc/评审` → 改名 `doc/review`） | **doc/ 目录分类改造**：原顶层平铺 8 件 + `spec/` 单层，验证产物、治理件、评审件混放，"九步流水线产物 / 设计承诺 / 评审取证"三类证据面不可分辨。四类落位：`00-问题记录.md`→`doc/process/`；`03/04/05`→`doc/verify/`；`01-评审记录.md`＋`01-评审探针/`→`doc/review/`；`doc/design/` 预留（`.gitkeep`）；治理四件（项目开发流程/AI角色与职责/环境搭建/交接说明）留顶层 | 清扫 744 处（doc/NN 703 + 评审件 37 + 裸形态 4）/ 残留复扫 0；`gate check` 24 PASS / 0 WARN / 0 FAIL；`path_audit` 缺失 0／骨架 0／占位 0；冻结证据 30 件零改动 | 已解决 | R0 指令（非《驱动计划》派生）；R8 落实并自留痕（`run_cmd/AutoQueue.yaml` Q-035） | ① 迁移与清扫：`git mv` 四件 + 三批脚本（明细 `doc/verify/05-回归记录.md` R-058）② 判据同步：`script/gate.py`（台账/看板常量、`scanned_docs()` 改递归、`char_freq()` 语料根钉 `doc/`、`BASE_GLOBS` 改指 `doc/review/*-评审探针/`）与 `script/path_audit.py`（骨架 +4 目录）③ 声明面：`AGENTS.md` §2 目录约定、`README.md`、`doc/交接说明.md`（实测刷新：知识库硬编码 10 文件 26 处）、`run_cmd/AutoQueue.yaml`、HANDOFF、`.qoder/agents/*.md` ④ 长基线按 ADR-4 重折（旧→新 `sha256:77efa13c…`→`sha256:aabdd271…`，明细 `doc/verify/05-回归记录.md` R-059）⑤ 提交 `5dce8c0` | 2026-09-23 |
| **ISS-056** | 平台 | 2026-09-23 | Q-004 恢复前例行 `rocheck --baseline`（`doc/verify/05` R-062） | **工作树行尾批量物质化事件**：2026-09-23T22:33:06 全树同秒批量重写（97 件 mtime 同秒）；内容侧 37 件中 36 件为纯 LF→CRLF（CRLF 归一后与长基线 md5 全等、且与 `git show 46bbc45:<f>` blob md5 相等 ⇒ 语义零变更），1 件为 R-060 已披露的 `sim/covdb/README.md` 补修；另 `sim/covdb/.gitkeep`（未入库占位件、`.gitignore` 覆盖）被删。**来源未确证、非项目角色写入**（无任何角色声明的写面包含这些件） | 全量差异复算（37 MODIFIED / 1 DELETED / 60 MTIME-ONLY）；逐件「CRLF 归一后 md5＝基线 md5」对照＝36/36 全等；`git show 46bbc45:filelist/.gitkeep` 与 `iss/README.md` 的 blob md5 与基线记录相等；`git status` 净（git 归一化视图下无改动） | 已解决 | 平台类，按红线 R2 由 R8 直接处置并登记 | 长基线按 ADR-4 例行折入（`snapshot --refresh --reason`，本轮 ≤1 次）→ `script/gate/integrity_baseline.json`：`sha256:aabdd271…f87ff21`→`sha256:098fca46…8bb75`；R-060「预期 MODIFIED 1」更正为实况 37＋1；留痕 `doc/verify/05-回归记录.md` R-062 | 2026-09-23 |
| **ISS-057** | 文档 | 2026-09-23 | D-14 终局全量复核轮·章节片⑤（`spec/01` §2/§4/§5/§7/§8，auditor 只读；`doc/verify/05` R-064／评审记录附 M） | **章节片⑤新增 9 阻塞**：B-1 回滚终止规则不覆盖非分支/无 rd 分支的冲刷点；B-2 冲刷当拍 P8 档无撤销/归还载体；B-3 回滚路径 P7 在途组物理号归还缺失；B-4 `disp_x_t.lq_id/sq_id` 的 S→IQ 载体缺失（与已闭合 H-01 同型断链）；B-5 `HIST_DEPTH`"待定"残留＋悬空参数名 `CHECKPOINT_NUM`；B-6 BP1 门控三口径并存；B-7 §8.2"首现标注"与正文不符；B-8 ckpt 槽生命周期两边界未定义；B-9 §7 A4 不可判定。另需澄清 4（C-1~C-4）、存疑 1（S-1 **已裁：加域前缀区分**，并入落文批；见 `doc/verify/05` R-065） | 报告全文＝`doc/review/01-评审记录.md` 附 M；窗口核查＝`doc/review/01-评审探针/rocheck_r17_window.txt` | 已解决 | 机制取舍（B-1/B-2/B-3/B-4/B-6/B-8/B-9）由决策 AI 按 §12.2 自决留 ADR（**D1/D2 均已回收**：ADR-B1/B2/B3/B8 见 `doc/verify/05` R-067、ADR-B4/B6/B9 见 R-066，**均须进 §9.5 ④段**）；文本类（B-5/B-7/C-1~C-4）随决策落文批 | **处置中**：D1/D2 均已回收（R-067／R-066）；**L1 落文片待派**（决策落地＋文本类，分片顺序执行以免同文件并发写）；落文与复核待其后轮次；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环（附 M~附 Y；R-062~R-093；Q-034 置 done）** | 2026-09-24 |
| **ISS-058** | 文档 | 2026-09-24 | D-14 增量复核片 A1（技术核销；auditor 只读；`doc/verify/05` R-074／评审记录**附 N**） | **增量复核（r17 处置批 L1~L6）判 C2 仍不成立**：旧问题核销＝通过 11／已改但连带新发现 3（B-1/B-2/B-4）／部分 1（B-9）／未核销 1（C-3）；**新增阻塞 4**——阻-1 §5.3 终止谓词等值反例（`older(e,f) ≜ ((f−e) mod 256) < 128` 在 `e==f` 判真 ⇒ `!older(e_top,B)` 恒不弹 B 自身，与同句括注「B 自身映射必被撤销」相反；领队亲证成立；同源存-1 触 §3.19 规则 1）；阻-2 `flush_apply` 当拍 S 级压栈与 `SP := SP_ck` 双写者次序未定（A2 同判）；阻-3 §7 A4 `##1` 与「组合重算不得打一拍延迟」不等价（A2 同判）；阻-4 §8.1「最新」引件停 r12（r13/r17/r17b 三代已存）。另澄清 2（澄-1 `sq_id` 5/6 对账未落；澄-2 R-071 机判行引失败调用、已加 R-071 备注）、存疑 1（存-1）。抽查复算 6/6 与机判一致、闭包「未展开」16 项分流无真断链 | 报告全文＝评审记录**附 N**；窗口核查＝`rocheck --window 2026-09-24T00:44:59`（MODIFIED/ADDED/DELETED/MTIME-ONLY 全 0）；机判件＝`doc/review/01-评审探针/machine_r17b_*`（基线 fee7449） | 已解决 | 机械/文本类由 R8 派落文片处置；阻-1~阻-3＋存-1 涉机制语义 ⇒ 决策 AI（D3 片）自决留 ADR；阻-4 随落文批 | **处置中**：**D3/D4 决策片均已回收**（`doc/verify/05` R-076／R-077；D3＝ADR-D3-①~⑤、D4＝ADR-D4-①~④）；**落文批 L7 已完成**（R-078；42/42 锚点）；**A3 增量复核已回收**（R-079：通过 41／部分 4／阻塞 1＝阻-L7-1）→ 续处置 D5/L8；其后 A4（窄）→A5 终局全量；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环（附 M~附 Y；R-062~R-093；Q-034 置 done）** | 2026-09-24 |
| **ISS-059** | 文档 | 2026-09-24 | D-14 §12.5 决策审核（风险主轴·一次·非决定性；auditor 只读；`doc/verify/05` R-074／评审记录**附 O**） | **七 ADR（B-1/B-2/B-3/B-8/B-4/B-6/B-9）风险主轴审核 17 行（去重 16）**：可接受 0／需进 §9.5 ④段 7（同一来源＝D-13 未收 B-1~B-9）／需决策 AI 回应 10——B1-① 建议「回滚活性 ≤6 拍」与 §5.2 延迟公式（5＋ceil(33/8)＝10 拍）不可勾连；B1-② 起弹拍/同拍压栈可见性未写；B1-③ 回滚期二次冲刷再定向未定义；B8-① `ckpt_pend_v` 置位/清零二读＋R→S 窗口无判据；B6-① `rob_alloc_grant` 全库无定义（领队亲核：`doc/spec/` 仅 A1 断言行 1 处）；B4-① `sq_id` 口径（同澄-1）；B6-②/B8-② `spec/13` 登记缺独立载体。②不可逆 0（七项回退方式均在位）；①判错 2；④自洽 3；⑤致死（可构造触发）4 | 报告全文＝评审记录**附 O**；七项 ADR 全文＝`doc/verify/05` R-066/R-067 | 已解决 | §12.5 发现**非决定性**——由决策 AI 在「剩余分歧」逐条回应（可修可维持）并随 D-13 进 §9.5 ③/④段供人签署时读；D-13 ④段汇集义务同 ISS-058 | **处置中**：**D3/D4 均已回收并逐项回应**（R-076＝B1-①②③/B2-①/B9-① 五份；R-077＝B4-①/B6-①/B6-②/B8-①/B8-② 四份；含 R-066 自报未决① `sq_id` 由 ADR-D4-① 关闭）；L7 已完成（R-078）；A3 复核回收（R-079，附 P）判 1 阻塞；**④段汇集已随 L7 落**（D-13 行扩面）；续处置 D5/L8；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环（附 M~附 Y；R-062~R-093；Q-034 置 done）** | 2026-09-24 |
| **ISS-060** | 文档 | 2026-09-24 | D-14 §12.5 审核 A2 发现（B6-②/B8-②）＋`doc/verify/05` R-077 ADR-D4-④ | **`spec/13` 性能建模输入无独立登记载体**：①R-066 自报"B-6 停顿率影响归 `spec/13`（登记）"②B-8① 使 `needs_ckpt=0` 回滚档（`5+ceil(n/8)`，最坏 10 拍）由稀有转常态均无载体——A2 取证三面（`spec/00` §10、`doc/verify/03` §0 D-13、`spec/01` §4/§1.1）全落空。R-077 补实前该"（登记）"字样不成立（诚实性附注在案） | 评审记录**附 O**（B6-②/B8-②）；`doc/verify/05` R-066/R-067/R-077 | 处理中 | 决策 AI 自决（ADR-D4-④：组合载体；R8 开列台账号） | **处置中**：**落文批 L7 待做**——`spec/00` §10 新增行（本号）＋`spec/01` §4 BP1/§5.2 行内注＋`doc/verify/03` §0 D-13 行扩面；解除＝`spec/13` 成稿建模收录并回填 | — |
| **ISS-061** | 文档 | 2026-09-24 | D-14 增量复核片 A3（L7 落文批；auditor 只读；`doc/verify/05` R-079／评审记录**附 P**） | **A3 判 C2 仍不成立**：核对 46＝通过 41／部分 4／**阻塞 1**（阻-L7-1：§5.3「回滚期二次冲刷」判据 `older(B_o, B)` 非严格 ⇒ 同级态与「同级⇒致命」相抵）；需澄清 4（澄-L7-1 `ckpt_pend_v` 窗口 (a) 被杀路径清零点火源；澄-L7-2 `rob_head_id`/`rob_head_type` 悬挂名；澄-L7-3 §8.1 引件代差；澄-L7-4 联动义务载体）；抽查复算 7/7 机判可信 | 报告全文＝评审记录**附 P**；窗口核查＝`rocheck --window 2026-09-24T02:12:36`（全 0） | 已解决 | 阻-L7-1＋澄-L7-1＋澄-L7-2 派 D5 决策片（小片）；澄-L7-3 随 L8 落文；澄-L7-4 由 R8 落队列；其余 41 项通过 | **处置中**：D5（R-080）→ L8（R-081）→ A4（R-082：11/11 通过）→ **A5 终局全量回收**（R-084／附 R：旧问题 38＝通过 37／部分 1／未改 0；新增 1＝BL-1）→ **BL-1 由 L9 修复**（R-083；附记 `fdirty` 一并更正）＋CL-A（枚举补 ISS-054）／CL-B（队列载体补）→ **A6 窄确认待做** → C2 判定材料呈 R0；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环（附 M~附 Y；R-062~R-093；Q-034 置 done）** | 2026-09-24 |
| **ISS-062** | 文档 | 2026-09-24 | A7 终局全量复核轮（R0 指定·最终态；auditor 只读；`doc/verify/05` R-086／评审记录**附 T**） | **A7 判 C2 两要件按最终态均不成立**：旧问题核对 41 行＝通过 39／未成立 2（澄-L7-4≡CL-B 同一缺口，因 Q-034 landable 三笔清单被 R8 收口改写覆盖——BLK-2/SUS-1）；新增＝B 类 2（BLK-3 §3.19 规则 4 "`S` 级写 `src_val`"标签与 A9"W 后读拍"相抵（R-052 自报线在）；BLK-4 §7 A6 释放判据"被冲刷回滚完成"与 B-8②"冲刷生效拍"统一相抵）＋文本 1（CLR-1 版本表 v0.3 状态行停留第 9 轮）＋流程/证据面 3（BLK-1 `machine_r20_changed_all.txt` 因 gate.py 在 GBK 控制台崩于 `⚠` 打印而损坏；CLR-3 `doc/verify/03` D-14 行状态停留；CLR-4 ISS-053 两态并存）＋跨篇澄清 1（CLR-2 `spec/04` 自报 K=2 三处回写未见执行）；C 类 0；存疑 1（SUS-1） | 报告全文＝评审记录**附 T**；窗口核查＝`rocheck --window 2026-09-24T07:17:08` | 已解决 | **即处置（R8）**：BLK-1 重生成件（UTF-8）＋`gate.py` 输出编码根治（stdout/stderr `reconfigure(encoding=utf-8)`，防复发）／BLK-2 清单恢复（本轮修复批）／CLR-3 D-14 行刷新／CLR-4 ISS-053 实核关闭；**L10 微落文**：BLK-3／BLK-4／CLR-1／CLR-2（以 R-048/R-049/R-052 为锚）；SUS-1 确证（非外部改写） | **处置已完成**：R8 修复批（`4afd5df`/`51531b6`）＋L10 微落文（R-087：BLK-3/BLK-4/CLR-1/CLR-2 全改）；跨篇清单扩第④笔（`spec/04` `d_S=1` 对账）；`§3.19` 锁定块 (b) 括注转复核判；（R-088）→ B-1/B-3 即修 → （R-089）A9 微确认 → **（R-090/附 W）A10 再裁全量：旧问题 56/56 满足、新增 4 低烈度** → L11 全部处置（R-091）→ A11 微确认（R-092）→ **A12（R0 续裁第三轮全量，R-093／附 Y）：新增 0、旧问题 56/56、缺失型清单为空 → C2 两要件按最严格字面均满足，呈 R0**；台账口径：本行待 R0 裁决后可转已解决（呈人）；**2026-09-24 R0 裁决 C2 成立 → D-14 全链闭环（附 M~附 Y；R-062~R-093；Q-034 置 done）** | 2026-09-24 |
| **ISS-063** | 验证代码 | 2026-09-24 | Q-010 收口片·ISS 互校（探针 P1/P2 内联，见 `doc/verify/05` R-096） | **golden ISS 接受 SLLI 保留编码**：`SLLI` 在 bit30=1 ∧ funct3=001（RV64 保留构型、规范行 37342 域）时**未报 illegal**——探针 P1 `exc=0`；同型 `SLLIW`（P2）正确报 illegal，属实现漏检（该编码在 RV64 下非法，须按 IALIGN=16/保留编码规则拒绝） | `iss/vriss/*.py`（只读互校）+ 探针 P1/P2（命令与结果记 R-096；**未落盘件**，修复时应补落 `iss/tests/probes/`） | 已解决 | 判责＝ISS 侧（spec/02 §16.3 D-1 已列报）；按红线 R2 交 verifier 直接修并登记 | **处置中**：verifier 片修 `iss/` 保留编码拒绝路径＋补落盘探针＋回归 `iss/tests/`；修复后回归 `test_instr` 全通过；**2026-09-24 修复回收**：`iss/vriss/machine.py` 直接修（红线 R2 直修路径）＋探针落盘 `iss/tests/probes/rr_probe13.py`／`rr_out13.txt`（修复前 8/17 失败→修复后 17/17 PASS）＋定向用例 `iss/tests/test_isa_semantics.py`（+9 断言，全量 RC=0）；留痕 `doc/verify/05` R-097 | 2026-09-24 |
| **ISS-064** | 验证代码 | 2026-09-24 | Q-010 收口片·ISS 互校（探针 P3 内联，见 `doc/verify/05` R-096） | **MRET 在 MPP=M 时误清 `mstatus.MPRV`**：探针 P3 实测 1→0；规范行 45130「If y≠M, xRET also sets MPRV=0」——MPP=M 时**不得**清 MPRV，属实现多清（特权语义错） | `iss/vriss/*.py`（只读互校）+ 探针 P3（命令与结果记 R-096；**未落盘件**，修复时补落 `iss/tests/probes/`） | 已解决 | 判责＝ISS 侧（spec/02 §16.3 D-2 已列报）；按红线 R2 交 verifier 直接修并登记 | **处置中**：verifier 片修 MRET/MPRV 语义＋补落盘探针＋定向用例（MPP=M/MPP≠M 两分支）；修复后回归；**2026-09-24 修复回收**：`iss/vriss/machine.py` 直接修（红线 R2 直修路径）＋探针落盘 `iss/tests/probes/rr_probe13.py`／`rr_out13.txt`（修复前 8/17 失败→修复后 17/17 PASS）＋定向用例 `iss/tests/test_isa_semantics.py`（+9 断言，全量 RC=0）；留痕 `doc/verify/05` R-097 | 2026-09-24 |
| **ISS-065** | 验证代码 | 2026-09-24 | ISS-063/064 修复期同族扫描（同表达式整改，见 `doc/verify/05` R-097） | **同族两项**：① W 型（`SLLIW`/`SRLIW`/`SRAIW`）`imm[5]≠0` 保留构型被**静默接受**（未报 illegal；锚＝提取件行 3516–3518）② `ADDIW` 被按移位域**过拒绝**（imm=100/−1/0x400 等合法立即数假 illegal；锚＝行 3417）——同一表达式整改时一并修复 | `iss/vriss/machine.py`（`_op_imm`／`_op_imm32` 整改）+ `rr_out13.txt`（17 检查含 W 型/ADDIW 域断言）+ `test_isa_semantics.py`（+9 断言含 W 型 imm[5] 与 ADDIW 域） | 已解决 | 验证代码类，随 ISS-063/064 直修并登记（红线 R2） | 落点＝`doc/verify/05` R-097／`iss/vriss/machine.py`／`iss/tests/probes/rr_probe13.py`／`iss/tests/test_isa_semantics.py`（修复后 17/17 PASS、语义全量 RC=0）；本项与 ISS-063/ISS-064 同批进 Q-010 复核片 | 2026-09-24 |
| **ISS-066** | 文档 | 2026-09-24 | Q-010 增量复核轮（auditor 只读；`doc/verify/05` R-098／评审记录**附 Z**） | **1 阻塞＋4 澄清**：阻塞-1＝`spec/02` §16.4-(2) T-7 边界句称"Table 2 未列 rd=rs1"与提取件行 2750–2757（Table 2 第五行在）＋2762–2765（"only pushed"）相抵；澄-1＝D-3/D-4 证据未落盘＋P 编号同号不同物（R-096 文 vs rr_probe13 件）；澄-2＝AN-27/AN-29 行号上界不一致；澄-3＝md5 不可复算（记录面）；澄-4＝保留 vs 非法判读依据缺（待 T-9/spec/10） | 报告全文＝评审记录**附 Z**；窗口 `09:47:21` | 已解决 | 阻塞-1＋澄-2＋澄-4 ⇒ L12（spec/02 文本三更正）；澄-1 ⇒ verifier 微片（探针落盘＋对照表）；澄-3 记录面留痕 | **2026-09-24 闭环**：处置批＝L12/L12b（R-099）＋verifier 微片（R-100）＋L12c（R-101）；A13 增量复核（R-104／附 AA）判**阻塞-1 与澄-1/2/4 技术面已消**（无阻塞）；澄-3 记录面处置转入 **ISS-068**（待决策）；落点＝`doc/spec/02`／`iss/tests/probes/rr_probe14.py`／`rr_out14.txt`／`doc/verify/05` R-099~R-104 | 2026-09-24 |
| **ISS-067** | 文档 | 2026-09-24 | Q-010 处置批增量复核轮 A13（auditor 只读；`doc/verify/05` R-104／评审记录**附 AA**） | **新增 6 项（全低烈度，无阻塞）**：澄-A＝§16.5/§16.6 T-7 锚点仍 2722–2746 未随 §16.4-(2) 对齐；澄-B＝rr_probe14 文件头"R-096 称 0x88 为复位值"转述无原件；澄-C＝D-2 行 P3b（MPP=S）vs 落盘 P4b（MPP=U）子项未对账；澄-D＝T-7 承接义务未入队列级跨篇指针；澄-E＝Q-010 landable 过期；澄-F＝片级 numstat 之和与区间差 1 行 | 报告全文＝评审记录**附 AA**；窗口 `10:36:28` | 已解决 | 澄-A ⇒ L12d；澄-B/澄-C ⇒ verifier 收口微片；澄-D/澄-E/澄-F ⇒ R8 收口动作（红线 R2 直接处置＋登记） | **2026-09-24 同批闭环**：澄-A＝`doc/spec/02` §16.5/§16.6 对齐 2722–2757（R-103）；澄-C＝P14-3（MPP=S→MPRV 清 0，实测 `mstatus=0x88 priv=1`）＋D-2 行落盘指针（R-102/R-103）；澄-B＝转述改述（R-102）；澄-D＝Q-034 跨篇清单⑥＋Q-006 `done_when`；澄-E＝landable 刷新；澄-F＝对账说明（差 1 行＝§16.3.1 D-4 行被 L12/L12c 先后编辑各记一次）；落点 `doc/verify/05` R-102/R-103/R-104 | 2026-09-24 |
| **ISS-068** | 流程 | 2026-09-24 | Q-010 处置批增量复核轮 A13·存疑-1（auditor 只读；`doc/verify/05` R-104／评审记录**附 AA**） | **澄-3 记录面留痕是否足够（md5 不可复算的能力限制）**：`machine.py` 修复前后 md5 本环境不可独立复算；替代核验在案＝rr_out13 块头与 R-097 表述一字一致、四处修复实读在位、R-100 同值复述、rr_out14/13 块头 md5 由实跑写入；改动集不含 `iss/vriss/machine.py`（ISS 零改动有机器面佐证）。**待裁定：上述替代核验是否视为足以闭合"哈希级复算缺失"（判定口径类，AI 不自行推进）** | 附 AA 存疑-1；`doc/review/01-评审探针/machine_r25_changed_all.txt`（不含 `iss/vriss/machine.py`） | 已解决 | **R0 裁决（门禁权在人）：①认可替代核验足够（闭合）**；该裁定属判定口径类，R8 不自决（「全 A」中的第①项） | 落点＝`doc/verify/05-回归记录.md` R-108；判据＝替代核验三面在案（`rr_out13`/`rr_out14` 块头 md5 由实跑写入＋修复实读在位＋改动集不含 `iss/vriss/machine.py`），任一可执行环境可复算比对；另见评审记录**附 AA** 存疑-1 | 2026-09-24 |
| **ISS-073** | 文档 | 2026-09-24 | Q-006 续写片（`spec/03`，设计面；`doc/verify/05` R-109／HANDOFF `- [Q-006]` 条） | **两条接口缺口（设计片按篇首②退回领队，G1 前必须闭合）**：①`is_jalr_ret` 新谓词（`rd≠rs1`）需 `rs1`/`rd` 架构号，而 `disp_x_t`（BRU 输入）只有 `rs1_val`、无架构源号 ⇒ `spec/01` §3.12「RAS 更新用」在现接口上不可产生；②校正队列出队身份键＋`bp_upd_id` 提交侧载体——ROB 链（`spec/01` §3.6/§3.7/§3.17）无 `ftq_id`/`bp_upd_id`，计数近似或 `pc` 匹配均附不安全性/死锁构造性论证 | `doc/spec/03` §2.8（e)(f)＋§7 S8 缺口负面证据；`spec/01` §3.4/§3.5/§3.12 只读实读 | 已解决 | 接口/位宽变更归 `spec/01` 升版（G1 前走接口升版回写；R1 片承接）——候选方案已由设计片列于 `spec/03` §2.8；R8 登记并编入 G1 前缺口清单；具体承接片随 `spec/01` 后续片排期 | **ADR-S1（RD 自决，2026-09-24）：以身份键面关闭**——`ftq_id` 六环全链＋队首唯一命中/同号并存消歧/C8 三合取；落文＝Q-036 v0.4＋L13（R-114），独立复核＝S-01b（R-118，0 阻塞）；`bp_upd_id` 提交侧载体**不作关闭要件**，转出 **ISS-082**（承接 T3-4＋接口片，G1 前闭合） | 2026-09-24 |
| **ISS-082** | 文档 | 2026-09-24 | S-01/S-01b 复核（附 AE S1／附 AF）＋**ADR-S1（RD 自决）转出登记** | **`bp_upd_id` 提交侧载体**：ROB 链（`doc/spec/01` §3.6/§3.7/§3.17）无该域、现链止于 `br_resolve_t`；消费机制（IUM 更新事件／§2.7(c) 提交面学习与分配载荷）与「预测更新表」结构·深度（T3-2①）均未定 | `doc/spec/03` §2.9(b)/§2.9 表行/§4 T3-2①·T3-4·T3-5/§5；`doc/spec/01` §3.6/§3.7/§3.17（待落） | 处理中 | 承接＝`doc/spec/03` T3-4 收口片＋后续 `doc/spec/01` 接口片**一块落地**；**G1 前必须闭合**；关闭判据＝载体全链落文＋（涉位宽时）`width_check` 复算＋独立复核（A 案落文清单见 `doc/verify/05` R-120） | **待承接片**（G1 前） | — |
| **ISS-085** | 文档 | 2026-09-24 | S-03（Q-006 收口·评审片 δ 复核；`doc/verify/05` R-123／评审记录**附 AG**） | **3 阻塞＋3 澄清**：B1＝`doc/spec/01` §3.11「逐字一致」未改净（域命名差异，K-1 类残留）；B2＝`doc/spec/03` §2.8(c) `01` push 未写入队句、(d) 上溢「丢弃」未明入队与否（触发＝boundary 后仅 push 段被冲刷；按字面实现逆放无条目源）；B3＝校正队列满（8 在途）行为未定义（实现三选自担：环形已否/丢弃破坏可复现/停 P2 属新增反压）；C-1 §2.8(a) 标题口径、C-2 `spec/00` §4.8「（未建）」残留、C-3「尾调用」括注名实 | `doc/spec/03` §2.8(c)(d)(e)(f)/(a)；`doc/spec/01` §3.11/§3.1；`doc/spec/00` §4.8；提取件行 2762–2765 | 已解决 | **B1＝已即修**（`spec/01` §3.11 改「同构＋域命名换算」）；**B2／B3＝已裁（ADR-S04-B2/B3，RD 自决，`doc/verify/05` R-125）**：`01` 入队（`val` 不消费写 0）／上溢不入队／**队列满＝停 `P2`**（无死锁构造性论证；三候选对比在案）；C-1＝§2.8(a) 改「同构」、C-3＝`spec/01` §3.1 括注「尾调用」→「协程」；C-2 转 `spec/00` 升版登记；**落文清单 OP-1~OP-11 待本体执行**（无位宽变更；`width_check` 预期 28/28 不变） | **2026-09-24 闭环**：B1 即修（`spec/01` §3.11 同构）；B2/B3 经 **ADR-S04-B2/B3**（R-125）裁定并**落文完成**（`spec/03` §2.8(a)(b)(c)(d)(e)＋I3-10＋§4/§5/F3＋版本表 v0.1 续6；`spec/01` §3.1＋版本表 v0.4·S-04；**无位宽变更**，`width_check` 28/28）；C-2 转 `spec/00` 升版登记 | 2026-09-24 |
| **ISS-086** | 文档 | 2026-09-24 | S-05（Q-006 收口·关闭片复核；`doc/verify/05` R-127／评审记录**附 AH**） | **1 阻塞＋3 澄清**：B-1＝`doc/spec/03` §2.8(c)(d) 组合缺格 **`op=11` 且 `sp==0`** 的入队格式未明文（分解读法可推 `{2'b01,0}` 自洽；复合读法 `val` 无来源、逆放不能复原 ⇒ I3-9/I3-10 该角点不可判；R-124 已点名建议全组合对照）；C-1＝OP-7（(e)2 补「＝I3-10」）字面未落、引用以反向表述存在；C-2＝OP-9「§3.2／F3 交叉引」两读（F3 已落、§3.2 正文未增引）；C-3＝「`val` 不消费」句落点由 (e) 挪到 (c)（语意覆盖） | `doc/spec/03` §2.8(c)(d)(e)；`doc/verify/05` R-125/R-124；提取件行 2762–2765 | 已解决 | **B-1＝已裁（ADR-S05-B1，RD 自决，`doc/verify/05` R-128）**：取**分解/降格**读法——`11`@`sp==0` ⇒ 弹半未遂不入队＋压半按 `01` 入队**降格条目 `{2'b01,0}`**（逆放 `sp:=sp−1` 自洽）；随批写死「逐半按序、按应用后状态判前提 ⇒ `11` 无上溢路径」；读②复合字面经验算 I3-9/I3-10 俱破（否）；全组合 1,188 格缺格清零。C-1＝补正向引（OP-7 落实）；C-2＝§3.2 正文增引；C-3＝认可 (c) 单源；**无位宽变更**。**落文清单 8 op 待本体执行（S-05b）** | **2026-09-24 闭环**：B-1 经 **ADR-S05-B1**（R-128：分解/降格读法，全组合 1,188 格缺格清零；复合读法经两读验算破 I3-9/I3-10 已否）裁定并**落文完成**（S-05b：`spec/03` 8 op；**无位宽变更**，`width_check` 28/28）；C-1 补正向引／C-2 §3.2 增引／C-3 认可单源；复核随 S-05c | 2026-09-24 |
| **ISS-087** | 流程 | 2026-09-24 | R0 指令（"把'不要停'说得更死一点"；最高 ROI＝强化第一层开工 prompt） | **"不得中途停"固化三处**：①`AGENTS.md` 规则 24 ⑤ 改写——未撞停机四条件则**必须连续执行循环五步，不得停轮、不得以"汇报进展/询问是否继续"结束本轮**，停下须明写"因撞【哪条】而停"；②`doc/自动推进说明.md` §0 新增第 7 条（同口径，为该文件内"开工协议"的硬约束）；③**更新定时任务 `VR1 自动循环推进` 的开工 prompt**（加入同句）。口径来源＝R0 2026-09-24 指令；与规则 23/24 配套（23＝核销纪律，24＝循环触发，本项＝**循环不得中断**） | `AGENTS.md` §5.2 规则 24⑤；`doc/自动推进说明.md` §0.7；定时任务 prompt（Automations） | 已解决 | R0 直接指令；R8 落实并自留痕 | 落点＝`doc/verify/05-回归记录.md` **R-129** | 2026-09-24 |
| **ISS-088** | 文档 | 2026-09-24 | S-05c（Q-006 终局复核；`doc/verify/05` R-131／评审记录**附 AI**） | **2 阻塞＋2 澄清＋1 存疑**：B-1＝`doc/spec/03` r34 面退化为整文件级单 hunk（+378/-376）＋新行 EOL 畸形（双 CR×2／裸 LF×2，**恰为 R8 本批新增行**）；B-2＝`doc/verify/03` §1 `S-05` 行粘连重复（20 管）；C-1＝RD 剩余分歧 3 条处置未定；C-2＝「行尾零违规」声明无证据件；S-1＝Q-006 关闭签署事实（呈 R0） | `doc/spec/03` 行尾统计；`doc/verify/03` §1；`machine_r34_changed_all.txt` | 已解决 | R8 即修：①`spec/03` 双 CR×2＋裸 LF×2 归零（378 行全 CRLF）；②§1 粘连行拆分（10 管复原）；**根因＝R8 插入行的两处机械缺陷**（`s[:e]+row+s[e:]` 生 `\r\r`；LF 模板追加）——同源残留全库扫描：台账双 CR 11 处、摘要件裸 LF 35 处，一并归零；③B-1 事实 1 消解：修后 `git diff --numstat a3524f4 -- spec/03`＝**+8/−6，8 hunk**（正常粒度），r34 面已重生成；④C-2 声明更正（行尾改为「控制字零违规（机判）＋行尾手工扫描」）；C-1 转承接登记；S-1 呈 R0 | 落点＝`doc/spec/03-分支预测与取指前端.md`（EOL 归零）／`doc/verify/03-验证计划.md`（§1 行修复）／`doc/verify/05-回归记录.md` **R-131**；证据件 `doc/review/01-评审探针/rocheck_r34_window.txt` | 2026-09-24 |
| **ISS-083** | 流程 | 2026-09-24 | R0 指令（"把自动循环执行写到什么地方固化下来……没有我都要反复强调才能自己开始"） | **自动循环固化**：`AGENTS.md` **新增规则 24「开工即自动推进」**（开工三步／循环五步／待决项分流／停机四条件／默认不请示）＋`doc/自动推进说明.md` 新增 **§0 开工协议**；使新会话/新接手 AI **读到即自动进入循环**，不再依赖人重复强调 | `AGENTS.md` §5.2 规则 24（标题同步改「二十四条」）；`doc/自动推进说明.md` §0 | 已解决 | R0 直接指令；R8 落实并自留痕；与规则 23（R8 编排）互补——23 定"怎么核销"，24 定"何时开始、何时继续、何时停" | 落点＝`doc/verify/05-回归记录.md` **R-121** | 2026-09-24 |
| **ISS-084** | 流程 | 2026-09-24 | R0 指令（"将怎么循环 角色怎么干 都固化下"） | **角色作业守则（SOP）固化**：`doc/自动推进说明.md` 新增 **§7**（5 方块：本体／审计／决策 RD／R8／R0——各自"开工读什么→怎么干→产出/落点→停在哪里→留痕纪律"）；`AGENTS.md` 规则 24 指针补 SOP 锚 | `doc/自动推进说明.md` §7；`AGENTS.md` §5.2 规则 24 | 已解决 | R0 直接指令；R8 落实并自留痕；与规则 19/23/24 互补（19＝角色边界，23＝核销纪律，24＝循环触发，§7＝每角色操作细则） | 落点＝`doc/verify/05-回归记录.md` **R-122** | 2026-09-24 |
| **ISS-069** | 流程 | 2026-09-24 | R8 规则 22 窗口核查执行顺序自检（本轮收口时发现；`doc/verify/05` R-104／证据件 `doc/review/01-评审探针/rocheck_r25_window.txt`） | **A13 轮的规则 22 窗口未隔离评审者**：`--window 2026-09-24T10:36:28` INIT 后，同窗内先有处置批写者（verifier 收口微片 R-102、L12d R-103）动手，再有 R8 归档写面（R-104）——核查结果 MODIFIED 10 件虽**全数可归因、无未声明写者**，但"评审者零写入"**无法由该窗口单独隔离**（r17 先例的窗口当时无同窗写者，故可隔离） | `doc/review/01-评审探针/rocheck_r25_window.txt`（原始输出＋完整清单补录）；10 件归因表见 R-104 | 已解决 | 纠正口径＝「freeze → INIT → 派只读 → **收工即查**（任何写者启动前）」；**2026-09-24 A14 轮实做**：INIT `11:33:48`（写者未启动前）→ 派只读 → 收工即查 ⇒ MODIFIED 1（`run_cmd/AutoQueue.yaml`＝R8 attempts 写入）／ADDED 0／DELETED 0 ⇒ **评审者零写入、可隔离窗口达成**（后续落盘的本件/证据件属核查后产物，已在证据件内注明） | 落点＝`doc/verify/05-回归记录.md` **R-105**／证据件 `doc/review/01-评审探针/rocheck_r26_window.txt`（原始输出）／评审记录**附 AB** 领队注①（判据：`script/gate.py` rocheck；`rocheck_r26_window.txt` 内容级证据） | 2026-09-24 |
| **ISS-070** | 流程 | 2026-09-24 | Q-010 终局 δ 复核轮 A14（auditor 只读；`doc/verify/05` R-105／评审记录**附 AB**） | **四项（技术面 0）**：B-1＝`doc/verify/05` R-104 ②段「ADDED/DELETED 0」与证据件 `rocheck_r25_window.txt`（内含 ADDED 2：R8 一次性临时脚本与中间输出）相抵，且 2 件无归因；C-1＝r26 复核面基线两说（HANDOFF R-104 条「312cd51」vs 机器件「fff7415」）；C-2＝Q-010 队列字段滞后（`state: ready`／`review_rounds: 0`／`attempts: 1` 无留痕说明）；C-3＝`gate.py` `queue-full-round-cap` 对已闭合项误标「未达限」 | 附 AB 全节；`doc/review/01-评审探针/rocheck_r25_window.txt` | 已解决 | 记录/证据面按红线 R2 由 R8 直接处置并登记；工具措辞属工具自述面，同行修正 | **2026-09-24 同批闭环**：B-1＝R-104 ②段按证据件更正（ADDED 2 归因＝R8 一次性临时件；原句以行内更正标注；**A16/D-1 再更正：措辞应为「两件落盘前首查 ADDED 0（未独立留档）」，原「删后复跑为 0」时序失真——见 ISS-071／ISS-072**）＋附 AA 领队注①引注统一；C-1＝权威基线定为 **fff7415**（机器件自述），HANDOFF 更正条；C-2＝Q-010 回填 `state: in_review`／`review_rounds: 3`（附 Z／A13／A14；≤REVIEW_CAP 5），`attempts: 1` 系 A14 首派连接中断（ISS-053 写入口）；C-3＝`script/gate.py` 措辞改「已闭合，cap 不适用」（Q-004/Q-015 现输出如示）；落点＝`doc/verify/05` R-105 | 2026-09-24 |
| **ISS-071** | 流程 | 2026-09-24 | Q-010 δ 复核轮 A15（auditor 只读；`doc/verify/05` R-106／评审记录**附 AC**） | **五项（技术面 0、阻塞 0、相抵 0）**：C-14＝Q-010 `landable`「下一手」句停在 A14；C-15＝「删后二次核查为 ADDED 0」时序失真（首查在临时件落盘**前**）且该次复跑无输出留痕；C-16＝`rocheck` 单槽窗口语义被误述（对旧 ISO 重跑＝重新 INIT 覆盖现窗，而非「显示 ADDED 1~2」）；C-17＝跨篇清单①（`spec/02` 异常提交抑制写对照）未编入 Q-010 `done_when`；C-18＝长基线自 R-095 后未再折入 | 附 AC 全节；`doc/review/01-评审探针/rocheck_r26_window.txt`；`script/gate/integrity_baseline.json` | 已解决 | 记录/登记面按红线 R2 由 R8 直接处置并登记；长基线按规则 22 走 `snapshot --refresh --reason`（本轮 ≤1 次） | **2026-09-24 同批闭环**：C-14＝`landable` 刷新；C-15＝四处措辞更正（R-104 ②／R-105 ②／HANDOFF 更正条／附 AB 领队注②）并披露留痕缺口；C-16＝证据件注更正＋运维口径（勿对旧 ISO 重跑）；C-17＝Q-010 `done_when` 编入清单①（未落文项随呈判呈 R0）；C-18＝长基线折入（旧→新指纹 `57ca0712…` → `8f1eb3fa029e3…`，148 件；规则 22 独立提交＋留痕）；落点＝`doc/verify/05-回归记录.md` R-106 | 2026-09-24 |
| **ISS-072** | 流程 | 2026-09-24 | Q-010 δ 终局确认轮 A16（auditor 只读；`doc/verify/05` R-107／评审记录**附 AD**） | **四项（技术面 0、阻塞 0、相抵 0）**：D-1＝ISS-070 行残留旧表述「删后复跑为 0」（非引述位置的唯一残留，与更正后四处相抵）；D-2＝R-106 批对 HANDOFF「更正 Q-010 A13 回收入库条」做了**行内改写**，与 `HANDOFF.md` 头注「一条记录不得跨轮修改；要更正就加新记录」相抵（本链首次此类违规）；D-3＝C-16 更正文未分述「现槽仍持该 ISO 时重跑＝四分类分支」与「已换 ISO 时重跑＝re-INIT 覆盖」两种槽态；D-4＝Q-010 `landable` 链叙述缺 A14 段（附 AB／R-105） | 附 AD 全节；`.qoder/handoff/HANDOFF.md` 头注；`script/gate.py` `cmd_rocheck` | 已解决 | 记录/文本面按红线 R2 由 R8 直接处置并登记；D-2 的纠正＝**回滚该行内改写（恢复原句）＋新增加更正条**（遵头注「只追加」口径） | **2026-09-24 同批闭环**：D-1＝ISS-070 行加注并指向 ISS-071／ISS-072；D-2＝HANDOFF 该条恢复原句＋新增「更正 Q-010 A15 回收入库条#1」条；D-3＝证据件注内补两分支分述；D-4＝`landable` 链补 A14 段；另**存量 E-1**（`doc/verify/04`「流水线与接口」行自相矛盾）同批刷新；落点＝`doc/verify/05` R-107 | 2026-09-24 |
| **ISS-074** | 文档 | 2026-09-24 | Q-036 轮开工 `gate.py check` 基线跑（R1 执行） | **`doc/verify/03-验证计划.md` 第 44 行表格列数 10≠5**：R-110 重排行以**字面转义文本「反斜杠r反斜杠n」四字符**（而非真换行）追加，致 R-038 行与 R-110 行粘连为一行（`md-integrity` 判 FAIL；gate 基线实跑 23 PASS/**1 FAIL**/0 WARN） | `python script/gate.py check` 基线输出行 `FAIL md-integrity doc/verify/03-验证计划.md:44 表格列数 10≠5`；字节级实读＝R-038 行尾接字面反斜杠转义文本再接 R-110 行（同一物理行） | 已解决 | 格式面机械修复（不改任何表格内容语义）：四字符字面转义 → 真 CRLF，两表行复原 | 落点＝`doc/verify/03-验证计划.md` 重排记录表复原为两行（R-038＋R-110 各自成行）；复跑判据＝`python script/gate.py check` 末行 `24 PASS / 0 WARN / 0 FAIL`（Q-036 轮实跑，2026-09-24）；字节校验＝该文件 bare-LF 计数 0（CRLF 未翻转） | 2026-09-24 |
| **ISS-075** | 流程 | 2026-09-24 | R0 指令（本会话三连：优化派发成本／计划分层／计划角色归属） | **计划面三件**：①**计划角色并入 R8 本体**（子代理 `vr1-planner` 保留备用）——§0「计划 AI 唯一作者」的独立性被合并，须按"R8 兼计划"口径受审；②建 **`run_cmd/派发词模板.md`**（短派发词口径：差异 ≤40 行＋预算字段＋δ 面三件引用）；③**δ 复核面收窄口径**：只审「本批改动集 ∩ 本批声明落点」，[元] 类记录文本不入 δ 面（批次末/G1 前一次性扫），全量档 §9.4b 不变 | `run_cmd/派发词模板.md`；`doc/verify/03` §0（相位行＋角色合并声明）＋§1 切片计划首版 | 已解决 | R0 直接指令（非《驱动计划》派生）；R8 落实并自留痕；**独立性合并已显式声明**（规则 19/21 口径） | 落点＝`doc/verify/05-回归记录.md` **R-110**；口径落文流程件（`doc/项目开发流程.md` §9.4 同步）随最近一派发批 | 2026-09-24 |
| **ISS-076** | 流程 | 2026-09-24 | R8 计划面收口批写录自检（`doc/verify/05` R-110 备注） | **`.qoder/handoff/HANDOFF.md` 被截断为 0 字节**：R8 追加写函数在 `open(...,"wb")` 之后因参数类型错（bytes 当 str）抛错，文件已先被截断；并行会话两条未提交交接条（`- [Q-036] 角色=designer` 等）随事故丢失 | `ls -la .qoder/handoff/`＝0 字节；`git status` 该件为 M；恢复后 151,565 B／695 行 | 已解决 | R8 直接处置（红线 R2）：`git checkout -- .qoder/handoff/HANDOFF.md` 自 `613bf9c` 恢复；丢失条按实件**重建**（标注〔重建条〕）；教训＝写盘前校验参数类型＋append 类写入改用「读-改-写」并在失败时不停留在截断态 | 落点＝`doc/verify/05-回归记录.md` **R-110 备注**；重建条在 `HANDOFF.md` 文末〔重建条〕 | 2026-09-24 |
| **ISS-077** | 流程 | 2026-09-24 | R0 明确指令（"将计划 ai 不设置成子智能体 和 本体合并写入本地知识库"）＋双确认（"全部同意"） | **本地知识库写库三件**：①新建 `virtual_riscv经验/12-计划分层与计划角色合并.md`（L0/L1/L2 分层、2~4h 切片与机判核销、计划角色并入本体、短派发词、δ 复核面收窄、写盘事故规程）②卡 10 加「2026-09-24 修订」指引（替代"计划角色单独维护、PM 只执行"的绝对表述）③`AI索引.md` 行 11 篇→12 篇＋卡片名 | `D:/IC验证知识库/virtual_riscv经验/12-计划分层与计划角色合并.md`（4,544 B／CRLF）；卡 10 注入段；`AI索引.md` 索引行 | 已解决 | 知识库红线「只读；须人明确指令＋双确认＋留痕」——本次全满足（指令＋二次确认＋本行与 R-111 留痕）；写盘走读-改-写＋非空断言；**④顺带修复存量缺陷**：台账内 `virtual_test经验`＋0x05＋`-` 路径控制字符（自 HEAD 即存在）修为 `virtual_test经验/05-`——与本批 R8 路径转义事故同源（单反斜杠路径被当转义：反斜杠+v→VT、反斜杠+12→LF），`doc/verify/05` R-111 行内同源损伤已并行修复（行内换行重接、控制字符清零；R-111 备注另记） | 落点＝`doc/verify/05-回归记录.md` **R-111**（含三件终态字节/行尾数）；索引可达性＝`AI索引.md` 经验行 | 2026-09-24 |
| **ISS-078** | 文档 | 2026-09-24 | S-01（Q-036 交付面 δ 复核；`doc/verify/05` R-112／评审记录**附 AE**） | **3 阻塞＋8 澄清＋1 存疑**：B1＝`rs1_arch`／`disp_x_t.rs1_arch` 无生产端载体（R→S 链断，`spec/01` §3.5 已替换架构源号）；B2＝`is_jalr_ret` 算式漏 `rs1∈{x1,x5}` 合取（反例 `jalr x0,x2,0`；与 `spec/02` T-7 相抵）；B3＝`commit_t.ftq_id` 无通路至校正队列（`bp_upd_t` 无该域）；C1~C8＝文本/引节/匹配语义/清点口径；**S1**＝ISS-073 缺口②范围两说（身份键＋`bp_upd_id` 并列 vs 本批只闭身份键）→ 呈 R0 | `doc/spec/01` §3.5/§3.8/§3.11/§3.12/§3.6/§3.7/§3.17；`doc/spec/03` §2.8/§2.9/§4/§5/§7；`doc/spec/00` §4.8/§6 | 已解决 | 处置片（设计面）：**L13a**＝B2 算式更正＋C1~C6 文本面；**L13b**＝B1 生产端载体（含 §4.8 级间行连带）；**L13c**＝B3 通路（`bp_upd_t`＋`ftq_id`；含 C8 唯一性/队首限定）——三者完成后 **S-01b 复核回收**再走 S-02 | **2026-09-24 闭环**：L13 并批落文（R-114：B1/B2/B3＋C1~C6）→ **S-01b 复核（R-118／附 AF）：阻塞 0／澄清 4**；K-1/K-2/K-4 即修、K-3 转 RD（ISS-081）；**`bp_upd_id` 承接口径仍待 R0（S1）**——ISS-073 维持处理中 | 2026-09-24 |
| **ISS-081** | 文档 | 2026-09-24 | S-01b（L13 处置批 δ 复核；`doc/verify/05` R-118／评审记录**附 AF**） | **四项澄清（无阻塞）**：K-1＝「逐字一致」宣称的字面边界（域命名差异，宜改"同构＋域命名换算"）；K-2＝`fq_head` 名称未定义（子系统内部名）；K-3＝`spec/00` §4.8 数值变更**未留版本行**（草稿期预评审修正是否豁免升版行＝口径，存量同形）；K-4＝ISS-078 行"下一手"栏滞后 | `doc/spec/03` §2.8(f)(h)；`doc/spec/00` §4.8／版本表；`doc/process/00` ISS-078 行 | 已解决 | **K-1／K-2／K-4 即修（R-118 批）**；**K-3 已决——RD 自决（ADR-K3，2026-09-24）：不豁免**——草稿期数值变更亦须版本表留行，形态＝同版号批次行（`v0.1 续N`、状态保持"草稿（待 Requirement Review）"、含批次 ID／节号／旧→新（Δ）／`doc/verify/05` 回链），**不 bump 版号**；形式升版＋重过 RR 属门禁（R0）未触碰。落文已由本体执行：`doc/spec/00` 版本表 **v0.1 续1（Q-036）／v0.1 续2（L13）**＋表尾体例注；2026-09-24 前批次声明式不回填 | 落点＝`doc/spec/00-总体规格.md` 版本表 v0.1 续1／续2（L13 批）＋表尾体例注；`doc/verify/05-回归记录.md` **R-119**（ADR-K3 全文）；本行决策列 | 2026-09-24 |
| **ISS-079** | 流程 | 2026-09-24 | R0 指令（"将开发 ai 和验证 ai 也合到本体"） | **执行侧三角色并入本体**：计划（前批 ISS-075）＋**开发（设计面）**＋**验证（验证面）**——`vr1-designer`／`vr1-verifier`／`vr1-planner` 不再默认派发，spec/RTL 与 tb/iss/裁判类切片由本体直做；**唯审计（`vr1-auditor`，角色R6＋R7）保留独立子代理派发**（独立性四来源之②仅存此一环）。**独立性削减已显式声明**；兜底＝①类外部锚点（规范/ISS/探针/覆盖率原始件）＋R0 签署（`doc/AI角色与职责.md` §1.2 不变） | `doc/AI角色与职责.md` §2 修订注；`doc/verify/03` §0 合并声明·扩；`doc/自动推进说明.md` §1/§4 | 已解决 | R0 直接指令（非《驱动计划》派生）；R8 落实并自留痕（规则 19/21 口径） | 落点＝`doc/verify/05-回归记录.md` **R-114**；知识库卡 12 的对应扩写待**双确认**（本地库写入门槛） | 2026-09-24 |
| **ISS-080** | 流程 | 2026-09-24 | R0 指令（"增加一个决策 ai"） | **新增角色 RD＝决策 AI（独立子代理 `vr1-decider`）**：设计/规格类取舍自决（≥2 方案择优＋**ADR 四要素**：选项评估/选定/外部锚点/下游义务）；**必报面第 0 次转人**（放宽比对/waiver/改检查器/删 bins、无复核手段的 ✅ 陈述、环境权限类）；**同一问题 3 次未决熔断**出 ≤5 选项决策包；**独立于本体派发**（与审计并列）；记号＝`RD`（**R9 为红线专用，不得作角色号**） | `doc/AI角色与职责.md` §2（RD 行＋记号规约补句）；`doc/自动推进说明.md` §1/§4；知识库卡 05（既有规范定义） | 已解决 | R0 指令；R8 落实并自留痕；口径来源＝知识库《virtual_riscv经验/05-决策AI与ADR实务》与 `doc/项目开发流程.md` §12.2/§12.5 | 落点＝`doc/verify/05-回归记录.md` **R-116**（补齐角色表缺位） | 2026-09-24 |
| **ISS-089** | 文档 | 2026-09-24 | S-06 段② 落文批（`doc/spec/02` §18，A 11 成员语义；本体执行；`doc/verify/05` R-136） | **四项（三项即修＋一项待裁）**：① §9.1.5 备注列 LR／SC 两行「拆 2 μop」系 D-11 整族套用——LR／SC 各只写 rd、不涉双写口，`spec/00` §4.3＋SI-11＋提取件行 8674–8675 均指向「不拆（1 μop）」⇒ **即修**；② 本篇「`spec/00` 行 151」行号引用漂移（现行＝§4.3 `UOP_MAX_SPLIT` 行）⇒ **即修**（改引节号）；③ `spec/00` §8 `LR/SC` 行旧文「VA 低 12 位」与 ADR-S2-3「LR/SC 保留键同改 PA 全比对」相抵 ⇒ **即修**（§8 追加更正注；ISS-020 落地清单所列「§8 LR/SC 行」由此落现）；④ **`Zama16`／`Zama16b` 声称**：`spec/00` §2「一期附带 `Zama16`（对齐原子性 16B）」＋§8 `AMO` 行依据列「Zama16 对齐保证」＋`spec/02` §1「A 进（Zama16 对齐原子保证）」与 §2 口径统一「不支持非对齐访存、非对齐一律报 fault」＋`MISALIGNED_EN=0` **正面相抵**（规范侧：`Zama16b`＝行 9097–9100；实现则 16B 粒内非对齐 load/store/AMO 不报异常且须原子；行 47310–47324 PMA 详解；LR/SC 不受该 PMA 影响＝行 47328；「`Zama16`」非规范名） | `doc/spec/02` §9.1.5／§9.1.6／§14／§18；`doc/spec/00` §2／§4.3／§4.6／§8；提取件行 8674–8675／9097–9100／47310–47328 | 已解决 | ①②③＝文内自洽更正（锚点在位、无判定口径放宽）由本体即修并登记；④ 涉 ISA 声称与对齐判定口径冲突 ⇒ **派 RD 决策片（ADR-S06-A）**，落定前 `spec/00` §2／§8 与本篇 §1 行原文不动、不据其写行为 | **已解决（2026-09-24）** | **已落**：§18 全文＋§9.1.5 更正注＋§6／§14 T-14 回填＋`spec/00` §8 更正注；**ADR 回收同日落文（R-136／R-137）**：ADR-S06-A 选定 A1（取消 `Zama16` 声称：`spec/00` §2／§8＋`spec/02` 行 22 三处作废式更正＋版本表 `v0.1 续4`；ISS/Spike 与验证面零改动）、ADR-S06-B 选定 B1（LR／SC＝1 μop；措辞改「SC 为单一条件 store、同源判定不可拆」并已落文） | 2026-09-24 |
| **ISS-090** | 流程 | 2026-09-24 | S-11 送审批（`vr1-auditor` 独立只读·非决定性；报告＝评审记录**附 AJ**；`doc/verify/05` R-143） | **门禁实施送审发现 10 条质疑（通过 4）**：A-2 窗口静默重建（ISO 不等即覆盖旧快照、rc=0 无告警；可造「改后重建」假绿）／A-3 长基线刷新六条硬规则①~⑤无判据、⑥半判据（靠自觉）／A-4 绕过构造（先删 JSON 再 `snapshot` 无 reason ⇒ rc=0）／A-5 `capability-landed` 反例（子命令名在而分支删、声明迁未扫件）／A-7 账实相抵（Q-028 队列 ready/未开工 vs 实已落地）／B-1 `ra-token-defined` 漏面（三角色件＋两流程/计划件未扫）／B-2 漏匹配（`RA-5`/`RA 5`/`ra5`、规则序号无判据）／B-3 子串假阳（`EXTRA5`）／B-4 豁免标记整行全免（无理由/审批）／B-5 HANDOFF 末条解析失效（只认 `^- \[Q-`，落后 ≥2 条，字段缺失静默跳过） | 报告全文＝评审记录**附 AJ**；机判件 `machine_r35_{check,width,changed_all}.txt`；窗口 `rocheck_r35_window{,_INIT}.txt` | 已解决 | 即修（R8）：**B-1** 扩面（`RA_LIVE_FILES` 增五件；双向验证：注入⇒FAIL、撤除⇒sha256 一致）／**A-7** 队列置 done；**余 7 项**（A-2/A-3/A-4/A-5/B-2/B-4/B-5）转队列 **Q-038**（门禁增强片） | **已全处置（R-147）**：B-1（扩面＋双向验证）／A-7（Q-028 置 done）／B-2（词边界＋等形式）／B-5（末条记录解析）／A-2（窗口异 ISO 拒跑）／A-4（建立亦须 `--reason`）／A-5（`--window` 分支探针＋字段/reason 校验）／B-4（豁免须带理由）均落且**双向/三路验证**在案；A-3 余四条明列「不做＋理由」（跨轮过程状态，改由流程面承载）；Q-038 置 done。落点＝`script/gate.py`＋`doc/verify/05-回归记录.md`（R-143／R-145／R-146／R-147）＋`run_cmd/AutoQueue.yaml`（Q-037／Q-038） | 2026-09-24 |
| **ISS-091** | 平台 | 2026-09-24 | 收尾巡检（`windows-locked-file-safe-write` 手册 R5/R7；`doc/verify/05` R-151） | **shell 事故散件两类**：①`run_cmd/` 下 **4 个失败写残留** `AutoQueue.yaml.tmp{E,J,L,P}`（46,627~47,724 B；`tmpP` 与现行队列同尺寸＝同位副本，另三个为更旧中间版）②仓根 **0 字节整句式散件若干**（名字含全角「：／＝／（」或私用区码点）——shell 重定向/heredoc 中断把文本片段当文件名落盘 | 巡检＝`list_strays.py`（只列不删）＋逐个尺寸/内容比对；清理留痕＝本行与 R-151 | 已解决 | 平台/流程类（红线 R2 直接处置）：人工确认后删除（0 字节＋整句式名＝垃圾；`.tmp*` 已被成功写入取代）；学得纪律＝写盘一律「读-改-写＋`.tmpN`＋`os.replace`」，WinError 5 按手册 R2.2 降级（先备份→原位写→读回校验），禁无备份截断写 | 落点＝`doc/verify/05-回归记录.md` **R-151**；手册＝`windows-locked-file-safe-write`（R5/R7） | 2026-09-24 |
| **ISS-092** | 文档 | 2026-09-25 | 附 AK 复核（spec/05~14 首片；`vr1-auditor` 独立只读；`doc/verify/05` R-165） | **核对 31＝通过 21／阻塞 3／澄清 6／存疑 1**；3 阻塞（512KB 自造值／`ptw_mem_req_t` 假闭包／锚 46131 半行）与 C2/C3 计数口径**已即修**；**余待处置 4＋1**：C1「ALU→ALU 3 拍」节号引注不准（实体在 `spec/00` §1.0/§10 ISS-012，非 §4.4）／C4 `slot16_vld` 连带落点错位（应指 `spec/01` §3.16）／C5 批内「（待建）」状态词过时／C6 A19「接受面」消歧；**K1 `doc/verify/02` 缺 `xw_carry` 验证点组（ADR-1 下游义务×6）＝应落未落** | 报告全文＝评审记录**附 AK**；`doc/verify/05` R-165 | 已解决 | 机械/文本类由 R8 即修；余项已清；K1 已补建（VP-X06a~f） | **已即修**：512KB→256KB（`spec/06`/`spec/12`）／`ptw_mem_req_t` 改显式登记（`spec/06`/`spec/08`）／锚 46130–46131（`spec/10`）／C2 口径（`spec/14`）／C3 计数（`spec/10`）；**待**：C1/C4/C5/C6＋K1；**δ 轮 1（附 AL）新增 N1/N2/N3 已即修（R-166）**；**δ 轮 2（附 AM）新增 N4/N5 即修（R-169）**；**余项全清（R-169）**：C1（引注改 `spec/00` §1.0／§10 ISS-012）／C4（`spec/09` §4 连带改 §3.16＋回引）／C5（批内 15 处扫除；已收口篇残留转 ISS-094）／C6（接受面消歧）／**K1＝`doc/verify/02` VP-X06a~f 补建并回标** | **2026-09-25 关闭（R-169）**：余项清账＋K1 落地；收敛判据由 δ 轮 3 承担 |
| **ISS-093** | 流程 | 2026-09-25 | δ 轮 2（`audit-round2` 独立只读；报告＝评审记录**附 AM**；`doc/verify/05` R-169） | **`gate.py` `spec14-905-count` 两项机制面缺陷**：N4 ③ 取空 fail-open（`cnt=0` 时守卫静默跳过、仍可报 OK——与自述「fail-closed」相抵）／N5 ④ 口径差（自述「§3 出现」实为整篇文本包含；`D5-[①②③]` 实文零命中） | 报告全文＝附 AM；`script/gate.py` `check_spec14_905` | 已解决 | 即修（R-169）：③ 改 `cnt==0 ⇒ FAIL`；④ 改 §3 节内作用域＋三条 fail-closed（§3 未定位／D-13 行缺失／0 token）；双向验证三组（表头改字⇒FAIL／节外 token `B-0`⇒FAIL／撤除⇒sha256 复原＋25 PASS） | 落点＝`script/gate.py`（R-169） | 2026-09-25 |
| **ISS-094** | 文档 | 2026-09-25 | 附 AM 处置批（C5 扫除扩面盘点；`doc/verify/05` R-169） | **「（该篇待建）」残留存量 20 处（已收口篇）**：`spec/01` 19 处＋`spec/03` 1 处——所指篇目（02/05/06/08/09/10/11/13）现均已在册，状态词全部过时；C5 批内扫除只覆盖 `spec/05/06/08` 15 处，已收口篇须专片（改文涉复核面） | 盘点＝全文检索「待建」（01×19／03×1／05×2 已扫／06×8 已扫／08×5 已扫）；C5 原条目＝ISS-092 | 处理中 | 转专片：全量扫除＋逐处按篇定稿态措辞改写，片内带 δ 复核；G1 前完成 | 待落文（专片） | — |

---

## 待决策清单（需人拍板，AI 不自行推进）

> 收录口径＝**含未拍板决策点的条目**（台账状态多为「待决策」，少数为「处理中」但其中某一子项待人定，如 ISS-003/ISS-012）。
> **闭环项一律移出本清单**——由 `script/gate.py check` 的 `pending-list-stale` 判据机判（2026-09-23 加，
> 起因：ISS-001/ISS-030/ISS-032 三行已闭环却仍留在清单里，旧判据只查单向、抓不到）。
> **2026-09-23 口径变更后本清单只收“真需人拍板”项**（ISS-042）：放宽/判定口径类、环境安装类、人的安排类、无锚点且不可回退的价值取舍；设计/规格类已转决策 AI 自决（见台账各行决策列）。

| ID | 决策点 | 影响面 | 建议 |
|---|---|---|---|
| ISS-002 | **WSL2 + 工具链安装（需管理员 + 重启）** | M0/编译/ISS 互检/性能测试全阻塞 | 按 `doc/环境搭建.md` 一次做完 |
| ISS-029 | 是否新增 `ENV_BLOCKED`环境级阻塞档与许可词表 | 回归判定口径（R10）；环境故障不再计入用例 FAIL | 建议采纳：批级优先级高于 ABORTED、退出码 5 |
| ISS-023 | G1 冻结后是否开「R1 受控放宽」通道 | RTL 修改权限边界（需与 R5 口径合并） | 暂不设；但应在宣布基线前先定好协议 |
